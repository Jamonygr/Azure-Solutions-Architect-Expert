<!-- BEGIN GENERATED AZ305 V1 -->
# LAB-20 answer key

Use after completing the learner assessment. Every choice has a specific explanation.

## LAB20-Q01 — answer C

'Classify every interaction contract' awaits approval from logistics product owner. Approval requires a positive result plus this independent negative assertion: Routing a command, event, stream, and synchronous API through one service without semantic justification must fail. Which response provides the acceptance rule that makes LAB20-REQ-01 testable?

- ✗ **A. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests before checking Classify every interaction contract. Then, consider it sufficient that a successful deployment will later prove the architecture constraint.** — The decision tension comes from the fact that routing a command, event, stream, and synchronous API through one service without semantic justification must fail. The resulting architectural conclusion is that a deployment result cannot prove LAB20-REQ-01, and Event Hubs as the transport for all commands, events, telemetry, and partner requests still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Validate durable command processing to approve Classify every interaction contract; afterward, take it as conclusive that one control establishes an unrelated acceptance boundary.** — The safe operating boundary says that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. The resulting architectural conclusion is that that outcome belongs to Validate durable command processing and leaves Classify every interaction contract unverified.
- ✓ **C. Require the documented positive state for Classify every interaction contract; for the recorded decision, verify that routing a command, event, stream, and synchronous API through one service without semantic justification must fail.** — The retained result must be reconciled with the fact that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. The resulting architectural conclusion is that the positive state and an independent negative assertion jointly make LAB20-REQ-01 testable.
- ✗ **D. Choose Storage queues and direct partner webhooks with application-owned routing and skip the Classify every interaction contract negative assertion; then treat it as established that the candidate has the lowest implementation effort.** — The traceable checkpoint outcome is that decouple depot and partner changes while ensuring accepted shipments are processed once effectively and status events reach authorized consumers. The resulting architectural conclusion is that implementation effort cannot justify skipping the negative assertion or displace LAB20-REQ-01.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q02 — answer B

'Classify every interaction contract' is reopened at the request of integration platform lead. The selected architecture is Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners; object existence alone is not success. Which choice gives the team the intended successful finding?

- ✗ **A. Use only the negative assertion 'Routing a command, event, stream, and synchronous API through one service without semantic justification must fail' as the success result; in a separate step, proceed on the belief that absence proves every required positive property.** — The recovery guidance assumes that routing a command, event, stream, and synchronous API through one service without semantic justification must fail. The resulting architectural conclusion is that this is the independent prohibited-state assertion, not a successful finding.
- ✓ **B. Record every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. Independently, classify it as success for LAB20-REQ-01.** — The failure model establishes that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. The resulting architectural conclusion is that this is the authored target state for Classify every interaction contract and directly supports LAB20-REQ-01.
- ✗ **C. Use the successful finding from Prove event fan-out and telemetry separation as the result for Classify every interaction contract. As another control, treat as decisive the assertion that a property from the current checkpoint does not need to be inspected.** — The WAF consequence identifies that state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented. The resulting architectural conclusion is that evidence for Prove event fan-out and telemetry separation cannot substitute for the properties required at Classify every interaction contract.
- ✗ **D. Record the failure condition 'Misclassified interactions create brittle retries, lost ordering, or unnecessary coupling' as a successful state; as a separate check, base approval on the claim that the command returned an object.** — The command-level assertion is anchored in the fact that misclassified interactions create brittle retries, lost ordering, or unnecessary coupling. The resulting architectural conclusion is that resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q03 — answer B

A design review of 'Classify every interaction contract' includes partner engineering manager. Evidence must address this risk without retaining credentials: Misclassified interactions create brittle retries, lost ordering, or unnecessary coupling. What is sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Govern partner APIs for Classify every interaction contract. Separately, use as justification the claim that a related checkpoint proves the current expected state.** — The controlling fact is that preserve the redacted policy export, API specification hash, negative test results, and current revision mapping. Under the stated constraint, that evidence supports Govern partner APIs, so it cannot demonstrate Every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements.
- ✓ **B. Retain save the interaction catalog, semantic classification, and service-selection matrix; in a separate step, exclude credentials and unrelated response fields.** — save the interaction catalog, semantic classification, and service-selection matrix. Under the stated constraint, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **C. Store unredacted Classify every interaction contract output with operator, tenant, token, and request context; for this decision, accept without proof that reproduction requires every captured field.** — The authored acceptance boundary states that unredacted implementation output. Under the stated constraint, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Classify every interaction contract positive inspection's exit status. Before sign-off, rely on the claim that projected properties and assertion results can be reconstructed later.** — The relevant observation is that the positive inspection's exit status. Under the stated constraint, an exit code alone does not show whether every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q04 — answer C

The team asks security architect to assess 'Classify every interaction contract'. The target is Every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements, but the latest evidence does not show it. Which recommendation supplies the most likely cause?

- ✗ **A. Treat 'Independent service dashboards can all appear healthy while the business transaction stops between boundaries' as grounds to reject Classify every interaction contract; then use the premise that validate end-to-end failure handling's failure model applies unchanged here.** — The scenario makes clear that independent service dashboards can all appear healthy while the business transaction stops between boundaries. Under the stated constraint, that condition belongs to Validate end-to-end failure handling and does not by itself invalidate Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.
- ✗ **B. Ignore the negative assertion 'Routing a command, event, stream, and synchronous API through one service without semantic justification must fail'. Independently, consider it sufficient that a later material change will make it unnecessary.** — The architecture evidence must show that routing a command, event, stream, and synchronous API through one service without semantic justification must fail. Under the stated constraint, the negative assertion must be evaluated now, independent of a later business change.
- ✓ **C. Investigate misclassified interactions create brittle retries, lost ordering, or unnecessary coupling; next, isolate that cause before changing Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.** — The checkpoint specifically records that misclassified interactions create brittle retries, lost ordering, or unnecessary coupling. Under the stated constraint, it is the checkpoint's causal failure model and should be isolated before retrying Classify every interaction contract.
- ✗ **D. Investigate Validate durable command processing instead of diagnosing Classify every interaction contract. Next, take it as conclusive that a passing result at Validate durable command processing identifies the current cause.** — The applicable design condition is that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. Under the stated constraint, a passing result at Validate durable command processing gives no causal evidence for the failure at Classify every interaction contract.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q05 — answer B

A recommendation on 'Classify every interaction contract' is requested by site reliability engineering lead. The run encountered this modeled failure: Misclassified interactions create brittle retries, lost ordering, or unnecessary coupling. Choose the safest recovery action.

- ✗ **A. Perform cleanup immediately: Remove local catalog exports; discovery changes no service; as a separate check, rely on the belief that the failed operation and its returned identifiers do not need reconciliation.** — The retained result must be reconciled with the fact that remove local catalog exports; discovery changes no service. Under the stated constraint, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✓ **B. Reclassify the disputed contract with producer and consumer owners, then rescore the candidates; for this decision, preserve the current run identity and evidence.** — The review is governed by this fact: reclassify the disputed contract with producer and consumer owners, then rescore the candidates. Under the stated constraint, it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **C. Create a different run identity before diagnosing 'Misclassified interactions create brittle retries, lost ordering, or unnecessary coupling'. Afterward, proceed on the belief that the first state record and returned identifiers can be discarded.** — The decision tension comes from the fact that misclassified interactions create brittle retries, lost ordering, or unnecessary coupling. Under the stated constraint, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Validate durable command processing instead; next, treat as decisive the assertion that success at Validate durable command processing will repair the failed state at Classify every interaction contract.** — The safe operating boundary says that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. Under the stated constraint, altering an already separate checkpoint does not repair the modeled failure at Classify every interaction contract.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q06 — answer D

'Classify every interaction contract' is assigned to logistics product owner. Without making a new change, the team must inspect the risk 'Misclassified interactions create brittle retries, lost ordering, or unnecessary coupling' using the Azure CLI lane. Which answer describes the read-only, lane-correct inspection?

- ✗ **A. Rerun the Classify every interaction contract implementation command and infer the expected state. Before sign-off, treat it as established that absence of a shell error proves every property.** — The failure model establishes that the implementation command. Under the stated constraint, it can mutate state and shell success does not independently assert the expected properties.
- ✗ **B. Run only this negative inspection for Classify every interaction contract: Routing a command, event, stream, and synchronous API through one service without semantic justification must fail; as an independent condition, use as justification the claim that an empty negative result reports every required positive property.** — The recovery guidance assumes that the negative inspection. Under the stated constraint, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **C. Run the positive inspection for Prove event fan-out and telemetry separation and apply it to Classify every interaction contract; before approval, accept without proof that any command from the same lane proves the current checkpoint.** — The WAF consequence identifies that the positive inspection for Prove event fan-out and telemetry separation. Under the stated constraint, it is lane-correct but proves Prove event fan-out and telemetry separation, not Classify every interaction contract.
- ✓ **D. Inspect the documented properties for Classify every interaction contract. Then, retain this evidence: save the interaction catalog, semantic classification, and service-selection matrix.** — The traceable checkpoint outcome is that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. Under the stated constraint, the read-only inspection directly tests the properties required at Classify every interaction contract.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q07 — answer A

An assurance review of 'Classify every interaction contract' includes integration platform lead. A passing positive check does not by itself prove this negative assertion: Routing a command, event, stream, and synchronous API through one service without semantic justification must fail. What should be recorded as the assertion pair that proves both conditions independently?

- ✓ **A. Verify the positive properties for Classify every interaction contract. Next, independently verify that routing a command, event, stream, and synchronous API through one service without semantic justification must fail.** — The command-level assertion is anchored in the fact that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements; Routing a command, event, stream, and synchronous API through one service without semantic justification must fail. Under the stated constraint, two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **B. Verify only the positive result for Classify every interaction contract and report full compliance. Next, base approval on the claim that every prohibited parallel state must therefore be absent.** — every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. This matters because the positive result alone does not test the explicit anti-condition 'Routing a command, event, stream, and synchronous API through one service without semantic justification must fail'.
- ✗ **C. Prove only that routing a command, event, stream, and synchronous API through one service without semantic justification must fail and report the intended configuration as present, and then use the premise that absence is equivalent to positive-state evidence.** — The controlling fact is that routing a command, event, stream, and synchronous API through one service without semantic justification must fail. This matters because absence evidence cannot demonstrate the required positive state 'Every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements'.
- ✗ **D. Use Govern partner APIs's negative assertion for Classify every interaction contract. Also, consider it sufficient that negative assertions are interchangeable between checkpoints.** — The authored acceptance boundary states that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. This matters because the second assertion is valid for Govern partner APIs but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q08 — answer D

Approval of 'Classify every interaction contract' is questioned by partner engineering manager. The board wants the Well-Architected consequence of mitigating this risk: Misclassified interactions create brittle retries, lost ordering, or unnecessary coupling. Which proposal supplies the consequence attributable to this checkpoint?

- ✗ **A. Use the Validate end-to-end failure handling consequence as the result for Classify every interaction contract; next, rely on the claim that a pillar statement remains valid when moved away from Validate end-to-end failure handling.** — The checkpoint specifically records that cost Optimization: service-specific tiers and retention are sized from measured interaction demand rather than a uniform premium choice. This matters because that tradeoff belongs to Validate end-to-end failure handling and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Classify every interaction contract outcome. In addition, rely on the belief that a moderate cost classification outweighs the mandatory architecture state.** — The scenario makes clear that the required outcome at Classify every interaction contract. This matters because cost Optimization cannot remove the acceptance condition 'Every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements'.
- ✗ **C. Treat 'Operational Excellence: explicit contracts make ownership and incident diagnosis clear across service boundaries' as proof that all five pillars pass; before approval, proceed on the belief that the checkpoint 'Classify every interaction contract' no longer needs its separate negative check.** — The architecture evidence must show that routing a command, event, stream, and synchronous API through one service without semantic justification must fail. This matters because one positive command cannot establish every pillar, especially while the negative state remains unchecked.
- ✓ **D. Record this consequence: Operational Excellence: explicit contracts make ownership and incident diagnosis clear across service boundaries. As another control, tie it to LAB20-REQ-01.** — The relevant observation is that operational Excellence: explicit contracts make ownership and incident diagnosis clear across service boundaries. This matters because it states the authored pillar consequence of the control evaluated at Classify every interaction contract.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q09 — answer B

The implementation review has reached 'Classify every interaction contract'. A material change now applies: A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season. Which option best represents the correct revision to the decision record?

- ✗ **A. Retain Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Classify every interaction contract without recalculating criteria or eligibility; as a second control, take it as conclusive that the original weighted result is permanent.** — The review is governed by this fact: service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners. This matters because the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✓ **B. Re-score Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners and both alternatives for Classify every interaction contract. In addition, supersede the ADR using the changed evidence for LAB20-REQ-01.** — The applicable design condition is that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Classify every interaction contract. This matters because the material change 'A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **C. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests for Classify every interaction contract without rechecking its mandatory constraints. Then, treat it as established that being different from the current design is an architecture criterion.** — The retained result must be reconciled with the fact that event Hubs as the transport for all commands, events, telemetry, and partner requests. This matters because being different is not a criterion, and the candidate still must avoid the prohibited state at Classify every interaction contract.
- ✗ **D. Keep Storage queues and direct partner webhooks with application-owned routing eligible at Classify every interaction contract by downgrading LAB20-REQ-01; afterward, use as justification the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The decision tension comes from the fact that lAB20-REQ-01. This matters because an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q10 — answer A

The approach to 'Classify every interaction contract' is challenged by site reliability engineering lead. After a partial run, cleanup must follow this dependency: Remove local catalog exports; discovery changes no service. Which course of action provides the dependency-safe cleanup plan?

- ✓ **A. Verify exact run-state IDs and ownership tags for Classify every interaction contract. Before sign-off, follow this dependency rule without purge: Remove local catalog exports; discovery changes no service.** — The safe operating boundary says that remove local catalog exports; discovery changes no service. This matters because exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **B. Apply the cleanup rule for Prove event fan-out and telemetry separation before reconciling the current dependency. Also, treat as decisive the assertion that removing a parent needed to identify Classify every interaction contract is harmless.** — The traceable checkpoint outcome is that remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks. This matters because a cleanup rule for Prove event fan-out and telemetry separation cannot override the dependency declared for Classify every interaction contract.
- ✗ **C. Delete candidates by display name before comparing the Classify every interaction contract ownership tags; in a separate step, base approval on the claim that the dependency rule 'Remove local catalog exports; discovery changes no service' is optional.** — The failure model establishes that save the interaction catalog, semantic classification, and service-selection matrix. This matters because names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Classify every interaction contract negative assertion 'Routing a command, event, stream, and synchronous API through one service without semantic justification must fail'. As another control, use the premise that remaining command logs are sufficient recovery evidence.** — The recovery guidance assumes that routing a command, event, stream, and synchronous API through one service without semantic justification must fail. This matters because irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q11 — answer C

A decision test for 'Validate durable command processing' includes integration platform lead. Approval requires a positive result plus this independent negative assertion: Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. Which choice gives the team the acceptance rule that makes LAB20-REQ-02 testable?

- ✗ **A. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests before checking Validate durable command processing; then proceed on the belief that a successful deployment will later prove the architecture constraint.** — The command-level assertion is anchored in the fact that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. This matters because a deployment result cannot prove LAB20-REQ-02, and Event Hubs as the transport for all commands, events, telemetry, and partner requests still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Classify every interaction contract to approve Validate durable command processing. Independently, treat as decisive the assertion that one control establishes an unrelated acceptance boundary.** — every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. The checkpoint therefore requires that that outcome belongs to Classify every interaction contract and leaves Validate durable command processing unverified.
- ✓ **C. Require the documented positive state for Validate durable command processing; then verify that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail.** — The WAF consequence identifies that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. This matters because the positive state and an independent negative assertion jointly make LAB20-REQ-02 testable.
- ✗ **D. Choose Storage queues and direct partner webhooks with application-owned routing and skip the Validate durable command processing negative assertion. Next, base approval on the claim that the candidate has the lowest implementation effort.** — The controlling fact is that decouple depot and partner changes while ensuring accepted shipments are processed once effectively and status events reach authorized consumers. The checkpoint therefore requires that implementation effort cannot justify skipping the negative assertion or displace LAB20-REQ-02.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q12 — answer C

The architecture board reconsiders 'Validate durable command processing' with partner engineering manager. The selected architecture is Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners; object existence alone is not success. What is the intended successful finding?

- ✗ **A. Use only the negative assertion 'Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail' as the success result; as a separate check, use as justification the claim that absence proves every required positive property.** — The relevant observation is that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. The checkpoint therefore requires that this is the independent prohibited-state assertion, not a successful finding.
- ✗ **B. Use the successful finding from Prove event fan-out and telemetry separation as the result for Validate durable command processing. Afterward, accept without proof that a property from the current checkpoint does not need to be inspected.** — The checkpoint specifically records that state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented. The checkpoint therefore requires that evidence for Prove event fan-out and telemetry separation cannot substitute for the properties required at Validate durable command processing.
- ✓ **C. Record commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. Also, classify it as success for LAB20-REQ-02.** — The authored acceptance boundary states that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. The checkpoint therefore requires that this is the authored target state for Validate durable command processing and directly supports LAB20-REQ-02.
- ✗ **D. Record the failure condition 'At-least-once delivery can create duplicate business effects despite a healthy queue' as a successful state; next, rely on the claim that the command returned an object.** — The scenario makes clear that at-least-once delivery can create duplicate business effects despite a healthy queue. The checkpoint therefore requires that resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q13 — answer D

A review of 'Validate durable command processing' begins with input from security architect. Evidence must address this risk without retaining credentials: At-least-once delivery can create duplicate business effects despite a healthy queue. Which recommendation supplies sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Govern partner APIs for Validate durable command processing. Before sign-off, use the premise that a related checkpoint proves the current expected state.** — The applicable design condition is that preserve the redacted policy export, API specification hash, negative test results, and current revision mapping. The checkpoint therefore requires that that evidence supports Govern partner APIs, so it cannot demonstrate Commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow.
- ✗ **B. Store unredacted Validate durable command processing output with operator, tenant, token, and request context; as an independent condition, consider it sufficient that reproduction requires every captured field.** — The review is governed by this fact: unredacted implementation output. The checkpoint therefore requires that identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **C. Record only the Validate durable command processing positive inspection's exit status; as a second control, take it as conclusive that projected properties and assertion results can be reconstructed later.** — The retained result must be reconciled with the fact that the positive inspection's exit status. The checkpoint therefore requires that an exit code alone does not show whether commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow.
- ✓ **D. Retain preserve sanitized message IDs, session ordering, retry counts, dead-letter reason, and handler outcome. Afterward, exclude credentials and unrelated response fields.** — The architecture evidence must show that preserve sanitized message IDs, session ordering, retry counts, dead-letter reason, and handler outcome. The checkpoint therefore requires that it captures the checkpoint's observable properties while keeping the evidence boundary narrow.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q14 — answer A

'Validate durable command processing' awaits approval from site reliability engineering lead. The target is Commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow, but the latest evidence does not show it. Choose the most likely cause.

- ✓ **A. Investigate at-least-once delivery can create duplicate business effects despite a healthy queue. Separately, isolate that cause before changing Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.** — The decision tension comes from the fact that at-least-once delivery can create duplicate business effects despite a healthy queue. The checkpoint therefore requires that it is the checkpoint's causal failure model and should be isolated before retrying Validate durable command processing.
- ✗ **B. Treat 'Independent service dashboards can all appear healthy while the business transaction stops between boundaries' as grounds to reject Validate durable command processing. Next, rely on the belief that validate end-to-end failure handling's failure model applies unchanged here.** — The safe operating boundary says that independent service dashboards can all appear healthy while the business transaction stops between boundaries. The checkpoint therefore requires that that condition belongs to Validate end-to-end failure handling and does not by itself invalidate Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.
- ✗ **C. Ignore the negative assertion 'Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail', and then proceed on the belief that a later material change will make it unnecessary.** — The traceable checkpoint outcome is that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. The checkpoint therefore requires that the negative assertion must be evaluated now, independent of a later business change.
- ✗ **D. Investigate Classify every interaction contract instead of diagnosing Validate durable command processing. Also, treat as decisive the assertion that a passing result at Classify every interaction contract identifies the current cause.** — The failure model establishes that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. The checkpoint therefore requires that a passing result at Classify every interaction contract gives no causal evidence for the failure at Validate durable command processing.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q15 — answer A

'Validate durable command processing' is reopened at the request of logistics product owner. The run encountered this modeled failure: At-least-once delivery can create duplicate business effects despite a healthy queue. Which answer describes the safest recovery action?

- ✓ **A. Correct the idempotency record or session key and replay only the quarantined synthetic message; before closing the checkpoint, preserve the current run identity and evidence.** — The recovery guidance assumes that correct the idempotency record or session key and replay only the quarantined synthetic message. The checkpoint therefore requires that it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **B. Perform cleanup immediately: Purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity; next, treat it as established that the failed operation and its returned identifiers do not need reconciliation.** — The WAF consequence identifies that purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity. The checkpoint therefore requires that cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **C. Create a different run identity before diagnosing 'At-least-once delivery can create duplicate business effects despite a healthy queue'. In addition, use as justification the claim that the first state record and returned identifiers can be discarded.** — The command-level assertion is anchored in the fact that at-least-once delivery can create duplicate business effects despite a healthy queue. The checkpoint therefore requires that discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Classify every interaction contract instead; before approval, accept without proof that success at Classify every interaction contract will repair the failed state at Validate durable command processing.** — every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. In the decision record, altering an already separate checkpoint does not repair the modeled failure at Validate durable command processing.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q16 — answer A

A design review of 'Validate durable command processing' includes integration platform lead. Without making a new change, the team must inspect the risk 'At-least-once delivery can create duplicate business effects despite a healthy queue' using the Azure CLI lane. What should be recorded as the read-only, lane-correct inspection?

- ✓ **A. Inspect the documented properties for Validate durable command processing. Independently, retain this evidence: preserve sanitized message IDs, session ordering, retry counts, dead-letter reason, and handler outcome.** — The controlling fact is that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. In the decision record, the read-only inspection directly tests the properties required at Validate durable command processing.
- ✗ **B. Rerun the Validate durable command processing implementation command and infer the expected state; during the same review, base approval on the claim that absence of a shell error proves every property.** — The authored acceptance boundary states that the implementation command. In the decision record, it can mutate state and shell success does not independently assert the expected properties.
- ✗ **C. Run only this negative inspection for Validate durable command processing: Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. Then, use the premise that an empty negative result reports every required positive property.** — The relevant observation is that the negative inspection. In the decision record, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Prove event fan-out and telemetry separation and apply it to Validate durable command processing; afterward, consider it sufficient that any command from the same lane proves the current checkpoint.** — The checkpoint specifically records that the positive inspection for Prove event fan-out and telemetry separation. In the decision record, it is lane-correct but proves Prove event fan-out and telemetry separation, not Validate durable command processing.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q17 — answer D

The team asks partner engineering manager to assess 'Validate durable command processing'. A passing positive check does not by itself prove this negative assertion: Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. Which proposal supplies the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Validate durable command processing and report full compliance. Also, rely on the claim that every prohibited parallel state must therefore be absent.** — The architecture evidence must show that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. In the decision record, the positive result alone does not test the explicit anti-condition 'Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail'.
- ✗ **B. Prove only that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail and report the intended configuration as present; in a separate step, rely on the belief that absence is equivalent to positive-state evidence.** — The applicable design condition is that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. In the decision record, absence evidence cannot demonstrate the required positive state 'Commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow'.
- ✗ **C. Use Govern partner APIs's negative assertion for Validate durable command processing. As another control, proceed on the belief that negative assertions are interchangeable between checkpoints.** — The review is governed by this fact: an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. In the decision record, the second assertion is valid for Govern partner APIs but leaves this checkpoint's prohibited state untested.
- ✓ **D. Verify the positive properties for Validate durable command processing; in a separate step, independently verify that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail.** — The scenario makes clear that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow; Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. In the decision record, two independent observations prevent a passing positive check from concealing an unsafe parallel state.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q18 — answer B

A recommendation on 'Validate durable command processing' is requested by security architect. The board wants the Well-Architected consequence of mitigating this risk: At-least-once delivery can create duplicate business effects despite a healthy queue. Which option best represents the consequence attributable to this checkpoint?

- ✗ **A. Use the Validate end-to-end failure handling consequence as the result for Validate durable command processing; before approval, take it as conclusive that a pillar statement remains valid when moved away from Validate end-to-end failure handling.** — The decision tension comes from the fact that cost Optimization: service-specific tiers and retention are sized from measured interaction demand rather than a uniform premium choice. In the decision record, that tradeoff belongs to Validate end-to-end failure handling and does not explain this checkpoint's decision.
- ✓ **B. Record this consequence: Reliability: sessions, dead lettering, and idempotency protect accepted commands through transient failure; next, tie it to LAB20-REQ-02.** — The retained result must be reconciled with the fact that reliability: sessions, dead lettering, and idempotency protect accepted commands through transient failure. In the decision record, it states the authored pillar consequence of the control evaluated at Validate durable command processing.
- ✗ **C. Remove the control responsible for the Validate durable command processing outcome. Separately, treat it as established that a moderate cost classification outweighs the mandatory architecture state.** — The safe operating boundary says that the required outcome at Validate durable command processing. In the decision record, cost Optimization cannot remove the acceptance condition 'Commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow'.
- ✗ **D. Treat 'Reliability: sessions, dead lettering, and idempotency protect accepted commands through transient failure' as proof that all five pillars pass; for this decision, use as justification the claim that the checkpoint 'Validate durable command processing' no longer needs its separate negative check.** — The traceable checkpoint outcome is that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. In the decision record, one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q19 — answer D

'Validate durable command processing' is assigned to site reliability engineering lead. A material change now applies: A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season. Which course of action provides the correct revision to the decision record?

- ✗ **A. Retain Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Validate durable command processing without recalculating criteria or eligibility; afterward, treat as decisive the assertion that the original weighted result is permanent.** — The recovery guidance assumes that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners. In the decision record, the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests for Validate durable command processing without rechecking its mandatory constraints; then base approval on the claim that being different from the current design is an architecture criterion.** — The WAF consequence identifies that event Hubs as the transport for all commands, events, telemetry, and partner requests. In the decision record, being different is not a criterion, and the candidate still must avoid the prohibited state at Validate durable command processing.
- ✗ **C. Keep Storage queues and direct partner webhooks with application-owned routing eligible at Validate durable command processing by downgrading LAB20-REQ-02. Independently, use the premise that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The command-level assertion is anchored in the fact that lAB20-REQ-02. In the decision record, an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
- ✓ **D. Re-score Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners and both alternatives for Validate durable command processing; for this decision, supersede the ADR using the changed evidence for LAB20-REQ-02.** — The failure model establishes that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Validate durable command processing. In the decision record, the material change 'A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season.' requires fresh eligibility, weighted analysis, and a superseding decision.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q20 — answer A

An assurance review of 'Validate durable command processing' includes logistics product owner. After a partial run, cleanup must follow this dependency: Purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity. Which finding constitutes the dependency-safe cleanup plan?

- ✓ **A. Verify exact run-state IDs and ownership tags for Validate durable command processing. Then, follow this dependency rule without purge: Purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity.** — purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity. The independent assertion shows why exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **B. Apply the cleanup rule for Prove event fan-out and telemetry separation before reconciling the current dependency. As another control, accept without proof that removing a parent needed to identify Validate durable command processing is harmless.** — The controlling fact is that remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks. The independent assertion shows why a cleanup rule for Prove event fan-out and telemetry separation cannot override the dependency declared for Validate durable command processing.
- ✗ **C. Delete candidates by display name before comparing the Validate durable command processing ownership tags; as a separate check, rely on the claim that the dependency rule 'Purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity' is optional.** — The authored acceptance boundary states that preserve sanitized message IDs, session ordering, retry counts, dead-letter reason, and handler outcome. The independent assertion shows why names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Validate durable command processing negative assertion 'Completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail'. Afterward, rely on the belief that remaining command logs are sufficient recovery evidence.** — The relevant observation is that completing a duplicate shipment, abandoning poison messages indefinitely, or ordering globally when only per-shipment order is needed must fail. The independent assertion shows why irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q21 — answer D

Approval of 'Prove event fan-out and telemetry separation' is questioned by partner engineering manager. Approval requires a positive result plus this independent negative assertion: Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. What is the acceptance rule that makes LAB20-REQ-03 testable?

- ✗ **A. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests before checking Prove event fan-out and telemetry separation. Next, use as justification the claim that a successful deployment will later prove the architecture constraint.** — The scenario makes clear that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. The independent assertion shows why a deployment result cannot prove LAB20-REQ-03, and Event Hubs as the transport for all commands, events, telemetry, and partner requests still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Classify every interaction contract to approve Prove event fan-out and telemetry separation, and then accept without proof that one control establishes an unrelated acceptance boundary.** — The architecture evidence must show that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. The independent assertion shows why that outcome belongs to Classify every interaction contract and leaves Prove event fan-out and telemetry separation unverified.
- ✗ **C. Choose Storage queues and direct partner webhooks with application-owned routing and skip the Prove event fan-out and telemetry separation negative assertion. Also, rely on the claim that the candidate has the lowest implementation effort.** — The applicable design condition is that decouple depot and partner changes while ensuring accepted shipments are processed once effectively and status events reach authorized consumers. The independent assertion shows why implementation effort cannot justify skipping the negative assertion or displace LAB20-REQ-03.
- ✓ **D. Require the documented positive state for Prove event fan-out and telemetry separation, and then verify that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail.** — The checkpoint specifically records that state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented. The independent assertion shows why the positive state and an independent negative assertion jointly make LAB20-REQ-03 testable.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q22 — answer A

The implementation review has reached 'Prove event fan-out and telemetry separation'. The selected architecture is Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners; object existence alone is not success. Which recommendation supplies the intended successful finding?

- ✓ **A. Record state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented; as a separate check, classify it as success for LAB20-REQ-03.** — The review is governed by this fact: state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented. The independent assertion shows why this is the authored target state for Prove event fan-out and telemetry separation and directly supports LAB20-REQ-03.
- ✗ **B. Use only the negative assertion 'Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail' as the success result; next, use the premise that absence proves every required positive property.** — The retained result must be reconciled with the fact that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. The independent assertion shows why this is the independent prohibited-state assertion, not a successful finding.
- ✗ **C. Use the successful finding from Validate durable command processing as the result for Prove event fan-out and telemetry separation. In addition, consider it sufficient that a property from the current checkpoint does not need to be inspected.** — The decision tension comes from the fact that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. The independent assertion shows why evidence for Validate durable command processing cannot substitute for the properties required at Prove event fan-out and telemetry separation.
- ✗ **D. Record the failure condition 'Poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on' as a successful state; before approval, take it as conclusive that the command returned an object.** — The safe operating boundary says that poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on. The independent assertion shows why resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q23 — answer A

The approach to 'Prove event fan-out and telemetry separation' is challenged by site reliability engineering lead. Evidence must address this risk without retaining credentials: Poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on. Choose sufficient, properly scoped evidence.

- ✓ **A. Retain save subscription filters, event schemas, partition-key rationale, consumer groups, and replay tests; before approval, exclude credentials and unrelated response fields.** — The traceable checkpoint outcome is that save subscription filters, event schemas, partition-key rationale, consumer groups, and replay tests. The independent assertion shows why it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **B. Substitute the evidence from Govern partner APIs for Prove event fan-out and telemetry separation; for the recorded decision, rely on the belief that a related checkpoint proves the current expected state.** — The failure model establishes that preserve the redacted policy export, API specification hash, negative test results, and current revision mapping. The independent assertion shows why that evidence supports Govern partner APIs, so it cannot demonstrate State events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented.
- ✗ **C. Store unredacted Prove event fan-out and telemetry separation output with operator, tenant, token, and request context. Then, proceed on the belief that reproduction requires every captured field.** — The recovery guidance assumes that unredacted implementation output. The independent assertion shows why identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Prove event fan-out and telemetry separation positive inspection's exit status; afterward, treat as decisive the assertion that projected properties and assertion results can be reconstructed later.** — The WAF consequence identifies that the positive inspection's exit status. The independent assertion shows why an exit code alone does not show whether state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q24 — answer C

A decision test for 'Prove event fan-out and telemetry separation' includes logistics product owner. The target is State events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented, but the latest evidence does not show it. Which answer describes the most likely cause?

- ✗ **A. Treat 'Independent service dashboards can all appear healthy while the business transaction stops between boundaries' as grounds to reject Prove event fan-out and telemetry separation. Also, treat it as established that validate end-to-end failure handling's failure model applies unchanged here.** — independent service dashboards can all appear healthy while the business transaction stops between boundaries. Operationally, that condition belongs to Validate end-to-end failure handling and does not by itself invalidate Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.
- ✗ **B. Ignore the negative assertion 'Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail'; in a separate step, use as justification the claim that a later material change will make it unnecessary.** — The controlling fact is that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. Operationally, the negative assertion must be evaluated now, independent of a later business change.
- ✓ **C. Investigate poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on; as an independent condition, isolate that cause before changing Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.** — The command-level assertion is anchored in the fact that poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on. The independent assertion shows why it is the checkpoint's causal failure model and should be isolated before retrying Prove event fan-out and telemetry separation.
- ✗ **D. Investigate Classify every interaction contract instead of diagnosing Prove event fan-out and telemetry separation. As another control, accept without proof that a passing result at Classify every interaction contract identifies the current cause.** — The authored acceptance boundary states that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. Operationally, a passing result at Classify every interaction contract gives no causal evidence for the failure at Prove event fan-out and telemetry separation.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q25 — answer A

The architecture board reconsiders 'Prove event fan-out and telemetry separation' with integration platform lead. The run encountered this modeled failure: Poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on. What should be recorded as the safest recovery action?

- ✓ **A. Refine the event contract and filter, then replay the same synthetic event set to isolated consumers; then preserve the current run identity and evidence.** — The relevant observation is that refine the event contract and filter, then replay the same synthetic event set to isolated consumers. Operationally, it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **B. Perform cleanup immediately: Remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks; before approval, base approval on the claim that the failed operation and its returned identifiers do not need reconciliation.** — The checkpoint specifically records that remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks. Operationally, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **C. Create a different run identity before diagnosing 'Poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on'. Separately, use the premise that the first state record and returned identifiers can be discarded.** — The scenario makes clear that poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on. Operationally, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Classify every interaction contract instead; for this decision, consider it sufficient that success at Classify every interaction contract will repair the failed state at Prove event fan-out and telemetry separation.** — The architecture evidence must show that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. Operationally, altering an already separate checkpoint does not repair the modeled failure at Prove event fan-out and telemetry separation.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q26 — answer D

A review of 'Prove event fan-out and telemetry separation' begins with input from partner engineering manager. Without making a new change, the team must inspect the risk 'Poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on' using the Azure CLI lane. Which proposal supplies the read-only, lane-correct inspection?

- ✗ **A. Rerun the Prove event fan-out and telemetry separation implementation command and infer the expected state; afterward, rely on the claim that absence of a shell error proves every property.** — The review is governed by this fact: the implementation command. Operationally, it can mutate state and shell success does not independently assert the expected properties.
- ✗ **B. Run only this negative inspection for Prove event fan-out and telemetry separation: Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail; then rely on the belief that an empty negative result reports every required positive property.** — The retained result must be reconciled with the fact that the negative inspection. Operationally, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **C. Run the positive inspection for Validate durable command processing and apply it to Prove event fan-out and telemetry separation. Independently, proceed on the belief that any command from the same lane proves the current checkpoint.** — The decision tension comes from the fact that the positive inspection for Validate durable command processing. Operationally, it is lane-correct but proves Validate durable command processing, not Prove event fan-out and telemetry separation.
- ✓ **D. Inspect the documented properties for Prove event fan-out and telemetry separation. Also, retain this evidence: save subscription filters, event schemas, partition-key rationale, consumer groups, and replay tests.** — The applicable design condition is that state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented. Operationally, the read-only inspection directly tests the properties required at Prove event fan-out and telemetry separation.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q27 — answer B

'Prove event fan-out and telemetry separation' awaits approval from security architect. A passing positive check does not by itself prove this negative assertion: Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. Which option best represents the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Prove event fan-out and telemetry separation and report full compliance. As another control, take it as conclusive that every prohibited parallel state must therefore be absent.** — The traceable checkpoint outcome is that state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented. Operationally, the positive result alone does not test the explicit anti-condition 'Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail'.
- ✓ **B. Verify the positive properties for Prove event fan-out and telemetry separation. Afterward, independently verify that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail.** — The safe operating boundary says that state events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented; Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. Operationally, two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **C. Prove only that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail and report the intended configuration as present; as a separate check, treat it as established that absence is equivalent to positive-state evidence.** — The failure model establishes that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. Operationally, absence evidence cannot demonstrate the required positive state 'State events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented'.
- ✗ **D. Use Govern partner APIs's negative assertion for Prove event fan-out and telemetry separation. Afterward, use as justification the claim that negative assertions are interchangeable between checkpoints.** — The recovery guidance assumes that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. Operationally, the second assertion is valid for Govern partner APIs but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q28 — answer C

'Prove event fan-out and telemetry separation' is reopened at the request of site reliability engineering lead. The board wants the Well-Architected consequence of mitigating this risk: Poor filters or partition keys can overload subscribers or destroy the ordering consumers rely on. Which course of action provides the consequence attributable to this checkpoint?

- ✗ **A. Use the Validate end-to-end failure handling consequence as the result for Prove event fan-out and telemetry separation; for this decision, treat as decisive the assertion that a pillar statement remains valid when moved away from Validate end-to-end failure handling.** — The command-level assertion is anchored in the fact that cost Optimization: service-specific tiers and retention are sized from measured interaction demand rather than a uniform premium choice. Operationally, that tradeoff belongs to Validate end-to-end failure handling and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Prove event fan-out and telemetry separation outcome. Before sign-off, base approval on the claim that a moderate cost classification outweighs the mandatory architecture state.** — the required outcome at Prove event fan-out and telemetry separation. The requirement-to-evidence link establishes that cost Optimization cannot remove the acceptance condition 'State events use filtered fan-out, telemetry uses partitioned streams and separate consumer groups, and replay ownership is documented'.
- ✓ **C. Record this consequence: Performance Efficiency: streams and partitioning handle telemetry throughput without burdening command processing. Separately, tie it to LAB20-REQ-03.** — The WAF consequence identifies that performance Efficiency: streams and partitioning handle telemetry throughput without burdening command processing. Operationally, it states the authored pillar consequence of the control evaluated at Prove event fan-out and telemetry separation.
- ✗ **D. Treat 'Performance Efficiency: streams and partitioning handle telemetry throughput without burdening command processing' as proof that all five pillars pass; as an independent condition, use the premise that the checkpoint 'Prove event fan-out and telemetry separation' no longer needs its separate negative check.** — The controlling fact is that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. The requirement-to-evidence link establishes that one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q29 — answer D

A design review of 'Prove event fan-out and telemetry separation' includes logistics product owner. A material change now applies: A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season. Which finding constitutes the correct revision to the decision record?

- ✗ **A. Retain Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Prove event fan-out and telemetry separation without recalculating criteria or eligibility. Independently, accept without proof that the original weighted result is permanent.** — The relevant observation is that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners. The requirement-to-evidence link establishes that the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests for Prove event fan-out and telemetry separation without rechecking its mandatory constraints. Next, rely on the claim that being different from the current design is an architecture criterion.** — The checkpoint specifically records that event Hubs as the transport for all commands, events, telemetry, and partner requests. The requirement-to-evidence link establishes that being different is not a criterion, and the candidate still must avoid the prohibited state at Prove event fan-out and telemetry separation.
- ✗ **C. Keep Storage queues and direct partner webhooks with application-owned routing eligible at Prove event fan-out and telemetry separation by downgrading LAB20-REQ-03, and then rely on the belief that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The scenario makes clear that lAB20-REQ-03. The requirement-to-evidence link establishes that an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
- ✓ **D. Re-score Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners and both alternatives for Prove event fan-out and telemetry separation; independently, supersede the ADR using the changed evidence for LAB20-REQ-03.** — The authored acceptance boundary states that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Prove event fan-out and telemetry separation. The requirement-to-evidence link establishes that the material change 'A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season.' requires fresh eligibility, weighted analysis, and a superseding decision.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q30 — answer A

The team asks integration platform lead to assess 'Prove event fan-out and telemetry separation'. After a partial run, cleanup must follow this dependency: Remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks. Which recommendation delivers the dependency-safe cleanup plan?

- ✓ **A. Verify exact run-state IDs and ownership tags for Prove event fan-out and telemetry separation. Independently, follow this dependency rule without purge: Remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks.** — The architecture evidence must show that remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks. The requirement-to-evidence link establishes that exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **B. Apply the cleanup rule for Validate durable command processing before reconciling the current dependency. Afterward, consider it sufficient that removing a parent needed to identify Prove event fan-out and telemetry separation is harmless.** — The applicable design condition is that purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity. The requirement-to-evidence link establishes that a cleanup rule for Validate durable command processing cannot override the dependency declared for Prove event fan-out and telemetry separation.
- ✗ **C. Delete candidates by display name before comparing the Prove event fan-out and telemetry separation ownership tags; next, take it as conclusive that the dependency rule 'Remove only run-owned subscriptions, consumer groups, and synthetic events after ownership checks' is optional.** — The review is governed by this fact: save subscription filters, event schemas, partition-key rationale, consumer groups, and replay tests. The requirement-to-evidence link establishes that names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Prove event fan-out and telemetry separation negative assertion 'Sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail'. In addition, treat it as established that remaining command logs are sufficient recovery evidence.** — The retained result must be reconciled with the fact that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. The requirement-to-evidence link establishes that irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q31 — answer D

A recommendation on 'Govern partner APIs' is requested by security architect. Approval requires a positive result plus this independent negative assertion: An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. Which recommendation supplies the acceptance rule that makes LAB20-REQ-04 testable?

- ✗ **A. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests before checking Govern partner APIs. Also, use the premise that a successful deployment will later prove the architecture constraint.** — The safe operating boundary says that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. The requirement-to-evidence link establishes that a deployment result cannot prove LAB20-REQ-04, and Event Hubs as the transport for all commands, events, telemetry, and partner requests still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Classify every interaction contract to approve Govern partner APIs; in a separate step, consider it sufficient that one control establishes an unrelated acceptance boundary.** — The traceable checkpoint outcome is that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. The requirement-to-evidence link establishes that that outcome belongs to Classify every interaction contract and leaves Govern partner APIs unverified.
- ✗ **C. Choose Storage queues and direct partner webhooks with application-owned routing and skip the Govern partner APIs negative assertion. As another control, take it as conclusive that the candidate has the lowest implementation effort.** — The failure model establishes that decouple depot and partner changes while ensuring accepted shipments are processed once effectively and status events reach authorized consumers. The requirement-to-evidence link establishes that implementation effort cannot justify skipping the negative assertion or displace LAB20-REQ-04.
- ✓ **D. Require the documented positive state for Govern partner APIs. As another control, verify that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail.** — The decision tension comes from the fact that the API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry. The requirement-to-evidence link establishes that the positive state and an independent negative assertion jointly make LAB20-REQ-04 testable.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q32 — answer B

'Govern partner APIs' is assigned to site reliability engineering lead. The selected architecture is Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners; object existence alone is not success. Choose the intended successful finding.

- ✗ **A. Use only the negative assertion 'An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail' as the success result; before approval, rely on the belief that absence proves every required positive property.** — The WAF consequence identifies that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. The requirement-to-evidence link establishes that this is the independent prohibited-state assertion, not a successful finding.
- ✓ **B. Record the API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry. In addition, classify it as success for LAB20-REQ-04.** — The recovery guidance assumes that the API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry. The requirement-to-evidence link establishes that this is the authored target state for Govern partner APIs and directly supports LAB20-REQ-04.
- ✗ **C. Use the successful finding from Validate durable command processing as the result for Govern partner APIs. Separately, proceed on the belief that a property from the current checkpoint does not need to be inspected.** — The command-level assertion is anchored in the fact that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. The requirement-to-evidence link establishes that evidence for Validate durable command processing cannot substitute for the properties required at Govern partner APIs.
- ✗ **D. Record the failure condition 'A reachable API can still expose data, overload a backend, or duplicate business transactions' as a successful state; for this decision, treat as decisive the assertion that the command returned an object.** — a reachable API can still expose data, overload a backend, or duplicate business transactions. Consequently, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q33 — answer B

An assurance review of 'Govern partner APIs' includes logistics product owner. Evidence must address this risk without retaining credentials: A reachable API can still expose data, overload a backend, or duplicate business transactions. Which answer describes sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Prove event fan-out and telemetry separation for Govern partner APIs; afterward, treat it as established that a related checkpoint proves the current expected state.** — The authored acceptance boundary states that save subscription filters, event schemas, partition-key rationale, consumer groups, and replay tests. Consequently, that evidence supports Prove event fan-out and telemetry separation, so it cannot demonstrate The API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry.
- ✓ **B. Retain preserve the redacted policy export, API specification hash, negative test results, and current revision mapping. Before sign-off, exclude credentials and unrelated response fields.** — The controlling fact is that preserve the redacted policy export, API specification hash, negative test results, and current revision mapping. Consequently, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **C. Store unredacted Govern partner APIs output with operator, tenant, token, and request context; then use as justification the claim that reproduction requires every captured field.** — The relevant observation is that unredacted implementation output. Consequently, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Govern partner APIs positive inspection's exit status. Independently, accept without proof that projected properties and assertion results can be reconstructed later.** — The checkpoint specifically records that the positive inspection's exit status. Consequently, an exit code alone does not show whether the API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q34 — answer A

Approval of 'Govern partner APIs' is questioned by integration platform lead. The target is The API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry, but the latest evidence does not show it. What should be recorded as the most likely cause?

- ✓ **A. Investigate a reachable API can still expose data, overload a backend, or duplicate business transactions; afterward, isolate that cause before changing Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.** — The scenario makes clear that a reachable API can still expose data, overload a backend, or duplicate business transactions. Consequently, it is the checkpoint's causal failure model and should be isolated before retrying Govern partner APIs.
- ✗ **B. Treat 'Independent service dashboards can all appear healthy while the business transaction stops between boundaries' as grounds to reject Govern partner APIs. As another control, base approval on the claim that validate end-to-end failure handling's failure model applies unchanged here.** — The architecture evidence must show that independent service dashboards can all appear healthy while the business transaction stops between boundaries. Consequently, that condition belongs to Validate end-to-end failure handling and does not by itself invalidate Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.
- ✗ **C. Ignore the negative assertion 'An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail'; as a separate check, use the premise that a later material change will make it unnecessary.** — The applicable design condition is that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. Consequently, the negative assertion must be evaluated now, independent of a later business change.
- ✗ **D. Investigate Classify every interaction contract instead of diagnosing Govern partner APIs. Afterward, consider it sufficient that a passing result at Classify every interaction contract identifies the current cause.** — The review is governed by this fact: every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. Consequently, a passing result at Classify every interaction contract gives no causal evidence for the failure at Govern partner APIs.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q35 — answer A

The implementation review has reached 'Govern partner APIs'. The run encountered this modeled failure: A reachable API can still expose data, overload a backend, or duplicate business transactions. Which proposal supplies the safest recovery action?

- ✓ **A. Correct the policy or revision in the controlled reference, then rerun positive and negative partner tests, and then preserve the current run identity and evidence.** — The retained result must be reconciled with the fact that correct the policy or revision in the controlled reference, then rerun positive and negative partner tests. Consequently, it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **B. Perform cleanup immediately: Remove only run-owned API revisions and synthetic subscriptions; retain shared service and production APIs; for this decision, rely on the claim that the failed operation and its returned identifiers do not need reconciliation.** — The decision tension comes from the fact that remove only run-owned API revisions and synthetic subscriptions; retain shared service and production APIs. Consequently, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **C. Create a different run identity before diagnosing 'A reachable API can still expose data, overload a backend, or duplicate business transactions'. Before sign-off, rely on the belief that the first state record and returned identifiers can be discarded.** — The safe operating boundary says that a reachable API can still expose data, overload a backend, or duplicate business transactions. Consequently, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Classify every interaction contract instead; as an independent condition, proceed on the belief that success at Classify every interaction contract will repair the failed state at Govern partner APIs.** — The traceable checkpoint outcome is that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. Consequently, altering an already separate checkpoint does not repair the modeled failure at Govern partner APIs.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q36 — answer C

The approach to 'Govern partner APIs' is challenged by security architect. Without making a new change, the team must inspect the risk 'A reachable API can still expose data, overload a backend, or duplicate business transactions' using the Azure CLI lane. Which option best represents the read-only, lane-correct inspection?

- ✗ **A. Rerun the Govern partner APIs implementation command and infer the expected state. Independently, take it as conclusive that absence of a shell error proves every property.** — The recovery guidance assumes that the implementation command. Consequently, it can mutate state and shell success does not independently assert the expected properties.
- ✗ **B. Run only this negative inspection for Govern partner APIs: An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. Next, treat it as established that an empty negative result reports every required positive property.** — The WAF consequence identifies that the negative inspection. Consequently, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✓ **C. Inspect the documented properties for Govern partner APIs; as a separate check, retain this evidence: preserve the redacted policy export, API specification hash, negative test results, and current revision mapping.** — The failure model establishes that the API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry. Consequently, the read-only inspection directly tests the properties required at Govern partner APIs.
- ✗ **D. Run the positive inspection for Validate durable command processing and apply it to Govern partner APIs, and then use as justification the claim that any command from the same lane proves the current checkpoint.** — The command-level assertion is anchored in the fact that the positive inspection for Validate durable command processing. Consequently, it is lane-correct but proves Validate durable command processing, not Govern partner APIs.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q37 — answer C

A decision test for 'Govern partner APIs' includes site reliability engineering lead. A passing positive check does not by itself prove this negative assertion: An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. Which course of action provides the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Govern partner APIs and report full compliance. Afterward, treat as decisive the assertion that every prohibited parallel state must therefore be absent.** — The controlling fact is that the API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry. For this case, the positive result alone does not test the explicit anti-condition 'An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail'.
- ✗ **B. Prove only that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail and report the intended configuration as present; next, base approval on the claim that absence is equivalent to positive-state evidence.** — The authored acceptance boundary states that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. For this case, absence evidence cannot demonstrate the required positive state 'The API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry'.
- ✓ **C. Verify the positive properties for Govern partner APIs; before approval, independently verify that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail.** — the API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry; An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. For this case, two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **D. Use Prove event fan-out and telemetry separation's negative assertion for Govern partner APIs. In addition, use the premise that negative assertions are interchangeable between checkpoints.** — The relevant observation is that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. For this case, the second assertion is valid for Prove event fan-out and telemetry separation but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q38 — answer D

The architecture board reconsiders 'Govern partner APIs' with logistics product owner. The board wants the Well-Architected consequence of mitigating this risk: A reachable API can still expose data, overload a backend, or duplicate business transactions. Which finding constitutes the consequence attributable to this checkpoint?

- ✗ **A. Use the Validate end-to-end failure handling consequence as the result for Govern partner APIs; as an independent condition, accept without proof that a pillar statement remains valid when moved away from Validate end-to-end failure handling.** — The scenario makes clear that cost Optimization: service-specific tiers and retention are sized from measured interaction demand rather than a uniform premium choice. For this case, that tradeoff belongs to Validate end-to-end failure handling and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Govern partner APIs outcome; for the final assessment, rely on the claim that a moderate cost classification outweighs the mandatory architecture state.** — The architecture evidence must show that the required outcome at Govern partner APIs. For this case, cost Optimization cannot remove the acceptance condition 'The API contract defines authentication, authorization, quota, rate limit, schema validation, versioning, transformation, correlation, and safe backend retry'.
- ✗ **C. Treat 'Security: centralized authentication, validation, quotas, and redaction reduce partner-facing attack surface' as proof that all five pillars pass. Then, rely on the belief that the checkpoint 'Govern partner APIs' no longer needs its separate negative check.** — The applicable design condition is that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. For this case, one positive command cannot establish every pillar, especially while the negative state remains unchecked.
- ✓ **D. Record this consequence: Security: centralized authentication, validation, quotas, and redaction reduce partner-facing attack surface; as an independent condition, tie it to LAB20-REQ-04.** — The checkpoint specifically records that security: centralized authentication, validation, quotas, and redaction reduce partner-facing attack surface. For this case, it states the authored pillar consequence of the control evaluated at Govern partner APIs.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q39 — answer C

A review of 'Govern partner APIs' begins with input from integration platform lead. A material change now applies: A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season. Which recommendation delivers the correct revision to the decision record?

- ✗ **A. Retain Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Govern partner APIs without recalculating criteria or eligibility, and then consider it sufficient that the original weighted result is permanent.** — The retained result must be reconciled with the fact that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners. For this case, the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests for Govern partner APIs without rechecking its mandatory constraints. Also, take it as conclusive that being different from the current design is an architecture criterion.** — The decision tension comes from the fact that event Hubs as the transport for all commands, events, telemetry, and partner requests. For this case, being different is not a criterion, and the candidate still must avoid the prohibited state at Govern partner APIs.
- ✓ **C. Re-score Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners and both alternatives for Govern partner APIs; then supersede the ADR using the changed evidence for LAB20-REQ-04.** — The review is governed by this fact: service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Govern partner APIs. For this case, the material change 'A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **D. Keep Storage queues and direct partner webhooks with application-owned routing eligible at Govern partner APIs by downgrading LAB20-REQ-04; in a separate step, treat it as established that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The safe operating boundary says that lAB20-REQ-04. For this case, an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q40 — answer B

'Govern partner APIs' awaits approval from partner engineering manager. After a partial run, cleanup must follow this dependency: Remove only run-owned API revisions and synthetic subscriptions; retain shared service and production APIs. Which response meets the need for the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Validate durable command processing before reconciling the current dependency. In addition, proceed on the belief that removing a parent needed to identify Govern partner APIs is harmless.** — The failure model establishes that purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity. For this case, a cleanup rule for Validate durable command processing cannot override the dependency declared for Govern partner APIs.
- ✓ **B. Verify exact run-state IDs and ownership tags for Govern partner APIs. Also, follow this dependency rule without purge: Remove only run-owned API revisions and synthetic subscriptions; retain shared service and production APIs.** — The traceable checkpoint outcome is that remove only run-owned API revisions and synthetic subscriptions; retain shared service and production APIs. For this case, exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **C. Delete candidates by display name before comparing the Govern partner APIs ownership tags; before approval, treat as decisive the assertion that the dependency rule 'Remove only run-owned API revisions and synthetic subscriptions; retain shared service and production APIs' is optional.** — The recovery guidance assumes that preserve the redacted policy export, API specification hash, negative test results, and current revision mapping. For this case, names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Govern partner APIs negative assertion 'An anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail'. Separately, base approval on the claim that remaining command logs are sufficient recovery evidence.** — The WAF consequence identifies that an anonymous operation, unbounded partner traffic, breaking in-place change, or retry of a non-idempotent request must fail. For this case, irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q41 — answer B

'Validate end-to-end failure handling' is reopened at the request of site reliability engineering lead. Approval requires a positive result plus this independent negative assertion: Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. Choose the acceptance rule that makes LAB20-REQ-05 testable.

- ✗ **A. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests before checking Validate end-to-end failure handling. As another control, rely on the belief that a successful deployment will later prove the architecture constraint.** — infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. That evidence means a deployment result cannot prove LAB20-REQ-05, and Event Hubs as the transport for all commands, events, telemetry, and partner requests still has to meet the mandatory boundary.
- ✓ **B. Require the documented positive state for Validate end-to-end failure handling; next, verify that infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall.** — The command-level assertion is anchored in the fact that a correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion. For this case, the positive state and an independent negative assertion jointly make LAB20-REQ-05 testable.
- ✗ **C. Use the passing result from Classify every interaction contract to approve Validate end-to-end failure handling; as a separate check, proceed on the belief that one control establishes an unrelated acceptance boundary.** — The controlling fact is that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. That evidence means that outcome belongs to Classify every interaction contract and leaves Validate end-to-end failure handling unverified.
- ✗ **D. Choose Storage queues and direct partner webhooks with application-owned routing and skip the Validate end-to-end failure handling negative assertion. Afterward, treat as decisive the assertion that the candidate has the lowest implementation effort.** — The authored acceptance boundary states that decouple depot and partner changes while ensuring accepted shipments are processed once effectively and status events reach authorized consumers. That evidence means implementation effort cannot justify skipping the negative assertion or displace LAB20-REQ-05.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q42 — answer D

A design review of 'Validate end-to-end failure handling' includes logistics product owner. The selected architecture is Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners; object existence alone is not success. Which answer describes the intended successful finding?

- ✗ **A. Use only the negative assertion 'Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall' as the success result; for this decision, treat it as established that absence proves every required positive property.** — The checkpoint specifically records that infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. That evidence means this is the independent prohibited-state assertion, not a successful finding.
- ✗ **B. Use the successful finding from Validate durable command processing as the result for Validate end-to-end failure handling. Before sign-off, use as justification the claim that a property from the current checkpoint does not need to be inspected.** — The scenario makes clear that commands use sessions where ordering is required, bounded delivery attempts, duplicate detection, idempotent handlers, and a governed dead-letter workflow. That evidence means evidence for Validate durable command processing cannot substitute for the properties required at Validate end-to-end failure handling.
- ✗ **C. Record the failure condition 'Independent service dashboards can all appear healthy while the business transaction stops between boundaries' as a successful state; as an independent condition, accept without proof that the command returned an object.** — The architecture evidence must show that independent service dashboards can all appear healthy while the business transaction stops between boundaries. That evidence means resource existence or command output does not convert the documented failure condition into success.
- ✓ **D. Record a correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion; for this decision, classify it as success for LAB20-REQ-05.** — The relevant observation is that a correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion. That evidence means this is the authored target state for Validate end-to-end failure handling and directly supports LAB20-REQ-05.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q43 — answer B

The team asks integration platform lead to assess 'Validate end-to-end failure handling'. Evidence must address this risk without retaining credentials: Independent service dashboards can all appear healthy while the business transaction stops between boundaries. What should be recorded as sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Prove event fan-out and telemetry separation for Validate end-to-end failure handling. Independently, base approval on the claim that a related checkpoint proves the current expected state.** — The review is governed by this fact: save subscription filters, event schemas, partition-key rationale, consumer groups, and replay tests. That evidence means that evidence supports Prove event fan-out and telemetry separation, so it cannot demonstrate A correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion.
- ✓ **B. Retain archive correlation IDs, timestamps, entity metrics, consumer acknowledgements, dead-letter checks, and business outcome. Then, exclude credentials and unrelated response fields.** — The applicable design condition is that archive correlation IDs, timestamps, entity metrics, consumer acknowledgements, dead-letter checks, and business outcome. That evidence means it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **C. Store unredacted Validate end-to-end failure handling output with operator, tenant, token, and request context. Next, use the premise that reproduction requires every captured field.** — The retained result must be reconciled with the fact that unredacted implementation output. That evidence means identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Validate end-to-end failure handling positive inspection's exit status, and then consider it sufficient that projected properties and assertion results can be reconstructed later.** — The decision tension comes from the fact that the positive inspection's exit status. That evidence means an exit code alone does not show whether a correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q44 — answer A

A recommendation on 'Validate end-to-end failure handling' is requested by partner engineering manager. The target is A correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion, but the latest evidence does not show it. Which proposal supplies the most likely cause?

- ✓ **A. Investigate independent service dashboards can all appear healthy while the business transaction stops between boundaries. Next, isolate that cause before changing Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.** — The safe operating boundary says that independent service dashboards can all appear healthy while the business transaction stops between boundaries. That evidence means it is the checkpoint's causal failure model and should be isolated before retrying Validate end-to-end failure handling.
- ✗ **B. Treat 'A reachable API can still expose data, overload a backend, or duplicate business transactions' as grounds to reject Validate end-to-end failure handling. Afterward, rely on the claim that govern partner APIs's failure model applies unchanged here.** — The traceable checkpoint outcome is that a reachable API can still expose data, overload a backend, or duplicate business transactions. That evidence means that condition belongs to Govern partner APIs and does not by itself invalidate Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners.
- ✗ **C. Ignore the negative assertion 'Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall'; next, rely on the belief that a later material change will make it unnecessary.** — The failure model establishes that infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. That evidence means the negative assertion must be evaluated now, independent of a later business change.
- ✗ **D. Investigate Classify every interaction contract instead of diagnosing Validate end-to-end failure handling. In addition, proceed on the belief that a passing result at Classify every interaction contract identifies the current cause.** — The recovery guidance assumes that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. That evidence means a passing result at Classify every interaction contract gives no causal evidence for the failure at Validate end-to-end failure handling.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q45 — answer B

'Validate end-to-end failure handling' is assigned to security architect. The run encountered this modeled failure: Independent service dashboards can all appear healthy while the business transaction stops between boundaries. Which option best represents the safest recovery action?

- ✗ **A. Perform cleanup immediately: Remove run-owned entities in dependency order only after proving that no synthetic message remains active; as an independent condition, take it as conclusive that the failed operation and its returned identifiers do not need reconciliation.** — The command-level assertion is anchored in the fact that remove run-owned entities in dependency order only after proving that no synthetic message remains active. That evidence means cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✓ **B. Resume from the failed idempotent boundary using the same correlation ID and preserve the original trace. As another control, preserve the current run identity and evidence.** — The WAF consequence identifies that resume from the failed idempotent boundary using the same correlation ID and preserve the original trace. That evidence means it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **C. Create a different run identity before diagnosing 'Independent service dashboards can all appear healthy while the business transaction stops between boundaries'; separately, treat it as established that the first state record and returned identifiers can be discarded.** — independent service dashboards can all appear healthy while the business transaction stops between boundaries. The resulting architectural conclusion is that discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Classify every interaction contract instead. Then, use as justification the claim that success at Classify every interaction contract will repair the failed state at Validate end-to-end failure handling.** — The controlling fact is that every interaction records producer, consumer, temporal coupling, ordering, throughput, replay, fan-out, latency, and delivery requirements. The resulting architectural conclusion is that altering an already separate checkpoint does not repair the modeled failure at Validate end-to-end failure handling.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q46 — answer C

An assurance review of 'Validate end-to-end failure handling' includes site reliability engineering lead. Without making a new change, the team must inspect the risk 'Independent service dashboards can all appear healthy while the business transaction stops between boundaries' using the Azure CLI lane. Which course of action provides the read-only, lane-correct inspection?

- ✗ **A. Rerun the Validate end-to-end failure handling implementation command and infer the expected state, and then treat as decisive the assertion that absence of a shell error proves every property.** — The relevant observation is that the implementation command. The resulting architectural conclusion is that it can mutate state and shell success does not independently assert the expected properties.
- ✗ **B. Run only this negative inspection for Validate end-to-end failure handling: Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. Also, base approval on the claim that an empty negative result reports every required positive property.** — The checkpoint specifically records that the negative inspection. The resulting architectural conclusion is that absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✓ **C. Inspect the documented properties for Validate end-to-end failure handling. In addition, retain this evidence: archive correlation IDs, timestamps, entity metrics, consumer acknowledgements, dead-letter checks, and business outcome.** — The authored acceptance boundary states that a correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion. The resulting architectural conclusion is that the read-only inspection directly tests the properties required at Validate end-to-end failure handling.
- ✗ **D. Run the positive inspection for Validate durable command processing and apply it to Validate end-to-end failure handling; in a separate step, use the premise that any command from the same lane proves the current checkpoint.** — The scenario makes clear that the positive inspection for Validate durable command processing. The resulting architectural conclusion is that it is lane-correct but proves Validate durable command processing, not Validate end-to-end failure handling.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q47 — answer D

Approval of 'Validate end-to-end failure handling' is questioned by logistics product owner. A passing positive check does not by itself prove this negative assertion: Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. Which finding constitutes the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Validate end-to-end failure handling and report full compliance. In addition, accept without proof that every prohibited parallel state must therefore be absent.** — The applicable design condition is that a correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion. The resulting architectural conclusion is that the positive result alone does not test the explicit anti-condition 'Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall'.
- ✗ **B. Prove only that infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall and report the intended configuration as present; before approval, rely on the claim that absence is equivalent to positive-state evidence.** — The review is governed by this fact: infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. The resulting architectural conclusion is that absence evidence cannot demonstrate the required positive state 'A correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion'.
- ✗ **C. Use Prove event fan-out and telemetry separation's negative assertion for Validate end-to-end failure handling. Separately, rely on the belief that negative assertions are interchangeable between checkpoints.** — The retained result must be reconciled with the fact that sending high-volume telemetry through the command queue or sharing offsets between unrelated consumers must fail. The resulting architectural conclusion is that the second assertion is valid for Prove event fan-out and telemetry separation but leaves this checkpoint's prohibited state untested.
- ✓ **D. Verify the positive properties for Validate end-to-end failure handling. Before sign-off, independently verify that infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall.** — The architecture evidence must show that a correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion; Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. The resulting architectural conclusion is that two independent observations prevent a passing positive check from concealing an unsafe parallel state.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q48 — answer C

The implementation review has reached 'Validate end-to-end failure handling'. The board wants the Well-Architected consequence of mitigating this risk: Independent service dashboards can all appear healthy while the business transaction stops between boundaries. Which recommendation delivers the consequence attributable to this checkpoint?

- ✗ **A. Use the Govern partner APIs consequence as the result for Validate end-to-end failure handling. Then, consider it sufficient that a pillar statement remains valid when moved away from Govern partner APIs.** — The safe operating boundary says that security: centralized authentication, validation, quotas, and redaction reduce partner-facing attack surface. The resulting architectural conclusion is that that tradeoff belongs to Govern partner APIs and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Validate end-to-end failure handling outcome; afterward, take it as conclusive that a moderate cost classification outweighs the mandatory architecture state.** — The traceable checkpoint outcome is that the required outcome at Validate end-to-end failure handling. The resulting architectural conclusion is that cost Optimization cannot remove the acceptance condition 'A correlated synthetic shipment crosses the API, command, state-event, and telemetry paths with bounded retries and observable business completion'.
- ✓ **C. Record this consequence: Cost Optimization: service-specific tiers and retention are sized from measured interaction demand rather than a uniform premium choice; afterward, tie it to LAB20-REQ-05.** — The decision tension comes from the fact that cost Optimization: service-specific tiers and retention are sized from measured interaction demand rather than a uniform premium choice. The resulting architectural conclusion is that it states the authored pillar consequence of the control evaluated at Validate end-to-end failure handling.
- ✗ **D. Treat 'Cost Optimization: service-specific tiers and retention are sized from measured interaction demand rather than a uniform premium choice' as proof that all five pillars pass; then treat it as established that the checkpoint 'Validate end-to-end failure handling' no longer needs its separate negative check.** — The failure model establishes that infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. The resulting architectural conclusion is that one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q49 — answer B

The approach to 'Validate end-to-end failure handling' is challenged by partner engineering manager. A material change now applies: A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season. Which response meets the need for the correct revision to the decision record?

- ✗ **A. Retain Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Validate end-to-end failure handling without recalculating criteria or eligibility; in a separate step, proceed on the belief that the original weighted result is permanent.** — The WAF consequence identifies that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners. The resulting architectural conclusion is that the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✓ **B. Re-score Service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners and both alternatives for Validate end-to-end failure handling, and then supersede the ADR using the changed evidence for LAB20-REQ-05.** — The recovery guidance assumes that service Bus for commands, Event Grid for state events, Event Hubs for telemetry, and API Management for partners at Validate end-to-end failure handling. The resulting architectural conclusion is that the material change 'A carrier contract now requires commands for each shipment to remain ordered while also sustaining ten times the telemetry volume during peak season.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **C. Select Event Hubs as the transport for all commands, events, telemetry, and partner requests for Validate end-to-end failure handling without rechecking its mandatory constraints. As another control, treat as decisive the assertion that being different from the current design is an architecture criterion.** — The command-level assertion is anchored in the fact that event Hubs as the transport for all commands, events, telemetry, and partner requests. The resulting architectural conclusion is that being different is not a criterion, and the candidate still must avoid the prohibited state at Validate end-to-end failure handling.
- ✗ **D. Keep Storage queues and direct partner webhooks with application-owned routing eligible at Validate end-to-end failure handling by downgrading LAB20-REQ-05; as a separate check, base approval on the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.** — lAB20-REQ-05. Under the stated constraint, an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)

## LAB20-Q50 — answer C

A decision test for 'Validate end-to-end failure handling' includes security architect. After a partial run, cleanup must follow this dependency: Remove run-owned entities in dependency order only after proving that no synthetic message remains active. Which choice should be approved as the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Validate durable command processing before reconciling the current dependency. Separately, use as justification the claim that removing a parent needed to identify Validate end-to-end failure handling is harmless.** — The authored acceptance boundary states that purge only run-owned synthetic messages through the documented test receiver; never purge a shared entity. Under the stated constraint, a cleanup rule for Validate durable command processing cannot override the dependency declared for Validate end-to-end failure handling.
- ✗ **B. Delete candidates by display name before comparing the Validate end-to-end failure handling ownership tags; for this decision, accept without proof that the dependency rule 'Remove run-owned entities in dependency order only after proving that no synthetic message remains active' is optional.** — The relevant observation is that archive correlation IDs, timestamps, entity metrics, consumer acknowledgements, dead-letter checks, and business outcome. Under the stated constraint, names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✓ **C. Verify exact run-state IDs and ownership tags for Validate end-to-end failure handling; as a separate check, follow this dependency rule without purge: Remove run-owned entities in dependency order only after proving that no synthetic message remains active.** — The controlling fact is that remove run-owned entities in dependency order only after proving that no synthetic message remains active. Under the stated constraint, exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **D. Destroy recoverable copies before retaining the Validate end-to-end failure handling negative assertion 'Infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall'. Before sign-off, rely on the claim that remaining command logs are sufficient recovery evidence.** — The checkpoint specifically records that infrastructure success with a missing notification, duplicate route, unresolved dead letter, or broken correlation must fail overall. Under the stated constraint, irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/service-bus-messaging/compare-messaging-services (verified 2026-09-02)
<!-- END GENERATED AZ305 V1 -->
