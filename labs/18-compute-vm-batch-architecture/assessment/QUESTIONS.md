<!-- BEGIN GENERATED AZ305 V1 -->
# LAB-18 learner assessment

Choose one answer for each item. This page intentionally contains no answers or explanations.

## LAB18-Q01 — foundational

A recommendation on 'Translate workload demand into compute requirements' is requested by aerospace simulation lead. Approval requires a positive result plus this independent negative assertion: Selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail. Which choice should be approved as the acceptance rule that makes LAB18-REQ-01 testable?

- A. Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Translate workload demand into compute requirements; for the final assessment, proceed on the belief that a successful deployment will later prove the architecture constraint.
- B. Require the documented positive state for Translate workload demand into compute requirements; in a separate step, verify that selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail.
- C. Use the passing result from Compile and lint the compute Bicep design to approve Translate workload demand into compute requirements. Then, treat as decisive the assertion that one control establishes an unrelated acceptance boundary.
- D. Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Translate workload demand into compute requirements negative assertion; afterward, base approval on the claim that the candidate has the lowest implementation effort.

## LAB18-Q02 — foundational

'Translate workload demand into compute requirements' is assigned to compute platform owner. The selected architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is not success. What should the team use as the intended successful finding?

- A. Record the requirement matrix captures instruction set, vCPU, memory, scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity needs; next, classify it as success for LAB18-REQ-01.
- B. Use only the negative assertion 'Selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail' as the success result. Also, use as justification the claim that absence proves every required positive property.
- C. Use the successful finding from Preview the tagged reference deployment as the result for Translate workload demand into compute requirements; in a separate step, accept without proof that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'A technically supported SKU can miss the batch window or be unavailable at the required quota' as a successful state. As another control, rely on the claim that the command returned an object.

## LAB18-Q03 — foundational

An assurance review of 'Translate workload demand into compute requirements' includes security engineering lead. Evidence must address this risk without retaining credentials: A technically supported SKU can miss the batch window or be unavailable at the required quota. Which option best establishes sufficient, properly scoped evidence?

- A. Substitute the evidence from Deploy and verify the compute reference for Translate workload demand into compute requirements; before approval, use the premise that a related checkpoint proves the current expected state.
- B. Store unredacted Translate workload demand into compute requirements output with operator, tenant, token, and request context. Separately, consider it sufficient that reproduction requires every captured field.
- C. Record only the Translate workload demand into compute requirements positive inspection's exit status; for this decision, take it as conclusive that projected properties and assertion results can be reconstructed later.
- D. Retain save the demand model, SKU capability output, benchmark assumption, and normal-versus-peak capacity calculation; for this decision, exclude credentials and unrelated response fields.

## LAB18-Q04 — applied

Approval of 'Translate workload demand into compute requirements' is questioned by finOps analyst. The target is The requirement matrix captures instruction set, vCPU, memory, scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity needs, but the latest evidence does not show it. Which answer identifies the most likely cause?

- A. Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together' as grounds to reject Translate workload demand into compute requirements; afterward, rely on the belief that validate Batch behavior and graceful degradation's failure model applies unchanged here.
- B. Ignore the negative assertion 'Selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail'; then proceed on the belief that a later material change will make it unnecessary.
- C. Investigate a technically supported SKU can miss the batch window or be unavailable at the required quota. Then, isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.
- D. Investigate Compile and lint the compute Bicep design instead of diagnosing Translate workload demand into compute requirements. Independently, treat as decisive the assertion that a passing result at Compile and lint the compute Bicep design identifies the current cause.

## LAB18-Q05 — applied

The implementation review has reached 'Translate workload demand into compute requirements'. The run encountered this modeled failure: A technically supported SKU can miss the batch window or be unavailable at the required quota. What recommendation gives the reviewers the safest recovery action?

- A. Perform cleanup immediately: Remove local SKU exports; this checkpoint creates no compute or quota request. As another control, treat it as established that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'A technically supported SKU can miss the batch window or be unavailable at the required quota'; as a separate check, use as justification the claim that the first state record and returned identifiers can be discarded.
- C. Change Compile and lint the compute Bicep design instead. Afterward, accept without proof that success at Compile and lint the compute Bicep design will repair the failed state at Translate workload demand into compute requirements.
- D. Substitute the approved fallback SKU, rerun the throughput model, and update the cost comparison. Next, preserve the current run identity and evidence.

## LAB18-Q06 — applied

The approach to 'Translate workload demand into compute requirements' is challenged by aerospace simulation lead. Without making a new change, the team must inspect the risk 'A technically supported SKU can miss the batch window or be unavailable at the required quota' using the Azure CLI + Bicep lane. Which action produces the read-only, lane-correct inspection?

- A. Rerun the Translate workload demand into compute requirements implementation command and infer the expected state; for this decision, base approval on the claim that absence of a shell error proves every property.
- B. Run only this negative inspection for Translate workload demand into compute requirements: Selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail. Before sign-off, use the premise that an empty negative result reports every required positive property.
- C. Run the positive inspection for Preview the tagged reference deployment and apply it to Translate workload demand into compute requirements; as an independent condition, consider it sufficient that any command from the same lane proves the current checkpoint.
- D. Inspect the documented properties for Translate workload demand into compute requirements. As another control, retain this evidence: save the demand model, SKU capability output, benchmark assumption, and normal-versus-peak capacity calculation.

## LAB18-Q07 — applied

A decision test for 'Translate workload demand into compute requirements' includes compute platform owner. A passing positive check does not by itself prove this negative assertion: Selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail. What best demonstrates the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Translate workload demand into compute requirements and report full compliance. Independently, rely on the claim that every prohibited parallel state must therefore be absent.
- B. Verify the positive properties for Translate workload demand into compute requirements. In addition, independently verify that selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail.
- C. Prove only that selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail and report the intended configuration as present. Next, rely on the belief that absence is equivalent to positive-state evidence.
- D. Use Deploy and verify the compute reference's negative assertion for Translate workload demand into compute requirements, and then proceed on the belief that negative assertions are interchangeable between checkpoints.

## LAB18-Q08 — applied

The architecture board reconsiders 'Translate workload demand into compute requirements' with security engineering lead. The board wants the Well-Architected consequence of mitigating this risk: A technically supported SKU can miss the batch window or be unavailable at the required quota. Which option is the consequence attributable to this checkpoint?

- A. Use the Validate Batch behavior and graceful degradation consequence as the result for Translate workload demand into compute requirements. Afterward, take it as conclusive that a pillar statement remains valid when moved away from Validate Batch behavior and graceful degradation.
- B. Remove the control responsible for the Translate workload demand into compute requirements outcome; next, treat it as established that a moderate cost classification outweighs the mandatory architecture state.
- C. Treat 'Performance Efficiency: requirements drive heterogeneous sizing instead of one oversized worker profile' as proof that all five pillars pass. In addition, use as justification the claim that the checkpoint 'Translate workload demand into compute requirements' no longer needs its separate negative check.
- D. Record this consequence: Performance Efficiency: requirements drive heterogeneous sizing instead of one oversized worker profile. Before sign-off, tie it to LAB18-REQ-01.

## LAB18-Q09 — advanced

A review of 'Translate workload demand into compute requirements' begins with input from finOps analyst. A material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the processing window is shortened from eight hours to five. What should the architect select as the correct revision to the decision record?

- A. Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Translate workload demand into compute requirements; afterward, supersede the ADR using the changed evidence for LAB18-REQ-01.
- B. Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Translate workload demand into compute requirements without recalculating criteria or eligibility; as an independent condition, treat as decisive the assertion that the original weighted result is permanent.
- C. Select Flexible Virtual Machine Scale Sets running custom queue workers for Translate workload demand into compute requirements without rechecking its mandatory constraints; before approval, base approval on the claim that being different from the current design is an architecture criterion.
- D. Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Translate workload demand into compute requirements by downgrading LAB18-REQ-01. Then, use the premise that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB18-Q10 — advanced

'Translate workload demand into compute requirements' awaits approval from operations manager. After a partial run, cleanup must follow this dependency: Remove local SKU exports; this checkpoint creates no compute or quota request. Select the dependency-safe cleanup plan.

- A. Apply the cleanup rule for Preview the tagged reference deployment before reconciling the current dependency, and then accept without proof that removing a parent needed to identify Translate workload demand into compute requirements is harmless.
- B. Delete candidates by display name before comparing the Translate workload demand into compute requirements ownership tags. Also, rely on the claim that the dependency rule 'Remove local SKU exports; this checkpoint creates no compute or quota request' is optional.
- C. Destroy recoverable copies before retaining the Translate workload demand into compute requirements negative assertion 'Selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail'; in a separate step, rely on the belief that remaining command logs are sufficient recovery evidence.
- D. Verify exact run-state IDs and ownership tags for Translate workload demand into compute requirements, and then follow this dependency rule without purge: Remove local SKU exports; this checkpoint creates no compute or quota request.

## LAB18-Q11 — foundational

'Compile and lint the compute Bicep design' is reopened at the request of compute platform owner. Approval requires a positive result plus this independent negative assertion: An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review. What should the team use as the acceptance rule that makes LAB18-REQ-02 testable?

- A. Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Compile and lint the compute Bicep design; afterward, use as justification the claim that a successful deployment will later prove the architecture constraint.
- B. Require the documented positive state for Compile and lint the compute Bicep design. Afterward, verify that an unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review.
- C. Use the passing result from Translate workload demand into compute requirements to approve Compile and lint the compute Bicep design; then accept without proof that one control establishes an unrelated acceptance boundary.
- D. Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Compile and lint the compute Bicep design negative assertion. Independently, rely on the claim that the candidate has the lowest implementation effort.

## LAB18-Q12 — foundational

A design review of 'Compile and lint the compute Bicep design' includes security engineering lead. The selected architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is not success. Which option best establishes the intended successful finding?

- A. Use only the negative assertion 'An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review' as the success result. As another control, use the premise that absence proves every required positive property.
- B. Use the successful finding from Preview the tagged reference deployment as the result for Compile and lint the compute Bicep design; as a separate check, consider it sufficient that a property from the current checkpoint does not need to be inspected.
- C. Record compilation succeeds and the template contains a tagged, parameterized, three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no embedded secret. Separately, classify it as success for LAB18-REQ-02.
- D. Record the failure condition 'A template can compile while violating security, ownership, or lifecycle requirements' as a successful state. Afterward, take it as conclusive that the command returned an object.

## LAB18-Q13 — foundational

The team asks finOps analyst to assess 'Compile and lint the compute Bicep design'. Evidence must address this risk without retaining credentials: A template can compile while violating security, ownership, or lifecycle requirements. Which answer identifies sufficient, properly scoped evidence?

- A. Substitute the evidence from Deploy and verify the compute reference for Compile and lint the compute Bicep design; for this decision, rely on the belief that a related checkpoint proves the current expected state.
- B. Store unredacted Compile and lint the compute Bicep design output with operator, tenant, token, and request context. Before sign-off, proceed on the belief that reproduction requires every captured field.
- C. Retain preserve the Bicep source hash, compiler output, parameter review, and architecture traceability record; as another gate, exclude credentials and unrelated response fields.
- D. Record only the Compile and lint the compute Bicep design positive inspection's exit status; as an independent condition, treat as decisive the assertion that projected properties and assertion results can be reconstructed later.

## LAB18-Q14 — applied

A recommendation on 'Compile and lint the compute Bicep design' is requested by operations manager. The target is Compilation succeeds and the template contains a tagged, parameterized, three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no embedded secret, but the latest evidence does not show it. What recommendation gives the reviewers the most likely cause?

- A. Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together' as grounds to reject Compile and lint the compute Bicep design. Independently, treat it as established that validate Batch behavior and graceful degradation's failure model applies unchanged here.
- B. Ignore the negative assertion 'An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review'. Next, use as justification the claim that a later material change will make it unnecessary.
- C. Investigate Translate workload demand into compute requirements instead of diagnosing Compile and lint the compute Bicep design, and then accept without proof that a passing result at Translate workload demand into compute requirements identifies the current cause.
- D. Investigate a template can compile while violating security, ownership, or lifecycle requirements. Independently, isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.

## LAB18-Q15 — applied

'Compile and lint the compute Bicep design' is assigned to aerospace simulation lead. The run encountered this modeled failure: A template can compile while violating security, ownership, or lifecycle requirements. Which action produces the safest recovery action?

- A. Perform cleanup immediately: Delete generated local JSON if retained; keep the authored Bicep source. Afterward, base approval on the claim that the failed operation and its returned identifiers do not need reconciliation.
- B. Correct the smallest failing module and rerun compilation plus semantic assertions before deployment preview; in a separate step, preserve the current run identity and evidence.
- C. Create a different run identity before diagnosing 'A template can compile while violating security, ownership, or lifecycle requirements'; next, use the premise that the first state record and returned identifiers can be discarded.
- D. Change Translate workload demand into compute requirements instead. In addition, consider it sufficient that success at Translate workload demand into compute requirements will repair the failed state at Compile and lint the compute Bicep design.

## LAB18-Q16 — applied

An assurance review of 'Compile and lint the compute Bicep design' includes compute platform owner. Without making a new change, the team must inspect the risk 'A template can compile while violating security, ownership, or lifecycle requirements' using the Azure CLI + Bicep lane. What best demonstrates the read-only, lane-correct inspection?

- A. Rerun the Compile and lint the compute Bicep design implementation command and infer the expected state; as an independent condition, rely on the claim that absence of a shell error proves every property.
- B. Run only this negative inspection for Compile and lint the compute Bicep design: An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review; as a second control, rely on the belief that an empty negative result reports every required positive property.
- C. Run the positive inspection for Preview the tagged reference deployment and apply it to Compile and lint the compute Bicep design. Then, proceed on the belief that any command from the same lane proves the current checkpoint.
- D. Inspect the documented properties for Compile and lint the compute Bicep design; next, retain this evidence: preserve the Bicep source hash, compiler output, parameter review, and architecture traceability record.

## LAB18-Q17 — applied

Approval of 'Compile and lint the compute Bicep design' is questioned by security engineering lead. A passing positive check does not by itself prove this negative assertion: An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review. Which option is the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Compile and lint the compute Bicep design and report full compliance, and then take it as conclusive that every prohibited parallel state must therefore be absent.
- B. Prove only that an unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review and report the intended configuration as present. Also, treat it as established that absence is equivalent to positive-state evidence.
- C. Use Deploy and verify the compute reference's negative assertion for Compile and lint the compute Bicep design; in a separate step, use as justification the claim that negative assertions are interchangeable between checkpoints.
- D. Verify the positive properties for Compile and lint the compute Bicep design; for this decision, independently verify that an unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review.

## LAB18-Q18 — applied

The implementation review has reached 'Compile and lint the compute Bicep design'. The board wants the Well-Architected consequence of mitigating this risk: A template can compile while violating security, ownership, or lifecycle requirements. What should the architect select as the consequence attributable to this checkpoint?

- A. Use the Validate Batch behavior and graceful degradation consequence as the result for Compile and lint the compute Bicep design. In addition, treat as decisive the assertion that a pillar statement remains valid when moved away from Validate Batch behavior and graceful degradation.
- B. Record this consequence: Security: managed identity, restricted ingress, and secret-free templates reduce control-plane exposure. Then, tie it to LAB18-REQ-02.
- C. Remove the control responsible for the Compile and lint the compute Bicep design outcome; before approval, base approval on the claim that a moderate cost classification outweighs the mandatory architecture state.
- D. Treat 'Security: managed identity, restricted ingress, and secret-free templates reduce control-plane exposure' as proof that all five pillars pass. Separately, use the premise that the checkpoint 'Compile and lint the compute Bicep design' no longer needs its separate negative check.

## LAB18-Q19 — advanced

The approach to 'Compile and lint the compute Bicep design' is challenged by operations manager. A material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the processing window is shortened from eight hours to five. Select the correct revision to the decision record.

- A. Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Compile and lint the compute Bicep design without recalculating criteria or eligibility. Then, accept without proof that the original weighted result is permanent.
- B. Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Compile and lint the compute Bicep design. Next, supersede the ADR using the changed evidence for LAB18-REQ-02.
- C. Select Flexible Virtual Machine Scale Sets running custom queue workers for Compile and lint the compute Bicep design without rechecking its mandatory constraints; afterward, rely on the claim that being different from the current design is an architecture criterion.
- D. Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Compile and lint the compute Bicep design by downgrading LAB18-REQ-02; then rely on the belief that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB18-Q20 — advanced

A decision test for 'Compile and lint the compute Bicep design' includes aerospace simulation lead. After a partial run, cleanup must follow this dependency: Delete generated local JSON if retained; keep the authored Bicep source. Which response provides the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Preview the tagged reference deployment before reconciling the current dependency; in a separate step, consider it sufficient that removing a parent needed to identify Compile and lint the compute Bicep design is harmless.
- B. Verify exact run-state IDs and ownership tags for Compile and lint the compute Bicep design. As another control, follow this dependency rule without purge: Delete generated local JSON if retained; keep the authored Bicep source.
- C. Delete candidates by display name before comparing the Compile and lint the compute Bicep design ownership tags. As another control, take it as conclusive that the dependency rule 'Delete generated local JSON if retained; keep the authored Bicep source' is optional.
- D. Destroy recoverable copies before retaining the Compile and lint the compute Bicep design negative assertion 'An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must fail lint review'; as a separate check, treat it as established that remaining command logs are sufficient recovery evidence.

## LAB18-Q21 — foundational

The architecture board reconsiders 'Preview the tagged reference deployment' with security engineering lead. Approval requires a positive result plus this independent negative assertion: Any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution. Which option best establishes the acceptance rule that makes LAB18-REQ-03 testable?

- A. Require the documented positive state for Preview the tagged reference deployment; before approval, verify that any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution.
- B. Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Preview the tagged reference deployment. Independently, use the premise that a successful deployment will later prove the architecture constraint.
- C. Use the passing result from Translate workload demand into compute requirements to approve Preview the tagged reference deployment. Next, consider it sufficient that one control establishes an unrelated acceptance boundary.
- D. Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Preview the tagged reference deployment negative assertion, and then take it as conclusive that the candidate has the lowest implementation effort.

## LAB18-Q22 — foundational

A review of 'Preview the tagged reference deployment' begins with input from finOps analyst. The selected architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is not success. Which answer identifies the intended successful finding?

- A. Use only the negative assertion 'Any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution' as the success result. Afterward, rely on the belief that absence proves every required positive property.
- B. Use the successful finding from Compile and lint the compute Bicep design as the result for Preview the tagged reference deployment; next, proceed on the belief that a property from the current checkpoint does not need to be inspected.
- C. Record what-if contains only new run-owned resources, zero control and worker capacity, no public IP allocation, and the required ownership and expiry tags; as an independent condition, classify it as success for LAB18-REQ-03.
- D. Record the failure condition 'Parameter drift can expand cost or affect a pre-existing resource despite a valid template' as a successful state. In addition, treat as decisive the assertion that the command returned an object.

## LAB18-Q23 — foundational

'Preview the tagged reference deployment' awaits approval from operations manager. Evidence must address this risk without retaining credentials: Parameter drift can expand cost or affect a pre-existing resource despite a valid template. What recommendation gives the reviewers sufficient, properly scoped evidence?

- A. Substitute the evidence from Deploy and verify the compute reference for Preview the tagged reference deployment; as an independent condition, treat it as established that a related checkpoint proves the current expected state.
- B. Store unredacted Preview the tagged reference deployment output with operator, tenant, token, and request context; during the same review, use as justification the claim that reproduction requires every captured field.
- C. Retain archive the complete what-if result, reviewer decision, estimated hourly ceiling, and template and parameter hashes; then exclude credentials and unrelated response fields.
- D. Record only the Preview the tagged reference deployment positive inspection's exit status. Then, accept without proof that projected properties and assertion results can be reconstructed later.

## LAB18-Q24 — applied

'Preview the tagged reference deployment' is reopened at the request of aerospace simulation lead. The target is What-if contains only new run-owned resources, zero control and worker capacity, no public IP allocation, and the required ownership and expiry tags, but the latest evidence does not show it. Which action produces the most likely cause?

- A. Investigate parameter drift can expand cost or affect a pre-existing resource despite a valid template. Also, isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.
- B. Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together' as grounds to reject Preview the tagged reference deployment, and then base approval on the claim that validate Batch behavior and graceful degradation's failure model applies unchanged here.
- C. Ignore the negative assertion 'Any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution'. Also, use the premise that a later material change will make it unnecessary.
- D. Investigate Translate workload demand into compute requirements instead of diagnosing Preview the tagged reference deployment; in a separate step, consider it sufficient that a passing result at Translate workload demand into compute requirements identifies the current cause.

## LAB18-Q25 — applied

A design review of 'Preview the tagged reference deployment' includes compute platform owner. The run encountered this modeled failure: Parameter drift can expand cost or affect a pre-existing resource despite a valid template. What best demonstrates the safest recovery action?

- A. Perform cleanup immediately: Remove preview output if it contains sensitive topology; what-if itself creates no resource. In addition, rely on the claim that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'Parameter drift can expand cost or affect a pre-existing resource despite a valid template'; before approval, rely on the belief that the first state record and returned identifiers can be discarded.
- C. Change Translate workload demand into compute requirements instead. Separately, proceed on the belief that success at Translate workload demand into compute requirements will repair the failed state at Preview the tagged reference deployment.
- D. Correct parameters, regenerate state before any mutation, and obtain a fresh reviewed what-if result. Afterward, preserve the current run identity and evidence.

## LAB18-Q26 — applied

The team asks security engineering lead to assess 'Preview the tagged reference deployment'. Without making a new change, the team must inspect the risk 'Parameter drift can expand cost or affect a pre-existing resource despite a valid template' using the Azure CLI + Bicep lane. Which option is the read-only, lane-correct inspection?

- A. Inspect the documented properties for Preview the tagged reference deployment. Separately, retain this evidence: archive the complete what-if result, reviewer decision, estimated hourly ceiling, and template and parameter hashes.
- B. Rerun the Preview the tagged reference deployment implementation command and infer the expected state. Then, take it as conclusive that absence of a shell error proves every property.
- C. Run only this negative inspection for Preview the tagged reference deployment: Any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution; afterward, treat it as established that an empty negative result reports every required positive property.
- D. Run the positive inspection for Compile and lint the compute Bicep design and apply it to Preview the tagged reference deployment; then use as justification the claim that any command from the same lane proves the current checkpoint.

## LAB18-Q27 — applied

A recommendation on 'Preview the tagged reference deployment' is requested by finOps analyst. A passing positive check does not by itself prove this negative assertion: Any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution. What should the architect select as the assertion pair that proves both conditions independently?

- A. Verify the positive properties for Preview the tagged reference deployment; for the final assessment, independently verify that any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution.
- B. Verify only the positive result for Preview the tagged reference deployment and report full compliance; in a separate step, treat as decisive the assertion that every prohibited parallel state must therefore be absent.
- C. Prove only that any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution and report the intended configuration as present. As another control, base approval on the claim that absence is equivalent to positive-state evidence.
- D. Use Deploy and verify the compute reference's negative assertion for Preview the tagged reference deployment; as a separate check, use the premise that negative assertions are interchangeable between checkpoints.

## LAB18-Q28 — applied

'Preview the tagged reference deployment' is assigned to operations manager. The board wants the Well-Architected consequence of mitigating this risk: Parameter drift can expand cost or affect a pre-existing resource despite a valid template. Select the consequence attributable to this checkpoint.

- A. Use the Validate Batch behavior and graceful degradation consequence as the result for Preview the tagged reference deployment. Separately, accept without proof that a pillar statement remains valid when moved away from Validate Batch behavior and graceful degradation.
- B. Record this consequence: Cost Optimization: bounded autoscale and reviewed preview make the maximum spend visible before deployment. Independently, tie it to LAB18-REQ-03.
- C. Remove the control responsible for the Preview the tagged reference deployment outcome; for this decision, rely on the claim that a moderate cost classification outweighs the mandatory architecture state.
- D. Treat 'Cost Optimization: bounded autoscale and reviewed preview make the maximum spend visible before deployment' as proof that all five pillars pass. Before sign-off, rely on the belief that the checkpoint 'Preview the tagged reference deployment' no longer needs its separate negative check.

## LAB18-Q29 — advanced

An assurance review of 'Preview the tagged reference deployment' includes aerospace simulation lead. A material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the processing window is shortened from eight hours to five. Which response provides the correct revision to the decision record?

- A. Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Preview the tagged reference deployment without recalculating criteria or eligibility; then consider it sufficient that the original weighted result is permanent.
- B. Select Flexible Virtual Machine Scale Sets running custom queue workers for Preview the tagged reference deployment without rechecking its mandatory constraints. Independently, take it as conclusive that being different from the current design is an architecture criterion.
- C. Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Preview the tagged reference deployment; in a separate step, supersede the ADR using the changed evidence for LAB18-REQ-03.
- D. Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Preview the tagged reference deployment by downgrading LAB18-REQ-03. Next, treat it as established that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB18-Q30 — advanced

Approval of 'Preview the tagged reference deployment' is questioned by compute platform owner. After a partial run, cleanup must follow this dependency: Remove preview output if it contains sensitive topology; what-if itself creates no resource. Which choice gives the team the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Compile and lint the compute Bicep design before reconciling the current dependency; as a separate check, proceed on the belief that removing a parent needed to identify Preview the tagged reference deployment is harmless.
- B. Verify exact run-state IDs and ownership tags for Preview the tagged reference deployment; next, follow this dependency rule without purge: Remove preview output if it contains sensitive topology; what-if itself creates no resource.
- C. Delete candidates by display name before comparing the Preview the tagged reference deployment ownership tags. Afterward, treat as decisive the assertion that the dependency rule 'Remove preview output if it contains sensitive topology; what-if itself creates no resource' is optional.
- D. Destroy recoverable copies before retaining the Preview the tagged reference deployment negative assertion 'Any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution'; next, base approval on the claim that remaining command logs are sufficient recovery evidence.

## LAB18-Q31 — foundational

The implementation review has reached 'Deploy and verify the compute reference'. Approval requires a positive result plus this independent negative assertion: Missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe. Which answer identifies the acceptance rule that makes LAB18-REQ-04 testable?

- A. Require the documented positive state for Deploy and verify the compute reference. Before sign-off, verify that missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe.
- B. Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Deploy and verify the compute reference, and then rely on the belief that a successful deployment will later prove the architecture constraint.
- C. Use the passing result from Translate workload demand into compute requirements to approve Deploy and verify the compute reference. Also, proceed on the belief that one control establishes an unrelated acceptance boundary.
- D. Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Deploy and verify the compute reference negative assertion; in a separate step, treat as decisive the assertion that the candidate has the lowest implementation effort.

## LAB18-Q32 — foundational

The approach to 'Deploy and verify the compute reference' is challenged by operations manager. The selected architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is not success. What recommendation gives the reviewers the intended successful finding?

- A. Use only the negative assertion 'Missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe' as the success result. In addition, treat it as established that absence proves every required positive property.
- B. Record the bounded reference footprint deploys only after execution and cost acknowledgements, and returned IDs are persisted immediately in run state; afterward, classify it as success for LAB18-REQ-04.
- C. Use the successful finding from Compile and lint the compute Bicep design as the result for Deploy and verify the compute reference; before approval, use as justification the claim that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'Partial deployment can leave chargeable workers or dependencies even when the top-level operation fails' as a successful state. Separately, accept without proof that the command returned an object.

## LAB18-Q33 — foundational

A decision test for 'Deploy and verify the compute reference' includes aerospace simulation lead. Evidence must address this risk without retaining credentials: Partial deployment can leave chargeable workers or dependencies even when the top-level operation fails. Which action produces sufficient, properly scoped evidence?

- A. Substitute the evidence from Preview the tagged reference deployment for Deploy and verify the compute reference. Then, base approval on the claim that a related checkpoint proves the current expected state.
- B. Retain preserve deployment operations, schema-valid run state, exact resource IDs, effective tags, and assertion outcomes, and then exclude credentials and unrelated response fields.
- C. Store unredacted Deploy and verify the compute reference output with operator, tenant, token, and request context; afterward, use the premise that reproduction requires every captured field.
- D. Record only the Deploy and verify the compute reference positive inspection's exit status; then consider it sufficient that projected properties and assertion results can be reconstructed later.

## LAB18-Q34 — applied

The architecture board reconsiders 'Deploy and verify the compute reference' with compute platform owner. The target is The bounded reference footprint deploys only after execution and cost acknowledgements, and returned IDs are persisted immediately in run state, but the latest evidence does not show it. What best demonstrates the most likely cause?

- A. Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together' as grounds to reject Deploy and verify the compute reference; in a separate step, rely on the claim that validate Batch behavior and graceful degradation's failure model applies unchanged here.
- B. Ignore the negative assertion 'Missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe'. As another control, rely on the belief that a later material change will make it unnecessary.
- C. Investigate partial deployment can leave chargeable workers or dependencies even when the top-level operation fails; as a separate check, isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.
- D. Investigate Translate workload demand into compute requirements instead of diagnosing Deploy and verify the compute reference; as a separate check, proceed on the belief that a passing result at Translate workload demand into compute requirements identifies the current cause.

## LAB18-Q35 — applied

A review of 'Deploy and verify the compute reference' begins with input from security engineering lead. The run encountered this modeled failure: Partial deployment can leave chargeable workers or dependencies even when the top-level operation fails. Which option is the safest recovery action?

- A. Perform cleanup immediately: Delete only exact run-state IDs whose purpose, labId, runId, and expiresOn tags all match; follow reverse dependency order. Separately, take it as conclusive that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'Partial deployment can leave chargeable workers or dependencies even when the top-level operation fails'; for this decision, treat it as established that the first state record and returned identifiers can be discarded.
- C. Change Translate workload demand into compute requirements instead. Before sign-off, use as justification the claim that success at Translate workload demand into compute requirements will repair the failed state at Deploy and verify the compute reference.
- D. Reconcile deployment operations with run state, correct the failure, and redeploy idempotently with the same RunId; before approval, preserve the current run identity and evidence.

## LAB18-Q36 — applied

'Deploy and verify the compute reference' awaits approval from finOps analyst. Without making a new change, the team must inspect the risk 'Partial deployment can leave chargeable workers or dependencies even when the top-level operation fails' using the Azure CLI + Bicep lane. What should the architect select as the read-only, lane-correct inspection?

- A. Rerun the Deploy and verify the compute reference implementation command and infer the expected state; then treat as decisive the assertion that absence of a shell error proves every property.
- B. Inspect the documented properties for Deploy and verify the compute reference; as an independent condition, retain this evidence: preserve deployment operations, schema-valid run state, exact resource IDs, effective tags, and assertion outcomes.
- C. Run only this negative inspection for Deploy and verify the compute reference: Missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe. Independently, base approval on the claim that an empty negative result reports every required positive property.
- D. Run the positive inspection for Compile and lint the compute Bicep design and apply it to Deploy and verify the compute reference. Next, use the premise that any command from the same lane proves the current checkpoint.

## LAB18-Q37 — applied

'Deploy and verify the compute reference' is reopened at the request of operations manager. A passing positive check does not by itself prove this negative assertion: Missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe. Select the assertion pair that proves both conditions independently.

- A. Verify only the positive result for Deploy and verify the compute reference and report full compliance; as a separate check, accept without proof that every prohibited parallel state must therefore be absent.
- B. Prove only that missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe and report the intended configuration as present. Afterward, rely on the claim that absence is equivalent to positive-state evidence.
- C. Verify the positive properties for Deploy and verify the compute reference; then independently verify that missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe.
- D. Use Preview the tagged reference deployment's negative assertion for Deploy and verify the compute reference; next, rely on the belief that negative assertions are interchangeable between checkpoints.

## LAB18-Q38 — applied

A design review of 'Deploy and verify the compute reference' includes aerospace simulation lead. The board wants the Well-Architected consequence of mitigating this risk: Partial deployment can leave chargeable workers or dependencies even when the top-level operation fails. Which response provides the consequence attributable to this checkpoint?

- A. Record this consequence: Operational Excellence: declarative deployment and immediate state capture make partial failure recoverable. Also, tie it to LAB18-REQ-04.
- B. Use the Validate Batch behavior and graceful degradation consequence as the result for Deploy and verify the compute reference. Before sign-off, consider it sufficient that a pillar statement remains valid when moved away from Validate Batch behavior and graceful degradation.
- C. Remove the control responsible for the Deploy and verify the compute reference outcome; as an independent condition, take it as conclusive that a moderate cost classification outweighs the mandatory architecture state.
- D. Treat 'Operational Excellence: declarative deployment and immediate state capture make partial failure recoverable' as proof that all five pillars pass; before closing the checkpoint, treat it as established that the checkpoint 'Deploy and verify the compute reference' no longer needs its separate negative check.

## LAB18-Q39 — advanced

The team asks compute platform owner to assess 'Deploy and verify the compute reference'. A material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the processing window is shortened from eight hours to five. Which choice gives the team the correct revision to the decision record?

- A. Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Deploy and verify the compute reference without recalculating criteria or eligibility. Next, proceed on the belief that the original weighted result is permanent.
- B. Select Flexible Virtual Machine Scale Sets running custom queue workers for Deploy and verify the compute reference without rechecking its mandatory constraints, and then treat as decisive the assertion that being different from the current design is an architecture criterion.
- C. Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Deploy and verify the compute reference. Afterward, supersede the ADR using the changed evidence for LAB18-REQ-04.
- D. Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Deploy and verify the compute reference by downgrading LAB18-REQ-04. Also, base approval on the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB18-Q40 — advanced

A recommendation on 'Deploy and verify the compute reference' is requested by security engineering lead. After a partial run, cleanup must follow this dependency: Delete only exact run-state IDs whose purpose, labId, runId, and expiresOn tags all match; follow reverse dependency order. What is the dependency-safe cleanup plan?

- A. Verify exact run-state IDs and ownership tags for Deploy and verify the compute reference. Separately, follow this dependency rule without purge: Delete only exact run-state IDs whose purpose, labId, runId, and expiresOn tags all match; follow reverse dependency order.
- B. Apply the cleanup rule for Compile and lint the compute Bicep design before reconciling the current dependency; next, use as justification the claim that removing a parent needed to identify Deploy and verify the compute reference is harmless.
- C. Delete candidates by display name before comparing the Deploy and verify the compute reference ownership tags. In addition, accept without proof that the dependency rule 'Delete only exact run-state IDs whose purpose, labId, runId, and expiresOn tags all match; follow reverse dependency order' is optional.
- D. Destroy recoverable copies before retaining the Deploy and verify the compute reference negative assertion 'Missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe'; before approval, rely on the claim that remaining command logs are sufficient recovery evidence.

## LAB18-Q41 — foundational

'Validate Batch behavior and graceful degradation' is assigned to operations manager. Approval requires a positive result plus this independent negative assertion: A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review. What recommendation gives the reviewers the acceptance rule that makes LAB18-REQ-05 testable?

- A. Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Validate Batch behavior and graceful degradation; in a separate step, treat it as established that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Translate workload demand into compute requirements to approve Validate Batch behavior and graceful degradation. As another control, use as justification the claim that one control establishes an unrelated acceptance boundary.
- C. Require the documented positive state for Validate Batch behavior and graceful degradation. Then, verify that a nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review.
- D. Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Validate Batch behavior and graceful degradation negative assertion; as a separate check, accept without proof that the candidate has the lowest implementation effort.

## LAB18-Q42 — foundational

An assurance review of 'Validate Batch behavior and graceful degradation' includes aerospace simulation lead. The selected architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is not success. Which action produces the intended successful finding?

- A. Record the exact pool emitted by the deployment remains at zero nodes, uses no public node IPs, and records the image, VM size, subnet, and later scale prerequisites without running a job. Next, classify it as success for LAB18-REQ-05.
- B. Use only the negative assertion 'A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review' as the success result. Separately, base approval on the claim that absence proves every required positive property.
- C. Use the successful finding from Compile and lint the compute Bicep design as the result for Validate Batch behavior and graceful degradation; for this decision, use the premise that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together' as a successful state. Before sign-off, consider it sufficient that the command returned an object.

## LAB18-Q43 — foundational

Approval of 'Validate Batch behavior and graceful degradation' is questioned by compute platform owner. Evidence must address this risk without retaining credentials: A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together. What best demonstrates sufficient, properly scoped evidence?

- A. Retain store the exact pool ARM ID, zero-node scale settings, image and node-agent pair, subnet, public-IP mode, and documented outbound prerequisite. As another control, exclude credentials and unrelated response fields.
- B. Substitute the evidence from Preview the tagged reference deployment for Validate Batch behavior and graceful degradation; then rely on the claim that a related checkpoint proves the current expected state.
- C. Store unredacted Validate Batch behavior and graceful degradation output with operator, tenant, token, and request context. Independently, rely on the belief that reproduction requires every captured field.
- D. Record only the Validate Batch behavior and graceful degradation positive inspection's exit status. Next, proceed on the belief that projected properties and assertion results can be reconstructed later.

## LAB18-Q44 — applied

The implementation review has reached 'Validate Batch behavior and graceful degradation'. The target is The exact pool emitted by the deployment remains at zero nodes, uses no public node IPs, and records the image, VM size, subnet, and later scale prerequisites without running a job, but the latest evidence does not show it. Which option is the most likely cause?

- A. Investigate a syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together. In addition, isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.
- B. Treat 'Partial deployment can leave chargeable workers or dependencies even when the top-level operation fails' as grounds to reject Validate Batch behavior and graceful degradation; as a separate check, take it as conclusive that deploy and verify the compute reference's failure model applies unchanged here.
- C. Ignore the negative assertion 'A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review'. Afterward, treat it as established that a later material change will make it unnecessary.
- D. Investigate Translate workload demand into compute requirements instead of diagnosing Validate Batch behavior and graceful degradation; next, use as justification the claim that a passing result at Translate workload demand into compute requirements identifies the current cause.

## LAB18-Q45 — applied

The approach to 'Validate Batch behavior and graceful degradation' is challenged by finOps analyst. The run encountered this modeled failure: A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together. What should the architect select as the safest recovery action?

- A. Perform cleanup immediately: Stop and delete the run-owned pool before other dependencies, then verify no active managed object remains. Before sign-off, treat as decisive the assertion that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together'; as an independent condition, base approval on the claim that the first state record and returned identifiers can be discarded.
- C. Restore both node targets to zero, validate the current image/SKU pair, and document explicit NAT or inspected-firewall egress before any separately authorized scale test. Before sign-off, preserve the current run identity and evidence.
- D. Change Translate workload demand into compute requirements instead; for the final assessment, use the premise that success at Translate workload demand into compute requirements will repair the failed state at Validate Batch behavior and graceful degradation.

## LAB18-Q46 — applied

A decision test for 'Validate Batch behavior and graceful degradation' includes operations manager. Without making a new change, the team must inspect the risk 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together' using the Azure CLI + Bicep lane. Select the read-only, lane-correct inspection.

- A. Rerun the Validate Batch behavior and graceful degradation implementation command and infer the expected state. Next, accept without proof that absence of a shell error proves every property.
- B. Run only this negative inspection for Validate Batch behavior and graceful degradation: A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review, and then rely on the claim that an empty negative result reports every required positive property.
- C. Inspect the documented properties for Validate Batch behavior and graceful degradation; afterward, retain this evidence: store the exact pool ARM ID, zero-node scale settings, image and node-agent pair, subnet, public-IP mode, and documented outbound prerequisite.
- D. Run the positive inspection for Compile and lint the compute Bicep design and apply it to Validate Batch behavior and graceful degradation. Also, rely on the belief that any command from the same lane proves the current checkpoint.

## LAB18-Q47 — applied

The architecture board reconsiders 'Validate Batch behavior and graceful degradation' with aerospace simulation lead. A passing positive check does not by itself prove this negative assertion: A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review. Which response provides the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Validate Batch behavior and graceful degradation and report full compliance; next, consider it sufficient that every prohibited parallel state must therefore be absent.
- B. Verify the positive properties for Validate Batch behavior and graceful degradation, and then independently verify that a nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review.
- C. Prove only that a nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review and report the intended configuration as present. In addition, take it as conclusive that absence is equivalent to positive-state evidence.
- D. Use Preview the tagged reference deployment's negative assertion for Validate Batch behavior and graceful degradation; before approval, treat it as established that negative assertions are interchangeable between checkpoints.

## LAB18-Q48 — applied

A review of 'Validate Batch behavior and graceful degradation' begins with input from compute platform owner. The board wants the Well-Architected consequence of mitigating this risk: A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together. Which choice gives the team the consequence attributable to this checkpoint?

- A. Use the Deploy and verify the compute reference consequence as the result for Validate Batch behavior and graceful degradation; separately, proceed on the belief that a pillar statement remains valid when moved away from Deploy and verify the compute reference.
- B. Remove the control responsible for the Validate Batch behavior and graceful degradation outcome. Then, treat as decisive the assertion that a moderate cost classification outweighs the mandatory architecture state.
- C. Treat 'Reliability: idempotent tasks and resilient orchestration tolerate individual worker and zone failures' as proof that all five pillars pass; afterward, base approval on the claim that the checkpoint 'Validate Batch behavior and graceful degradation' no longer needs its separate negative check.
- D. Record this consequence: Reliability: idempotent tasks and resilient orchestration tolerate individual worker and zone failures; as a separate check, tie it to LAB18-REQ-05.

## LAB18-Q49 — advanced

'Validate Batch behavior and graceful degradation' awaits approval from security engineering lead. A material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the processing window is shortened from eight hours to five. What is the correct revision to the decision record?

- A. Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Validate Batch behavior and graceful degradation; before approval, supersede the ADR using the changed evidence for LAB18-REQ-05.
- B. Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Validate Batch behavior and graceful degradation without recalculating criteria or eligibility. Also, use as justification the claim that the original weighted result is permanent.
- C. Select Flexible Virtual Machine Scale Sets running custom queue workers for Validate Batch behavior and graceful degradation without rechecking its mandatory constraints; in a separate step, accept without proof that being different from the current design is an architecture criterion.
- D. Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Validate Batch behavior and graceful degradation by downgrading LAB18-REQ-05. As another control, rely on the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB18-Q50 — advanced

'Validate Batch behavior and graceful degradation' is reopened at the request of finOps analyst. After a partial run, cleanup must follow this dependency: Stop and delete the run-owned pool before other dependencies, then verify no active managed object remains. Which recommendation supplies the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Compile and lint the compute Bicep design before reconciling the current dependency; before approval, use the premise that removing a parent needed to identify Validate Batch behavior and graceful degradation is harmless.
- B. Delete candidates by display name before comparing the Validate Batch behavior and graceful degradation ownership tags. Separately, consider it sufficient that the dependency rule 'Stop and delete the run-owned pool before other dependencies, then verify no active managed object remains' is optional.
- C. Destroy recoverable copies before retaining the Validate Batch behavior and graceful degradation negative assertion 'A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review'; for this decision, take it as conclusive that remaining command logs are sufficient recovery evidence.
- D. Verify exact run-state IDs and ownership tags for Validate Batch behavior and graceful degradation; as an independent condition, follow this dependency rule without purge: Stop and delete the run-owned pool before other dependencies, then verify no active managed object remains.
<!-- END GENERATED AZ305 V1 -->
