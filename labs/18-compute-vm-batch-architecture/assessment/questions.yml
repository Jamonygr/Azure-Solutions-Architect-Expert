# BEGIN GENERATED AZ305 V1
schemaVersion: 1.0.0
labId: LAB-18
questions:
- id: LAB18-Q01
  difficulty: foundational
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: 'A recommendation on ''Translate workload demand into compute requirements'' is requested by aerospace simulation
    lead. Approval requires a positive result plus this independent negative assertion: Selecting a familiar SKU
    before checking batch-window throughput, quota, or regional restrictions must fail. Which choice should be approved
    as the acceptance rule that makes LAB18-REQ-01 testable?'
  options:
    A: Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Translate workload
      demand into compute requirements; for the final assessment, proceed on the belief that a successful deployment
      will later prove the architecture constraint.
    B: Require the documented positive state for Translate workload demand into compute requirements; in a separate
      step, verify that selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions
      must fail.
    C: Use the passing result from Compile and lint the compute Bicep design to approve Translate workload demand
      into compute requirements. Then, treat as decisive the assertion that one control establishes an unrelated
      acceptance boundary.
    D: Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Translate
      workload demand into compute requirements negative assertion; afterward, base approval on the claim that the
      candidate has the lowest implementation effort.
  answer: B
  explanations:
    A: The controlling fact is that selecting a familiar SKU before checking batch-window throughput, quota, or
      regional restrictions must fail. That evidence means a deployment result cannot prove LAB18-REQ-01, and Flexible
      Virtual Machine Scale Sets running custom queue workers still has to meet the mandatory boundary.
    B: the requirement matrix captures instruction set, vCPU, memory, scratch space, network, image, job duration,
      concurrency, availability, quota, and data-gravity needs. That evidence means the positive state and an independent
      negative assertion jointly make LAB18-REQ-01 testable.
    C: The authored acceptance boundary states that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. That evidence means that outcome belongs to Compile and lint the compute Bicep design and
      leaves Translate workload demand into compute requirements unverified.
    D: The relevant observation is that complete simulation batches inside the overnight window while keeping orchestration
      available and paying for burst capacity only when jobs require it. That evidence means implementation effort
      cannot justify skipping the negative assertion or displace LAB18-REQ-01.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q02
  difficulty: foundational
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: '''Translate workload demand into compute requirements'' is assigned to compute platform owner. The selected
    architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is
    not success. What should the team use as the intended successful finding?'
  options:
    A: Record the requirement matrix captures instruction set, vCPU, memory, scratch space, network, image, job
      duration, concurrency, availability, quota, and data-gravity needs; next, classify it as success for LAB18-REQ-01.
    B: Use only the negative assertion 'Selecting a familiar SKU before checking batch-window throughput, quota,
      or regional restrictions must fail' as the success result. Also, use as justification the claim that absence
      proves every required positive property.
    C: Use the successful finding from Preview the tagged reference deployment as the result for Translate workload
      demand into compute requirements; in a separate step, accept without proof that a property from the current
      checkpoint does not need to be inspected.
    D: Record the failure condition 'A technically supported SKU can miss the batch window or be unavailable at
      the required quota' as a successful state. As another control, rely on the claim that the command returned
      an object.
  answer: A
  explanations:
    A: The checkpoint specifically records that the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. That evidence
      means this is the authored target state for Translate workload demand into compute requirements and directly
      supports LAB18-REQ-01.
    B: The scenario makes clear that selecting a familiar SKU before checking batch-window throughput, quota, or
      regional restrictions must fail. That evidence means this is the independent prohibited-state assertion, not
      a successful finding.
    C: The architecture evidence must show that what-if contains only new run-owned resources, zero control and
      worker capacity, no public IP allocation, and the required ownership and expiry tags. That evidence means
      evidence for Preview the tagged reference deployment cannot substitute for the properties required at Translate
      workload demand into compute requirements.
    D: The applicable design condition is that a technically supported SKU can miss the batch window or be unavailable
      at the required quota. That evidence means resource existence or command output does not convert the documented
      failure condition into success.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q03
  difficulty: foundational
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: 'An assurance review of ''Translate workload demand into compute requirements'' includes security engineering
    lead. Evidence must address this risk without retaining credentials: A technically supported SKU can miss the
    batch window or be unavailable at the required quota. Which option best establishes sufficient, properly scoped
    evidence?'
  options:
    A: Substitute the evidence from Deploy and verify the compute reference for Translate workload demand into compute
      requirements; before approval, use the premise that a related checkpoint proves the current expected state.
    B: Store unredacted Translate workload demand into compute requirements output with operator, tenant, token,
      and request context. Separately, consider it sufficient that reproduction requires every captured field.
    C: Record only the Translate workload demand into compute requirements positive inspection's exit status; for
      this decision, take it as conclusive that projected properties and assertion results can be reconstructed
      later.
    D: Retain save the demand model, SKU capability output, benchmark assumption, and normal-versus-peak capacity
      calculation; for this decision, exclude credentials and unrelated response fields.
  answer: D
  explanations:
    A: The retained result must be reconciled with the fact that preserve deployment operations, schema-valid run
      state, exact resource IDs, effective tags, and assertion outcomes. That evidence means that evidence supports
      Deploy and verify the compute reference, so it cannot demonstrate The requirement matrix captures instruction
      set, vCPU, memory, scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity
      needs.
    B: The decision tension comes from the fact that unredacted implementation output. That evidence means identity,
      tenant, or token material exceeds the non-secret evidence contract.
    C: The safe operating boundary says that the positive inspection's exit status. That evidence means an exit
      code alone does not show whether the requirement matrix captures instruction set, vCPU, memory, scratch space,
      network, image, job duration, concurrency, availability, quota, and data-gravity needs.
    D: 'The review is governed by this fact: save the demand model, SKU capability output, benchmark assumption,
      and normal-versus-peak capacity calculation. That evidence means it captures the checkpoint''s observable
      properties while keeping the evidence boundary narrow.'
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q04
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: Approval of 'Translate workload demand into compute requirements' is questioned by finOps analyst. The target
    is The requirement matrix captures instruction set, vCPU, memory, scratch space, network, image, job duration,
    concurrency, availability, quota, and data-gravity needs, but the latest evidence does not show it. Which answer
    identifies the most likely cause?
  options:
    A: Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites
      are not checked together' as grounds to reject Translate workload demand into compute requirements; afterward,
      rely on the belief that validate Batch behavior and graceful degradation's failure model applies unchanged
      here.
    B: Ignore the negative assertion 'Selecting a familiar SKU before checking batch-window throughput, quota, or
      regional restrictions must fail'; then proceed on the belief that a later material change will make it unnecessary.
    C: Investigate a technically supported SKU can miss the batch window or be unavailable at the required quota.
      Then, isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.
    D: Investigate Compile and lint the compute Bicep design instead of diagnosing Translate workload demand into
      compute requirements. Independently, treat as decisive the assertion that a passing result at Compile and
      lint the compute Bicep design identifies the current cause.
  answer: C
  explanations:
    A: The failure model establishes that a syntactically valid pool can allocate billable or unreachable nodes
      when scale and outbound prerequisites are not checked together. That evidence means that condition belongs
      to Validate Batch behavior and graceful degradation and does not by itself invalidate Zonal control virtual
      machines with an autoscaled Azure Batch pool.
    B: The recovery guidance assumes that selecting a familiar SKU before checking batch-window throughput, quota,
      or regional restrictions must fail. That evidence means the negative assertion must be evaluated now, independent
      of a later business change.
    C: The traceable checkpoint outcome is that a technically supported SKU can miss the batch window or be unavailable
      at the required quota. That evidence means it is the checkpoint's causal failure model and should be isolated
      before retrying Translate workload demand into compute requirements.
    D: The WAF consequence identifies that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. That evidence means a passing result at Compile and lint the compute Bicep design gives no
      causal evidence for the failure at Translate workload demand into compute requirements.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q05
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: 'The implementation review has reached ''Translate workload demand into compute requirements''. The run
    encountered this modeled failure: A technically supported SKU can miss the batch window or be unavailable at
    the required quota. What recommendation gives the reviewers the safest recovery action?'
  options:
    A: 'Perform cleanup immediately: Remove local SKU exports; this checkpoint creates no compute or quota request.
      As another control, treat it as established that the failed operation and its returned identifiers do not
      need reconciliation.'
    B: Create a different run identity before diagnosing 'A technically supported SKU can miss the batch window
      or be unavailable at the required quota'; as a separate check, use as justification the claim that the first
      state record and returned identifiers can be discarded.
    C: Change Compile and lint the compute Bicep design instead. Afterward, accept without proof that success at
      Compile and lint the compute Bicep design will repair the failed state at Translate workload demand into compute
      requirements.
    D: Substitute the approved fallback SKU, rerun the throughput model, and update the cost comparison. Next, preserve
      the current run identity and evidence.
  answer: D
  explanations:
    A: remove local SKU exports; this checkpoint creates no compute or quota request. The resulting architectural
      conclusion is that cleanup before reconciliation can erase evidence or strand a partially created dependency.
    B: The controlling fact is that a technically supported SKU can miss the batch window or be unavailable at the
      required quota. The resulting architectural conclusion is that discarding the original run identity breaks
      ownership reconciliation and can duplicate a partial operation.
    C: The authored acceptance boundary states that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. The resulting architectural conclusion is that altering an already separate checkpoint does
      not repair the modeled failure at Translate workload demand into compute requirements.
    D: The command-level assertion is anchored in the fact that substitute the approved fallback SKU, rerun the
      throughput model, and update the cost comparison. That evidence means it corrects the narrow cause while retaining
      the same recovery trail and decision scope.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q06
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: The approach to 'Translate workload demand into compute requirements' is challenged by aerospace simulation
    lead. Without making a new change, the team must inspect the risk 'A technically supported SKU can miss the
    batch window or be unavailable at the required quota' using the Azure CLI + Bicep lane. Which action produces
    the read-only, lane-correct inspection?
  options:
    A: Rerun the Translate workload demand into compute requirements implementation command and infer the expected
      state; for this decision, base approval on the claim that absence of a shell error proves every property.
    B: 'Run only this negative inspection for Translate workload demand into compute requirements: Selecting a familiar
      SKU before checking batch-window throughput, quota, or regional restrictions must fail. Before sign-off, use
      the premise that an empty negative result reports every required positive property.'
    C: Run the positive inspection for Preview the tagged reference deployment and apply it to Translate workload
      demand into compute requirements; as an independent condition, consider it sufficient that any command from
      the same lane proves the current checkpoint.
    D: 'Inspect the documented properties for Translate workload demand into compute requirements. As another control,
      retain this evidence: save the demand model, SKU capability output, benchmark assumption, and normal-versus-peak
      capacity calculation.'
  answer: D
  explanations:
    A: The checkpoint specifically records that the implementation command. The resulting architectural conclusion
      is that it can mutate state and shell success does not independently assert the expected properties.
    B: The scenario makes clear that the negative inspection. The resulting architectural conclusion is that absence
      of the prohibited condition is necessary but does not establish the positive architecture state.
    C: The architecture evidence must show that the positive inspection for Preview the tagged reference deployment.
      The resulting architectural conclusion is that it is lane-correct but proves Preview the tagged reference
      deployment, not Translate workload demand into compute requirements.
    D: The relevant observation is that the requirement matrix captures instruction set, vCPU, memory, scratch space,
      network, image, job duration, concurrency, availability, quota, and data-gravity needs. The resulting architectural
      conclusion is that the read-only inspection directly tests the properties required at Translate workload demand
      into compute requirements.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q07
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: 'A decision test for ''Translate workload demand into compute requirements'' includes compute platform owner.
    A passing positive check does not by itself prove this negative assertion: Selecting a familiar SKU before checking
    batch-window throughput, quota, or regional restrictions must fail. What best demonstrates the assertion pair
    that proves both conditions independently?'
  options:
    A: Verify only the positive result for Translate workload demand into compute requirements and report full compliance.
      Independently, rely on the claim that every prohibited parallel state must therefore be absent.
    B: Verify the positive properties for Translate workload demand into compute requirements. In addition, independently
      verify that selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions
      must fail.
    C: Prove only that selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions
      must fail and report the intended configuration as present. Next, rely on the belief that absence is equivalent
      to positive-state evidence.
    D: Use Deploy and verify the compute reference's negative assertion for Translate workload demand into compute
      requirements, and then proceed on the belief that negative assertions are interchangeable between checkpoints.
  answer: B
  explanations:
    A: 'The review is governed by this fact: the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. The resulting
      architectural conclusion is that the positive result alone does not test the explicit anti-condition ''Selecting
      a familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail''.'
    B: The applicable design condition is that the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs; Selecting a
      familiar SKU before checking batch-window throughput, quota, or regional restrictions must fail. The resulting
      architectural conclusion is that two independent observations prevent a passing positive check from concealing
      an unsafe parallel state.
    C: The retained result must be reconciled with the fact that selecting a familiar SKU before checking batch-window
      throughput, quota, or regional restrictions must fail. The resulting architectural conclusion is that absence
      evidence cannot demonstrate the required positive state 'The requirement matrix captures instruction set,
      vCPU, memory, scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity
      needs'.
    D: The decision tension comes from the fact that missing pre-mutation state, a failed ownership assertion, or
      an unrecorded returned resource ID makes the deployment unsafe. The resulting architectural conclusion is
      that the second assertion is valid for Deploy and verify the compute reference but leaves this checkpoint's
      prohibited state untested.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q08
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: 'The architecture board reconsiders ''Translate workload demand into compute requirements'' with security
    engineering lead. The board wants the Well-Architected consequence of mitigating this risk: A technically supported
    SKU can miss the batch window or be unavailable at the required quota. Which option is the consequence attributable
    to this checkpoint?'
  options:
    A: Use the Validate Batch behavior and graceful degradation consequence as the result for Translate workload
      demand into compute requirements. Afterward, take it as conclusive that a pillar statement remains valid when
      moved away from Validate Batch behavior and graceful degradation.
    B: Remove the control responsible for the Translate workload demand into compute requirements outcome; next,
      treat it as established that a moderate cost classification outweighs the mandatory architecture state.
    C: 'Treat ''Performance Efficiency: requirements drive heterogeneous sizing instead of one oversized worker
      profile'' as proof that all five pillars pass. In addition, use as justification the claim that the checkpoint
      ''Translate workload demand into compute requirements'' no longer needs its separate negative check.'
    D: 'Record this consequence: Performance Efficiency: requirements drive heterogeneous sizing instead of one
      oversized worker profile. Before sign-off, tie it to LAB18-REQ-01.'
  answer: D
  explanations:
    A: 'The traceable checkpoint outcome is that reliability: idempotent tasks and resilient orchestration tolerate
      individual worker and zone failures. The resulting architectural conclusion is that that tradeoff belongs
      to Validate Batch behavior and graceful degradation and does not explain this checkpoint''s decision.'
    B: The failure model establishes that the required outcome at Translate workload demand into compute requirements.
      The resulting architectural conclusion is that cost Optimization cannot remove the acceptance condition 'The
      requirement matrix captures instruction set, vCPU, memory, scratch space, network, image, job duration, concurrency,
      availability, quota, and data-gravity needs'.
    C: The recovery guidance assumes that selecting a familiar SKU before checking batch-window throughput, quota,
      or regional restrictions must fail. The resulting architectural conclusion is that one positive command cannot
      establish every pillar, especially while the negative state remains unchecked.
    D: 'The safe operating boundary says that performance Efficiency: requirements drive heterogeneous sizing instead
      of one oversized worker profile. The resulting architectural conclusion is that it states the authored pillar
      consequence of the control evaluated at Translate workload demand into compute requirements.'
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q09
  difficulty: advanced
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: 'A review of ''Translate workload demand into compute requirements'' begins with input from finOps analyst.
    A material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the
    processing window is shortened from eight hours to five. What should the architect select as the correct revision
    to the decision record?'
  options:
    A: Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Translate
      workload demand into compute requirements; afterward, supersede the ADR using the changed evidence for LAB18-REQ-01.
    B: Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Translate workload demand into
      compute requirements without recalculating criteria or eligibility; as an independent condition, treat as
      decisive the assertion that the original weighted result is permanent.
    C: Select Flexible Virtual Machine Scale Sets running custom queue workers for Translate workload demand into
      compute requirements without rechecking its mandatory constraints; before approval, base approval on the claim
      that being different from the current design is an architecture criterion.
    D: Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Translate workload
      demand into compute requirements by downgrading LAB18-REQ-01. Then, use the premise that stakeholder approval
      is unnecessary when that requirement blocks the candidate.
  answer: A
  explanations:
    A: The WAF consequence identifies that zonal control virtual machines with an autoscaled Azure Batch pool at
      Translate workload demand into compute requirements. The resulting architectural conclusion is that the material
      change 'Export-controlled simulations can no longer use low-priority capacity, and the processing window is
      shortened from eight hours to five.' requires fresh eligibility, weighted analysis, and a superseding decision.
    B: The command-level assertion is anchored in the fact that zonal control virtual machines with an autoscaled
      Azure Batch pool. The resulting architectural conclusion is that the original ADR remains historical evidence,
      but its score cannot answer a changed mandatory condition.
    C: flexible Virtual Machine Scale Sets running custom queue workers. Under the stated constraint, being different
      is not a criterion, and the candidate still must avoid the prohibited state at Translate workload demand into
      compute requirements.
    D: The controlling fact is that lAB18-REQ-01. Under the stated constraint, an architect cannot silently downgrade
      a stakeholder-owned mandatory requirement to protect a candidate.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q10
  difficulty: advanced
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP01
  stem: '''Translate workload demand into compute requirements'' awaits approval from operations manager. After
    a partial run, cleanup must follow this dependency: Remove local SKU exports; this checkpoint creates no compute
    or quota request. Select the dependency-safe cleanup plan.'
  options:
    A: Apply the cleanup rule for Preview the tagged reference deployment before reconciling the current dependency,
      and then accept without proof that removing a parent needed to identify Translate workload demand into compute
      requirements is harmless.
    B: Delete candidates by display name before comparing the Translate workload demand into compute requirements
      ownership tags. Also, rely on the claim that the dependency rule 'Remove local SKU exports; this checkpoint
      creates no compute or quota request' is optional.
    C: Destroy recoverable copies before retaining the Translate workload demand into compute requirements negative
      assertion 'Selecting a familiar SKU before checking batch-window throughput, quota, or regional restrictions
      must fail'; in a separate step, rely on the belief that remaining command logs are sufficient recovery evidence.
    D: 'Verify exact run-state IDs and ownership tags for Translate workload demand into compute requirements, and
      then follow this dependency rule without purge: Remove local SKU exports; this checkpoint creates no compute
      or quota request.'
  answer: D
  explanations:
    A: The relevant observation is that remove preview output if it contains sensitive topology; what-if itself
      creates no resource. Under the stated constraint, a cleanup rule for Preview the tagged reference deployment
      cannot override the dependency declared for Translate workload demand into compute requirements.
    B: The checkpoint specifically records that save the demand model, SKU capability output, benchmark assumption,
      and normal-versus-peak capacity calculation. Under the stated constraint, names are not ownership proof; deletion
      requires the exact recorded identifier and every required tag.
    C: The scenario makes clear that selecting a familiar SKU before checking batch-window throughput, quota, or
      regional restrictions must fail. Under the stated constraint, irreversible purge is outside the lab contract
      and destroys evidence needed for residual-state validation.
    D: The authored acceptance boundary states that remove local SKU exports; this checkpoint creates no compute
      or quota request. Under the stated constraint, exact identity, complete ownership tags, and the authored dependency
      order constrain cleanup and preserve recovery.
  remediationAnchor: checkpoint-1
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q11
  difficulty: foundational
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: '''Compile and lint the compute Bicep design'' is reopened at the request of compute platform owner. Approval
    requires a positive result plus this independent negative assertion: An unpinned image, unrestricted management
    ingress, missing shutdown control, or plaintext credential must fail lint review. What should the team use as
    the acceptance rule that makes LAB18-REQ-02 testable?'
  options:
    A: Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Compile and lint
      the compute Bicep design; afterward, use as justification the claim that a successful deployment will later
      prove the architecture constraint.
    B: Require the documented positive state for Compile and lint the compute Bicep design. Afterward, verify that
      an unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must
      fail lint review.
    C: Use the passing result from Translate workload demand into compute requirements to approve Compile and lint
      the compute Bicep design; then accept without proof that one control establishes an unrelated acceptance boundary.
    D: Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Compile
      and lint the compute Bicep design negative assertion. Independently, rely on the claim that the candidate
      has the lowest implementation effort.
  answer: B
  explanations:
    A: The applicable design condition is that an unpinned image, unrestricted management ingress, missing shutdown
      control, or plaintext credential must fail lint review. Under the stated constraint, a deployment result cannot
      prove LAB18-REQ-02, and Flexible Virtual Machine Scale Sets running custom queue workers still has to meet
      the mandatory boundary.
    B: The architecture evidence must show that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. Under the stated constraint, the positive state and an independent negative assertion jointly
      make LAB18-REQ-02 testable.
    C: 'The review is governed by this fact: the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. Under the stated
      constraint, that outcome belongs to Translate workload demand into compute requirements and leaves Compile
      and lint the compute Bicep design unverified.'
    D: The retained result must be reconciled with the fact that complete simulation batches inside the overnight
      window while keeping orchestration available and paying for burst capacity only when jobs require it. Under
      the stated constraint, implementation effort cannot justify skipping the negative assertion or displace LAB18-REQ-02.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q12
  difficulty: foundational
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: A design review of 'Compile and lint the compute Bicep design' includes security engineering lead. The selected
    architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is
    not success. Which option best establishes the intended successful finding?
  options:
    A: Use only the negative assertion 'An unpinned image, unrestricted management ingress, missing shutdown control,
      or plaintext credential must fail lint review' as the success result. As another control, use the premise
      that absence proves every required positive property.
    B: Use the successful finding from Preview the tagged reference deployment as the result for Compile and lint
      the compute Bicep design; as a separate check, consider it sufficient that a property from the current checkpoint
      does not need to be inspected.
    C: Record compilation succeeds and the template contains a tagged, parameterized, three-zone-capable VMSS at
      zero capacity plus a private, zero-node Batch pool, networking, identity, and no embedded secret. Separately,
      classify it as success for LAB18-REQ-02.
    D: Record the failure condition 'A template can compile while violating security, ownership, or lifecycle requirements'
      as a successful state. Afterward, take it as conclusive that the command returned an object.
  answer: C
  explanations:
    A: The safe operating boundary says that an unpinned image, unrestricted management ingress, missing shutdown
      control, or plaintext credential must fail lint review. Under the stated constraint, this is the independent
      prohibited-state assertion, not a successful finding.
    B: The traceable checkpoint outcome is that what-if contains only new run-owned resources, zero control and
      worker capacity, no public IP allocation, and the required ownership and expiry tags. Under the stated constraint,
      evidence for Preview the tagged reference deployment cannot substitute for the properties required at Compile
      and lint the compute Bicep design.
    C: The decision tension comes from the fact that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. Under the stated constraint, this is the authored target state for Compile and lint the compute
      Bicep design and directly supports LAB18-REQ-02.
    D: The failure model establishes that a template can compile while violating security, ownership, or lifecycle
      requirements. Under the stated constraint, resource existence or command output does not convert the documented
      failure condition into success.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q13
  difficulty: foundational
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: 'The team asks finOps analyst to assess ''Compile and lint the compute Bicep design''. Evidence must address
    this risk without retaining credentials: A template can compile while violating security, ownership, or lifecycle
    requirements. Which answer identifies sufficient, properly scoped evidence?'
  options:
    A: Substitute the evidence from Deploy and verify the compute reference for Compile and lint the compute Bicep
      design; for this decision, rely on the belief that a related checkpoint proves the current expected state.
    B: Store unredacted Compile and lint the compute Bicep design output with operator, tenant, token, and request
      context. Before sign-off, proceed on the belief that reproduction requires every captured field.
    C: Retain preserve the Bicep source hash, compiler output, parameter review, and architecture traceability record;
      as another gate, exclude credentials and unrelated response fields.
    D: Record only the Compile and lint the compute Bicep design positive inspection's exit status; as an independent
      condition, treat as decisive the assertion that projected properties and assertion results can be reconstructed
      later.
  answer: C
  explanations:
    A: The WAF consequence identifies that preserve deployment operations, schema-valid run state, exact resource
      IDs, effective tags, and assertion outcomes. Under the stated constraint, that evidence supports Deploy and
      verify the compute reference, so it cannot demonstrate Compilation succeeds and the template contains a tagged,
      parameterized, three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking,
      identity, and no embedded secret.
    B: The command-level assertion is anchored in the fact that unredacted implementation output. Under the stated
      constraint, identity, tenant, or token material exceeds the non-secret evidence contract.
    C: The recovery guidance assumes that preserve the Bicep source hash, compiler output, parameter review, and
      architecture traceability record. Under the stated constraint, it captures the checkpoint's observable properties
      while keeping the evidence boundary narrow.
    D: the positive inspection's exit status. This matters because an exit code alone does not show whether compilation
      succeeds and the template contains a tagged, parameterized, three-zone-capable VMSS at zero capacity plus
      a private, zero-node Batch pool, networking, identity, and no embedded secret.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q14
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: A recommendation on 'Compile and lint the compute Bicep design' is requested by operations manager. The
    target is Compilation succeeds and the template contains a tagged, parameterized, three-zone-capable VMSS at
    zero capacity plus a private, zero-node Batch pool, networking, identity, and no embedded secret, but the latest
    evidence does not show it. What recommendation gives the reviewers the most likely cause?
  options:
    A: Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites
      are not checked together' as grounds to reject Compile and lint the compute Bicep design. Independently, treat
      it as established that validate Batch behavior and graceful degradation's failure model applies unchanged
      here.
    B: Ignore the negative assertion 'An unpinned image, unrestricted management ingress, missing shutdown control,
      or plaintext credential must fail lint review'. Next, use as justification the claim that a later material
      change will make it unnecessary.
    C: Investigate Translate workload demand into compute requirements instead of diagnosing Compile and lint the
      compute Bicep design, and then accept without proof that a passing result at Translate workload demand into
      compute requirements identifies the current cause.
    D: Investigate a template can compile while violating security, ownership, or lifecycle requirements. Independently,
      isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.
  answer: D
  explanations:
    A: The authored acceptance boundary states that a syntactically valid pool can allocate billable or unreachable
      nodes when scale and outbound prerequisites are not checked together. This matters because that condition
      belongs to Validate Batch behavior and graceful degradation and does not by itself invalidate Zonal control
      virtual machines with an autoscaled Azure Batch pool.
    B: The relevant observation is that an unpinned image, unrestricted management ingress, missing shutdown control,
      or plaintext credential must fail lint review. This matters because the negative assertion must be evaluated
      now, independent of a later business change.
    C: The checkpoint specifically records that the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. This matters
      because a passing result at Translate workload demand into compute requirements gives no causal evidence for
      the failure at Compile and lint the compute Bicep design.
    D: The controlling fact is that a template can compile while violating security, ownership, or lifecycle requirements.
      This matters because it is the checkpoint's causal failure model and should be isolated before retrying Compile
      and lint the compute Bicep design.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q15
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: '''Compile and lint the compute Bicep design'' is assigned to aerospace simulation lead. The run encountered
    this modeled failure: A template can compile while violating security, ownership, or lifecycle requirements.
    Which action produces the safest recovery action?'
  options:
    A: 'Perform cleanup immediately: Delete generated local JSON if retained; keep the authored Bicep source. Afterward,
      base approval on the claim that the failed operation and its returned identifiers do not need reconciliation.'
    B: Correct the smallest failing module and rerun compilation plus semantic assertions before deployment preview;
      in a separate step, preserve the current run identity and evidence.
    C: Create a different run identity before diagnosing 'A template can compile while violating security, ownership,
      or lifecycle requirements'; next, use the premise that the first state record and returned identifiers can
      be discarded.
    D: Change Translate workload demand into compute requirements instead. In addition, consider it sufficient that
      success at Translate workload demand into compute requirements will repair the failed state at Compile and
      lint the compute Bicep design.
  answer: B
  explanations:
    A: The architecture evidence must show that delete generated local JSON if retained; keep the authored Bicep
      source. This matters because cleanup before reconciliation can erase evidence or strand a partially created
      dependency.
    B: The scenario makes clear that correct the smallest failing module and rerun compilation plus semantic assertions
      before deployment preview. This matters because it corrects the narrow cause while retaining the same recovery
      trail and decision scope.
    C: The applicable design condition is that a template can compile while violating security, ownership, or lifecycle
      requirements. This matters because discarding the original run identity breaks ownership reconciliation and
      can duplicate a partial operation.
    D: 'The review is governed by this fact: the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. This matters
      because altering an already separate checkpoint does not repair the modeled failure at Compile and lint the
      compute Bicep design.'
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q16
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: An assurance review of 'Compile and lint the compute Bicep design' includes compute platform owner. Without
    making a new change, the team must inspect the risk 'A template can compile while violating security, ownership,
    or lifecycle requirements' using the Azure CLI + Bicep lane. What best demonstrates the read-only, lane-correct
    inspection?
  options:
    A: Rerun the Compile and lint the compute Bicep design implementation command and infer the expected state;
      as an independent condition, rely on the claim that absence of a shell error proves every property.
    B: 'Run only this negative inspection for Compile and lint the compute Bicep design: An unpinned image, unrestricted
      management ingress, missing shutdown control, or plaintext credential must fail lint review; as a second control,
      rely on the belief that an empty negative result reports every required positive property.'
    C: Run the positive inspection for Preview the tagged reference deployment and apply it to Compile and lint
      the compute Bicep design. Then, proceed on the belief that any command from the same lane proves the current
      checkpoint.
    D: 'Inspect the documented properties for Compile and lint the compute Bicep design; next, retain this evidence:
      preserve the Bicep source hash, compiler output, parameter review, and architecture traceability record.'
  answer: D
  explanations:
    A: The decision tension comes from the fact that the implementation command. This matters because it can mutate
      state and shell success does not independently assert the expected properties.
    B: The safe operating boundary says that the negative inspection. This matters because absence of the prohibited
      condition is necessary but does not establish the positive architecture state.
    C: The traceable checkpoint outcome is that the positive inspection for Preview the tagged reference deployment.
      This matters because it is lane-correct but proves Preview the tagged reference deployment, not Compile and
      lint the compute Bicep design.
    D: The retained result must be reconciled with the fact that compilation succeeds and the template contains
      a tagged, parameterized, three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking,
      identity, and no embedded secret. This matters because the read-only inspection directly tests the properties
      required at Compile and lint the compute Bicep design.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q17
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: 'Approval of ''Compile and lint the compute Bicep design'' is questioned by security engineering lead. A
    passing positive check does not by itself prove this negative assertion: An unpinned image, unrestricted management
    ingress, missing shutdown control, or plaintext credential must fail lint review. Which option is the assertion
    pair that proves both conditions independently?'
  options:
    A: Verify only the positive result for Compile and lint the compute Bicep design and report full compliance,
      and then take it as conclusive that every prohibited parallel state must therefore be absent.
    B: Prove only that an unpinned image, unrestricted management ingress, missing shutdown control, or plaintext
      credential must fail lint review and report the intended configuration as present. Also, treat it as established
      that absence is equivalent to positive-state evidence.
    C: Use Deploy and verify the compute reference's negative assertion for Compile and lint the compute Bicep design;
      in a separate step, use as justification the claim that negative assertions are interchangeable between checkpoints.
    D: Verify the positive properties for Compile and lint the compute Bicep design; for this decision, independently
      verify that an unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential
      must fail lint review.
  answer: D
  explanations:
    A: The recovery guidance assumes that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. This matters because the positive result alone does not test the explicit anti-condition
      'An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must
      fail lint review'.
    B: The WAF consequence identifies that an unpinned image, unrestricted management ingress, missing shutdown
      control, or plaintext credential must fail lint review. This matters because absence evidence cannot demonstrate
      the required positive state 'Compilation succeeds and the template contains a tagged, parameterized, three-zone-capable
      VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no embedded secret'.
    C: The command-level assertion is anchored in the fact that missing pre-mutation state, a failed ownership assertion,
      or an unrecorded returned resource ID makes the deployment unsafe. This matters because the second assertion
      is valid for Deploy and verify the compute reference but leaves this checkpoint's prohibited state untested.
    D: The failure model establishes that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret; An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext
      credential must fail lint review. This matters because two independent observations prevent a passing positive
      check from concealing an unsafe parallel state.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q18
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: 'The implementation review has reached ''Compile and lint the compute Bicep design''. The board wants the
    Well-Architected consequence of mitigating this risk: A template can compile while violating security, ownership,
    or lifecycle requirements. What should the architect select as the consequence attributable to this checkpoint?'
  options:
    A: Use the Validate Batch behavior and graceful degradation consequence as the result for Compile and lint the
      compute Bicep design. In addition, treat as decisive the assertion that a pillar statement remains valid when
      moved away from Validate Batch behavior and graceful degradation.
    B: 'Record this consequence: Security: managed identity, restricted ingress, and secret-free templates reduce
      control-plane exposure. Then, tie it to LAB18-REQ-02.'
    C: Remove the control responsible for the Compile and lint the compute Bicep design outcome; before approval,
      base approval on the claim that a moderate cost classification outweighs the mandatory architecture state.
    D: 'Treat ''Security: managed identity, restricted ingress, and secret-free templates reduce control-plane exposure''
      as proof that all five pillars pass. Separately, use the premise that the checkpoint ''Compile and lint the
      compute Bicep design'' no longer needs its separate negative check.'
  answer: B
  explanations:
    A: 'The controlling fact is that reliability: idempotent tasks and resilient orchestration tolerate individual
      worker and zone failures. The checkpoint therefore requires that that tradeoff belongs to Validate Batch behavior
      and graceful degradation and does not explain this checkpoint''s decision.'
    B: 'security: managed identity, restricted ingress, and secret-free templates reduce control-plane exposure.
      The checkpoint therefore requires that it states the authored pillar consequence of the control evaluated
      at Compile and lint the compute Bicep design.'
    C: The authored acceptance boundary states that the required outcome at Compile and lint the compute Bicep design.
      The checkpoint therefore requires that cost Optimization cannot remove the acceptance condition 'Compilation
      succeeds and the template contains a tagged, parameterized, three-zone-capable VMSS at zero capacity plus
      a private, zero-node Batch pool, networking, identity, and no embedded secret'.
    D: The relevant observation is that an unpinned image, unrestricted management ingress, missing shutdown control,
      or plaintext credential must fail lint review. The checkpoint therefore requires that one positive command
      cannot establish every pillar, especially while the negative state remains unchecked.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q19
  difficulty: advanced
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: 'The approach to ''Compile and lint the compute Bicep design'' is challenged by operations manager. A material
    change now applies: Export-controlled simulations can no longer use low-priority capacity, and the processing
    window is shortened from eight hours to five. Select the correct revision to the decision record.'
  options:
    A: Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Compile and lint the compute
      Bicep design without recalculating criteria or eligibility. Then, accept without proof that the original weighted
      result is permanent.
    B: Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Compile
      and lint the compute Bicep design. Next, supersede the ADR using the changed evidence for LAB18-REQ-02.
    C: Select Flexible Virtual Machine Scale Sets running custom queue workers for Compile and lint the compute
      Bicep design without rechecking its mandatory constraints; afterward, rely on the claim that being different
      from the current design is an architecture criterion.
    D: Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Compile and
      lint the compute Bicep design by downgrading LAB18-REQ-02; then rely on the belief that stakeholder approval
      is unnecessary when that requirement blocks the candidate.
  answer: B
  explanations:
    A: The scenario makes clear that zonal control virtual machines with an autoscaled Azure Batch pool. The checkpoint
      therefore requires that the original ADR remains historical evidence, but its score cannot answer a changed
      mandatory condition.
    B: The checkpoint specifically records that zonal control virtual machines with an autoscaled Azure Batch pool
      at Compile and lint the compute Bicep design. The checkpoint therefore requires that the material change 'Export-controlled
      simulations can no longer use low-priority capacity, and the processing window is shortened from eight hours
      to five.' requires fresh eligibility, weighted analysis, and a superseding decision.
    C: The architecture evidence must show that flexible Virtual Machine Scale Sets running custom queue workers.
      The checkpoint therefore requires that being different is not a criterion, and the candidate still must avoid
      the prohibited state at Compile and lint the compute Bicep design.
    D: The applicable design condition is that lAB18-REQ-02. The checkpoint therefore requires that an architect
      cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q20
  difficulty: advanced
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP02
  stem: 'A decision test for ''Compile and lint the compute Bicep design'' includes aerospace simulation lead. After
    a partial run, cleanup must follow this dependency: Delete generated local JSON if retained; keep the authored
    Bicep source. Which response provides the dependency-safe cleanup plan?'
  options:
    A: Apply the cleanup rule for Preview the tagged reference deployment before reconciling the current dependency;
      in a separate step, consider it sufficient that removing a parent needed to identify Compile and lint the
      compute Bicep design is harmless.
    B: 'Verify exact run-state IDs and ownership tags for Compile and lint the compute Bicep design. As another
      control, follow this dependency rule without purge: Delete generated local JSON if retained; keep the authored
      Bicep source.'
    C: Delete candidates by display name before comparing the Compile and lint the compute Bicep design ownership
      tags. As another control, take it as conclusive that the dependency rule 'Delete generated local JSON if retained;
      keep the authored Bicep source' is optional.
    D: Destroy recoverable copies before retaining the Compile and lint the compute Bicep design negative assertion
      'An unpinned image, unrestricted management ingress, missing shutdown control, or plaintext credential must
      fail lint review'; as a separate check, treat it as established that remaining command logs are sufficient
      recovery evidence.
  answer: B
  explanations:
    A: The retained result must be reconciled with the fact that remove preview output if it contains sensitive
      topology; what-if itself creates no resource. The checkpoint therefore requires that a cleanup rule for Preview
      the tagged reference deployment cannot override the dependency declared for Compile and lint the compute Bicep
      design.
    B: 'The review is governed by this fact: delete generated local JSON if retained; keep the authored Bicep source.
      The checkpoint therefore requires that exact identity, complete ownership tags, and the authored dependency
      order constrain cleanup and preserve recovery.'
    C: The decision tension comes from the fact that preserve the Bicep source hash, compiler output, parameter
      review, and architecture traceability record. The checkpoint therefore requires that names are not ownership
      proof; deletion requires the exact recorded identifier and every required tag.
    D: The safe operating boundary says that an unpinned image, unrestricted management ingress, missing shutdown
      control, or plaintext credential must fail lint review. The checkpoint therefore requires that irreversible
      purge is outside the lab contract and destroys evidence needed for residual-state validation.
  remediationAnchor: checkpoint-2
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q21
  difficulty: foundational
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: 'The architecture board reconsiders ''Preview the tagged reference deployment'' with security engineering
    lead. Approval requires a positive result plus this independent negative assertion: Any destructive change,
    unbounded autoscale maximum, untagged resource, or unrelated scope must block execution. Which option best establishes
    the acceptance rule that makes LAB18-REQ-03 testable?'
  options:
    A: Require the documented positive state for Preview the tagged reference deployment; before approval, verify
      that any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block
      execution.
    B: Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Preview the tagged
      reference deployment. Independently, use the premise that a successful deployment will later prove the architecture
      constraint.
    C: Use the passing result from Translate workload demand into compute requirements to approve Preview the tagged
      reference deployment. Next, consider it sufficient that one control establishes an unrelated acceptance boundary.
    D: Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Preview
      the tagged reference deployment negative assertion, and then take it as conclusive that the candidate has
      the lowest implementation effort.
  answer: A
  explanations:
    A: The traceable checkpoint outcome is that what-if contains only new run-owned resources, zero control and
      worker capacity, no public IP allocation, and the required ownership and expiry tags. The checkpoint therefore
      requires that the positive state and an independent negative assertion jointly make LAB18-REQ-03 testable.
    B: The failure model establishes that any destructive change, unbounded autoscale maximum, untagged resource,
      or unrelated scope must block execution. The checkpoint therefore requires that a deployment result cannot
      prove LAB18-REQ-03, and Flexible Virtual Machine Scale Sets running custom queue workers still has to meet
      the mandatory boundary.
    C: The recovery guidance assumes that the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. The checkpoint
      therefore requires that that outcome belongs to Translate workload demand into compute requirements and leaves
      Preview the tagged reference deployment unverified.
    D: The WAF consequence identifies that complete simulation batches inside the overnight window while keeping
      orchestration available and paying for burst capacity only when jobs require it. The checkpoint therefore
      requires that implementation effort cannot justify skipping the negative assertion or displace LAB18-REQ-03.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q22
  difficulty: foundational
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: A review of 'Preview the tagged reference deployment' begins with input from finOps analyst. The selected
    architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is
    not success. Which answer identifies the intended successful finding?
  options:
    A: Use only the negative assertion 'Any destructive change, unbounded autoscale maximum, untagged resource,
      or unrelated scope must block execution' as the success result. Afterward, rely on the belief that absence
      proves every required positive property.
    B: Use the successful finding from Compile and lint the compute Bicep design as the result for Preview the tagged
      reference deployment; next, proceed on the belief that a property from the current checkpoint does not need
      to be inspected.
    C: Record what-if contains only new run-owned resources, zero control and worker capacity, no public IP allocation,
      and the required ownership and expiry tags; as an independent condition, classify it as success for LAB18-REQ-03.
    D: Record the failure condition 'Parameter drift can expand cost or affect a pre-existing resource despite a
      valid template' as a successful state. In addition, treat as decisive the assertion that the command returned
      an object.
  answer: C
  explanations:
    A: any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution.
      In the decision record, this is the independent prohibited-state assertion, not a successful finding.
    B: The controlling fact is that compilation succeeds and the template contains a tagged, parameterized, three-zone-capable
      VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no embedded secret.
      In the decision record, evidence for Compile and lint the compute Bicep design cannot substitute for the properties
      required at Preview the tagged reference deployment.
    C: The command-level assertion is anchored in the fact that what-if contains only new run-owned resources, zero
      control and worker capacity, no public IP allocation, and the required ownership and expiry tags. The checkpoint
      therefore requires that this is the authored target state for Preview the tagged reference deployment and
      directly supports LAB18-REQ-03.
    D: The authored acceptance boundary states that parameter drift can expand cost or affect a pre-existing resource
      despite a valid template. In the decision record, resource existence or command output does not convert the
      documented failure condition into success.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q23
  difficulty: foundational
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: '''Preview the tagged reference deployment'' awaits approval from operations manager. Evidence must address
    this risk without retaining credentials: Parameter drift can expand cost or affect a pre-existing resource despite
    a valid template. What recommendation gives the reviewers sufficient, properly scoped evidence?'
  options:
    A: Substitute the evidence from Deploy and verify the compute reference for Preview the tagged reference deployment;
      as an independent condition, treat it as established that a related checkpoint proves the current expected
      state.
    B: Store unredacted Preview the tagged reference deployment output with operator, tenant, token, and request
      context; during the same review, use as justification the claim that reproduction requires every captured
      field.
    C: Retain archive the complete what-if result, reviewer decision, estimated hourly ceiling, and template and
      parameter hashes; then exclude credentials and unrelated response fields.
    D: Record only the Preview the tagged reference deployment positive inspection's exit status. Then, accept without
      proof that projected properties and assertion results can be reconstructed later.
  answer: C
  explanations:
    A: The checkpoint specifically records that preserve deployment operations, schema-valid run state, exact resource
      IDs, effective tags, and assertion outcomes. In the decision record, that evidence supports Deploy and verify
      the compute reference, so it cannot demonstrate What-if contains only new run-owned resources, zero control
      and worker capacity, no public IP allocation, and the required ownership and expiry tags.
    B: The scenario makes clear that unredacted implementation output. In the decision record, identity, tenant,
      or token material exceeds the non-secret evidence contract.
    C: The relevant observation is that archive the complete what-if result, reviewer decision, estimated hourly
      ceiling, and template and parameter hashes. In the decision record, it captures the checkpoint's observable
      properties while keeping the evidence boundary narrow.
    D: The architecture evidence must show that the positive inspection's exit status. In the decision record, an
      exit code alone does not show whether what-if contains only new run-owned resources, zero control and worker
      capacity, no public IP allocation, and the required ownership and expiry tags.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q24
  difficulty: applied
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: '''Preview the tagged reference deployment'' is reopened at the request of aerospace simulation lead. The
    target is What-if contains only new run-owned resources, zero control and worker capacity, no public IP allocation,
    and the required ownership and expiry tags, but the latest evidence does not show it. Which action produces
    the most likely cause?'
  options:
    A: Investigate parameter drift can expand cost or affect a pre-existing resource despite a valid template. Also,
      isolate that cause before changing Zonal control virtual machines with an autoscaled Azure Batch pool.
    B: Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites
      are not checked together' as grounds to reject Preview the tagged reference deployment, and then base approval
      on the claim that validate Batch behavior and graceful degradation's failure model applies unchanged here.
    C: Ignore the negative assertion 'Any destructive change, unbounded autoscale maximum, untagged resource, or
      unrelated scope must block execution'. Also, use the premise that a later material change will make it unnecessary.
    D: Investigate Translate workload demand into compute requirements instead of diagnosing Preview the tagged
      reference deployment; in a separate step, consider it sufficient that a passing result at Translate workload
      demand into compute requirements identifies the current cause.
  answer: A
  explanations:
    A: The applicable design condition is that parameter drift can expand cost or affect a pre-existing resource
      despite a valid template. In the decision record, it is the checkpoint's causal failure model and should be
      isolated before retrying Preview the tagged reference deployment.
    B: 'The review is governed by this fact: a syntactically valid pool can allocate billable or unreachable nodes
      when scale and outbound prerequisites are not checked together. In the decision record, that condition belongs
      to Validate Batch behavior and graceful degradation and does not by itself invalidate Zonal control virtual
      machines with an autoscaled Azure Batch pool.'
    C: The retained result must be reconciled with the fact that any destructive change, unbounded autoscale maximum,
      untagged resource, or unrelated scope must block execution. In the decision record, the negative assertion
      must be evaluated now, independent of a later business change.
    D: The decision tension comes from the fact that the requirement matrix captures instruction set, vCPU, memory,
      scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. In
      the decision record, a passing result at Translate workload demand into compute requirements gives no causal
      evidence for the failure at Preview the tagged reference deployment.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q25
  difficulty: applied
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: 'A design review of ''Preview the tagged reference deployment'' includes compute platform owner. The run
    encountered this modeled failure: Parameter drift can expand cost or affect a pre-existing resource despite
    a valid template. What best demonstrates the safest recovery action?'
  options:
    A: 'Perform cleanup immediately: Remove preview output if it contains sensitive topology; what-if itself creates
      no resource. In addition, rely on the claim that the failed operation and its returned identifiers do not
      need reconciliation.'
    B: Create a different run identity before diagnosing 'Parameter drift can expand cost or affect a pre-existing
      resource despite a valid template'; before approval, rely on the belief that the first state record and returned
      identifiers can be discarded.
    C: Change Translate workload demand into compute requirements instead. Separately, proceed on the belief that
      success at Translate workload demand into compute requirements will repair the failed state at Preview the
      tagged reference deployment.
    D: Correct parameters, regenerate state before any mutation, and obtain a fresh reviewed what-if result. Afterward,
      preserve the current run identity and evidence.
  answer: D
  explanations:
    A: The traceable checkpoint outcome is that remove preview output if it contains sensitive topology; what-if
      itself creates no resource. In the decision record, cleanup before reconciliation can erase evidence or strand
      a partially created dependency.
    B: The failure model establishes that parameter drift can expand cost or affect a pre-existing resource despite
      a valid template. In the decision record, discarding the original run identity breaks ownership reconciliation
      and can duplicate a partial operation.
    C: The recovery guidance assumes that the requirement matrix captures instruction set, vCPU, memory, scratch
      space, network, image, job duration, concurrency, availability, quota, and data-gravity needs. In the decision
      record, altering an already separate checkpoint does not repair the modeled failure at Preview the tagged
      reference deployment.
    D: The safe operating boundary says that correct parameters, regenerate state before any mutation, and obtain
      a fresh reviewed what-if result. In the decision record, it corrects the narrow cause while retaining the
      same recovery trail and decision scope.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q26
  difficulty: applied
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: The team asks security engineering lead to assess 'Preview the tagged reference deployment'. Without making
    a new change, the team must inspect the risk 'Parameter drift can expand cost or affect a pre-existing resource
    despite a valid template' using the Azure CLI + Bicep lane. Which option is the read-only, lane-correct inspection?
  options:
    A: 'Inspect the documented properties for Preview the tagged reference deployment. Separately, retain this evidence:
      archive the complete what-if result, reviewer decision, estimated hourly ceiling, and template and parameter
      hashes.'
    B: Rerun the Preview the tagged reference deployment implementation command and infer the expected state. Then,
      take it as conclusive that absence of a shell error proves every property.
    C: 'Run only this negative inspection for Preview the tagged reference deployment: Any destructive change, unbounded
      autoscale maximum, untagged resource, or unrelated scope must block execution; afterward, treat it as established
      that an empty negative result reports every required positive property.'
    D: Run the positive inspection for Compile and lint the compute Bicep design and apply it to Preview the tagged
      reference deployment; then use as justification the claim that any command from the same lane proves the current
      checkpoint.
  answer: A
  explanations:
    A: The WAF consequence identifies that what-if contains only new run-owned resources, zero control and worker
      capacity, no public IP allocation, and the required ownership and expiry tags. In the decision record, the
      read-only inspection directly tests the properties required at Preview the tagged reference deployment.
    B: The command-level assertion is anchored in the fact that the implementation command. In the decision record,
      it can mutate state and shell success does not independently assert the expected properties.
    C: the negative inspection. The independent assertion shows why absence of the prohibited condition is necessary
      but does not establish the positive architecture state.
    D: The controlling fact is that the positive inspection for Compile and lint the compute Bicep design. The independent
      assertion shows why it is lane-correct but proves Compile and lint the compute Bicep design, not Preview the
      tagged reference deployment.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q27
  difficulty: applied
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: 'A recommendation on ''Preview the tagged reference deployment'' is requested by finOps analyst. A passing
    positive check does not by itself prove this negative assertion: Any destructive change, unbounded autoscale
    maximum, untagged resource, or unrelated scope must block execution. What should the architect select as the
    assertion pair that proves both conditions independently?'
  options:
    A: Verify the positive properties for Preview the tagged reference deployment; for the final assessment, independently
      verify that any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must
      block execution.
    B: Verify only the positive result for Preview the tagged reference deployment and report full compliance; in
      a separate step, treat as decisive the assertion that every prohibited parallel state must therefore be absent.
    C: Prove only that any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope
      must block execution and report the intended configuration as present. As another control, base approval on
      the claim that absence is equivalent to positive-state evidence.
    D: Use Deploy and verify the compute reference's negative assertion for Preview the tagged reference deployment;
      as a separate check, use the premise that negative assertions are interchangeable between checkpoints.
  answer: A
  explanations:
    A: The authored acceptance boundary states that what-if contains only new run-owned resources, zero control
      and worker capacity, no public IP allocation, and the required ownership and expiry tags; Any destructive
      change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution. The independent
      assertion shows why two independent observations prevent a passing positive check from concealing an unsafe
      parallel state.
    B: The relevant observation is that what-if contains only new run-owned resources, zero control and worker capacity,
      no public IP allocation, and the required ownership and expiry tags. The independent assertion shows why the
      positive result alone does not test the explicit anti-condition 'Any destructive change, unbounded autoscale
      maximum, untagged resource, or unrelated scope must block execution'.
    C: The checkpoint specifically records that any destructive change, unbounded autoscale maximum, untagged resource,
      or unrelated scope must block execution. The independent assertion shows why absence evidence cannot demonstrate
      the required positive state 'What-if contains only new run-owned resources, zero control and worker capacity,
      no public IP allocation, and the required ownership and expiry tags'.
    D: The scenario makes clear that missing pre-mutation state, a failed ownership assertion, or an unrecorded
      returned resource ID makes the deployment unsafe. The independent assertion shows why the second assertion
      is valid for Deploy and verify the compute reference but leaves this checkpoint's prohibited state untested.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q28
  difficulty: applied
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: '''Preview the tagged reference deployment'' is assigned to operations manager. The board wants the Well-Architected
    consequence of mitigating this risk: Parameter drift can expand cost or affect a pre-existing resource despite
    a valid template. Select the consequence attributable to this checkpoint.'
  options:
    A: Use the Validate Batch behavior and graceful degradation consequence as the result for Preview the tagged
      reference deployment. Separately, accept without proof that a pillar statement remains valid when moved away
      from Validate Batch behavior and graceful degradation.
    B: 'Record this consequence: Cost Optimization: bounded autoscale and reviewed preview make the maximum spend
      visible before deployment. Independently, tie it to LAB18-REQ-03.'
    C: Remove the control responsible for the Preview the tagged reference deployment outcome; for this decision,
      rely on the claim that a moderate cost classification outweighs the mandatory architecture state.
    D: 'Treat ''Cost Optimization: bounded autoscale and reviewed preview make the maximum spend visible before
      deployment'' as proof that all five pillars pass. Before sign-off, rely on the belief that the checkpoint
      ''Preview the tagged reference deployment'' no longer needs its separate negative check.'
  answer: B
  explanations:
    A: 'The applicable design condition is that reliability: idempotent tasks and resilient orchestration tolerate
      individual worker and zone failures. The independent assertion shows why that tradeoff belongs to Validate
      Batch behavior and graceful degradation and does not explain this checkpoint''s decision.'
    B: 'The architecture evidence must show that cost Optimization: bounded autoscale and reviewed preview make
      the maximum spend visible before deployment. The independent assertion shows why it states the authored pillar
      consequence of the control evaluated at Preview the tagged reference deployment.'
    C: 'The review is governed by this fact: the required outcome at Preview the tagged reference deployment. The
      independent assertion shows why cost Optimization cannot remove the acceptance condition ''What-if contains
      only new run-owned resources, zero control and worker capacity, no public IP allocation, and the required
      ownership and expiry tags''.'
    D: The retained result must be reconciled with the fact that any destructive change, unbounded autoscale maximum,
      untagged resource, or unrelated scope must block execution. The independent assertion shows why one positive
      command cannot establish every pillar, especially while the negative state remains unchecked.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q29
  difficulty: advanced
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: 'An assurance review of ''Preview the tagged reference deployment'' includes aerospace simulation lead.
    A material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the
    processing window is shortened from eight hours to five. Which response provides the correct revision to the
    decision record?'
  options:
    A: Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Preview the tagged reference
      deployment without recalculating criteria or eligibility; then consider it sufficient that the original weighted
      result is permanent.
    B: Select Flexible Virtual Machine Scale Sets running custom queue workers for Preview the tagged reference
      deployment without rechecking its mandatory constraints. Independently, take it as conclusive that being different
      from the current design is an architecture criterion.
    C: Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Preview
      the tagged reference deployment; in a separate step, supersede the ADR using the changed evidence for LAB18-REQ-03.
    D: Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Preview the
      tagged reference deployment by downgrading LAB18-REQ-03. Next, treat it as established that stakeholder approval
      is unnecessary when that requirement blocks the candidate.
  answer: C
  explanations:
    A: The safe operating boundary says that zonal control virtual machines with an autoscaled Azure Batch pool.
      The independent assertion shows why the original ADR remains historical evidence, but its score cannot answer
      a changed mandatory condition.
    B: The traceable checkpoint outcome is that flexible Virtual Machine Scale Sets running custom queue workers.
      The independent assertion shows why being different is not a criterion, and the candidate still must avoid
      the prohibited state at Preview the tagged reference deployment.
    C: The decision tension comes from the fact that zonal control virtual machines with an autoscaled Azure Batch
      pool at Preview the tagged reference deployment. The independent assertion shows why the material change 'Export-controlled
      simulations can no longer use low-priority capacity, and the processing window is shortened from eight hours
      to five.' requires fresh eligibility, weighted analysis, and a superseding decision.
    D: The failure model establishes that lAB18-REQ-03. The independent assertion shows why an architect cannot
      silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q30
  difficulty: advanced
  objectiveId: INF-COMP-05
  checkpointId: LAB18-CP03
  stem: 'Approval of ''Preview the tagged reference deployment'' is questioned by compute platform owner. After
    a partial run, cleanup must follow this dependency: Remove preview output if it contains sensitive topology;
    what-if itself creates no resource. Which choice gives the team the dependency-safe cleanup plan?'
  options:
    A: Apply the cleanup rule for Compile and lint the compute Bicep design before reconciling the current dependency;
      as a separate check, proceed on the belief that removing a parent needed to identify Preview the tagged reference
      deployment is harmless.
    B: 'Verify exact run-state IDs and ownership tags for Preview the tagged reference deployment; next, follow
      this dependency rule without purge: Remove preview output if it contains sensitive topology; what-if itself
      creates no resource.'
    C: Delete candidates by display name before comparing the Preview the tagged reference deployment ownership
      tags. Afterward, treat as decisive the assertion that the dependency rule 'Remove preview output if it contains
      sensitive topology; what-if itself creates no resource' is optional.
    D: Destroy recoverable copies before retaining the Preview the tagged reference deployment negative assertion
      'Any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution';
      next, base approval on the claim that remaining command logs are sufficient recovery evidence.
  answer: B
  explanations:
    A: The WAF consequence identifies that delete generated local JSON if retained; keep the authored Bicep source.
      The independent assertion shows why a cleanup rule for Compile and lint the compute Bicep design cannot override
      the dependency declared for Preview the tagged reference deployment.
    B: The recovery guidance assumes that remove preview output if it contains sensitive topology; what-if itself
      creates no resource. The independent assertion shows why exact identity, complete ownership tags, and the
      authored dependency order constrain cleanup and preserve recovery.
    C: The command-level assertion is anchored in the fact that archive the complete what-if result, reviewer decision,
      estimated hourly ceiling, and template and parameter hashes. The independent assertion shows why names are
      not ownership proof; deletion requires the exact recorded identifier and every required tag.
    D: any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution.
      Operationally, irreversible purge is outside the lab contract and destroys evidence needed for residual-state
      validation.
  remediationAnchor: checkpoint-3
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q31
  difficulty: foundational
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: 'The implementation review has reached ''Deploy and verify the compute reference''. Approval requires a
    positive result plus this independent negative assertion: Missing pre-mutation state, a failed ownership assertion,
    or an unrecorded returned resource ID makes the deployment unsafe. Which answer identifies the acceptance rule
    that makes LAB18-REQ-04 testable?'
  options:
    A: Require the documented positive state for Deploy and verify the compute reference. Before sign-off, verify
      that missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes
      the deployment unsafe.
    B: Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Deploy and verify
      the compute reference, and then rely on the belief that a successful deployment will later prove the architecture
      constraint.
    C: Use the passing result from Translate workload demand into compute requirements to approve Deploy and verify
      the compute reference. Also, proceed on the belief that one control establishes an unrelated acceptance boundary.
    D: Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Deploy and
      verify the compute reference negative assertion; in a separate step, treat as decisive the assertion that
      the candidate has the lowest implementation effort.
  answer: A
  explanations:
    A: The controlling fact is that the bounded reference footprint deploys only after execution and cost acknowledgements,
      and returned IDs are persisted immediately in run state. Operationally, the positive state and an independent
      negative assertion jointly make LAB18-REQ-04 testable.
    B: The authored acceptance boundary states that missing pre-mutation state, a failed ownership assertion, or
      an unrecorded returned resource ID makes the deployment unsafe. Operationally, a deployment result cannot
      prove LAB18-REQ-04, and Flexible Virtual Machine Scale Sets running custom queue workers still has to meet
      the mandatory boundary.
    C: The relevant observation is that the requirement matrix captures instruction set, vCPU, memory, scratch space,
      network, image, job duration, concurrency, availability, quota, and data-gravity needs. Operationally, that
      outcome belongs to Translate workload demand into compute requirements and leaves Deploy and verify the compute
      reference unverified.
    D: The checkpoint specifically records that complete simulation batches inside the overnight window while keeping
      orchestration available and paying for burst capacity only when jobs require it. Operationally, implementation
      effort cannot justify skipping the negative assertion or displace LAB18-REQ-04.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q32
  difficulty: foundational
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: The approach to 'Deploy and verify the compute reference' is challenged by operations manager. The selected
    architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object existence alone is
    not success. What recommendation gives the reviewers the intended successful finding?
  options:
    A: Use only the negative assertion 'Missing pre-mutation state, a failed ownership assertion, or an unrecorded
      returned resource ID makes the deployment unsafe' as the success result. In addition, treat it as established
      that absence proves every required positive property.
    B: Record the bounded reference footprint deploys only after execution and cost acknowledgements, and returned
      IDs are persisted immediately in run state; afterward, classify it as success for LAB18-REQ-04.
    C: Use the successful finding from Compile and lint the compute Bicep design as the result for Deploy and verify
      the compute reference; before approval, use as justification the claim that a property from the current checkpoint
      does not need to be inspected.
    D: Record the failure condition 'Partial deployment can leave chargeable workers or dependencies even when the
      top-level operation fails' as a successful state. Separately, accept without proof that the command returned
      an object.
  answer: B
  explanations:
    A: The architecture evidence must show that missing pre-mutation state, a failed ownership assertion, or an
      unrecorded returned resource ID makes the deployment unsafe. Operationally, this is the independent prohibited-state
      assertion, not a successful finding.
    B: The scenario makes clear that the bounded reference footprint deploys only after execution and cost acknowledgements,
      and returned IDs are persisted immediately in run state. Operationally, this is the authored target state
      for Deploy and verify the compute reference and directly supports LAB18-REQ-04.
    C: The applicable design condition is that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. Operationally, evidence for Compile and lint the compute Bicep design cannot substitute for
      the properties required at Deploy and verify the compute reference.
    D: 'The review is governed by this fact: partial deployment can leave chargeable workers or dependencies even
      when the top-level operation fails. Operationally, resource existence or command output does not convert the
      documented failure condition into success.'
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q33
  difficulty: foundational
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: 'A decision test for ''Deploy and verify the compute reference'' includes aerospace simulation lead. Evidence
    must address this risk without retaining credentials: Partial deployment can leave chargeable workers or dependencies
    even when the top-level operation fails. Which action produces sufficient, properly scoped evidence?'
  options:
    A: Substitute the evidence from Preview the tagged reference deployment for Deploy and verify the compute reference.
      Then, base approval on the claim that a related checkpoint proves the current expected state.
    B: Retain preserve deployment operations, schema-valid run state, exact resource IDs, effective tags, and assertion
      outcomes, and then exclude credentials and unrelated response fields.
    C: Store unredacted Deploy and verify the compute reference output with operator, tenant, token, and request
      context; afterward, use the premise that reproduction requires every captured field.
    D: Record only the Deploy and verify the compute reference positive inspection's exit status; then consider
      it sufficient that projected properties and assertion results can be reconstructed later.
  answer: B
  explanations:
    A: The decision tension comes from the fact that archive the complete what-if result, reviewer decision, estimated
      hourly ceiling, and template and parameter hashes. Operationally, that evidence supports Preview the tagged
      reference deployment, so it cannot demonstrate The bounded reference footprint deploys only after execution
      and cost acknowledgements, and returned IDs are persisted immediately in run state.
    B: The retained result must be reconciled with the fact that preserve deployment operations, schema-valid run
      state, exact resource IDs, effective tags, and assertion outcomes. Operationally, it captures the checkpoint's
      observable properties while keeping the evidence boundary narrow.
    C: The safe operating boundary says that unredacted implementation output. Operationally, identity, tenant,
      or token material exceeds the non-secret evidence contract.
    D: The traceable checkpoint outcome is that the positive inspection's exit status. Operationally, an exit code
      alone does not show whether the bounded reference footprint deploys only after execution and cost acknowledgements,
      and returned IDs are persisted immediately in run state.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q34
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: The architecture board reconsiders 'Deploy and verify the compute reference' with compute platform owner.
    The target is The bounded reference footprint deploys only after execution and cost acknowledgements, and returned
    IDs are persisted immediately in run state, but the latest evidence does not show it. What best demonstrates
    the most likely cause?
  options:
    A: Treat 'A syntactically valid pool can allocate billable or unreachable nodes when scale and outbound prerequisites
      are not checked together' as grounds to reject Deploy and verify the compute reference; in a separate step,
      rely on the claim that validate Batch behavior and graceful degradation's failure model applies unchanged
      here.
    B: Ignore the negative assertion 'Missing pre-mutation state, a failed ownership assertion, or an unrecorded
      returned resource ID makes the deployment unsafe'. As another control, rely on the belief that a later material
      change will make it unnecessary.
    C: Investigate partial deployment can leave chargeable workers or dependencies even when the top-level operation
      fails; as a separate check, isolate that cause before changing Zonal control virtual machines with an autoscaled
      Azure Batch pool.
    D: Investigate Translate workload demand into compute requirements instead of diagnosing Deploy and verify the
      compute reference; as a separate check, proceed on the belief that a passing result at Translate workload
      demand into compute requirements identifies the current cause.
  answer: C
  explanations:
    A: The recovery guidance assumes that a syntactically valid pool can allocate billable or unreachable nodes
      when scale and outbound prerequisites are not checked together. Operationally, that condition belongs to Validate
      Batch behavior and graceful degradation and does not by itself invalidate Zonal control virtual machines with
      an autoscaled Azure Batch pool.
    B: The WAF consequence identifies that missing pre-mutation state, a failed ownership assertion, or an unrecorded
      returned resource ID makes the deployment unsafe. Operationally, the negative assertion must be evaluated
      now, independent of a later business change.
    C: The failure model establishes that partial deployment can leave chargeable workers or dependencies even when
      the top-level operation fails. Operationally, it is the checkpoint's causal failure model and should be isolated
      before retrying Deploy and verify the compute reference.
    D: The command-level assertion is anchored in the fact that the requirement matrix captures instruction set,
      vCPU, memory, scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity
      needs. Operationally, a passing result at Translate workload demand into compute requirements gives no causal
      evidence for the failure at Deploy and verify the compute reference.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q35
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: 'A review of ''Deploy and verify the compute reference'' begins with input from security engineering lead.
    The run encountered this modeled failure: Partial deployment can leave chargeable workers or dependencies even
    when the top-level operation fails. Which option is the safest recovery action?'
  options:
    A: 'Perform cleanup immediately: Delete only exact run-state IDs whose purpose, labId, runId, and expiresOn
      tags all match; follow reverse dependency order. Separately, take it as conclusive that the failed operation
      and its returned identifiers do not need reconciliation.'
    B: Create a different run identity before diagnosing 'Partial deployment can leave chargeable workers or dependencies
      even when the top-level operation fails'; for this decision, treat it as established that the first state
      record and returned identifiers can be discarded.
    C: Change Translate workload demand into compute requirements instead. Before sign-off, use as justification
      the claim that success at Translate workload demand into compute requirements will repair the failed state
      at Deploy and verify the compute reference.
    D: Reconcile deployment operations with run state, correct the failure, and redeploy idempotently with the same
      RunId; before approval, preserve the current run identity and evidence.
  answer: D
  explanations:
    A: The controlling fact is that delete only exact run-state IDs whose purpose, labId, runId, and expiresOn tags
      all match; follow reverse dependency order. The requirement-to-evidence link establishes that cleanup before
      reconciliation can erase evidence or strand a partially created dependency.
    B: The authored acceptance boundary states that partial deployment can leave chargeable workers or dependencies
      even when the top-level operation fails. The requirement-to-evidence link establishes that discarding the
      original run identity breaks ownership reconciliation and can duplicate a partial operation.
    C: The relevant observation is that the requirement matrix captures instruction set, vCPU, memory, scratch space,
      network, image, job duration, concurrency, availability, quota, and data-gravity needs. The requirement-to-evidence
      link establishes that altering an already separate checkpoint does not repair the modeled failure at Deploy
      and verify the compute reference.
    D: reconcile deployment operations with run state, correct the failure, and redeploy idempotently with the same
      RunId. The requirement-to-evidence link establishes that it corrects the narrow cause while retaining the
      same recovery trail and decision scope.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q36
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: '''Deploy and verify the compute reference'' awaits approval from finOps analyst. Without making a new change,
    the team must inspect the risk ''Partial deployment can leave chargeable workers or dependencies even when the
    top-level operation fails'' using the Azure CLI + Bicep lane. What should the architect select as the read-only,
    lane-correct inspection?'
  options:
    A: Rerun the Deploy and verify the compute reference implementation command and infer the expected state; then
      treat as decisive the assertion that absence of a shell error proves every property.
    B: 'Inspect the documented properties for Deploy and verify the compute reference; as an independent condition,
      retain this evidence: preserve deployment operations, schema-valid run state, exact resource IDs, effective
      tags, and assertion outcomes.'
    C: 'Run only this negative inspection for Deploy and verify the compute reference: Missing pre-mutation state,
      a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe. Independently,
      base approval on the claim that an empty negative result reports every required positive property.'
    D: Run the positive inspection for Compile and lint the compute Bicep design and apply it to Deploy and verify
      the compute reference. Next, use the premise that any command from the same lane proves the current checkpoint.
  answer: B
  explanations:
    A: The scenario makes clear that the implementation command. The requirement-to-evidence link establishes that
      it can mutate state and shell success does not independently assert the expected properties.
    B: The checkpoint specifically records that the bounded reference footprint deploys only after execution and
      cost acknowledgements, and returned IDs are persisted immediately in run state. The requirement-to-evidence
      link establishes that the read-only inspection directly tests the properties required at Deploy and verify
      the compute reference.
    C: The architecture evidence must show that the negative inspection. The requirement-to-evidence link establishes
      that absence of the prohibited condition is necessary but does not establish the positive architecture state.
    D: The applicable design condition is that the positive inspection for Compile and lint the compute Bicep design.
      The requirement-to-evidence link establishes that it is lane-correct but proves Compile and lint the compute
      Bicep design, not Deploy and verify the compute reference.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q37
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: '''Deploy and verify the compute reference'' is reopened at the request of operations manager. A passing
    positive check does not by itself prove this negative assertion: Missing pre-mutation state, a failed ownership
    assertion, or an unrecorded returned resource ID makes the deployment unsafe. Select the assertion pair that
    proves both conditions independently.'
  options:
    A: Verify only the positive result for Deploy and verify the compute reference and report full compliance; as
      a separate check, accept without proof that every prohibited parallel state must therefore be absent.
    B: Prove only that missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource
      ID makes the deployment unsafe and report the intended configuration as present. Afterward, rely on the claim
      that absence is equivalent to positive-state evidence.
    C: Verify the positive properties for Deploy and verify the compute reference; then independently verify that
      missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the
      deployment unsafe.
    D: Use Preview the tagged reference deployment's negative assertion for Deploy and verify the compute reference;
      next, rely on the belief that negative assertions are interchangeable between checkpoints.
  answer: C
  explanations:
    A: The retained result must be reconciled with the fact that the bounded reference footprint deploys only after
      execution and cost acknowledgements, and returned IDs are persisted immediately in run state. The requirement-to-evidence
      link establishes that the positive result alone does not test the explicit anti-condition 'Missing pre-mutation
      state, a failed ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe'.
    B: The decision tension comes from the fact that missing pre-mutation state, a failed ownership assertion, or
      an unrecorded returned resource ID makes the deployment unsafe. The requirement-to-evidence link establishes
      that absence evidence cannot demonstrate the required positive state 'The bounded reference footprint deploys
      only after execution and cost acknowledgements, and returned IDs are persisted immediately in run state'.
    C: 'The review is governed by this fact: the bounded reference footprint deploys only after execution and cost
      acknowledgements, and returned IDs are persisted immediately in run state; Missing pre-mutation state, a failed
      ownership assertion, or an unrecorded returned resource ID makes the deployment unsafe. The requirement-to-evidence
      link establishes that two independent observations prevent a passing positive check from concealing an unsafe
      parallel state.'
    D: The safe operating boundary says that any destructive change, unbounded autoscale maximum, untagged resource,
      or unrelated scope must block execution. The requirement-to-evidence link establishes that the second assertion
      is valid for Preview the tagged reference deployment but leaves this checkpoint's prohibited state untested.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q38
  difficulty: applied
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: 'A design review of ''Deploy and verify the compute reference'' includes aerospace simulation lead. The
    board wants the Well-Architected consequence of mitigating this risk: Partial deployment can leave chargeable
    workers or dependencies even when the top-level operation fails. Which response provides the consequence attributable
    to this checkpoint?'
  options:
    A: 'Record this consequence: Operational Excellence: declarative deployment and immediate state capture make
      partial failure recoverable. Also, tie it to LAB18-REQ-04.'
    B: Use the Validate Batch behavior and graceful degradation consequence as the result for Deploy and verify
      the compute reference. Before sign-off, consider it sufficient that a pillar statement remains valid when
      moved away from Validate Batch behavior and graceful degradation.
    C: Remove the control responsible for the Deploy and verify the compute reference outcome; as an independent
      condition, take it as conclusive that a moderate cost classification outweighs the mandatory architecture
      state.
    D: 'Treat ''Operational Excellence: declarative deployment and immediate state capture make partial failure
      recoverable'' as proof that all five pillars pass; before closing the checkpoint, treat it as established
      that the checkpoint ''Deploy and verify the compute reference'' no longer needs its separate negative check.'
  answer: A
  explanations:
    A: 'The traceable checkpoint outcome is that operational Excellence: declarative deployment and immediate state
      capture make partial failure recoverable. The requirement-to-evidence link establishes that it states the
      authored pillar consequence of the control evaluated at Deploy and verify the compute reference.'
    B: 'The failure model establishes that reliability: idempotent tasks and resilient orchestration tolerate individual
      worker and zone failures. The requirement-to-evidence link establishes that that tradeoff belongs to Validate
      Batch behavior and graceful degradation and does not explain this checkpoint''s decision.'
    C: The recovery guidance assumes that the required outcome at Deploy and verify the compute reference. The requirement-to-evidence
      link establishes that cost Optimization cannot remove the acceptance condition 'The bounded reference footprint
      deploys only after execution and cost acknowledgements, and returned IDs are persisted immediately in run
      state'.
    D: The WAF consequence identifies that missing pre-mutation state, a failed ownership assertion, or an unrecorded
      returned resource ID makes the deployment unsafe. The requirement-to-evidence link establishes that one positive
      command cannot establish every pillar, especially while the negative state remains unchecked.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q39
  difficulty: advanced
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: 'The team asks compute platform owner to assess ''Deploy and verify the compute reference''. A material
    change now applies: Export-controlled simulations can no longer use low-priority capacity, and the processing
    window is shortened from eight hours to five. Which choice gives the team the correct revision to the decision
    record?'
  options:
    A: Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Deploy and verify the compute
      reference without recalculating criteria or eligibility. Next, proceed on the belief that the original weighted
      result is permanent.
    B: Select Flexible Virtual Machine Scale Sets running custom queue workers for Deploy and verify the compute
      reference without rechecking its mandatory constraints, and then treat as decisive the assertion that being
      different from the current design is an architecture criterion.
    C: Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Deploy
      and verify the compute reference. Afterward, supersede the ADR using the changed evidence for LAB18-REQ-04.
    D: Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Deploy and
      verify the compute reference by downgrading LAB18-REQ-04. Also, base approval on the claim that stakeholder
      approval is unnecessary when that requirement blocks the candidate.
  answer: C
  explanations:
    A: zonal control virtual machines with an autoscaled Azure Batch pool. Consequently, the original ADR remains
      historical evidence, but its score cannot answer a changed mandatory condition.
    B: The controlling fact is that flexible Virtual Machine Scale Sets running custom queue workers. Consequently,
      being different is not a criterion, and the candidate still must avoid the prohibited state at Deploy and
      verify the compute reference.
    C: The command-level assertion is anchored in the fact that zonal control virtual machines with an autoscaled
      Azure Batch pool at Deploy and verify the compute reference. The requirement-to-evidence link establishes
      that the material change 'Export-controlled simulations can no longer use low-priority capacity, and the processing
      window is shortened from eight hours to five.' requires fresh eligibility, weighted analysis, and a superseding
      decision.
    D: The authored acceptance boundary states that lAB18-REQ-04. Consequently, an architect cannot silently downgrade
      a stakeholder-owned mandatory requirement to protect a candidate.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q40
  difficulty: advanced
  objectiveId: INF-COMP-01
  checkpointId: LAB18-CP04
  stem: 'A recommendation on ''Deploy and verify the compute reference'' is requested by security engineering lead.
    After a partial run, cleanup must follow this dependency: Delete only exact run-state IDs whose purpose, labId,
    runId, and expiresOn tags all match; follow reverse dependency order. What is the dependency-safe cleanup plan?'
  options:
    A: 'Verify exact run-state IDs and ownership tags for Deploy and verify the compute reference. Separately, follow
      this dependency rule without purge: Delete only exact run-state IDs whose purpose, labId, runId, and expiresOn
      tags all match; follow reverse dependency order.'
    B: Apply the cleanup rule for Compile and lint the compute Bicep design before reconciling the current dependency;
      next, use as justification the claim that removing a parent needed to identify Deploy and verify the compute
      reference is harmless.
    C: Delete candidates by display name before comparing the Deploy and verify the compute reference ownership
      tags. In addition, accept without proof that the dependency rule 'Delete only exact run-state IDs whose purpose,
      labId, runId, and expiresOn tags all match; follow reverse dependency order' is optional.
    D: Destroy recoverable copies before retaining the Deploy and verify the compute reference negative assertion
      'Missing pre-mutation state, a failed ownership assertion, or an unrecorded returned resource ID makes the
      deployment unsafe'; before approval, rely on the claim that remaining command logs are sufficient recovery
      evidence.
  answer: A
  explanations:
    A: The relevant observation is that delete only exact run-state IDs whose purpose, labId, runId, and expiresOn
      tags all match; follow reverse dependency order. Consequently, exact identity, complete ownership tags, and
      the authored dependency order constrain cleanup and preserve recovery.
    B: The checkpoint specifically records that delete generated local JSON if retained; keep the authored Bicep
      source. Consequently, a cleanup rule for Compile and lint the compute Bicep design cannot override the dependency
      declared for Deploy and verify the compute reference.
    C: The scenario makes clear that preserve deployment operations, schema-valid run state, exact resource IDs,
      effective tags, and assertion outcomes. Consequently, names are not ownership proof; deletion requires the
      exact recorded identifier and every required tag.
    D: The architecture evidence must show that missing pre-mutation state, a failed ownership assertion, or an
      unrecorded returned resource ID makes the deployment unsafe. Consequently, irreversible purge is outside the
      lab contract and destroys evidence needed for residual-state validation.
  remediationAnchor: checkpoint-4
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q41
  difficulty: foundational
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: '''Validate Batch behavior and graceful degradation'' is assigned to operations manager. Approval requires
    a positive result plus this independent negative assertion: A nonzero node target, public node IP, autoscale
    formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review. What
    recommendation gives the reviewers the acceptance rule that makes LAB18-REQ-05 testable?'
  options:
    A: Select Flexible Virtual Machine Scale Sets running custom queue workers before checking Validate Batch behavior
      and graceful degradation; in a separate step, treat it as established that a successful deployment will later
      prove the architecture constraint.
    B: Use the passing result from Translate workload demand into compute requirements to approve Validate Batch
      behavior and graceful degradation. As another control, use as justification the claim that one control establishes
      an unrelated acceptance boundary.
    C: Require the documented positive state for Validate Batch behavior and graceful degradation. Then, verify
      that a nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound
      dependency must fail the bounded reference review.
    D: Choose Dedicated zonal virtual machines scheduled for the nightly processing window and skip the Validate
      Batch behavior and graceful degradation negative assertion; as a separate check, accept without proof that
      the candidate has the lowest implementation effort.
  answer: C
  explanations:
    A: 'The review is governed by this fact: a nonzero node target, public node IP, autoscale formula, unsupported
      image pair, or implicit outbound dependency must fail the bounded reference review. Consequently, a deployment
      result cannot prove LAB18-REQ-05, and Flexible Virtual Machine Scale Sets running custom queue workers still
      has to meet the mandatory boundary.'
    B: The retained result must be reconciled with the fact that the requirement matrix captures instruction set,
      vCPU, memory, scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity
      needs. Consequently, that outcome belongs to Translate workload demand into compute requirements and leaves
      Validate Batch behavior and graceful degradation unverified.
    C: The applicable design condition is that the exact pool emitted by the deployment remains at zero nodes, uses
      no public node IPs, and records the image, VM size, subnet, and later scale prerequisites without running
      a job. Consequently, the positive state and an independent negative assertion jointly make LAB18-REQ-05 testable.
    D: The decision tension comes from the fact that complete simulation batches inside the overnight window while
      keeping orchestration available and paying for burst capacity only when jobs require it. Consequently, implementation
      effort cannot justify skipping the negative assertion or displace LAB18-REQ-05.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q42
  difficulty: foundational
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: An assurance review of 'Validate Batch behavior and graceful degradation' includes aerospace simulation
    lead. The selected architecture is Zonal control virtual machines with an autoscaled Azure Batch pool; object
    existence alone is not success. Which action produces the intended successful finding?
  options:
    A: Record the exact pool emitted by the deployment remains at zero nodes, uses no public node IPs, and records
      the image, VM size, subnet, and later scale prerequisites without running a job. Next, classify it as success
      for LAB18-REQ-05.
    B: Use only the negative assertion 'A nonzero node target, public node IP, autoscale formula, unsupported image
      pair, or implicit outbound dependency must fail the bounded reference review' as the success result. Separately,
      base approval on the claim that absence proves every required positive property.
    C: Use the successful finding from Compile and lint the compute Bicep design as the result for Validate Batch
      behavior and graceful degradation; for this decision, use the premise that a property from the current checkpoint
      does not need to be inspected.
    D: Record the failure condition 'A syntactically valid pool can allocate billable or unreachable nodes when
      scale and outbound prerequisites are not checked together' as a successful state. Before sign-off, consider
      it sufficient that the command returned an object.
  answer: A
  explanations:
    A: The safe operating boundary says that the exact pool emitted by the deployment remains at zero nodes, uses
      no public node IPs, and records the image, VM size, subnet, and later scale prerequisites without running
      a job. Consequently, this is the authored target state for Validate Batch behavior and graceful degradation
      and directly supports LAB18-REQ-05.
    B: The traceable checkpoint outcome is that a nonzero node target, public node IP, autoscale formula, unsupported
      image pair, or implicit outbound dependency must fail the bounded reference review. Consequently, this is
      the independent prohibited-state assertion, not a successful finding.
    C: The failure model establishes that compilation succeeds and the template contains a tagged, parameterized,
      three-zone-capable VMSS at zero capacity plus a private, zero-node Batch pool, networking, identity, and no
      embedded secret. Consequently, evidence for Compile and lint the compute Bicep design cannot substitute for
      the properties required at Validate Batch behavior and graceful degradation.
    D: The recovery guidance assumes that a syntactically valid pool can allocate billable or unreachable nodes
      when scale and outbound prerequisites are not checked together. Consequently, resource existence or command
      output does not convert the documented failure condition into success.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q43
  difficulty: foundational
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: 'Approval of ''Validate Batch behavior and graceful degradation'' is questioned by compute platform owner.
    Evidence must address this risk without retaining credentials: A syntactically valid pool can allocate billable
    or unreachable nodes when scale and outbound prerequisites are not checked together. What best demonstrates
    sufficient, properly scoped evidence?'
  options:
    A: Retain store the exact pool ARM ID, zero-node scale settings, image and node-agent pair, subnet, public-IP
      mode, and documented outbound prerequisite. As another control, exclude credentials and unrelated response
      fields.
    B: Substitute the evidence from Preview the tagged reference deployment for Validate Batch behavior and graceful
      degradation; then rely on the claim that a related checkpoint proves the current expected state.
    C: Store unredacted Validate Batch behavior and graceful degradation output with operator, tenant, token, and
      request context. Independently, rely on the belief that reproduction requires every captured field.
    D: Record only the Validate Batch behavior and graceful degradation positive inspection's exit status. Next,
      proceed on the belief that projected properties and assertion results can be reconstructed later.
  answer: A
  explanations:
    A: The WAF consequence identifies that store the exact pool ARM ID, zero-node scale settings, image and node-agent
      pair, subnet, public-IP mode, and documented outbound prerequisite. Consequently, it captures the checkpoint's
      observable properties while keeping the evidence boundary narrow.
    B: The command-level assertion is anchored in the fact that archive the complete what-if result, reviewer decision,
      estimated hourly ceiling, and template and parameter hashes. Consequently, that evidence supports Preview
      the tagged reference deployment, so it cannot demonstrate The exact pool emitted by the deployment remains
      at zero nodes, uses no public node IPs, and records the image, VM size, subnet, and later scale prerequisites
      without running a job.
    C: unredacted implementation output. For this case, identity, tenant, or token material exceeds the non-secret
      evidence contract.
    D: The controlling fact is that the positive inspection's exit status. For this case, an exit code alone does
      not show whether the exact pool emitted by the deployment remains at zero nodes, uses no public node IPs,
      and records the image, VM size, subnet, and later scale prerequisites without running a job.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q44
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: The implementation review has reached 'Validate Batch behavior and graceful degradation'. The target is
    The exact pool emitted by the deployment remains at zero nodes, uses no public node IPs, and records the image,
    VM size, subnet, and later scale prerequisites without running a job, but the latest evidence does not show
    it. Which option is the most likely cause?
  options:
    A: Investigate a syntactically valid pool can allocate billable or unreachable nodes when scale and outbound
      prerequisites are not checked together. In addition, isolate that cause before changing Zonal control virtual
      machines with an autoscaled Azure Batch pool.
    B: Treat 'Partial deployment can leave chargeable workers or dependencies even when the top-level operation
      fails' as grounds to reject Validate Batch behavior and graceful degradation; as a separate check, take it
      as conclusive that deploy and verify the compute reference's failure model applies unchanged here.
    C: Ignore the negative assertion 'A nonzero node target, public node IP, autoscale formula, unsupported image
      pair, or implicit outbound dependency must fail the bounded reference review'. Afterward, treat it as established
      that a later material change will make it unnecessary.
    D: Investigate Translate workload demand into compute requirements instead of diagnosing Validate Batch behavior
      and graceful degradation; next, use as justification the claim that a passing result at Translate workload
      demand into compute requirements identifies the current cause.
  answer: A
  explanations:
    A: The authored acceptance boundary states that a syntactically valid pool can allocate billable or unreachable
      nodes when scale and outbound prerequisites are not checked together. For this case, it is the checkpoint's
      causal failure model and should be isolated before retrying Validate Batch behavior and graceful degradation.
    B: The relevant observation is that partial deployment can leave chargeable workers or dependencies even when
      the top-level operation fails. For this case, that condition belongs to Deploy and verify the compute reference
      and does not by itself invalidate Zonal control virtual machines with an autoscaled Azure Batch pool.
    C: The checkpoint specifically records that a nonzero node target, public node IP, autoscale formula, unsupported
      image pair, or implicit outbound dependency must fail the bounded reference review. For this case, the negative
      assertion must be evaluated now, independent of a later business change.
    D: The scenario makes clear that the requirement matrix captures instruction set, vCPU, memory, scratch space,
      network, image, job duration, concurrency, availability, quota, and data-gravity needs. For this case, a passing
      result at Translate workload demand into compute requirements gives no causal evidence for the failure at
      Validate Batch behavior and graceful degradation.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q45
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: 'The approach to ''Validate Batch behavior and graceful degradation'' is challenged by finOps analyst. The
    run encountered this modeled failure: A syntactically valid pool can allocate billable or unreachable nodes
    when scale and outbound prerequisites are not checked together. What should the architect select as the safest
    recovery action?'
  options:
    A: 'Perform cleanup immediately: Stop and delete the run-owned pool before other dependencies, then verify no
      active managed object remains. Before sign-off, treat as decisive the assertion that the failed operation
      and its returned identifiers do not need reconciliation.'
    B: Create a different run identity before diagnosing 'A syntactically valid pool can allocate billable or unreachable
      nodes when scale and outbound prerequisites are not checked together'; as an independent condition, base approval
      on the claim that the first state record and returned identifiers can be discarded.
    C: Restore both node targets to zero, validate the current image/SKU pair, and document explicit NAT or inspected-firewall
      egress before any separately authorized scale test. Before sign-off, preserve the current run identity and
      evidence.
    D: Change Translate workload demand into compute requirements instead; for the final assessment, use the premise
      that success at Translate workload demand into compute requirements will repair the failed state at Validate
      Batch behavior and graceful degradation.
  answer: C
  explanations:
    A: The applicable design condition is that stop and delete the run-owned pool before other dependencies, then
      verify no active managed object remains. For this case, cleanup before reconciliation can erase evidence or
      strand a partially created dependency.
    B: 'The review is governed by this fact: a syntactically valid pool can allocate billable or unreachable nodes
      when scale and outbound prerequisites are not checked together. For this case, discarding the original run
      identity breaks ownership reconciliation and can duplicate a partial operation.'
    C: The architecture evidence must show that restore both node targets to zero, validate the current image/SKU
      pair, and document explicit NAT or inspected-firewall egress before any separately authorized scale test.
      For this case, it corrects the narrow cause while retaining the same recovery trail and decision scope.
    D: The retained result must be reconciled with the fact that the requirement matrix captures instruction set,
      vCPU, memory, scratch space, network, image, job duration, concurrency, availability, quota, and data-gravity
      needs. For this case, altering an already separate checkpoint does not repair the modeled failure at Validate
      Batch behavior and graceful degradation.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q46
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: A decision test for 'Validate Batch behavior and graceful degradation' includes operations manager. Without
    making a new change, the team must inspect the risk 'A syntactically valid pool can allocate billable or unreachable
    nodes when scale and outbound prerequisites are not checked together' using the Azure CLI + Bicep lane. Select
    the read-only, lane-correct inspection.
  options:
    A: Rerun the Validate Batch behavior and graceful degradation implementation command and infer the expected
      state. Next, accept without proof that absence of a shell error proves every property.
    B: 'Run only this negative inspection for Validate Batch behavior and graceful degradation: A nonzero node target,
      public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded
      reference review, and then rely on the claim that an empty negative result reports every required positive
      property.'
    C: 'Inspect the documented properties for Validate Batch behavior and graceful degradation; afterward, retain
      this evidence: store the exact pool ARM ID, zero-node scale settings, image and node-agent pair, subnet, public-IP
      mode, and documented outbound prerequisite.'
    D: Run the positive inspection for Compile and lint the compute Bicep design and apply it to Validate Batch
      behavior and graceful degradation. Also, rely on the belief that any command from the same lane proves the
      current checkpoint.
  answer: C
  explanations:
    A: The safe operating boundary says that the implementation command. For this case, it can mutate state and
      shell success does not independently assert the expected properties.
    B: The traceable checkpoint outcome is that the negative inspection. For this case, absence of the prohibited
      condition is necessary but does not establish the positive architecture state.
    C: The decision tension comes from the fact that the exact pool emitted by the deployment remains at zero nodes,
      uses no public node IPs, and records the image, VM size, subnet, and later scale prerequisites without running
      a job. For this case, the read-only inspection directly tests the properties required at Validate Batch behavior
      and graceful degradation.
    D: The failure model establishes that the positive inspection for Compile and lint the compute Bicep design.
      For this case, it is lane-correct but proves Compile and lint the compute Bicep design, not Validate Batch
      behavior and graceful degradation.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q47
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: 'The architecture board reconsiders ''Validate Batch behavior and graceful degradation'' with aerospace
    simulation lead. A passing positive check does not by itself prove this negative assertion: A nonzero node target,
    public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail the bounded
    reference review. Which response provides the assertion pair that proves both conditions independently?'
  options:
    A: Verify only the positive result for Validate Batch behavior and graceful degradation and report full compliance;
      next, consider it sufficient that every prohibited parallel state must therefore be absent.
    B: Verify the positive properties for Validate Batch behavior and graceful degradation, and then independently
      verify that a nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit
      outbound dependency must fail the bounded reference review.
    C: Prove only that a nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit
      outbound dependency must fail the bounded reference review and report the intended configuration as present.
      In addition, take it as conclusive that absence is equivalent to positive-state evidence.
    D: Use Preview the tagged reference deployment's negative assertion for Validate Batch behavior and graceful
      degradation; before approval, treat it as established that negative assertions are interchangeable between
      checkpoints.
  answer: B
  explanations:
    A: The WAF consequence identifies that the exact pool emitted by the deployment remains at zero nodes, uses
      no public node IPs, and records the image, VM size, subnet, and later scale prerequisites without running
      a job. For this case, the positive result alone does not test the explicit anti-condition 'A nonzero node
      target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency must fail
      the bounded reference review'.
    B: The recovery guidance assumes that the exact pool emitted by the deployment remains at zero nodes, uses no
      public node IPs, and records the image, VM size, subnet, and later scale prerequisites without running a job;
      A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound dependency
      must fail the bounded reference review. For this case, two independent observations prevent a passing positive
      check from concealing an unsafe parallel state.
    C: The command-level assertion is anchored in the fact that a nonzero node target, public node IP, autoscale
      formula, unsupported image pair, or implicit outbound dependency must fail the bounded reference review. For
      this case, absence evidence cannot demonstrate the required positive state 'The exact pool emitted by the
      deployment remains at zero nodes, uses no public node IPs, and records the image, VM size, subnet, and later
      scale prerequisites without running a job'.
    D: any destructive change, unbounded autoscale maximum, untagged resource, or unrelated scope must block execution.
      That evidence means the second assertion is valid for Preview the tagged reference deployment but leaves this
      checkpoint's prohibited state untested.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q48
  difficulty: applied
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: 'A review of ''Validate Batch behavior and graceful degradation'' begins with input from compute platform
    owner. The board wants the Well-Architected consequence of mitigating this risk: A syntactically valid pool
    can allocate billable or unreachable nodes when scale and outbound prerequisites are not checked together. Which
    choice gives the team the consequence attributable to this checkpoint?'
  options:
    A: Use the Deploy and verify the compute reference consequence as the result for Validate Batch behavior and
      graceful degradation; separately, proceed on the belief that a pillar statement remains valid when moved away
      from Deploy and verify the compute reference.
    B: Remove the control responsible for the Validate Batch behavior and graceful degradation outcome. Then, treat
      as decisive the assertion that a moderate cost classification outweighs the mandatory architecture state.
    C: 'Treat ''Reliability: idempotent tasks and resilient orchestration tolerate individual worker and zone failures''
      as proof that all five pillars pass; afterward, base approval on the claim that the checkpoint ''Validate
      Batch behavior and graceful degradation'' no longer needs its separate negative check.'
    D: 'Record this consequence: Reliability: idempotent tasks and resilient orchestration tolerate individual worker
      and zone failures; as a separate check, tie it to LAB18-REQ-05.'
  answer: D
  explanations:
    A: 'The authored acceptance boundary states that operational Excellence: declarative deployment and immediate
      state capture make partial failure recoverable. That evidence means that tradeoff belongs to Deploy and verify
      the compute reference and does not explain this checkpoint''s decision.'
    B: The relevant observation is that the required outcome at Validate Batch behavior and graceful degradation.
      That evidence means cost Optimization cannot remove the acceptance condition 'The exact pool emitted by the
      deployment remains at zero nodes, uses no public node IPs, and records the image, VM size, subnet, and later
      scale prerequisites without running a job'.
    C: The checkpoint specifically records that a nonzero node target, public node IP, autoscale formula, unsupported
      image pair, or implicit outbound dependency must fail the bounded reference review. That evidence means one
      positive command cannot establish every pillar, especially while the negative state remains unchecked.
    D: 'The controlling fact is that reliability: idempotent tasks and resilient orchestration tolerate individual
      worker and zone failures. That evidence means it states the authored pillar consequence of the control evaluated
      at Validate Batch behavior and graceful degradation.'
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q49
  difficulty: advanced
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: '''Validate Batch behavior and graceful degradation'' awaits approval from security engineering lead. A
    material change now applies: Export-controlled simulations can no longer use low-priority capacity, and the
    processing window is shortened from eight hours to five. What is the correct revision to the decision record?'
  options:
    A: Re-score Zonal control virtual machines with an autoscaled Azure Batch pool and both alternatives for Validate
      Batch behavior and graceful degradation; before approval, supersede the ADR using the changed evidence for
      LAB18-REQ-05.
    B: Retain Zonal control virtual machines with an autoscaled Azure Batch pool at Validate Batch behavior and
      graceful degradation without recalculating criteria or eligibility. Also, use as justification the claim that
      the original weighted result is permanent.
    C: Select Flexible Virtual Machine Scale Sets running custom queue workers for Validate Batch behavior and graceful
      degradation without rechecking its mandatory constraints; in a separate step, accept without proof that being
      different from the current design is an architecture criterion.
    D: Keep Dedicated zonal virtual machines scheduled for the nightly processing window eligible at Validate Batch
      behavior and graceful degradation by downgrading LAB18-REQ-05. As another control, rely on the claim that
      stakeholder approval is unnecessary when that requirement blocks the candidate.
  answer: A
  explanations:
    A: The scenario makes clear that zonal control virtual machines with an autoscaled Azure Batch pool at Validate
      Batch behavior and graceful degradation. That evidence means the material change 'Export-controlled simulations
      can no longer use low-priority capacity, and the processing window is shortened from eight hours to five.'
      requires fresh eligibility, weighted analysis, and a superseding decision.
    B: The architecture evidence must show that zonal control virtual machines with an autoscaled Azure Batch pool.
      That evidence means the original ADR remains historical evidence, but its score cannot answer a changed mandatory
      condition.
    C: The applicable design condition is that flexible Virtual Machine Scale Sets running custom queue workers.
      That evidence means being different is not a criterion, and the candidate still must avoid the prohibited
      state at Validate Batch behavior and graceful degradation.
    D: 'The review is governed by this fact: lAB18-REQ-05. That evidence means an architect cannot silently downgrade
      a stakeholder-owned mandatory requirement to protect a candidate.'
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
- id: LAB18-Q50
  difficulty: advanced
  objectiveId: INF-COMP-02
  checkpointId: LAB18-CP05
  stem: '''Validate Batch behavior and graceful degradation'' is reopened at the request of finOps analyst. After
    a partial run, cleanup must follow this dependency: Stop and delete the run-owned pool before other dependencies,
    then verify no active managed object remains. Which recommendation supplies the dependency-safe cleanup plan?'
  options:
    A: Apply the cleanup rule for Compile and lint the compute Bicep design before reconciling the current dependency;
      before approval, use the premise that removing a parent needed to identify Validate Batch behavior and graceful
      degradation is harmless.
    B: Delete candidates by display name before comparing the Validate Batch behavior and graceful degradation ownership
      tags. Separately, consider it sufficient that the dependency rule 'Stop and delete the run-owned pool before
      other dependencies, then verify no active managed object remains' is optional.
    C: Destroy recoverable copies before retaining the Validate Batch behavior and graceful degradation negative
      assertion 'A nonzero node target, public node IP, autoscale formula, unsupported image pair, or implicit outbound
      dependency must fail the bounded reference review'; for this decision, take it as conclusive that remaining
      command logs are sufficient recovery evidence.
    D: 'Verify exact run-state IDs and ownership tags for Validate Batch behavior and graceful degradation; as an
      independent condition, follow this dependency rule without purge: Stop and delete the run-owned pool before
      other dependencies, then verify no active managed object remains.'
  answer: D
  explanations:
    A: The decision tension comes from the fact that delete generated local JSON if retained; keep the authored
      Bicep source. That evidence means a cleanup rule for Compile and lint the compute Bicep design cannot override
      the dependency declared for Validate Batch behavior and graceful degradation.
    B: The safe operating boundary says that store the exact pool ARM ID, zero-node scale settings, image and node-agent
      pair, subnet, public-IP mode, and documented outbound prerequisite. That evidence means names are not ownership
      proof; deletion requires the exact recorded identifier and every required tag.
    C: The traceable checkpoint outcome is that a nonzero node target, public node IP, autoscale formula, unsupported
      image pair, or implicit outbound dependency must fail the bounded reference review. That evidence means irreversible
      purge is outside the lab contract and destroys evidence needed for residual-state validation.
    D: The retained result must be reconciled with the fact that stop and delete the run-owned pool before other
      dependencies, then verify no active managed object remains. That evidence means exact identity, complete ownership
      tags, and the authored dependency order constrain cleanup and preserve recovery.
  remediationAnchor: checkpoint-5
  source: https://learn.microsoft.com/en-us/azure/architecture/guide/technology-choices/compute-decision-tree
  verifiedOn: '2026-09-02'
# END GENERATED AZ305 V1
