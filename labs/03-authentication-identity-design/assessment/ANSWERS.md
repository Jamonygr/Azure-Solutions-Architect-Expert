<!-- BEGIN GENERATED AZ305 V1 -->
# LAB-03 answer key

Use after completing the learner assessment. Every choice has a specific explanation.

## LAB03-Q01 — answer C

The implementation review has reached 'Establish identity population and tenant boundaries'. Approval requires a positive result plus this independent negative assertion: No production domain, tenant identifier, or live organization evidence is committed to the lab. Which proposal supplies the acceptance rule that makes LAB03-REQ-01 testable?

- ✗ **A. Select Single workforce tenant for employees partners and customers before checking Establish identity population and tenant boundaries; afterward, use the premise that a successful deployment will later prove the architecture constraint.** — The decision tension comes from the fact that no production domain, tenant identifier, or live organization evidence is committed to the lab. For this case, a deployment result cannot prove LAB03-REQ-01, and Single workforce tenant for employees partners and customers still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Select protocols and application registrations to approve Establish identity population and tenant boundaries; then consider it sufficient that one control establishes an unrelated acceptance boundary.** — The safe operating boundary says that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. For this case, that outcome belongs to Select protocols and application registrations and leaves Establish identity population and tenant boundaries unverified.
- ✓ **C. Require the documented positive state for Establish identity population and tenant boundaries. Afterward, verify that no production domain, tenant identifier, or live organization evidence is committed to the lab.** — The retained result must be reconciled with the fact that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. For this case, the positive state and an independent negative assertion jointly make LAB03-REQ-01 testable.
- ✗ **D. Choose Separate workforce tenants with custom application-managed customer credentials and skip the Establish identity population and tenant boundaries negative assertion. Independently, take it as conclusive that the candidate has the lowest implementation effort.** — The traceable checkpoint outcome is that provide secure low-friction sign-in for three populations while reducing credential and tenant-boundary risk. For this case, implementation effort cannot justify skipping the negative assertion or displace LAB03-REQ-01.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q02 — answer C

The approach to 'Establish identity population and tenant boundaries' is challenged by privacy counsel. The selected architecture is Workforce tenant plus a dedicated External ID external tenant and managed identities; object existence alone is not success. Which option best represents the intended successful finding?

- ✗ **A. Use only the negative assertion 'No production domain, tenant identifier, or live organization evidence is committed to the lab' as the success result. As another control, rely on the belief that absence proves every required positive property.** — The recovery guidance assumes that no production domain, tenant identifier, or live organization evidence is committed to the lab. For this case, this is the independent prohibited-state assertion, not a successful finding.
- ✗ **B. Use the successful finding from Design authentication methods and recovery as the result for Establish identity population and tenant boundaries; as a separate check, proceed on the belief that a property from the current checkpoint does not need to be inspected.** — The WAF consequence identifies that phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated. For this case, evidence for Design authentication methods and recovery cannot substitute for the properties required at Establish identity population and tenant boundaries.
- ✓ **C. Record workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. Separately, classify it as success for LAB03-REQ-01.** — The failure model establishes that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. For this case, this is the authored target state for Establish identity population and tenant boundaries and directly supports LAB03-REQ-01.
- ✗ **D. Record the failure condition 'A business population has conflicting collaboration, residency, or branding requirements' as a successful state. Afterward, treat as decisive the assertion that the command returned an object.** — The command-level assertion is anchored in the fact that a business population has conflicting collaboration, residency, or branding requirements. For this case, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q03 — answer A

A decision test for 'Establish identity population and tenant boundaries' includes chief information security officer. Evidence must address this risk without retaining credentials: A business population has conflicting collaboration, residency, or branding requirements. Which course of action provides sufficient, properly scoped evidence?

- ✓ **A. Retain synthetic tenant labels, boundary rationale, identity population, residency constraint, and administrative owner; before approval, exclude credentials and unrelated response fields.** — synthetic tenant labels, boundary rationale, identity population, residency constraint, and administrative owner. That evidence means it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **B. Substitute the evidence from Evaluate Conditional Access architecture for Establish identity population and tenant boundaries; for this decision, treat it as established that a related checkpoint proves the current expected state.** — The controlling fact is that synthetic policy matrix, included populations, excluded emergency accounts, grant controls, and rollout stage. That evidence means that evidence supports Evaluate Conditional Access architecture, so it cannot demonstrate Workforce, partner, customer, and workload identities have explicit tenant placement and data ownership.
- ✗ **C. Store unredacted Establish identity population and tenant boundaries output with operator, tenant, token, and request context. Before sign-off, use as justification the claim that reproduction requires every captured field.** — The authored acceptance boundary states that unredacted implementation output. That evidence means identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Establish identity population and tenant boundaries positive inspection's exit status; as an independent condition, accept without proof that projected properties and assertion results can be reconstructed later.** — The relevant observation is that the positive inspection's exit status. That evidence means an exit code alone does not show whether workforce, partner, customer, and workload identities have explicit tenant placement and data ownership.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q04 — answer B

The architecture board reconsiders 'Establish identity population and tenant boundaries' with patient application owner. The target is Workforce, partner, customer, and workload identities have explicit tenant placement and data ownership, but the latest evidence does not show it. Which finding constitutes the most likely cause?

- ✗ **A. Treat 'The destination service or hosting model does not support the required managed identity flow' as grounds to reject Establish identity population and tenant boundaries. Independently, base approval on the claim that prefer managed workload identities's failure model applies unchanged here.** — The scenario makes clear that the destination service or hosting model does not support the required managed identity flow. That evidence means that condition belongs to Prefer managed workload identities and does not by itself invalidate Workforce tenant plus a dedicated External ID external tenant and managed identities.
- ✓ **B. Investigate a business population has conflicting collaboration, residency, or branding requirements. Independently, isolate that cause before changing Workforce tenant plus a dedicated External ID external tenant and managed identities.** — The checkpoint specifically records that a business population has conflicting collaboration, residency, or branding requirements. That evidence means it is the checkpoint's causal failure model and should be isolated before retrying Establish identity population and tenant boundaries.
- ✗ **C. Ignore the negative assertion 'No production domain, tenant identifier, or live organization evidence is committed to the lab'. Next, use the premise that a later material change will make it unnecessary.** — The architecture evidence must show that no production domain, tenant identifier, or live organization evidence is committed to the lab. That evidence means the negative assertion must be evaluated now, independent of a later business change.
- ✗ **D. Investigate Select protocols and application registrations instead of diagnosing Establish identity population and tenant boundaries, and then consider it sufficient that a passing result at Select protocols and application registrations identifies the current cause.** — The applicable design condition is that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. That evidence means a passing result at Select protocols and application registrations gives no causal evidence for the failure at Establish identity population and tenant boundaries.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q05 — answer A

A review of 'Establish identity population and tenant boundaries' begins with input from partner operations. The run encountered this modeled failure: A business population has conflicting collaboration, residency, or branding requirements. Which recommendation delivers the safest recovery action?

- ✓ **A. Split the population decision and evaluate the constraint against a dedicated external tenant; in a separate step, preserve the current run identity and evidence.** — The review is governed by this fact: split the population decision and evaluate the constraint against a dedicated external tenant. That evidence means it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **B. Perform cleanup immediately: This design checkpoint is read-only and creates no directory object. Afterward, rely on the claim that the failed operation and its returned identifiers do not need reconciliation.** — The retained result must be reconciled with the fact that this design checkpoint is read-only and creates no directory object. That evidence means cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **C. Create a different run identity before diagnosing 'A business population has conflicting collaboration, residency, or branding requirements'; next, rely on the belief that the first state record and returned identifiers can be discarded.** — The decision tension comes from the fact that a business population has conflicting collaboration, residency, or branding requirements. That evidence means discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Select protocols and application registrations instead. In addition, proceed on the belief that success at Select protocols and application registrations will repair the failed state at Establish identity population and tenant boundaries.** — The safe operating boundary says that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. That evidence means altering an already separate checkpoint does not repair the modeled failure at Establish identity population and tenant boundaries.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q06 — answer B

'Establish identity population and tenant boundaries' awaits approval from privacy counsel. Without making a new change, the team must inspect the risk 'A business population has conflicting collaboration, residency, or branding requirements' using the PowerShell/Graph lane. Which response meets the need for the read-only, lane-correct inspection?

- ✗ **A. Rerun the Establish identity population and tenant boundaries implementation command and infer the expected state; as an independent condition, take it as conclusive that absence of a shell error proves every property.** — The failure model establishes that the implementation command. That evidence means it can mutate state and shell success does not independently assert the expected properties.
- ✓ **B. Inspect the documented properties for Establish identity population and tenant boundaries; next, retain this evidence: synthetic tenant labels, boundary rationale, identity population, residency constraint, and administrative owner.** — The traceable checkpoint outcome is that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. That evidence means the read-only inspection directly tests the properties required at Establish identity population and tenant boundaries.
- ✗ **C. Run only this negative inspection for Establish identity population and tenant boundaries: No production domain, tenant identifier, or live organization evidence is committed to the lab; as a second control, treat it as established that an empty negative result reports every required positive property.** — The recovery guidance assumes that the negative inspection. That evidence means absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Design authentication methods and recovery and apply it to Establish identity population and tenant boundaries. Then, use as justification the claim that any command from the same lane proves the current checkpoint.** — The WAF consequence identifies that the positive inspection for Design authentication methods and recovery. That evidence means it is lane-correct but proves Design authentication methods and recovery, not Establish identity population and tenant boundaries.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q07 — answer B

'Establish identity population and tenant boundaries' is reopened at the request of chief information security officer. A passing positive check does not by itself prove this negative assertion: No production domain, tenant identifier, or live organization evidence is committed to the lab. Which choice should be approved as the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Establish identity population and tenant boundaries and report full compliance, and then treat as decisive the assertion that every prohibited parallel state must therefore be absent.** — workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. The resulting architectural conclusion is that the positive result alone does not test the explicit anti-condition 'No production domain, tenant identifier, or live organization evidence is committed to the lab'.
- ✓ **B. Verify the positive properties for Establish identity population and tenant boundaries; for this decision, independently verify that no production domain, tenant identifier, or live organization evidence is committed to the lab.** — The command-level assertion is anchored in the fact that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership; No production domain, tenant identifier, or live organization evidence is committed to the lab. That evidence means two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **C. Prove only that no production domain, tenant identifier, or live organization evidence is committed to the lab and report the intended configuration as present. Also, base approval on the claim that absence is equivalent to positive-state evidence.** — The controlling fact is that no production domain, tenant identifier, or live organization evidence is committed to the lab. The resulting architectural conclusion is that absence evidence cannot demonstrate the required positive state 'Workforce, partner, customer, and workload identities have explicit tenant placement and data ownership'.
- ✗ **D. Use Evaluate Conditional Access architecture's negative assertion for Establish identity population and tenant boundaries; in a separate step, use the premise that negative assertions are interchangeable between checkpoints.** — The authored acceptance boundary states that no broad enforcement is accepted without emergency access exclusions and report-only evidence. The resulting architectural conclusion is that the second assertion is valid for Evaluate Conditional Access architecture but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q08 — answer D

A design review of 'Establish identity population and tenant boundaries' includes patient application owner. The board wants the Well-Architected consequence of mitigating this risk: A business population has conflicting collaboration, residency, or branding requirements. What should the team use as the consequence attributable to this checkpoint?

- ✗ **A. Use the Prefer managed workload identities consequence as the result for Establish identity population and tenant boundaries. In addition, accept without proof that a pillar statement remains valid when moved away from Prefer managed workload identities.** — The checkpoint specifically records that cost Optimization: reusable user-assigned identities are introduced only when their independent lifecycle justifies administration. The resulting architectural conclusion is that that tradeoff belongs to Prefer managed workload identities and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Establish identity population and tenant boundaries outcome; before approval, rely on the claim that a low cost classification outweighs the mandatory architecture state.** — The scenario makes clear that the required outcome at Establish identity population and tenant boundaries. The resulting architectural conclusion is that cost Optimization cannot remove the acceptance condition 'Workforce, partner, customer, and workload identities have explicit tenant placement and data ownership'.
- ✗ **C. Treat 'Reliability: explicit tenant ownership and emergency administration make each identity control plane recoverable' as proof that all five pillars pass. Separately, rely on the belief that the checkpoint 'Establish identity population and tenant boundaries' no longer needs its separate negative check.** — The architecture evidence must show that no production domain, tenant identifier, or live organization evidence is committed to the lab. The resulting architectural conclusion is that one positive command cannot establish every pillar, especially while the negative state remains unchecked.
- ✓ **D. Record this consequence: Reliability: explicit tenant ownership and emergency administration make each identity control plane recoverable. Then, tie it to LAB03-REQ-01.** — The relevant observation is that reliability: explicit tenant ownership and emergency administration make each identity control plane recoverable. The resulting architectural conclusion is that it states the authored pillar consequence of the control evaluated at Establish identity population and tenant boundaries.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q09 — answer A

The team asks partner operations to assess 'Establish identity population and tenant boundaries'. A material change now applies: A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary. Which option best establishes the correct revision to the decision record?

- ✓ **A. Re-score Workforce tenant plus a dedicated External ID external tenant and managed identities and both alternatives for Establish identity population and tenant boundaries. Next, supersede the ADR using the changed evidence for LAB03-REQ-01.** — The applicable design condition is that workforce tenant plus a dedicated External ID external tenant and managed identities at Establish identity population and tenant boundaries. The resulting architectural conclusion is that the material change 'A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **B. Retain Workforce tenant plus a dedicated External ID external tenant and managed identities at Establish identity population and tenant boundaries without recalculating criteria or eligibility. Then, consider it sufficient that the original weighted result is permanent.** — The review is governed by this fact: workforce tenant plus a dedicated External ID external tenant and managed identities. The resulting architectural conclusion is that the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **C. Select Single workforce tenant for employees partners and customers for Establish identity population and tenant boundaries without rechecking its mandatory constraints; afterward, take it as conclusive that being different from the current design is an architecture criterion.** — The retained result must be reconciled with the fact that single workforce tenant for employees partners and customers. The resulting architectural conclusion is that being different is not a criterion, and the candidate still must avoid the prohibited state at Establish identity population and tenant boundaries.
- ✗ **D. Keep Separate workforce tenants with custom application-managed customer credentials eligible at Establish identity population and tenant boundaries by downgrading LAB03-REQ-01; then treat it as established that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The decision tension comes from the fact that lAB03-REQ-01. The resulting architectural conclusion is that an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q10 — answer A

A recommendation on 'Establish identity population and tenant boundaries' is requested by privacy counsel. After a partial run, cleanup must follow this dependency: This design checkpoint is read-only and creates no directory object. Which answer identifies the dependency-safe cleanup plan?

- ✓ **A. Verify exact run-state IDs and ownership tags for Establish identity population and tenant boundaries. As another control, follow this dependency rule without purge: This design checkpoint is read-only and creates no directory object.** — The safe operating boundary says that this design checkpoint is read-only and creates no directory object. The resulting architectural conclusion is that exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **B. Apply the cleanup rule for Design authentication methods and recovery before reconciling the current dependency; in a separate step, proceed on the belief that removing a parent needed to identify Establish identity population and tenant boundaries is harmless.** — The traceable checkpoint outcome is that no authentication-method policy is changed by this inspection-only checkpoint. The resulting architectural conclusion is that a cleanup rule for Design authentication methods and recovery cannot override the dependency declared for Establish identity population and tenant boundaries.
- ✗ **C. Delete candidates by display name before comparing the Establish identity population and tenant boundaries ownership tags. As another control, treat as decisive the assertion that the dependency rule 'This design checkpoint is read-only and creates no directory object' is optional.** — The failure model establishes that synthetic tenant labels, boundary rationale, identity population, residency constraint, and administrative owner. The resulting architectural conclusion is that names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Establish identity population and tenant boundaries negative assertion 'No production domain, tenant identifier, or live organization evidence is committed to the lab'; as a separate check, base approval on the claim that remaining command logs are sufficient recovery evidence.** — The recovery guidance assumes that no production domain, tenant identifier, or live organization evidence is committed to the lab. The resulting architectural conclusion is that irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q11 — answer D

'Select protocols and application registrations' is assigned to privacy counsel. Approval requires a positive result plus this independent negative assertion: No long-lived client secret or unnecessarily broad multitenant audience is accepted. Which option best represents the acceptance rule that makes LAB03-REQ-02 testable?

- ✗ **A. Select Single workforce tenant for employees partners and customers before checking Select protocols and application registrations. Independently, rely on the belief that a successful deployment will later prove the architecture constraint.** — The command-level assertion is anchored in the fact that no long-lived client secret or unnecessarily broad multitenant audience is accepted. The resulting architectural conclusion is that a deployment result cannot prove LAB03-REQ-02, and Single workforce tenant for employees partners and customers still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Establish identity population and tenant boundaries to approve Select protocols and application registrations. Next, proceed on the belief that one control establishes an unrelated acceptance boundary.** — workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. Under the stated constraint, that outcome belongs to Establish identity population and tenant boundaries and leaves Select protocols and application registrations unverified.
- ✗ **C. Choose Separate workforce tenants with custom application-managed customer credentials and skip the Select protocols and application registrations negative assertion, and then treat as decisive the assertion that the candidate has the lowest implementation effort.** — The controlling fact is that provide secure low-friction sign-in for three populations while reducing credential and tenant-boundary risk. Under the stated constraint, implementation effort cannot justify skipping the negative assertion or displace LAB03-REQ-02.
- ✓ **D. Require the documented positive state for Select protocols and application registrations; before approval, verify that no long-lived client secret or unnecessarily broad multitenant audience is accepted.** — The WAF consequence identifies that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. The resulting architectural conclusion is that the positive state and an independent negative assertion jointly make LAB03-REQ-02 testable.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q12 — answer B

An assurance review of 'Select protocols and application registrations' includes chief information security officer. The selected architecture is Workforce tenant plus a dedicated External ID external tenant and managed identities; object existence alone is not success. Which course of action provides the intended successful finding?

- ✗ **A. Use only the negative assertion 'No long-lived client secret or unnecessarily broad multitenant audience is accepted' as the success result. Afterward, treat it as established that absence proves every required positive property.** — The relevant observation is that no long-lived client secret or unnecessarily broad multitenant audience is accepted. Under the stated constraint, this is the independent prohibited-state assertion, not a successful finding.
- ✓ **B. Record each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan; as an independent condition, classify it as success for LAB03-REQ-02.** — The authored acceptance boundary states that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. Under the stated constraint, this is the authored target state for Select protocols and application registrations and directly supports LAB03-REQ-02.
- ✗ **C. Use the successful finding from Design authentication methods and recovery as the result for Select protocols and application registrations; next, use as justification the claim that a property from the current checkpoint does not need to be inspected.** — The checkpoint specifically records that phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated. Under the stated constraint, evidence for Design authentication methods and recovery cannot substitute for the properties required at Select protocols and application registrations.
- ✗ **D. Record the failure condition 'A legacy application cannot support modern federation or secure redirect handling' as a successful state. In addition, accept without proof that the command returned an object.** — The scenario makes clear that a legacy application cannot support modern federation or secure redirect handling. Under the stated constraint, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q13 — answer C

Approval of 'Select protocols and application registrations' is questioned by patient application owner. Evidence must address this risk without retaining credentials: A legacy application cannot support modern federation or secure redirect handling. Which finding constitutes sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Evaluate Conditional Access architecture for Select protocols and application registrations; as an independent condition, base approval on the claim that a related checkpoint proves the current expected state.** — The applicable design condition is that synthetic policy matrix, included populations, excluded emergency accounts, grant controls, and rollout stage. Under the stated constraint, that evidence supports Evaluate Conditional Access architecture, so it cannot demonstrate Each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan.
- ✗ **B. Store unredacted Select protocols and application registrations output with operator, tenant, token, and request context; during the same review, use the premise that reproduction requires every captured field.** — The review is governed by this fact: unredacted implementation output. Under the stated constraint, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✓ **C. Retain synthetic application label, protocol, audience, redirect class, and permission rationale without credentials; then exclude credentials and unrelated response fields.** — The architecture evidence must show that synthetic application label, protocol, audience, redirect class, and permission rationale without credentials. Under the stated constraint, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **D. Record only the Select protocols and application registrations positive inspection's exit status. Then, consider it sufficient that projected properties and assertion results can be reconstructed later.** — The retained result must be reconciled with the fact that the positive inspection's exit status. Under the stated constraint, an exit code alone does not show whether each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q14 — answer D

The implementation review has reached 'Select protocols and application registrations'. The target is Each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan, but the latest evidence does not show it. Which recommendation delivers the most likely cause?

- ✗ **A. Treat 'The destination service or hosting model does not support the required managed identity flow' as grounds to reject Select protocols and application registrations, and then rely on the claim that prefer managed workload identities's failure model applies unchanged here.** — The safe operating boundary says that the destination service or hosting model does not support the required managed identity flow. Under the stated constraint, that condition belongs to Prefer managed workload identities and does not by itself invalidate Workforce tenant plus a dedicated External ID external tenant and managed identities.
- ✗ **B. Ignore the negative assertion 'No long-lived client secret or unnecessarily broad multitenant audience is accepted'. Also, rely on the belief that a later material change will make it unnecessary.** — The traceable checkpoint outcome is that no long-lived client secret or unnecessarily broad multitenant audience is accepted. Under the stated constraint, the negative assertion must be evaluated now, independent of a later business change.
- ✗ **C. Investigate Establish identity population and tenant boundaries instead of diagnosing Select protocols and application registrations; in a separate step, proceed on the belief that a passing result at Establish identity population and tenant boundaries identifies the current cause.** — The failure model establishes that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. Under the stated constraint, a passing result at Establish identity population and tenant boundaries gives no causal evidence for the failure at Select protocols and application registrations.
- ✓ **D. Investigate a legacy application cannot support modern federation or secure redirect handling. Also, isolate that cause before changing Workforce tenant plus a dedicated External ID external tenant and managed identities.** — The decision tension comes from the fact that a legacy application cannot support modern federation or secure redirect handling. Under the stated constraint, it is the checkpoint's causal failure model and should be isolated before retrying Select protocols and application registrations.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q15 — answer D

The approach to 'Select protocols and application registrations' is challenged by privacy counsel. The run encountered this modeled failure: A legacy application cannot support modern federation or secure redirect handling. Which response meets the need for the safest recovery action?

- ✗ **A. Perform cleanup immediately: No application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership. In addition, take it as conclusive that the failed operation and its returned identifiers do not need reconciliation.** — The WAF consequence identifies that no application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership. Under the stated constraint, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **B. Create a different run identity before diagnosing 'A legacy application cannot support modern federation or secure redirect handling'; before approval, treat it as established that the first state record and returned identifiers can be discarded.** — The command-level assertion is anchored in the fact that a legacy application cannot support modern federation or secure redirect handling. Under the stated constraint, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **C. Change Establish identity population and tenant boundaries instead. Separately, use as justification the claim that success at Establish identity population and tenant boundaries will repair the failed state at Select protocols and application registrations.** — workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. This matters because altering an already separate checkpoint does not repair the modeled failure at Select protocols and application registrations.
- ✓ **D. Introduce an identity-aware modernization facade or isolate the legacy protocol with compensating controls. Afterward, preserve the current run identity and evidence.** — The recovery guidance assumes that introduce an identity-aware modernization facade or isolate the legacy protocol with compensating controls. Under the stated constraint, it corrects the narrow cause while retaining the same recovery trail and decision scope.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q16 — answer A

A decision test for 'Select protocols and application registrations' includes chief information security officer. Without making a new change, the team must inspect the risk 'A legacy application cannot support modern federation or secure redirect handling' using the PowerShell/Graph lane. Which choice should be approved as the read-only, lane-correct inspection?

- ✓ **A. Inspect the documented properties for Select protocols and application registrations. Separately, retain this evidence: synthetic application label, protocol, audience, redirect class, and permission rationale without credentials.** — The controlling fact is that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. This matters because the read-only inspection directly tests the properties required at Select protocols and application registrations.
- ✗ **B. Rerun the Select protocols and application registrations implementation command and infer the expected state. Then, treat as decisive the assertion that absence of a shell error proves every property.** — The authored acceptance boundary states that the implementation command. This matters because it can mutate state and shell success does not independently assert the expected properties.
- ✗ **C. Run only this negative inspection for Select protocols and application registrations: No long-lived client secret or unnecessarily broad multitenant audience is accepted; afterward, base approval on the claim that an empty negative result reports every required positive property.** — The relevant observation is that the negative inspection. This matters because absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Design authentication methods and recovery and apply it to Select protocols and application registrations; then use the premise that any command from the same lane proves the current checkpoint.** — The checkpoint specifically records that the positive inspection for Design authentication methods and recovery. This matters because it is lane-correct but proves Design authentication methods and recovery, not Select protocols and application registrations.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q17 — answer C

The architecture board reconsiders 'Select protocols and application registrations' with patient application owner. A passing positive check does not by itself prove this negative assertion: No long-lived client secret or unnecessarily broad multitenant audience is accepted. What should the team use as the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Select protocols and application registrations and report full compliance; in a separate step, accept without proof that every prohibited parallel state must therefore be absent.** — The architecture evidence must show that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. This matters because the positive result alone does not test the explicit anti-condition 'No long-lived client secret or unnecessarily broad multitenant audience is accepted'.
- ✗ **B. Prove only that no long-lived client secret or unnecessarily broad multitenant audience is accepted and report the intended configuration as present. As another control, rely on the claim that absence is equivalent to positive-state evidence.** — The applicable design condition is that no long-lived client secret or unnecessarily broad multitenant audience is accepted. This matters because absence evidence cannot demonstrate the required positive state 'Each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan'.
- ✓ **C. Verify the positive properties for Select protocols and application registrations; for the recorded decision, independently verify that no long-lived client secret or unnecessarily broad multitenant audience is accepted.** — The scenario makes clear that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan; No long-lived client secret or unnecessarily broad multitenant audience is accepted. This matters because two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **D. Use Evaluate Conditional Access architecture's negative assertion for Select protocols and application registrations; as a separate check, rely on the belief that negative assertions are interchangeable between checkpoints.** — The review is governed by this fact: no broad enforcement is accepted without emergency access exclusions and report-only evidence. This matters because the second assertion is valid for Evaluate Conditional Access architecture but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q18 — answer C

A review of 'Select protocols and application registrations' begins with input from partner operations. The board wants the Well-Architected consequence of mitigating this risk: A legacy application cannot support modern federation or secure redirect handling. Which option best establishes the consequence attributable to this checkpoint?

- ✗ **A. Use the Prefer managed workload identities consequence as the result for Select protocols and application registrations. Separately, consider it sufficient that a pillar statement remains valid when moved away from Prefer managed workload identities.** — The decision tension comes from the fact that cost Optimization: reusable user-assigned identities are introduced only when their independent lifecycle justifies administration. This matters because that tradeoff belongs to Prefer managed workload identities and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Select protocols and application registrations outcome; for this decision, take it as conclusive that a low cost classification outweighs the mandatory architecture state.** — The safe operating boundary says that the required outcome at Select protocols and application registrations. This matters because cost Optimization cannot remove the acceptance condition 'Each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan'.
- ✓ **C. Record this consequence: Performance Efficiency: standardized federation avoids duplicating credential validation and identity stores in each application. Independently, tie it to LAB03-REQ-02.** — The retained result must be reconciled with the fact that performance Efficiency: standardized federation avoids duplicating credential validation and identity stores in each application. This matters because it states the authored pillar consequence of the control evaluated at Select protocols and application registrations.
- ✗ **D. Treat 'Performance Efficiency: standardized federation avoids duplicating credential validation and identity stores in each application' as proof that all five pillars pass. Before sign-off, treat it as established that the checkpoint 'Select protocols and application registrations' no longer needs its separate negative check.** — The traceable checkpoint outcome is that no long-lived client secret or unnecessarily broad multitenant audience is accepted. This matters because one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q19 — answer C

'Select protocols and application registrations' awaits approval from privacy counsel. A material change now applies: A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary. Which answer identifies the correct revision to the decision record?

- ✗ **A. Retain Workforce tenant plus a dedicated External ID external tenant and managed identities at Select protocols and application registrations without recalculating criteria or eligibility; then proceed on the belief that the original weighted result is permanent.** — The recovery guidance assumes that workforce tenant plus a dedicated External ID external tenant and managed identities. This matters because the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Single workforce tenant for employees partners and customers for Select protocols and application registrations without rechecking its mandatory constraints. Independently, treat as decisive the assertion that being different from the current design is an architecture criterion.** — The WAF consequence identifies that single workforce tenant for employees partners and customers. This matters because being different is not a criterion, and the candidate still must avoid the prohibited state at Select protocols and application registrations.
- ✓ **C. Re-score Workforce tenant plus a dedicated External ID external tenant and managed identities and both alternatives for Select protocols and application registrations; in a separate step, supersede the ADR using the changed evidence for LAB03-REQ-02.** — The failure model establishes that workforce tenant plus a dedicated External ID external tenant and managed identities at Select protocols and application registrations. This matters because the material change 'A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **D. Keep Separate workforce tenants with custom application-managed customer credentials eligible at Select protocols and application registrations by downgrading LAB03-REQ-02. Next, base approval on the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The command-level assertion is anchored in the fact that lAB03-REQ-02. This matters because an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q20 — answer B

'Select protocols and application registrations' is reopened at the request of chief information security officer. After a partial run, cleanup must follow this dependency: No application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership. What recommendation gives the reviewers the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Design authentication methods and recovery before reconciling the current dependency; as a separate check, use as justification the claim that removing a parent needed to identify Select protocols and application registrations is harmless.** — The controlling fact is that no authentication-method policy is changed by this inspection-only checkpoint. The checkpoint therefore requires that a cleanup rule for Design authentication methods and recovery cannot override the dependency declared for Select protocols and application registrations.
- ✓ **B. Verify exact run-state IDs and ownership tags for Select protocols and application registrations; next, follow this dependency rule without purge: No application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership.** — no application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership. The checkpoint therefore requires that exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **C. Delete candidates by display name before comparing the Select protocols and application registrations ownership tags. Afterward, accept without proof that the dependency rule 'No application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership' is optional.** — The authored acceptance boundary states that synthetic application label, protocol, audience, redirect class, and permission rationale without credentials. The checkpoint therefore requires that names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Select protocols and application registrations negative assertion 'No long-lived client secret or unnecessarily broad multitenant audience is accepted'; next, rely on the claim that remaining command logs are sufficient recovery evidence.** — The relevant observation is that no long-lived client secret or unnecessarily broad multitenant audience is accepted. The checkpoint therefore requires that irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q21 — answer C

A design review of 'Design authentication methods and recovery' includes chief information security officer. Approval requires a positive result plus this independent negative assertion: Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. Which course of action provides the acceptance rule that makes LAB03-REQ-03 testable?

- ✗ **A. Select Single workforce tenant for employees partners and customers before checking Design authentication methods and recovery, and then treat it as established that a successful deployment will later prove the architecture constraint.** — The scenario makes clear that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. The checkpoint therefore requires that a deployment result cannot prove LAB03-REQ-03, and Single workforce tenant for employees partners and customers still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Establish identity population and tenant boundaries to approve Design authentication methods and recovery. Also, use as justification the claim that one control establishes an unrelated acceptance boundary.** — The architecture evidence must show that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. The checkpoint therefore requires that that outcome belongs to Establish identity population and tenant boundaries and leaves Design authentication methods and recovery unverified.
- ✓ **C. Require the documented positive state for Design authentication methods and recovery. Before sign-off, verify that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles.** — The checkpoint specifically records that phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated. The checkpoint therefore requires that the positive state and an independent negative assertion jointly make LAB03-REQ-03 testable.
- ✗ **D. Choose Separate workforce tenants with custom application-managed customer credentials and skip the Design authentication methods and recovery negative assertion; in a separate step, accept without proof that the candidate has the lowest implementation effort.** — The applicable design condition is that provide secure low-friction sign-in for three populations while reducing credential and tenant-boundary risk. The checkpoint therefore requires that implementation effort cannot justify skipping the negative assertion or displace LAB03-REQ-03.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q22 — answer A

The team asks patient application owner to assess 'Design authentication methods and recovery'. The selected architecture is Workforce tenant plus a dedicated External ID external tenant and managed identities; object existence alone is not success. Which finding constitutes the intended successful finding?

- ✓ **A. Record phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated; afterward, classify it as success for LAB03-REQ-03.** — The review is governed by this fact: phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated. The checkpoint therefore requires that this is the authored target state for Design authentication methods and recovery and directly supports LAB03-REQ-03.
- ✗ **B. Use only the negative assertion 'Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles' as the success result. In addition, base approval on the claim that absence proves every required positive property.** — The retained result must be reconciled with the fact that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. The checkpoint therefore requires that this is the independent prohibited-state assertion, not a successful finding.
- ✗ **C. Use the successful finding from Select protocols and application registrations as the result for Design authentication methods and recovery; before approval, use the premise that a property from the current checkpoint does not need to be inspected.** — The decision tension comes from the fact that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. The checkpoint therefore requires that evidence for Select protocols and application registrations cannot substitute for the properties required at Design authentication methods and recovery.
- ✗ **D. Record the failure condition 'The target population lacks compatible devices or a safe bootstrap channel' as a successful state. Separately, consider it sufficient that the command returned an object.** — The safe operating boundary says that the target population lacks compatible devices or a safe bootstrap channel. The checkpoint therefore requires that resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q23 — answer B

A recommendation on 'Design authentication methods and recovery' is requested by partner operations. Evidence must address this risk without retaining credentials: The target population lacks compatible devices or a safe bootstrap channel. Which recommendation delivers sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Evaluate Conditional Access architecture for Design authentication methods and recovery. Then, rely on the claim that a related checkpoint proves the current expected state.** — The failure model establishes that synthetic policy matrix, included populations, excluded emergency accounts, grant controls, and rollout stage. The checkpoint therefore requires that that evidence supports Evaluate Conditional Access architecture, so it cannot demonstrate Phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated.
- ✓ **B. Retain method-class matrix, population scope, recovery owner, rollout dependency, and exception expiry, and then exclude credentials and unrelated response fields.** — The traceable checkpoint outcome is that method-class matrix, population scope, recovery owner, rollout dependency, and exception expiry. The checkpoint therefore requires that it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **C. Store unredacted Design authentication methods and recovery output with operator, tenant, token, and request context; afterward, rely on the belief that reproduction requires every captured field.** — The recovery guidance assumes that unredacted implementation output. The checkpoint therefore requires that identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Design authentication methods and recovery positive inspection's exit status; then proceed on the belief that projected properties and assertion results can be reconstructed later.** — The WAF consequence identifies that the positive inspection's exit status. The checkpoint therefore requires that an exit code alone does not show whether phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q24 — answer D

'Design authentication methods and recovery' is assigned to privacy counsel. The target is Phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated, but the latest evidence does not show it. Which response meets the need for the most likely cause?

- ✗ **A. Treat 'The destination service or hosting model does not support the required managed identity flow' as grounds to reject Design authentication methods and recovery; in a separate step, take it as conclusive that prefer managed workload identities's failure model applies unchanged here.** — the destination service or hosting model does not support the required managed identity flow. In the decision record, that condition belongs to Prefer managed workload identities and does not by itself invalidate Workforce tenant plus a dedicated External ID external tenant and managed identities.
- ✗ **B. Ignore the negative assertion 'Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles'. As another control, treat it as established that a later material change will make it unnecessary.** — The controlling fact is that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. In the decision record, the negative assertion must be evaluated now, independent of a later business change.
- ✗ **C. Investigate Establish identity population and tenant boundaries instead of diagnosing Design authentication methods and recovery; as a separate check, use as justification the claim that a passing result at Establish identity population and tenant boundaries identifies the current cause.** — The authored acceptance boundary states that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. In the decision record, a passing result at Establish identity population and tenant boundaries gives no causal evidence for the failure at Design authentication methods and recovery.
- ✓ **D. Investigate the target population lacks compatible devices or a safe bootstrap channel; as a separate check, isolate that cause before changing Workforce tenant plus a dedicated External ID external tenant and managed identities.** — The command-level assertion is anchored in the fact that the target population lacks compatible devices or a safe bootstrap channel. The checkpoint therefore requires that it is the checkpoint's causal failure model and should be isolated before retrying Design authentication methods and recovery.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q25 — answer B

An assurance review of 'Design authentication methods and recovery' includes chief information security officer. The run encountered this modeled failure: The target population lacks compatible devices or a safe bootstrap channel. Which choice should be approved as the safest recovery action?

- ✗ **A. Perform cleanup immediately: No authentication-method policy is changed by this inspection-only checkpoint. Separately, treat as decisive the assertion that the failed operation and its returned identifiers do not need reconciliation.** — The checkpoint specifically records that no authentication-method policy is changed by this inspection-only checkpoint. In the decision record, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✓ **B. Phase registration with Temporary Access Pass or an approved customer method and time-bound exceptions; before approval, preserve the current run identity and evidence.** — The relevant observation is that phase registration with Temporary Access Pass or an approved customer method and time-bound exceptions. In the decision record, it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **C. Create a different run identity before diagnosing 'The target population lacks compatible devices or a safe bootstrap channel'; for this decision, base approval on the claim that the first state record and returned identifiers can be discarded.** — The scenario makes clear that the target population lacks compatible devices or a safe bootstrap channel. In the decision record, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Establish identity population and tenant boundaries instead. Before sign-off, use the premise that success at Establish identity population and tenant boundaries will repair the failed state at Design authentication methods and recovery.** — The architecture evidence must show that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. In the decision record, altering an already separate checkpoint does not repair the modeled failure at Design authentication methods and recovery.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q26 — answer A

Approval of 'Design authentication methods and recovery' is questioned by patient application owner. Without making a new change, the team must inspect the risk 'The target population lacks compatible devices or a safe bootstrap channel' using the PowerShell/Graph lane. What should the team use as the read-only, lane-correct inspection?

- ✓ **A. Inspect the documented properties for Design authentication methods and recovery; as an independent condition, retain this evidence: method-class matrix, population scope, recovery owner, rollout dependency, and exception expiry.** — The applicable design condition is that phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated. In the decision record, the read-only inspection directly tests the properties required at Design authentication methods and recovery.
- ✗ **B. Rerun the Design authentication methods and recovery implementation command and infer the expected state; then accept without proof that absence of a shell error proves every property.** — The review is governed by this fact: the implementation command. In the decision record, it can mutate state and shell success does not independently assert the expected properties.
- ✗ **C. Run only this negative inspection for Design authentication methods and recovery: Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. Independently, rely on the claim that an empty negative result reports every required positive property.** — The retained result must be reconciled with the fact that the negative inspection. In the decision record, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Select protocols and application registrations and apply it to Design authentication methods and recovery. Next, rely on the belief that any command from the same lane proves the current checkpoint.** — The decision tension comes from the fact that the positive inspection for Select protocols and application registrations. In the decision record, it is lane-correct but proves Select protocols and application registrations, not Design authentication methods and recovery.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q27 — answer C

The implementation review has reached 'Design authentication methods and recovery'. A passing positive check does not by itself prove this negative assertion: Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. Which option best establishes the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Design authentication methods and recovery and report full compliance; as a separate check, consider it sufficient that every prohibited parallel state must therefore be absent.** — The traceable checkpoint outcome is that phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated. In the decision record, the positive result alone does not test the explicit anti-condition 'Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles'.
- ✗ **B. Prove only that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles and report the intended configuration as present. Afterward, take it as conclusive that absence is equivalent to positive-state evidence.** — The failure model establishes that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. In the decision record, absence evidence cannot demonstrate the required positive state 'Phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated'.
- ✓ **C. Verify the positive properties for Design authentication methods and recovery; then independently verify that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles.** — The safe operating boundary says that phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated; Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. In the decision record, two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **D. Use Evaluate Conditional Access architecture's negative assertion for Design authentication methods and recovery; next, treat it as established that negative assertions are interchangeable between checkpoints.** — The recovery guidance assumes that no broad enforcement is accepted without emergency access exclusions and report-only evidence. In the decision record, the second assertion is valid for Evaluate Conditional Access architecture but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q28 — answer B

The approach to 'Design authentication methods and recovery' is challenged by privacy counsel. The board wants the Well-Architected consequence of mitigating this risk: The target population lacks compatible devices or a safe bootstrap channel. Which answer identifies the consequence attributable to this checkpoint?

- ✗ **A. Use the Prefer managed workload identities consequence as the result for Design authentication methods and recovery. Before sign-off, proceed on the belief that a pillar statement remains valid when moved away from Prefer managed workload identities.** — The command-level assertion is anchored in the fact that cost Optimization: reusable user-assigned identities are introduced only when their independent lifecycle justifies administration. In the decision record, that tradeoff belongs to Prefer managed workload identities and does not explain this checkpoint's decision.
- ✓ **B. Record this consequence: Operational Excellence: phased registration and owned recovery paths make strong authentication supportable. Also, tie it to LAB03-REQ-03.** — The WAF consequence identifies that operational Excellence: phased registration and owned recovery paths make strong authentication supportable. In the decision record, it states the authored pillar consequence of the control evaluated at Design authentication methods and recovery.
- ✗ **C. Remove the control responsible for the Design authentication methods and recovery outcome; as an independent condition, treat as decisive the assertion that a low cost classification outweighs the mandatory architecture state.** — the required outcome at Design authentication methods and recovery. The independent assertion shows why cost Optimization cannot remove the acceptance condition 'Phishing-resistant workforce methods, customer journey methods, bootstrap, and account recovery are deliberately separated'.
- ✗ **D. Treat 'Operational Excellence: phased registration and owned recovery paths make strong authentication supportable' as proof that all five pillars pass; before closing the checkpoint, base approval on the claim that the checkpoint 'Design authentication methods and recovery' no longer needs its separate negative check.** — The controlling fact is that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. The independent assertion shows why one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q29 — answer D

A decision test for 'Design authentication methods and recovery' includes chief information security officer. A material change now applies: A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary. What recommendation gives the reviewers the correct revision to the decision record?

- ✗ **A. Retain Workforce tenant plus a dedicated External ID external tenant and managed identities at Design authentication methods and recovery without recalculating criteria or eligibility. Next, use as justification the claim that the original weighted result is permanent.** — The relevant observation is that workforce tenant plus a dedicated External ID external tenant and managed identities. The independent assertion shows why the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Single workforce tenant for employees partners and customers for Design authentication methods and recovery without rechecking its mandatory constraints, and then accept without proof that being different from the current design is an architecture criterion.** — The checkpoint specifically records that single workforce tenant for employees partners and customers. The independent assertion shows why being different is not a criterion, and the candidate still must avoid the prohibited state at Design authentication methods and recovery.
- ✗ **C. Keep Separate workforce tenants with custom application-managed customer credentials eligible at Design authentication methods and recovery by downgrading LAB03-REQ-03. Also, rely on the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The scenario makes clear that lAB03-REQ-03. The independent assertion shows why an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
- ✓ **D. Re-score Workforce tenant plus a dedicated External ID external tenant and managed identities and both alternatives for Design authentication methods and recovery. Afterward, supersede the ADR using the changed evidence for LAB03-REQ-03.** — The authored acceptance boundary states that workforce tenant plus a dedicated External ID external tenant and managed identities at Design authentication methods and recovery. The independent assertion shows why the material change 'A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary.' requires fresh eligibility, weighted analysis, and a superseding decision.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q30 — answer D

The architecture board reconsiders 'Design authentication methods and recovery' with patient application owner. After a partial run, cleanup must follow this dependency: No authentication-method policy is changed by this inspection-only checkpoint. Which action produces the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Select protocols and application registrations before reconciling the current dependency; next, use the premise that removing a parent needed to identify Design authentication methods and recovery is harmless.** — The applicable design condition is that no application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership. The independent assertion shows why a cleanup rule for Select protocols and application registrations cannot override the dependency declared for Design authentication methods and recovery.
- ✗ **B. Delete candidates by display name before comparing the Design authentication methods and recovery ownership tags. In addition, consider it sufficient that the dependency rule 'No authentication-method policy is changed by this inspection-only checkpoint' is optional.** — The review is governed by this fact: method-class matrix, population scope, recovery owner, rollout dependency, and exception expiry. The independent assertion shows why names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **C. Destroy recoverable copies before retaining the Design authentication methods and recovery negative assertion 'Weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles'; before approval, take it as conclusive that remaining command logs are sufficient recovery evidence.** — The retained result must be reconciled with the fact that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. The independent assertion shows why irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.
- ✓ **D. Verify exact run-state IDs and ownership tags for Design authentication methods and recovery. Separately, follow this dependency rule without purge: No authentication-method policy is changed by this inspection-only checkpoint.** — The architecture evidence must show that no authentication-method policy is changed by this inspection-only checkpoint. The independent assertion shows why exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q31 — answer D

A review of 'Evaluate Conditional Access architecture' begins with input from patient application owner. Approval requires a positive result plus this independent negative assertion: No broad enforcement is accepted without emergency access exclusions and report-only evidence. Which finding constitutes the acceptance rule that makes LAB03-REQ-04 testable?

- ✗ **A. Select Single workforce tenant for employees partners and customers before checking Evaluate Conditional Access architecture; in a separate step, base approval on the claim that a successful deployment will later prove the architecture constraint.** — The safe operating boundary says that no broad enforcement is accepted without emergency access exclusions and report-only evidence. The independent assertion shows why a deployment result cannot prove LAB03-REQ-04, and Single workforce tenant for employees partners and customers still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Establish identity population and tenant boundaries to approve Evaluate Conditional Access architecture. As another control, use the premise that one control establishes an unrelated acceptance boundary.** — The traceable checkpoint outcome is that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. The independent assertion shows why that outcome belongs to Establish identity population and tenant boundaries and leaves Evaluate Conditional Access architecture unverified.
- ✗ **C. Choose Separate workforce tenants with custom application-managed customer credentials and skip the Evaluate Conditional Access architecture negative assertion; as a separate check, consider it sufficient that the candidate has the lowest implementation effort.** — The failure model establishes that provide secure low-friction sign-in for three populations while reducing credential and tenant-boundary risk. The independent assertion shows why implementation effort cannot justify skipping the negative assertion or displace LAB03-REQ-04.
- ✓ **D. Require the documented positive state for Evaluate Conditional Access architecture. Then, verify that no broad enforcement is accepted without emergency access exclusions and report-only evidence.** — The decision tension comes from the fact that baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode. The independent assertion shows why the positive state and an independent negative assertion jointly make LAB03-REQ-04 testable.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q32 — answer A

'Evaluate Conditional Access architecture' awaits approval from partner operations. The selected architecture is Workforce tenant plus a dedicated External ID external tenant and managed identities; object existence alone is not success. Which recommendation delivers the intended successful finding?

- ✓ **A. Record baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode. Next, classify it as success for LAB03-REQ-04.** — The recovery guidance assumes that baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode. The independent assertion shows why this is the authored target state for Evaluate Conditional Access architecture and directly supports LAB03-REQ-04.
- ✗ **B. Use only the negative assertion 'No broad enforcement is accepted without emergency access exclusions and report-only evidence' as the success result. Separately, rely on the claim that absence proves every required positive property.** — The WAF consequence identifies that no broad enforcement is accepted without emergency access exclusions and report-only evidence. The independent assertion shows why this is the independent prohibited-state assertion, not a successful finding.
- ✗ **C. Use the successful finding from Select protocols and application registrations as the result for Evaluate Conditional Access architecture; for this decision, rely on the belief that a property from the current checkpoint does not need to be inspected.** — The command-level assertion is anchored in the fact that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. The independent assertion shows why evidence for Select protocols and application registrations cannot substitute for the properties required at Evaluate Conditional Access architecture.
- ✗ **D. Record the failure condition 'Overlapping policies create an untested lockout path or contradict a customer journey' as a successful state. Before sign-off, proceed on the belief that the command returned an object.** — overlapping policies create an untested lockout path or contradict a customer journey. Operationally, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q33 — answer A

'Evaluate Conditional Access architecture' is reopened at the request of privacy counsel. Evidence must address this risk without retaining credentials: Overlapping policies create an untested lockout path or contradict a customer journey. Which response meets the need for sufficient, properly scoped evidence?

- ✓ **A. Retain synthetic policy matrix, included populations, excluded emergency accounts, grant controls, and rollout stage. As another control, exclude credentials and unrelated response fields.** — The controlling fact is that synthetic policy matrix, included populations, excluded emergency accounts, grant controls, and rollout stage. Operationally, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **B. Substitute the evidence from Design authentication methods and recovery for Evaluate Conditional Access architecture; then take it as conclusive that a related checkpoint proves the current expected state.** — The authored acceptance boundary states that method-class matrix, population scope, recovery owner, rollout dependency, and exception expiry. Operationally, that evidence supports Design authentication methods and recovery, so it cannot demonstrate Baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode.
- ✗ **C. Store unredacted Evaluate Conditional Access architecture output with operator, tenant, token, and request context. Independently, treat it as established that reproduction requires every captured field.** — The relevant observation is that unredacted implementation output. Operationally, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Evaluate Conditional Access architecture positive inspection's exit status. Next, use as justification the claim that projected properties and assertion results can be reconstructed later.** — The checkpoint specifically records that the positive inspection's exit status. Operationally, an exit code alone does not show whether baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q34 — answer D

A design review of 'Evaluate Conditional Access architecture' includes chief information security officer. The target is Baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode, but the latest evidence does not show it. Which choice should be approved as the most likely cause?

- ✗ **A. Treat 'The destination service or hosting model does not support the required managed identity flow' as grounds to reject Evaluate Conditional Access architecture; as a separate check, treat as decisive the assertion that prefer managed workload identities's failure model applies unchanged here.** — The architecture evidence must show that the destination service or hosting model does not support the required managed identity flow. Operationally, that condition belongs to Prefer managed workload identities and does not by itself invalidate Workforce tenant plus a dedicated External ID external tenant and managed identities.
- ✗ **B. Ignore the negative assertion 'No broad enforcement is accepted without emergency access exclusions and report-only evidence'. Afterward, base approval on the claim that a later material change will make it unnecessary.** — The applicable design condition is that no broad enforcement is accepted without emergency access exclusions and report-only evidence. Operationally, the negative assertion must be evaluated now, independent of a later business change.
- ✗ **C. Investigate Establish identity population and tenant boundaries instead of diagnosing Evaluate Conditional Access architecture; next, use the premise that a passing result at Establish identity population and tenant boundaries identifies the current cause.** — The review is governed by this fact: workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. Operationally, a passing result at Establish identity population and tenant boundaries gives no causal evidence for the failure at Evaluate Conditional Access architecture.
- ✓ **D. Investigate overlapping policies create an untested lockout path or contradict a customer journey. In addition, isolate that cause before changing Workforce tenant plus a dedicated External ID external tenant and managed identities.** — The scenario makes clear that overlapping policies create an untested lockout path or contradict a customer journey. Operationally, it is the checkpoint's causal failure model and should be isolated before retrying Evaluate Conditional Access architecture.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q35 — answer D

The team asks patient application owner to assess 'Evaluate Conditional Access architecture'. The run encountered this modeled failure: Overlapping policies create an untested lockout path or contradict a customer journey. What should the team use as the safest recovery action?

- ✗ **A. Perform cleanup immediately: No policy mutation or deletion occurs; real tenant values are never retained. Before sign-off, accept without proof that the failed operation and its returned identifiers do not need reconciliation.** — The decision tension comes from the fact that no policy mutation or deletion occurs; real tenant values are never retained. Operationally, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **B. Create a different run identity before diagnosing 'Overlapping policies create an untested lockout path or contradict a customer journey'; as an independent condition, rely on the claim that the first state record and returned identifiers can be discarded.** — The safe operating boundary says that overlapping policies create an untested lockout path or contradict a customer journey. Operationally, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **C. Change Establish identity population and tenant boundaries instead; for the final assessment, rely on the belief that success at Establish identity population and tenant boundaries will repair the failed state at Evaluate Conditional Access architecture.** — The traceable checkpoint outcome is that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. Operationally, altering an already separate checkpoint does not repair the modeled failure at Evaluate Conditional Access architecture.
- ✓ **D. Evaluate policies in report-only mode against synthetic personas and resolve overlap before enforcement. Before sign-off, preserve the current run identity and evidence.** — The retained result must be reconciled with the fact that evaluate policies in report-only mode against synthetic personas and resolve overlap before enforcement. Operationally, it corrects the narrow cause while retaining the same recovery trail and decision scope.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q36 — answer B

A recommendation on 'Evaluate Conditional Access architecture' is requested by partner operations. Without making a new change, the team must inspect the risk 'Overlapping policies create an untested lockout path or contradict a customer journey' using the PowerShell/Graph lane. Which option best establishes the read-only, lane-correct inspection?

- ✗ **A. Rerun the Evaluate Conditional Access architecture implementation command and infer the expected state. Next, consider it sufficient that absence of a shell error proves every property.** — The recovery guidance assumes that the implementation command. Operationally, it can mutate state and shell success does not independently assert the expected properties.
- ✓ **B. Inspect the documented properties for Evaluate Conditional Access architecture; afterward, retain this evidence: synthetic policy matrix, included populations, excluded emergency accounts, grant controls, and rollout stage.** — The failure model establishes that baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode. Operationally, the read-only inspection directly tests the properties required at Evaluate Conditional Access architecture.
- ✗ **C. Run only this negative inspection for Evaluate Conditional Access architecture: No broad enforcement is accepted without emergency access exclusions and report-only evidence, and then take it as conclusive that an empty negative result reports every required positive property.** — The WAF consequence identifies that the negative inspection. Operationally, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Select protocols and application registrations and apply it to Evaluate Conditional Access architecture. Also, treat it as established that any command from the same lane proves the current checkpoint.** — The command-level assertion is anchored in the fact that the positive inspection for Select protocols and application registrations. Operationally, it is lane-correct but proves Select protocols and application registrations, not Evaluate Conditional Access architecture.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q37 — answer C

'Evaluate Conditional Access architecture' is assigned to privacy counsel. A passing positive check does not by itself prove this negative assertion: No broad enforcement is accepted without emergency access exclusions and report-only evidence. Which answer identifies the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Evaluate Conditional Access architecture and report full compliance; next, proceed on the belief that every prohibited parallel state must therefore be absent.** — The controlling fact is that baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode. The requirement-to-evidence link establishes that the positive result alone does not test the explicit anti-condition 'No broad enforcement is accepted without emergency access exclusions and report-only evidence'.
- ✗ **B. Prove only that no broad enforcement is accepted without emergency access exclusions and report-only evidence and report the intended configuration as present. In addition, treat as decisive the assertion that absence is equivalent to positive-state evidence.** — The authored acceptance boundary states that no broad enforcement is accepted without emergency access exclusions and report-only evidence. The requirement-to-evidence link establishes that absence evidence cannot demonstrate the required positive state 'Baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode'.
- ✓ **C. Verify the positive properties for Evaluate Conditional Access architecture, and then independently verify that no broad enforcement is accepted without emergency access exclusions and report-only evidence.** — baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode; No broad enforcement is accepted without emergency access exclusions and report-only evidence. The requirement-to-evidence link establishes that two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **D. Use Design authentication methods and recovery's negative assertion for Evaluate Conditional Access architecture; before approval, base approval on the claim that negative assertions are interchangeable between checkpoints.** — The relevant observation is that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. The requirement-to-evidence link establishes that the second assertion is valid for Design authentication methods and recovery but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q38 — answer A

An assurance review of 'Evaluate Conditional Access architecture' includes chief information security officer. The board wants the Well-Architected consequence of mitigating this risk: Overlapping policies create an untested lockout path or contradict a customer journey. What recommendation gives the reviewers the consequence attributable to this checkpoint?

- ✓ **A. Record this consequence: Security: contextual access policies strengthen verification while protecting an explicit emergency path; as a separate check, tie it to LAB03-REQ-04.** — The checkpoint specifically records that security: contextual access policies strengthen verification while protecting an explicit emergency path. The requirement-to-evidence link establishes that it states the authored pillar consequence of the control evaluated at Evaluate Conditional Access architecture.
- ✗ **B. Use the Prefer managed workload identities consequence as the result for Evaluate Conditional Access architecture; separately, use as justification the claim that a pillar statement remains valid when moved away from Prefer managed workload identities.** — The scenario makes clear that cost Optimization: reusable user-assigned identities are introduced only when their independent lifecycle justifies administration. The requirement-to-evidence link establishes that that tradeoff belongs to Prefer managed workload identities and does not explain this checkpoint's decision.
- ✗ **C. Remove the control responsible for the Evaluate Conditional Access architecture outcome. Then, accept without proof that a low cost classification outweighs the mandatory architecture state.** — The architecture evidence must show that the required outcome at Evaluate Conditional Access architecture. The requirement-to-evidence link establishes that cost Optimization cannot remove the acceptance condition 'Baseline, privileged, workload, guest, and customer access controls have explicit scope, exclusions, and rollout mode'.
- ✗ **D. Treat 'Security: contextual access policies strengthen verification while protecting an explicit emergency path' as proof that all five pillars pass; afterward, rely on the claim that the checkpoint 'Evaluate Conditional Access architecture' no longer needs its separate negative check.** — The applicable design condition is that no broad enforcement is accepted without emergency access exclusions and report-only evidence. The requirement-to-evidence link establishes that one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q39 — answer B

Approval of 'Evaluate Conditional Access architecture' is questioned by patient application owner. A material change now applies: A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary. Which action produces the correct revision to the decision record?

- ✗ **A. Retain Workforce tenant plus a dedicated External ID external tenant and managed identities at Evaluate Conditional Access architecture without recalculating criteria or eligibility. Also, use the premise that the original weighted result is permanent.** — The retained result must be reconciled with the fact that workforce tenant plus a dedicated External ID external tenant and managed identities. The requirement-to-evidence link establishes that the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✓ **B. Re-score Workforce tenant plus a dedicated External ID external tenant and managed identities and both alternatives for Evaluate Conditional Access architecture; before approval, supersede the ADR using the changed evidence for LAB03-REQ-04.** — The review is governed by this fact: workforce tenant plus a dedicated External ID external tenant and managed identities at Evaluate Conditional Access architecture. The requirement-to-evidence link establishes that the material change 'A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **C. Select Single workforce tenant for employees partners and customers for Evaluate Conditional Access architecture without rechecking its mandatory constraints; in a separate step, consider it sufficient that being different from the current design is an architecture criterion.** — The decision tension comes from the fact that single workforce tenant for employees partners and customers. The requirement-to-evidence link establishes that being different is not a criterion, and the candidate still must avoid the prohibited state at Evaluate Conditional Access architecture.
- ✗ **D. Keep Separate workforce tenants with custom application-managed customer credentials eligible at Evaluate Conditional Access architecture by downgrading LAB03-REQ-04. As another control, take it as conclusive that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The safe operating boundary says that lAB03-REQ-04. The requirement-to-evidence link establishes that an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q40 — answer A

The implementation review has reached 'Evaluate Conditional Access architecture'. After a partial run, cleanup must follow this dependency: No policy mutation or deletion occurs; real tenant values are never retained. What best demonstrates the dependency-safe cleanup plan?

- ✓ **A. Verify exact run-state IDs and ownership tags for Evaluate Conditional Access architecture; as an independent condition, follow this dependency rule without purge: No policy mutation or deletion occurs; real tenant values are never retained.** — The traceable checkpoint outcome is that no policy mutation or deletion occurs; real tenant values are never retained. The requirement-to-evidence link establishes that exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **B. Apply the cleanup rule for Select protocols and application registrations before reconciling the current dependency; before approval, rely on the belief that removing a parent needed to identify Evaluate Conditional Access architecture is harmless.** — The failure model establishes that no application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership. The requirement-to-evidence link establishes that a cleanup rule for Select protocols and application registrations cannot override the dependency declared for Evaluate Conditional Access architecture.
- ✗ **C. Delete candidates by display name before comparing the Evaluate Conditional Access architecture ownership tags. Separately, proceed on the belief that the dependency rule 'No policy mutation or deletion occurs; real tenant values are never retained' is optional.** — The recovery guidance assumes that synthetic policy matrix, included populations, excluded emergency accounts, grant controls, and rollout stage. The requirement-to-evidence link establishes that names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Evaluate Conditional Access architecture negative assertion 'No broad enforcement is accepted without emergency access exclusions and report-only evidence'; for this decision, treat as decisive the assertion that remaining command logs are sufficient recovery evidence.** — The WAF consequence identifies that no broad enforcement is accepted without emergency access exclusions and report-only evidence. The requirement-to-evidence link establishes that irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q41 — answer B

The approach to 'Prefer managed workload identities' is challenged by partner operations. Approval requires a positive result plus this independent negative assertion: No reusable application secret is selected where a managed identity can satisfy the trust boundary. Which recommendation delivers the acceptance rule that makes LAB03-REQ-05 testable?

- ✗ **A. Select Single workforce tenant for employees partners and customers before checking Prefer managed workload identities; as a separate check, rely on the claim that a successful deployment will later prove the architecture constraint.** — no reusable application secret is selected where a managed identity can satisfy the trust boundary. Consequently, a deployment result cannot prove LAB03-REQ-05, and Single workforce tenant for employees partners and customers still has to meet the mandatory boundary.
- ✓ **B. Require the documented positive state for Prefer managed workload identities. Independently, verify that no reusable application secret is selected where a managed identity can satisfy the trust boundary.** — The command-level assertion is anchored in the fact that azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership. The requirement-to-evidence link establishes that the positive state and an independent negative assertion jointly make LAB03-REQ-05 testable.
- ✗ **C. Use the passing result from Establish identity population and tenant boundaries to approve Prefer managed workload identities. Afterward, rely on the belief that one control establishes an unrelated acceptance boundary.** — The controlling fact is that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. Consequently, that outcome belongs to Establish identity population and tenant boundaries and leaves Prefer managed workload identities unverified.
- ✗ **D. Choose Separate workforce tenants with custom application-managed customer credentials and skip the Prefer managed workload identities negative assertion; next, proceed on the belief that the candidate has the lowest implementation effort.** — The authored acceptance boundary states that provide secure low-friction sign-in for three populations while reducing credential and tenant-boundary risk. Consequently, implementation effort cannot justify skipping the negative assertion or displace LAB03-REQ-05.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q42 — answer C

A decision test for 'Prefer managed workload identities' includes privacy counsel. The selected architecture is Workforce tenant plus a dedicated External ID external tenant and managed identities; object existence alone is not success. Which response meets the need for the intended successful finding?

- ✗ **A. Use only the negative assertion 'No reusable application secret is selected where a managed identity can satisfy the trust boundary' as the success result. Before sign-off, take it as conclusive that absence proves every required positive property.** — The checkpoint specifically records that no reusable application secret is selected where a managed identity can satisfy the trust boundary. Consequently, this is the independent prohibited-state assertion, not a successful finding.
- ✗ **B. Use the successful finding from Select protocols and application registrations as the result for Prefer managed workload identities; as an independent condition, treat it as established that a property from the current checkpoint does not need to be inspected.** — The scenario makes clear that each application has a supported OIDC or SAML pattern, audience, redirect ownership, and least-privilege API consent plan. Consequently, evidence for Select protocols and application registrations cannot substitute for the properties required at Prefer managed workload identities.
- ✓ **C. Record azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership; in a separate step, classify it as success for LAB03-REQ-05.** — The relevant observation is that azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership. Consequently, this is the authored target state for Prefer managed workload identities and directly supports LAB03-REQ-05.
- ✗ **D. Record the failure condition 'The destination service or hosting model does not support the required managed identity flow' as a successful state; separately, use as justification the claim that the command returned an object.** — The architecture evidence must show that the destination service or hosting model does not support the required managed identity flow. Consequently, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q43 — answer C

The architecture board reconsiders 'Prefer managed workload identities' with chief information security officer. Evidence must address this risk without retaining credentials: The destination service or hosting model does not support the required managed identity flow. Which choice should be approved as sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Design authentication methods and recovery for Prefer managed workload identities. Next, treat as decisive the assertion that a related checkpoint proves the current expected state.** — The review is governed by this fact: method-class matrix, population scope, recovery owner, rollout dependency, and exception expiry. Consequently, that evidence supports Design authentication methods and recovery, so it cannot demonstrate Azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership.
- ✗ **B. Store unredacted Prefer managed workload identities output with operator, tenant, token, and request context, and then base approval on the claim that reproduction requires every captured field.** — The retained result must be reconciled with the fact that unredacted implementation output. Consequently, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✓ **C. Retain synthetic workload identity label, identity type, lifecycle owner, token audience, and downstream dependency; next, exclude credentials and unrelated response fields.** — The applicable design condition is that synthetic workload identity label, identity type, lifecycle owner, token audience, and downstream dependency. Consequently, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **D. Record only the Prefer managed workload identities positive inspection's exit status. Also, use the premise that projected properties and assertion results can be reconstructed later.** — The decision tension comes from the fact that the positive inspection's exit status. Consequently, an exit code alone does not show whether azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q44 — answer C

A review of 'Prefer managed workload identities' begins with input from patient application owner. The target is Azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership, but the latest evidence does not show it. What should the team use as the most likely cause?

- ✗ **A. Treat 'Overlapping policies create an untested lockout path or contradict a customer journey' as grounds to reject Prefer managed workload identities; next, accept without proof that evaluate Conditional Access architecture's failure model applies unchanged here.** — The traceable checkpoint outcome is that overlapping policies create an untested lockout path or contradict a customer journey. Consequently, that condition belongs to Evaluate Conditional Access architecture and does not by itself invalidate Workforce tenant plus a dedicated External ID external tenant and managed identities.
- ✗ **B. Ignore the negative assertion 'No reusable application secret is selected where a managed identity can satisfy the trust boundary'. In addition, rely on the claim that a later material change will make it unnecessary.** — The failure model establishes that no reusable application secret is selected where a managed identity can satisfy the trust boundary. Consequently, the negative assertion must be evaluated now, independent of a later business change.
- ✓ **C. Investigate the destination service or hosting model does not support the required managed identity flow; for this decision, isolate that cause before changing Workforce tenant plus a dedicated External ID external tenant and managed identities.** — The safe operating boundary says that the destination service or hosting model does not support the required managed identity flow. Consequently, it is the checkpoint's causal failure model and should be isolated before retrying Prefer managed workload identities.
- ✗ **D. Investigate Establish identity population and tenant boundaries instead of diagnosing Prefer managed workload identities; before approval, rely on the belief that a passing result at Establish identity population and tenant boundaries identifies the current cause.** — The recovery guidance assumes that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. Consequently, a passing result at Establish identity population and tenant boundaries gives no causal evidence for the failure at Prefer managed workload identities.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q45 — answer A

'Prefer managed workload identities' awaits approval from partner operations. The run encountered this modeled failure: The destination service or hosting model does not support the required managed identity flow. Which option best establishes the safest recovery action?

- ✓ **A. Evaluate workload identity federation or a certificate credential stored in Key Vault with a short rotation interval. Then, preserve the current run identity and evidence.** — The WAF consequence identifies that evaluate workload identity federation or a certificate credential stored in Key Vault with a short rotation interval. Consequently, it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **B. Perform cleanup immediately: Directory objects are never deleted automatically; any future cleanup requires exact IDs and tenant-change acknowledgement; independently, consider it sufficient that the failed operation and its returned identifiers do not need reconciliation.** — The command-level assertion is anchored in the fact that directory objects are never deleted automatically; any future cleanup requires exact IDs and tenant-change acknowledgement. Consequently, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **C. Create a different run identity before diagnosing 'The destination service or hosting model does not support the required managed identity flow'. Then, take it as conclusive that the first state record and returned identifiers can be discarded.** — the destination service or hosting model does not support the required managed identity flow. For this case, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Establish identity population and tenant boundaries instead; afterward, treat it as established that success at Establish identity population and tenant boundaries will repair the failed state at Prefer managed workload identities.** — The controlling fact is that workforce, partner, customer, and workload identities have explicit tenant placement and data ownership. For this case, altering an already separate checkpoint does not repair the modeled failure at Prefer managed workload identities.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q46 — answer A

'Prefer managed workload identities' is reopened at the request of privacy counsel. Without making a new change, the team must inspect the risk 'The destination service or hosting model does not support the required managed identity flow' using the PowerShell/Graph lane. Which answer identifies the read-only, lane-correct inspection?

- ✓ **A. Inspect the documented properties for Prefer managed workload identities. Next, retain this evidence: synthetic workload identity label, identity type, lifecycle owner, token audience, and downstream dependency.** — The authored acceptance boundary states that azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership. For this case, the read-only inspection directly tests the properties required at Prefer managed workload identities.
- ✗ **B. Rerun the Prefer managed workload identities implementation command and infer the expected state. Also, proceed on the belief that absence of a shell error proves every property.** — The relevant observation is that the implementation command. For this case, it can mutate state and shell success does not independently assert the expected properties.
- ✗ **C. Run only this negative inspection for Prefer managed workload identities: No reusable application secret is selected where a managed identity can satisfy the trust boundary; in a separate step, treat as decisive the assertion that an empty negative result reports every required positive property.** — The checkpoint specifically records that the negative inspection. For this case, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Select protocols and application registrations and apply it to Prefer managed workload identities. As another control, base approval on the claim that any command from the same lane proves the current checkpoint.** — The scenario makes clear that the positive inspection for Select protocols and application registrations. For this case, it is lane-correct but proves Select protocols and application registrations, not Prefer managed workload identities.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q47 — answer D

A design review of 'Prefer managed workload identities' includes chief information security officer. A passing positive check does not by itself prove this negative assertion: No reusable application secret is selected where a managed identity can satisfy the trust boundary. What recommendation gives the reviewers the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Prefer managed workload identities and report full compliance; before approval, use as justification the claim that every prohibited parallel state must therefore be absent.** — The applicable design condition is that azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership. For this case, the positive result alone does not test the explicit anti-condition 'No reusable application secret is selected where a managed identity can satisfy the trust boundary'.
- ✗ **B. Prove only that no reusable application secret is selected where a managed identity can satisfy the trust boundary and report the intended configuration as present. Separately, accept without proof that absence is equivalent to positive-state evidence.** — The review is governed by this fact: no reusable application secret is selected where a managed identity can satisfy the trust boundary. For this case, absence evidence cannot demonstrate the required positive state 'Azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership'.
- ✗ **C. Use Design authentication methods and recovery's negative assertion for Prefer managed workload identities; for this decision, rely on the claim that negative assertions are interchangeable between checkpoints.** — The retained result must be reconciled with the fact that weak recovery factors are not treated as equivalent to phishing-resistant authentication for privileged roles. For this case, the second assertion is valid for Design authentication methods and recovery but leaves this checkpoint's prohibited state untested.
- ✓ **D. Verify the positive properties for Prefer managed workload identities. As another control, independently verify that no reusable application secret is selected where a managed identity can satisfy the trust boundary.** — The architecture evidence must show that azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership; No reusable application secret is selected where a managed identity can satisfy the trust boundary. For this case, two independent observations prevent a passing positive check from concealing an unsafe parallel state.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q48 — answer B

The team asks patient application owner to assess 'Prefer managed workload identities'. The board wants the Well-Architected consequence of mitigating this risk: The destination service or hosting model does not support the required managed identity flow. Which action produces the consequence attributable to this checkpoint?

- ✗ **A. Use the Evaluate Conditional Access architecture consequence as the result for Prefer managed workload identities; afterward, use the premise that a pillar statement remains valid when moved away from Evaluate Conditional Access architecture.** — The safe operating boundary says that security: contextual access policies strengthen verification while protecting an explicit emergency path. For this case, that tradeoff belongs to Evaluate Conditional Access architecture and does not explain this checkpoint's decision.
- ✓ **B. Record this consequence: Cost Optimization: reusable user-assigned identities are introduced only when their independent lifecycle justifies administration. In addition, tie it to LAB03-REQ-05.** — The decision tension comes from the fact that cost Optimization: reusable user-assigned identities are introduced only when their independent lifecycle justifies administration. For this case, it states the authored pillar consequence of the control evaluated at Prefer managed workload identities.
- ✗ **C. Remove the control responsible for the Prefer managed workload identities outcome; then consider it sufficient that a low cost classification outweighs the mandatory architecture state.** — The traceable checkpoint outcome is that the required outcome at Prefer managed workload identities. For this case, cost Optimization cannot remove the acceptance condition 'Azure-hosted components use system- or user-assigned managed identities with documented lifecycle ownership'.
- ✗ **D. Treat 'Cost Optimization: reusable user-assigned identities are introduced only when their independent lifecycle justifies administration' as proof that all five pillars pass. Independently, take it as conclusive that the checkpoint 'Prefer managed workload identities' no longer needs its separate negative check.** — The failure model establishes that no reusable application secret is selected where a managed identity can satisfy the trust boundary. For this case, one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q49 — answer D

A recommendation on 'Prefer managed workload identities' is requested by partner operations. A material change now applies: A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary. What best demonstrates the correct revision to the decision record?

- ✗ **A. Retain Workforce tenant plus a dedicated External ID external tenant and managed identities at Prefer managed workload identities without recalculating criteria or eligibility. As another control, rely on the belief that the original weighted result is permanent.** — The WAF consequence identifies that workforce tenant plus a dedicated External ID external tenant and managed identities. For this case, the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Single workforce tenant for employees partners and customers for Prefer managed workload identities without rechecking its mandatory constraints; as a separate check, proceed on the belief that being different from the current design is an architecture criterion.** — The command-level assertion is anchored in the fact that single workforce tenant for employees partners and customers. For this case, being different is not a criterion, and the candidate still must avoid the prohibited state at Prefer managed workload identities.
- ✗ **C. Keep Separate workforce tenants with custom application-managed customer credentials eligible at Prefer managed workload identities by downgrading LAB03-REQ-05. Afterward, treat as decisive the assertion that stakeholder approval is unnecessary when that requirement blocks the candidate.** — lAB03-REQ-05. That evidence means an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
- ✓ **D. Re-score Workforce tenant plus a dedicated External ID external tenant and managed identities and both alternatives for Prefer managed workload identities. Before sign-off, supersede the ADR using the changed evidence for LAB03-REQ-05.** — The recovery guidance assumes that workforce tenant plus a dedicated External ID external tenant and managed identities at Prefer managed workload identities. For this case, the material change 'A mobile patient application now requires native authentication and social federation while a regulator forbids health attributes from being stored in the customer directory; revise the journey and profile-data boundary.' requires fresh eligibility, weighted analysis, and a superseding decision.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)

## LAB03-Q50 — answer B

'Prefer managed workload identities' is assigned to privacy counsel. After a partial run, cleanup must follow this dependency: Directory objects are never deleted automatically; any future cleanup requires exact IDs and tenant-change acknowledgement. Which option is the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Select protocols and application registrations before reconciling the current dependency; for this decision, treat it as established that removing a parent needed to identify Prefer managed workload identities is harmless.** — The authored acceptance boundary states that no application is removed automatically; tenant changes require explicit acknowledgement and exact object ownership. That evidence means a cleanup rule for Select protocols and application registrations cannot override the dependency declared for Prefer managed workload identities.
- ✓ **B. Verify exact run-state IDs and ownership tags for Prefer managed workload identities; afterward, follow this dependency rule without purge: Directory objects are never deleted automatically; any future cleanup requires exact IDs and tenant-change acknowledgement.** — The controlling fact is that directory objects are never deleted automatically; any future cleanup requires exact IDs and tenant-change acknowledgement. That evidence means exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **C. Delete candidates by display name before comparing the Prefer managed workload identities ownership tags. Before sign-off, use as justification the claim that the dependency rule 'Directory objects are never deleted automatically; any future cleanup requires exact IDs and tenant-change acknowledgement' is optional.** — The relevant observation is that synthetic workload identity label, identity type, lifecycle owner, token audience, and downstream dependency. That evidence means names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Prefer managed workload identities negative assertion 'No reusable application secret is selected where a managed identity can satisfy the trust boundary'; as an independent condition, accept without proof that remaining command logs are sufficient recovery evidence.** — The checkpoint specifically records that no reusable application secret is selected where a managed identity can satisfy the trust boundary. That evidence means irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/entra/external-id/customers/overview-customers-ciam (verified 2026-09-02)
<!-- END GENERATED AZ305 V1 -->
