<!-- BEGIN GENERATED AZ305 V1 -->
# LAB-21 learner assessment

Choose one answer for each item. This page intentionally contains no answers or explanations.

## LAB21-Q01 — foundational

The architecture board reconsiders 'Define cache semantics and failure behavior' with digital commerce product owner. Approval requires a positive result plus this independent negative assertion: Treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail. Which answer describes the acceptance rule that makes LAB21-REQ-01 testable?

- A. Select Per-instance memory caches with image-baked configuration and imperative scripts before checking Define cache semantics and failure behavior. Afterward, treat it as established that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Separate configuration, features, and secrets to approve Define cache semantics and failure behavior; next, use as justification the claim that one control establishes an unrelated acceptance boundary.
- C. Require the documented positive state for Define cache semantics and failure behavior. Separately, verify that treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail.
- D. Choose Cosmos DB integrated cache with Key Vault references and Terraform delivery and skip the Define cache semantics and failure behavior negative assertion. In addition, accept without proof that the candidate has the lowest implementation effort.

## LAB21-Q02 — foundational

A review of 'Define cache semantics and failure behavior' begins with input from application platform lead. The selected architecture is Azure Managed Redis with Azure App Configuration and Bicep deployment stacks; object existence alone is not success. What should be recorded as the intended successful finding?

- A. Use only the negative assertion 'Treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail' as the success result; as an independent condition, base approval on the claim that absence proves every required positive property.
- B. Use the successful finding from Compile the delivery contract as the result for Define cache semantics and failure behavior; independently, use the premise that a property from the current checkpoint does not need to be inspected.
- C. Record the design states cache-aside behavior, key ownership, TTL, eviction, invalidation, stampede control, regional scope, and source-of-truth fallback; as another gate, classify it as success for LAB21-REQ-01.
- D. Record the failure condition 'Poor invalidation can serve stale prices while aggressive expiry overloads the database' as a successful state. Then, consider it sufficient that the command returned an object.

## LAB21-Q03 — foundational

'Define cache semantics and failure behavior' awaits approval from release engineering manager. Evidence must address this risk without retaining credentials: Poor invalidation can serve stale prices while aggressive expiry overloads the database. Which proposal supplies sufficient, properly scoped evidence?

- A. Substitute the evidence from Preview a bounded deployment for Define cache semantics and failure behavior, and then rely on the claim that a related checkpoint proves the current expected state.
- B. Store unredacted Define cache semantics and failure behavior output with operator, tenant, token, and request context. Also, rely on the belief that reproduction requires every captured field.
- C. Retain preserve access-pattern calculations, key and TTL examples, failure-mode tests, and service-selection rationale. Independently, exclude credentials and unrelated response fields.
- D. Record only the Define cache semantics and failure behavior positive inspection's exit status; in a separate step, proceed on the belief that projected properties and assertion results can be reconstructed later.

## LAB21-Q04 — applied

'Define cache semantics and failure behavior' is reopened at the request of security architect. The target is The design states cache-aside behavior, key ownership, TTL, eviction, invalidation, stampede control, regional scope, and source-of-truth fallback, but the latest evidence does not show it. Which option best represents the most likely cause?

- A. Treat 'Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds' as grounds to reject Define cache semantics and failure behavior. In addition, take it as conclusive that validate progressive configuration delivery's failure model applies unchanged here.
- B. Investigate poor invalidation can serve stale prices while aggressive expiry overloads the database; in a separate step, isolate that cause before changing Azure Managed Redis with Azure App Configuration and Bicep deployment stacks.
- C. Ignore the negative assertion 'Treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail'; before approval, treat it as established that a later material change will make it unnecessary.
- D. Investigate Separate configuration, features, and secrets instead of diagnosing Define cache semantics and failure behavior. Separately, use as justification the claim that a passing result at Separate configuration, features, and secrets identifies the current cause.

## LAB21-Q05 — applied

A design review of 'Define cache semantics and failure behavior' includes finOps analyst. The run encountered this modeled failure: Poor invalidation can serve stale prices while aggressive expiry overloads the database. Which course of action provides the safest recovery action?

- A. Perform cleanup immediately: Delete only run-owned synthetic cache keys; never flush a shared cache. Then, treat as decisive the assertion that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'Poor invalidation can serve stale prices while aggressive expiry overloads the database'; afterward, base approval on the claim that the first state record and returned identifiers can be discarded.
- C. Change Separate configuration, features, and secrets instead; then use the premise that success at Separate configuration, features, and secrets will repair the failed state at Define cache semantics and failure behavior.
- D. Adjust TTL and single-flight behavior, then replay the same read and invalidation workload; next, preserve the current run identity and evidence.

## LAB21-Q06 — applied

The team asks digital commerce product owner to assess 'Define cache semantics and failure behavior'. Without making a new change, the team must inspect the risk 'Poor invalidation can serve stale prices while aggressive expiry overloads the database' using the Azure PowerShell + Bicep lane. Which finding constitutes the read-only, lane-correct inspection?

- A. Rerun the Define cache semantics and failure behavior implementation command and infer the expected state; in a separate step, accept without proof that absence of a shell error proves every property.
- B. Run only this negative inspection for Define cache semantics and failure behavior: Treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail. As another control, rely on the claim that an empty negative result reports every required positive property.
- C. Run the positive inspection for Compile the delivery contract and apply it to Define cache semantics and failure behavior; as a separate check, rely on the belief that any command from the same lane proves the current checkpoint.
- D. Inspect the documented properties for Define cache semantics and failure behavior; for this decision, retain this evidence: preserve access-pattern calculations, key and TTL examples, failure-mode tests, and service-selection rationale.

## LAB21-Q07 — applied

A recommendation on 'Define cache semantics and failure behavior' is requested by application platform lead. A passing positive check does not by itself prove this negative assertion: Treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail. Which recommendation delivers the assertion pair that proves both conditions independently?

- A. Verify the positive properties for Define cache semantics and failure behavior. Then, independently verify that treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail.
- B. Verify only the positive result for Define cache semantics and failure behavior and report full compliance. Separately, consider it sufficient that every prohibited parallel state must therefore be absent.
- C. Prove only that treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail and report the intended configuration as present; for this decision, take it as conclusive that absence is equivalent to positive-state evidence.
- D. Use Preview a bounded deployment's negative assertion for Define cache semantics and failure behavior. Before sign-off, treat it as established that negative assertions are interchangeable between checkpoints.

## LAB21-Q08 — applied

'Define cache semantics and failure behavior' is assigned to release engineering manager. The board wants the Well-Architected consequence of mitigating this risk: Poor invalidation can serve stale prices while aggressive expiry overloads the database. Which response meets the need for the consequence attributable to this checkpoint?

- A. Record this consequence: Performance Efficiency: cache-aside with measured TTL reduces source latency and load while retaining a durable authority. Next, tie it to LAB21-REQ-01.
- B. Use the Validate progressive configuration delivery consequence as the result for Define cache semantics and failure behavior; then proceed on the belief that a pillar statement remains valid when moved away from Validate progressive configuration delivery.
- C. Remove the control responsible for the Define cache semantics and failure behavior outcome. Independently, treat as decisive the assertion that a moderate cost classification outweighs the mandatory architecture state.
- D. Treat 'Performance Efficiency: cache-aside with measured TTL reduces source latency and load while retaining a durable authority' as proof that all five pillars pass. Next, base approval on the claim that the checkpoint 'Define cache semantics and failure behavior' no longer needs its separate negative check.

## LAB21-Q09 — advanced

An assurance review of 'Define cache semantics and failure behavior' includes security architect. A material change now applies: A regulatory review requires the storefront to keep serving last-known-safe catalog data for thirty minutes during configuration-store or source-database unavailability. Which choice should be approved as the correct revision to the decision record?

- A. Retain Azure Managed Redis with Azure App Configuration and Bicep deployment stacks at Define cache semantics and failure behavior without recalculating criteria or eligibility; as a separate check, use as justification the claim that the original weighted result is permanent.
- B. Re-score Azure Managed Redis with Azure App Configuration and Bicep deployment stacks and both alternatives for Define cache semantics and failure behavior. As another control, supersede the ADR using the changed evidence for LAB21-REQ-01.
- C. Select Per-instance memory caches with image-baked configuration and imperative scripts for Define cache semantics and failure behavior without rechecking its mandatory constraints. Afterward, accept without proof that being different from the current design is an architecture criterion.
- D. Keep Cosmos DB integrated cache with Key Vault references and Terraform delivery eligible at Define cache semantics and failure behavior by downgrading LAB21-REQ-01; next, rely on the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB21-Q10 — advanced

Approval of 'Define cache semantics and failure behavior' is questioned by finOps analyst. After a partial run, cleanup must follow this dependency: Delete only run-owned synthetic cache keys; never flush a shared cache. What should the team use as the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Compile the delivery contract before reconciling the current dependency. Before sign-off, use the premise that removing a parent needed to identify Define cache semantics and failure behavior is harmless.
- B. Verify exact run-state IDs and ownership tags for Define cache semantics and failure behavior. In addition, follow this dependency rule without purge: Delete only run-owned synthetic cache keys; never flush a shared cache.
- C. Delete candidates by display name before comparing the Define cache semantics and failure behavior ownership tags; as an independent condition, consider it sufficient that the dependency rule 'Delete only run-owned synthetic cache keys; never flush a shared cache' is optional.
- D. Destroy recoverable copies before retaining the Define cache semantics and failure behavior negative assertion 'Treating cache contents as durable system of record, allowing unbounded keys, or failing closed on cache loss must fail'; during the same review, take it as conclusive that remaining command logs are sufficient recovery evidence.

## LAB21-Q11 — foundational

The implementation review has reached 'Separate configuration, features, and secrets'. Approval requires a positive result plus this independent negative assertion: A resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail. What should be recorded as the acceptance rule that makes LAB21-REQ-02 testable?

- A. Require the documented positive state for Separate configuration, features, and secrets; as an independent condition, verify that a resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail.
- B. Select Per-instance memory caches with image-baked configuration and imperative scripts before checking Separate configuration, features, and secrets. In addition, base approval on the claim that a successful deployment will later prove the architecture constraint.
- C. Use the passing result from Define cache semantics and failure behavior to approve Separate configuration, features, and secrets; before approval, use the premise that one control establishes an unrelated acceptance boundary.
- D. Choose Cosmos DB integrated cache with Key Vault references and Terraform delivery and skip the Separate configuration, features, and secrets negative assertion. Separately, consider it sufficient that the candidate has the lowest implementation effort.

## LAB21-Q12 — foundational

The approach to 'Separate configuration, features, and secrets' is challenged by release engineering manager. The selected architecture is Azure Managed Redis with Azure App Configuration and Bicep deployment stacks; object existence alone is not success. Which proposal supplies the intended successful finding?

- A. Use only the negative assertion 'A resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail' as the success result. Then, rely on the claim that absence proves every required positive property.
- B. Use the successful finding from Compile the delivery contract as the result for Separate configuration, features, and secrets; afterward, rely on the belief that a property from the current checkpoint does not need to be inspected.
- C. Record the failure condition 'Mixing secrets with ordinary configuration increases disclosure risk and complicates rotation' as a successful state; then proceed on the belief that the command returned an object.
- D. Record non-secret settings and feature flags live in App Configuration, secrets remain Key Vault references, and labels encode environment rather than tenant secrets; then classify it as success for LAB21-REQ-02.

## LAB21-Q13 — foundational

A decision test for 'Separate configuration, features, and secrets' includes security architect. Evidence must address this risk without retaining credentials: Mixing secrets with ordinary configuration increases disclosure risk and complicates rotation. Which option best represents sufficient, properly scoped evidence?

- A. Substitute the evidence from Preview a bounded deployment for Separate configuration, features, and secrets; in a separate step, take it as conclusive that a related checkpoint proves the current expected state.
- B. Store unredacted Separate configuration, features, and secrets output with operator, tenant, token, and request context. As another control, treat it as established that reproduction requires every captured field.
- C. Record only the Separate configuration, features, and secrets positive inspection's exit status; as a separate check, use as justification the claim that projected properties and assertion results can be reconstructed later.
- D. Retain save redacted key naming, label and feature-filter conventions, identity access matrix, and network decision. Also, exclude credentials and unrelated response fields.

## LAB21-Q14 — applied

The architecture board reconsiders 'Separate configuration, features, and secrets' with finOps analyst. The target is Non-secret settings and feature flags live in App Configuration, secrets remain Key Vault references, and labels encode environment rather than tenant secrets, but the latest evidence does not show it. Which course of action provides the most likely cause?

- A. Investigate mixing secrets with ordinary configuration increases disclosure risk and complicates rotation. Afterward, isolate that cause before changing Azure Managed Redis with Azure App Configuration and Bicep deployment stacks.
- B. Treat 'Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds' as grounds to reject Separate configuration, features, and secrets. Separately, treat as decisive the assertion that validate progressive configuration delivery's failure model applies unchanged here.
- C. Ignore the negative assertion 'A resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail'; for this decision, base approval on the claim that a later material change will make it unnecessary.
- D. Investigate Define cache semantics and failure behavior instead of diagnosing Separate configuration, features, and secrets. Before sign-off, use the premise that a passing result at Define cache semantics and failure behavior identifies the current cause.

## LAB21-Q15 — applied

A review of 'Separate configuration, features, and secrets' begins with input from digital commerce product owner. The run encountered this modeled failure: Mixing secrets with ordinary configuration increases disclosure risk and complicates rotation. Which finding constitutes the safest recovery action?

- A. Replace the sensitive value with a Key Vault reference and rerun recursive sensitive-field checks. Separately, preserve the current run identity and evidence.
- B. Perform cleanup immediately: Remove only run-owned non-secret keys and flags; never export or delete resolved secrets; then accept without proof that the failed operation and its returned identifiers do not need reconciliation.
- C. Create a different run identity before diagnosing 'Mixing secrets with ordinary configuration increases disclosure risk and complicates rotation'. Independently, rely on the claim that the first state record and returned identifiers can be discarded.
- D. Change Define cache semantics and failure behavior instead. Next, rely on the belief that success at Define cache semantics and failure behavior will repair the failed state at Separate configuration, features, and secrets.

## LAB21-Q16 — applied

'Separate configuration, features, and secrets' awaits approval from application platform lead. Without making a new change, the team must inspect the risk 'Mixing secrets with ordinary configuration increases disclosure risk and complicates rotation' using the Azure PowerShell + Bicep lane. Which recommendation delivers the read-only, lane-correct inspection?

- A. Rerun the Separate configuration, features, and secrets implementation command and infer the expected state; as a separate check, consider it sufficient that absence of a shell error proves every property.
- B. Inspect the documented properties for Separate configuration, features, and secrets; for the final assessment, retain this evidence: save redacted key naming, label and feature-filter conventions, identity access matrix, and network decision.
- C. Run only this negative inspection for Separate configuration, features, and secrets: A resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail. Afterward, take it as conclusive that an empty negative result reports every required positive property.
- D. Run the positive inspection for Compile the delivery contract and apply it to Separate configuration, features, and secrets; next, treat it as established that any command from the same lane proves the current checkpoint.

## LAB21-Q17 — applied

'Separate configuration, features, and secrets' is reopened at the request of release engineering manager. A passing positive check does not by itself prove this negative assertion: A resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail. Which response meets the need for the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Separate configuration, features, and secrets and report full compliance. Before sign-off, proceed on the belief that every prohibited parallel state must therefore be absent.
- B. Prove only that a resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail and report the intended configuration as present; as an independent condition, treat as decisive the assertion that absence is equivalent to positive-state evidence.
- C. Verify the positive properties for Separate configuration, features, and secrets. Independently, independently verify that a resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail.
- D. Use Preview a bounded deployment's negative assertion for Separate configuration, features, and secrets; for the recorded decision, base approval on the claim that negative assertions are interchangeable between checkpoints.

## LAB21-Q18 — applied

A design review of 'Separate configuration, features, and secrets' includes security architect. The board wants the Well-Architected consequence of mitigating this risk: Mixing secrets with ordinary configuration increases disclosure risk and complicates rotation. Which choice should be approved as the consequence attributable to this checkpoint?

- A. Use the Validate progressive configuration delivery consequence as the result for Separate configuration, features, and secrets. Next, use as justification the claim that a pillar statement remains valid when moved away from Validate progressive configuration delivery.
- B. Record this consequence: Security: managed identity, local-auth disablement, and Key Vault references keep secrets out of deployment artifacts; in a separate step, tie it to LAB21-REQ-02.
- C. Remove the control responsible for the Separate configuration, features, and secrets outcome, and then accept without proof that a moderate cost classification outweighs the mandatory architecture state.
- D. Treat 'Security: managed identity, local-auth disablement, and Key Vault references keep secrets out of deployment artifacts' as proof that all five pillars pass. Also, rely on the claim that the checkpoint 'Separate configuration, features, and secrets' no longer needs its separate negative check.

## LAB21-Q19 — advanced

The team asks finOps analyst to assess 'Separate configuration, features, and secrets'. A material change now applies: A regulatory review requires the storefront to keep serving last-known-safe catalog data for thirty minutes during configuration-store or source-database unavailability. What should the team use as the correct revision to the decision record?

- A. Re-score Azure Managed Redis with Azure App Configuration and Bicep deployment stacks and both alternatives for Separate configuration, features, and secrets; next, supersede the ADR using the changed evidence for LAB21-REQ-02.
- B. Retain Azure Managed Redis with Azure App Configuration and Bicep deployment stacks at Separate configuration, features, and secrets without recalculating criteria or eligibility; next, use the premise that the original weighted result is permanent.
- C. Select Per-instance memory caches with image-baked configuration and imperative scripts for Separate configuration, features, and secrets without rechecking its mandatory constraints. In addition, consider it sufficient that being different from the current design is an architecture criterion.
- D. Keep Cosmos DB integrated cache with Key Vault references and Terraform delivery eligible at Separate configuration, features, and secrets by downgrading LAB21-REQ-02; before approval, take it as conclusive that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB21-Q20 — advanced

A recommendation on 'Separate configuration, features, and secrets' is requested by digital commerce product owner. After a partial run, cleanup must follow this dependency: Remove only run-owned non-secret keys and flags; never export or delete resolved secrets. Which option best establishes the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Compile the delivery contract before reconciling the current dependency; as another gate, rely on the belief that removing a parent needed to identify Separate configuration, features, and secrets is harmless.
- B. Delete candidates by display name before comparing the Separate configuration, features, and secrets ownership tags. Then, proceed on the belief that the dependency rule 'Remove only run-owned non-secret keys and flags; never export or delete resolved secrets' is optional.
- C. Destroy recoverable copies before retaining the Separate configuration, features, and secrets negative assertion 'A resolved secret value, connection string, or environment-specific credential in Bicep parameters or application settings must fail'; afterward, treat as decisive the assertion that remaining command logs are sufficient recovery evidence.
- D. Verify exact run-state IDs and ownership tags for Separate configuration, features, and secrets; for this decision, follow this dependency rule without purge: Remove only run-owned non-secret keys and flags; never export or delete resolved secrets.

## LAB21-Q21 — foundational

'Compile the delivery contract' is assigned to release engineering manager. Approval requires a positive result plus this independent negative assertion: Hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail. Which proposal supplies the acceptance rule that makes LAB21-REQ-03 testable?

- A. Select Per-instance memory caches with image-baked configuration and imperative scripts before checking Compile the delivery contract. Separately, rely on the claim that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Define cache semantics and failure behavior to approve Compile the delivery contract; for this decision, rely on the belief that one control establishes an unrelated acceptance boundary.
- C. Choose Cosmos DB integrated cache with Key Vault references and Terraform delivery and skip the Compile the delivery contract negative assertion. Before sign-off, proceed on the belief that the candidate has the lowest implementation effort.
- D. Require the documented positive state for Compile the delivery contract; afterward, verify that hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail.

## LAB21-Q22 — foundational

An assurance review of 'Compile the delivery contract' includes security architect. The selected architecture is Azure Managed Redis with Azure App Configuration and Bicep deployment stacks; object existence alone is not success. Which option best represents the intended successful finding?

- A. Use only the negative assertion 'Hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail' as the success result; then take it as conclusive that absence proves every required positive property.
- B. Use the successful finding from Separate configuration, features, and secrets as the result for Compile the delivery contract. Independently, treat it as established that a property from the current checkpoint does not need to be inspected.
- C. Record the Bicep template compiles, pins GA resource APIs, includes the required Managed Redis database child, disables public access on both service parents, defines secret-free outputs, and states that private endpoints and DNS remain a required design step outside this what-if-only analogue, and then classify it as success for LAB21-REQ-03.
- D. Record the failure condition 'Declarative syntax alone does not prevent insecure defaults, oversized capacity, or destructive replacement' as a successful state. Next, use as justification the claim that the command returned an object.

## LAB21-Q23 — foundational

Approval of 'Compile the delivery contract' is questioned by finOps analyst. Evidence must address this risk without retaining credentials: Declarative syntax alone does not prevent insecure defaults, oversized capacity, or destructive replacement. Which course of action provides sufficient, properly scoped evidence?

- A. Substitute the evidence from Preview a bounded deployment for Compile the delivery contract; as a separate check, treat as decisive the assertion that a related checkpoint proves the current expected state.
- B. Retain preserve source and parameter hashes, compiler output, resource inventory, and traceability to configuration requirements; as a separate check, exclude credentials and unrelated response fields.
- C. Store unredacted Compile the delivery contract output with operator, tenant, token, and request context. Afterward, base approval on the claim that reproduction requires every captured field.
- D. Record only the Compile the delivery contract positive inspection's exit status; next, use the premise that projected properties and assertion results can be reconstructed later.

## LAB21-Q24 — applied

The implementation review has reached 'Compile the delivery contract'. The target is The Bicep template compiles, pins GA resource APIs, includes the required Managed Redis database child, disables public access on both service parents, defines secret-free outputs, and states that private endpoints and DNS remain a required design step outside this what-if-only analogue, but the latest evidence does not show it. Which finding constitutes the most likely cause?

- A. Treat 'Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds' as grounds to reject Compile the delivery contract. Before sign-off, accept without proof that validate progressive configuration delivery's failure model applies unchanged here.
- B. Ignore the negative assertion 'Hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail'; as an independent condition, rely on the claim that a later material change will make it unnecessary.
- C. Investigate Define cache semantics and failure behavior instead of diagnosing Compile the delivery contract; as another gate, rely on the belief that a passing result at Define cache semantics and failure behavior identifies the current cause.
- D. Investigate declarative syntax alone does not prevent insecure defaults, oversized capacity, or destructive replacement; before approval, isolate that cause before changing Azure Managed Redis with Azure App Configuration and Bicep deployment stacks.

## LAB21-Q25 — applied

The approach to 'Compile the delivery contract' is challenged by application platform lead. The run encountered this modeled failure: Declarative syntax alone does not prevent insecure defaults, oversized capacity, or destructive replacement. Which recommendation delivers the safest recovery action?

- A. Correct the failing declaration and repeat compile plus semantic assertions before preview; as an independent condition, preserve the current run identity and evidence.
- B. Perform cleanup immediately: Remove generated local JSON if retained; keep Bicep and the example parameter file. Next, consider it sufficient that the failed operation and its returned identifiers do not need reconciliation.
- C. Create a different run identity before diagnosing 'Declarative syntax alone does not prevent insecure defaults, oversized capacity, or destructive replacement', and then take it as conclusive that the first state record and returned identifiers can be discarded.
- D. Change Define cache semantics and failure behavior instead. Also, treat it as established that success at Define cache semantics and failure behavior will repair the failed state at Compile the delivery contract.

## LAB21-Q26 — applied

A decision test for 'Compile the delivery contract' includes release engineering manager. Without making a new change, the team must inspect the risk 'Declarative syntax alone does not prevent insecure defaults, oversized capacity, or destructive replacement' using the Azure PowerShell + Bicep lane. Which response meets the need for the read-only, lane-correct inspection?

- A. Rerun the Compile the delivery contract implementation command and infer the expected state; next, proceed on the belief that absence of a shell error proves every property.
- B. Run only this negative inspection for Compile the delivery contract: Hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail. In addition, treat as decisive the assertion that an empty negative result reports every required positive property.
- C. Inspect the documented properties for Compile the delivery contract; then retain this evidence: preserve source and parameter hashes, compiler output, resource inventory, and traceability to configuration requirements.
- D. Run the positive inspection for Separate configuration, features, and secrets and apply it to Compile the delivery contract; before approval, base approval on the claim that any command from the same lane proves the current checkpoint.

## LAB21-Q27 — applied

The architecture board reconsiders 'Compile the delivery contract' with security architect. A passing positive check does not by itself prove this negative assertion: Hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail. Which choice should be approved as the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Compile the delivery contract and report full compliance; without relying on inference, use as justification the claim that every prohibited parallel state must therefore be absent.
- B. Prove only that hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail and report the intended configuration as present. Then, accept without proof that absence is equivalent to positive-state evidence.
- C. Verify the positive properties for Compile the delivery contract. Also, independently verify that hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail.
- D. Use Preview a bounded deployment's negative assertion for Compile the delivery contract; afterward, rely on the claim that negative assertions are interchangeable between checkpoints.

## LAB21-Q28 — applied

A review of 'Compile the delivery contract' begins with input from finOps analyst. The board wants the Well-Architected consequence of mitigating this risk: Declarative syntax alone does not prevent insecure defaults, oversized capacity, or destructive replacement. What should the team use as the consequence attributable to this checkpoint?

- A. Record this consequence: Operational Excellence: a versioned declarative contract makes environment differences reviewable and repeatable. Afterward, tie it to LAB21-REQ-03.
- B. Use the Validate progressive configuration delivery consequence as the result for Compile the delivery contract. Also, use the premise that a pillar statement remains valid when moved away from Validate progressive configuration delivery.
- C. Remove the control responsible for the Compile the delivery contract outcome; in a separate step, consider it sufficient that a moderate cost classification outweighs the mandatory architecture state.
- D. Treat 'Operational Excellence: a versioned declarative contract makes environment differences reviewable and repeatable' as proof that all five pillars pass. As another control, take it as conclusive that the checkpoint 'Compile the delivery contract' no longer needs its separate negative check.

## LAB21-Q29 — advanced

'Compile the delivery contract' awaits approval from digital commerce product owner. A material change now applies: A regulatory review requires the storefront to keep serving last-known-safe catalog data for thirty minutes during configuration-store or source-database unavailability. Which option best establishes the correct revision to the decision record?

- A. Retain Azure Managed Redis with Azure App Configuration and Bicep deployment stacks at Compile the delivery contract without recalculating criteria or eligibility; before approval, rely on the belief that the original weighted result is permanent.
- B. Select Per-instance memory caches with image-baked configuration and imperative scripts for Compile the delivery contract without rechecking its mandatory constraints. Separately, proceed on the belief that being different from the current design is an architecture criterion.
- C. Keep Cosmos DB integrated cache with Key Vault references and Terraform delivery eligible at Compile the delivery contract by downgrading LAB21-REQ-03; for this decision, treat as decisive the assertion that stakeholder approval is unnecessary when that requirement blocks the candidate.
- D. Re-score Azure Managed Redis with Azure App Configuration and Bicep deployment stacks and both alternatives for Compile the delivery contract. Separately, supersede the ADR using the changed evidence for LAB21-REQ-03.

## LAB21-Q30 — advanced

'Compile the delivery contract' is reopened at the request of application platform lead. After a partial run, cleanup must follow this dependency: Remove generated local JSON if retained; keep Bicep and the example parameter file. Which answer identifies the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Separate configuration, features, and secrets before reconciling the current dependency; afterward, treat it as established that removing a parent needed to identify Compile the delivery contract is harmless.
- B. Delete candidates by display name before comparing the Compile the delivery contract ownership tags; then use as justification the claim that the dependency rule 'Remove generated local JSON if retained; keep Bicep and the example parameter file' is optional.
- C. Destroy recoverable copies before retaining the Compile the delivery contract negative assertion 'Hard-coded region names, plaintext secrets, missing expiry tags, or an unbounded production-sized cache must fail'. Independently, accept without proof that remaining command logs are sufficient recovery evidence.
- D. Verify exact run-state IDs and ownership tags for Compile the delivery contract; before approval, follow this dependency rule without purge: Remove generated local JSON if retained; keep Bicep and the example parameter file.

## LAB21-Q31 — foundational

A design review of 'Preview a bounded deployment' includes security architect. Approval requires a positive result plus this independent negative assertion: A destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail. Which option best represents the acceptance rule that makes LAB21-REQ-04 testable?

- A. Select Per-instance memory caches with image-baked configuration and imperative scripts before checking Preview a bounded deployment. Before sign-off, take it as conclusive that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Define cache semantics and failure behavior to approve Preview a bounded deployment; as an independent condition, treat it as established that one control establishes an unrelated acceptance boundary.
- C. Require the documented positive state for Preview a bounded deployment. Next, verify that a destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail.
- D. Choose Cosmos DB integrated cache with Key Vault references and Terraform delivery and skip the Preview a bounded deployment negative assertion; without relying on inference, use as justification the claim that the candidate has the lowest implementation effort.

## LAB21-Q32 — foundational

The team asks finOps analyst to assess 'Preview a bounded deployment'. The selected architecture is Azure Managed Redis with Azure App Configuration and Bicep deployment stacks; object existence alone is not success. Which course of action provides the intended successful finding?

- A. Use only the negative assertion 'A destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail' as the success result. Next, treat as decisive the assertion that absence proves every required positive property.
- B. Record template validation and reviewed what-if describe only the bounded analogue, with explicit ownership, expiry, identity, network, and capacity settings and no mutation. As another control, classify it as success for LAB21-REQ-04.
- C. Use the successful finding from Separate configuration, features, and secrets as the result for Preview a bounded deployment, and then base approval on the claim that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'An invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration' as a successful state. Also, use the premise that the command returned an object.

## LAB21-Q33 — foundational

A recommendation on 'Preview a bounded deployment' is requested by digital commerce product owner. Evidence must address this risk without retaining credentials: An invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration. Which finding constitutes sufficient, properly scoped evidence?

- A. Substitute the evidence from Compile the delivery contract for Preview a bounded deployment; next, accept without proof that a related checkpoint proves the current expected state.
- B. Retain archive template validation, reviewed what-if, cost estimate, policy outcomes, and exact source and parameter hashes. In addition, exclude credentials and unrelated response fields.
- C. Store unredacted Preview a bounded deployment output with operator, tenant, token, and request context. In addition, rely on the claim that reproduction requires every captured field.
- D. Record only the Preview a bounded deployment positive inspection's exit status; before approval, rely on the belief that projected properties and assertion results can be reconstructed later.

## LAB21-Q34 — applied

'Preview a bounded deployment' is assigned to application platform lead. The target is Template validation and reviewed what-if describe only the bounded analogue, with explicit ownership, expiry, identity, network, and capacity settings and no mutation, but the latest evidence does not show it. Which recommendation delivers the most likely cause?

- A. Treat 'Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds' as grounds to reject Preview a bounded deployment; before closing the checkpoint, consider it sufficient that validate progressive configuration delivery's failure model applies unchanged here.
- B. Ignore the negative assertion 'A destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail'. Then, take it as conclusive that a later material change will make it unnecessary.
- C. Investigate Define cache semantics and failure behavior instead of diagnosing Preview a bounded deployment; afterward, treat it as established that a passing result at Define cache semantics and failure behavior identifies the current cause.
- D. Investigate an invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration. Before sign-off, isolate that cause before changing Azure Managed Redis with Azure App Configuration and Bicep deployment stacks.

## LAB21-Q35 — applied

An assurance review of 'Preview a bounded deployment' includes release engineering manager. The run encountered this modeled failure: An invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration. Which response meets the need for the safest recovery action?

- A. Perform cleanup immediately: Delete local preview output only; the safe analogue must create no cloud object and never purge shared stores. Also, proceed on the belief that the failed operation and its returned identifiers do not need reconciliation.
- B. Correct the template or parameters and rerun validation and what-if using the same deterministic inputs; afterward, preserve the current run identity and evidence.
- C. Create a different run identity before diagnosing 'An invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration'; in a separate step, treat as decisive the assertion that the first state record and returned identifiers can be discarded.
- D. Change Define cache semantics and failure behavior instead. As another control, base approval on the claim that success at Define cache semantics and failure behavior will repair the failed state at Preview a bounded deployment.

## LAB21-Q36 — applied

Approval of 'Preview a bounded deployment' is questioned by security architect. Without making a new change, the team must inspect the risk 'An invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration' using the Azure PowerShell + Bicep lane. Which choice should be approved as the read-only, lane-correct inspection?

- A. Inspect the documented properties for Preview a bounded deployment, and then retain this evidence: archive template validation, reviewed what-if, cost estimate, policy outcomes, and exact source and parameter hashes.
- B. Rerun the Preview a bounded deployment implementation command and infer the expected state; before approval, use as justification the claim that absence of a shell error proves every property.
- C. Run only this negative inspection for Preview a bounded deployment: A destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail. Separately, accept without proof that an empty negative result reports every required positive property.
- D. Run the positive inspection for Separate configuration, features, and secrets and apply it to Preview a bounded deployment; for this decision, rely on the claim that any command from the same lane proves the current checkpoint.

## LAB21-Q37 — applied

The implementation review has reached 'Preview a bounded deployment'. A passing positive check does not by itself prove this negative assertion: A destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail. What should the team use as the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Preview a bounded deployment and report full compliance; afterward, use the premise that every prohibited parallel state must therefore be absent.
- B. Verify the positive properties for Preview a bounded deployment; as a separate check, independently verify that a destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail.
- C. Prove only that a destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail and report the intended configuration as present; then consider it sufficient that absence is equivalent to positive-state evidence.
- D. Use Compile the delivery contract's negative assertion for Preview a bounded deployment. Independently, take it as conclusive that negative assertions are interchangeable between checkpoints.

## LAB21-Q38 — applied

The approach to 'Preview a bounded deployment' is challenged by digital commerce product owner. The board wants the Well-Architected consequence of mitigating this risk: An invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration. Which option best establishes the consequence attributable to this checkpoint?

- A. Record this consequence: Cost Optimization: bounded capacity, expiry tags, and gated execution constrain the deployable reference footprint; before approval, tie it to LAB21-REQ-04.
- B. Use the Validate progressive configuration delivery consequence as the result for Preview a bounded deployment. As another control, rely on the belief that a pillar statement remains valid when moved away from Validate progressive configuration delivery.
- C. Remove the control responsible for the Preview a bounded deployment outcome; as a separate check, proceed on the belief that a moderate cost classification outweighs the mandatory architecture state.
- D. Treat 'Cost Optimization: bounded capacity, expiry tags, and gated execution constrain the deployable reference footprint' as proof that all five pillars pass. Afterward, treat as decisive the assertion that the checkpoint 'Preview a bounded deployment' no longer needs its separate negative check.

## LAB21-Q39 — advanced

A decision test for 'Preview a bounded deployment' includes application platform lead. A material change now applies: A regulatory review requires the storefront to keep serving last-known-safe catalog data for thirty minutes during configuration-store or source-database unavailability. Which answer identifies the correct revision to the decision record?

- A. Re-score Azure Managed Redis with Azure App Configuration and Bicep deployment stacks and both alternatives for Preview a bounded deployment; as an independent condition, supersede the ADR using the changed evidence for LAB21-REQ-04.
- B. Retain Azure Managed Redis with Azure App Configuration and Bicep deployment stacks at Preview a bounded deployment without recalculating criteria or eligibility; for this decision, treat it as established that the original weighted result is permanent.
- C. Select Per-instance memory caches with image-baked configuration and imperative scripts for Preview a bounded deployment without rechecking its mandatory constraints. Before sign-off, use as justification the claim that being different from the current design is an architecture criterion.
- D. Keep Cosmos DB integrated cache with Key Vault references and Terraform delivery eligible at Preview a bounded deployment by downgrading LAB21-REQ-04; as an independent condition, accept without proof that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB21-Q40 — advanced

The architecture board reconsiders 'Preview a bounded deployment' with release engineering manager. After a partial run, cleanup must follow this dependency: Delete local preview output only; the safe analogue must create no cloud object and never purge shared stores. What recommendation gives the reviewers the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Separate configuration, features, and secrets before reconciling the current dependency. Independently, base approval on the claim that removing a parent needed to identify Preview a bounded deployment is harmless.
- B. Verify exact run-state IDs and ownership tags for Preview a bounded deployment; then follow this dependency rule without purge: Delete local preview output only; the safe analogue must create no cloud object and never purge shared stores.
- C. Delete candidates by display name before comparing the Preview a bounded deployment ownership tags. Next, use the premise that the dependency rule 'Delete local preview output only; the safe analogue must create no cloud object and never purge shared stores' is optional.
- D. Destroy recoverable copies before retaining the Preview a bounded deployment negative assertion 'A destructive what-if, policy denial, production-scale cache, missing lifecycle tag, or persisted deployment from this safe analogue must fail', and then consider it sufficient that remaining command logs are sufficient recovery evidence.

## LAB21-Q41 — foundational

A review of 'Validate progressive configuration delivery' begins with input from finOps analyst. Approval requires a positive result plus this independent negative assertion: Global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail. Which course of action provides the acceptance rule that makes LAB21-REQ-05 testable?

- A. Select Per-instance memory caches with image-baked configuration and imperative scripts before checking Validate progressive configuration delivery; for the final assessment, treat as decisive the assertion that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Define cache semantics and failure behavior to approve Validate progressive configuration delivery. Then, base approval on the claim that one control establishes an unrelated acceptance boundary.
- C. Require the documented positive state for Validate progressive configuration delivery; in a separate step, verify that global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail.
- D. Choose Cosmos DB integrated cache with Key Vault references and Terraform delivery and skip the Validate progressive configuration delivery negative assertion; afterward, use the premise that the candidate has the lowest implementation effort.

## LAB21-Q42 — foundational

'Validate progressive configuration delivery' awaits approval from digital commerce product owner. The selected architecture is Azure Managed Redis with Azure App Configuration and Bicep deployment stacks; object existence alone is not success. Which finding constitutes the intended successful finding?

- A. Use only the negative assertion 'Global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail' as the success result. Also, accept without proof that absence proves every required positive property.
- B. Use the successful finding from Separate configuration, features, and secrets as the result for Validate progressive configuration delivery; in a separate step, rely on the claim that a property from the current checkpoint does not need to be inspected.
- C. Record synthetic clients refresh non-secret settings, feature exposure advances by a deterministic ring, cache fallback works, and a failed ring stops and rolls back; next, classify it as success for LAB21-REQ-05.
- D. Record the failure condition 'Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds' as a successful state. As another control, rely on the belief that the command returned an object.

## LAB21-Q43 — foundational

'Validate progressive configuration delivery' is reopened at the request of application platform lead. Evidence must address this risk without retaining credentials: Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds. Which recommendation delivers sufficient, properly scoped evidence?

- A. Substitute the evidence from Compile the delivery contract for Validate progressive configuration delivery; before approval, consider it sufficient that a related checkpoint proves the current expected state.
- B. Retain save flag versions, ring membership, refresh timestamps, cache and source latency, assertion results, and rollback record; for this decision, exclude credentials and unrelated response fields.
- C. Store unredacted Validate progressive configuration delivery output with operator, tenant, token, and request context. Separately, take it as conclusive that reproduction requires every captured field.
- D. Record only the Validate progressive configuration delivery positive inspection's exit status; for this decision, treat it as established that projected properties and assertion results can be reconstructed later.

## LAB21-Q44 — applied

A design review of 'Validate progressive configuration delivery' includes release engineering manager. The target is Synthetic clients refresh non-secret settings, feature exposure advances by a deterministic ring, cache fallback works, and a failed ring stops and rolls back, but the latest evidence does not show it. Which response meets the need for the most likely cause?

- A. Treat 'An invalid scope or unsafe default can make a later authorized deployment costly or expose shared configuration' as grounds to reject Validate progressive configuration delivery; afterward, proceed on the belief that preview a bounded deployment's failure model applies unchanged here.
- B. Ignore the negative assertion 'Global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail'; then treat as decisive the assertion that a later material change will make it unnecessary.
- C. Investigate Define cache semantics and failure behavior instead of diagnosing Validate progressive configuration delivery. Independently, base approval on the claim that a passing result at Define cache semantics and failure behavior identifies the current cause.
- D. Investigate configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds. Then, isolate that cause before changing Azure Managed Redis with Azure App Configuration and Bicep deployment stacks.

## LAB21-Q45 — applied

The team asks security architect to assess 'Validate progressive configuration delivery'. The run encountered this modeled failure: Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds. Which choice should be approved as the safest recovery action?

- A. Restore the last approved flag state and replay only the failed ring after fixing the assertion. Next, preserve the current run identity and evidence.
- B. Perform cleanup immediately: Disable and remove run-owned flags and synthetic keys before deleting run-owned infrastructure. As another control, use as justification the claim that the failed operation and its returned identifiers do not need reconciliation.
- C. Create a different run identity before diagnosing 'Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds'; as a separate check, accept without proof that the first state record and returned identifiers can be discarded.
- D. Change Define cache semantics and failure behavior instead. Afterward, rely on the claim that success at Define cache semantics and failure behavior will repair the failed state at Validate progressive configuration delivery.

## LAB21-Q46 — applied

A recommendation on 'Validate progressive configuration delivery' is requested by finOps analyst. Without making a new change, the team must inspect the risk 'Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds' using the Azure PowerShell + Bicep lane. What should the team use as the read-only, lane-correct inspection?

- A. Inspect the documented properties for Validate progressive configuration delivery. As another control, retain this evidence: save flag versions, ring membership, refresh timestamps, cache and source latency, assertion results, and rollback record.
- B. Rerun the Validate progressive configuration delivery implementation command and infer the expected state; for this decision, use the premise that absence of a shell error proves every property.
- C. Run only this negative inspection for Validate progressive configuration delivery: Global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail. Before sign-off, consider it sufficient that an empty negative result reports every required positive property.
- D. Run the positive inspection for Separate configuration, features, and secrets and apply it to Validate progressive configuration delivery; as an independent condition, take it as conclusive that any command from the same lane proves the current checkpoint.

## LAB21-Q47 — applied

'Validate progressive configuration delivery' is assigned to digital commerce product owner. A passing positive check does not by itself prove this negative assertion: Global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail. Which option best establishes the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Validate progressive configuration delivery and report full compliance. Independently, rely on the belief that every prohibited parallel state must therefore be absent.
- B. Prove only that global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail and report the intended configuration as present. Next, proceed on the belief that absence is equivalent to positive-state evidence.
- C. Use Compile the delivery contract's negative assertion for Validate progressive configuration delivery, and then treat as decisive the assertion that negative assertions are interchangeable between checkpoints.
- D. Verify the positive properties for Validate progressive configuration delivery. In addition, independently verify that global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail.

## LAB21-Q48 — applied

An assurance review of 'Validate progressive configuration delivery' includes application platform lead. The board wants the Well-Architected consequence of mitigating this risk: Configuration propagation can create inconsistent behavior even after infrastructure deployment succeeds. Which answer identifies the consequence attributable to this checkpoint?

- A. Use the Preview a bounded deployment consequence as the result for Validate progressive configuration delivery. Afterward, treat it as established that a pillar statement remains valid when moved away from Preview a bounded deployment.
- B. Remove the control responsible for the Validate progressive configuration delivery outcome; next, use as justification the claim that a moderate cost classification outweighs the mandatory architecture state.
- C. Record this consequence: Reliability: progressive exposure and tested fallback limit blast radius from configuration and cache faults. Before sign-off, tie it to LAB21-REQ-05.
- D. Treat 'Reliability: progressive exposure and tested fallback limit blast radius from configuration and cache faults' as proof that all five pillars pass. In addition, accept without proof that the checkpoint 'Validate progressive configuration delivery' no longer needs its separate negative check.

## LAB21-Q49 — advanced

Approval of 'Validate progressive configuration delivery' is questioned by release engineering manager. A material change now applies: A regulatory review requires the storefront to keep serving last-known-safe catalog data for thirty minutes during configuration-store or source-database unavailability. What recommendation gives the reviewers the correct revision to the decision record?

- A. Retain Azure Managed Redis with Azure App Configuration and Bicep deployment stacks at Validate progressive configuration delivery without recalculating criteria or eligibility; as an independent condition, base approval on the claim that the original weighted result is permanent.
- B. Re-score Azure Managed Redis with Azure App Configuration and Bicep deployment stacks and both alternatives for Validate progressive configuration delivery; afterward, supersede the ADR using the changed evidence for LAB21-REQ-05.
- C. Select Per-instance memory caches with image-baked configuration and imperative scripts for Validate progressive configuration delivery without rechecking its mandatory constraints; before approval, use the premise that being different from the current design is an architecture criterion.
- D. Keep Cosmos DB integrated cache with Key Vault references and Terraform delivery eligible at Validate progressive configuration delivery by downgrading LAB21-REQ-05. Then, consider it sufficient that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB21-Q50 — advanced

The implementation review has reached 'Validate progressive configuration delivery'. After a partial run, cleanup must follow this dependency: Disable and remove run-owned flags and synthetic keys before deleting run-owned infrastructure. Which action produces the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Separate configuration, features, and secrets before reconciling the current dependency, and then rely on the claim that removing a parent needed to identify Validate progressive configuration delivery is harmless.
- B. Delete candidates by display name before comparing the Validate progressive configuration delivery ownership tags. Also, rely on the belief that the dependency rule 'Disable and remove run-owned flags and synthetic keys before deleting run-owned infrastructure' is optional.
- C. Verify exact run-state IDs and ownership tags for Validate progressive configuration delivery, and then follow this dependency rule without purge: Disable and remove run-owned flags and synthetic keys before deleting run-owned infrastructure.
- D. Destroy recoverable copies before retaining the Validate progressive configuration delivery negative assertion 'Global flag activation, stale secret material, cache-failure outage, or rollout continuation after a failed assertion must fail'; in a separate step, proceed on the belief that remaining command logs are sufficient recovery evidence.
<!-- END GENERATED AZ305 V1 -->
