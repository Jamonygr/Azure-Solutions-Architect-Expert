<!-- BEGIN GENERATED AZ305 V1 -->
# LAB-13 learner assessment

Choose one answer for each item. This page intentionally contains no answers or explanations.

## LAB13-Q01 — foundational

'Select integration patterns and runtimes' is reopened at the request of data engineering. Approval requires a positive result plus this independent negative assertion: An undocumented shared self-hosted runtime is not accepted as a universal connectivity solution. Which recommendation delivers the acceptance rule that makes LAB13-REQ-01 testable?

- A. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse before checking Select integration patterns and runtimes. Also, treat as decisive the assertion that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Use managed identity for linked services to approve Select integration patterns and runtimes; in a separate step, base approval on the claim that one control establishes an unrelated acceptance boundary.
- C. Choose Azure Databricks with Azure Data Factory orchestration and Delta Lake and skip the Select integration patterns and runtimes negative assertion. As another control, use the premise that the candidate has the lowest implementation effort.
- D. Require the documented positive state for Select integration patterns and runtimes. As another control, verify that an undocumented shared self-hosted runtime is not accepted as a universal connectivity solution.

## LAB13-Q02 — foundational

A design review of 'Select integration patterns and runtimes' includes analytics consumers. The selected architecture is Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL; object existence alone is not success. Which response meets the need for the intended successful finding?

- A. Record batch, CDC, event, and private-source needs map to appropriate Azure or self-hosted integration runtime boundaries. In addition, classify it as success for LAB13-REQ-01.
- B. Use only the negative assertion 'An undocumented shared self-hosted runtime is not accepted as a universal connectivity solution' as the success result; before approval, accept without proof that absence proves every required positive property.
- C. Use the successful finding from Orchestrate idempotent ingestion as the result for Select integration patterns and runtimes. Separately, rely on the claim that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'A source protocol, firewall, or driver is unsupported by the selected runtime' as a successful state; for this decision, rely on the belief that the command returned an object.

## LAB13-Q03 — foundational

The team asks operational system owners to assess 'Select integration patterns and runtimes'. Evidence must address this risk without retaining credentials: A source protocol, firewall, or driver is unsupported by the selected runtime. Which choice should be approved as sufficient, properly scoped evidence?

- A. Retain source class, integration pattern, runtime type, network boundary, owner, and latency expectation. Before sign-off, exclude credentials and unrelated response fields.
- B. Substitute the evidence from Serve governed analytical queries for Select integration patterns and runtimes; afterward, consider it sufficient that a related checkpoint proves the current expected state.
- C. Store unredacted Select integration patterns and runtimes output with operator, tenant, token, and request context; then take it as conclusive that reproduction requires every captured field.
- D. Record only the Select integration patterns and runtimes positive inspection's exit status. Independently, treat it as established that projected properties and assertion results can be reconstructed later.

## LAB13-Q04 — applied

A recommendation on 'Select integration patterns and runtimes' is requested by data governance council. The target is Batch, CDC, event, and private-source needs map to appropriate Azure or self-hosted integration runtime boundaries, but the latest evidence does not show it. What should the team use as the most likely cause?

- A. Treat 'Monitoring records show success while downstream reconciliation detects missing or duplicate rows' as grounds to reject Select integration patterns and runtimes. As another control, proceed on the belief that validate lineage operations and spend's failure model applies unchanged here.
- B. Investigate a source protocol, firewall, or driver is unsupported by the selected runtime; afterward, isolate that cause before changing Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL.
- C. Ignore the negative assertion 'An undocumented shared self-hosted runtime is not accepted as a universal connectivity solution'; as a separate check, treat as decisive the assertion that a later material change will make it unnecessary.
- D. Investigate Use managed identity for linked services instead of diagnosing Select integration patterns and runtimes. Afterward, base approval on the claim that a passing result at Use managed identity for linked services identifies the current cause.

## LAB13-Q05 — applied

'Select integration patterns and runtimes' is assigned to data engineering. The run encountered this modeled failure: A source protocol, firewall, or driver is unsupported by the selected runtime. Which option best establishes the safest recovery action?

- A. Perform cleanup immediately: Stop and remove only a run-owned synthetic runtime after linked-service dependencies are removed; for this decision, use as justification the claim that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'A source protocol, firewall, or driver is unsupported by the selected runtime'. Before sign-off, accept without proof that the first state record and returned identifiers can be discarded.
- C. Validate connector and network prerequisites with synthetic endpoints before changing the platform decision, and then preserve the current run identity and evidence.
- D. Change Use managed identity for linked services instead; as an independent condition, rely on the claim that success at Use managed identity for linked services will repair the failed state at Select integration patterns and runtimes.

## LAB13-Q06 — applied

An assurance review of 'Select integration patterns and runtimes' includes analytics consumers. Without making a new change, the team must inspect the risk 'A source protocol, firewall, or driver is unsupported by the selected runtime' using the Azure PowerShell lane. Which answer identifies the read-only, lane-correct inspection?

- A. Rerun the Select integration patterns and runtimes implementation command and infer the expected state. Independently, use the premise that absence of a shell error proves every property.
- B. Run only this negative inspection for Select integration patterns and runtimes: An undocumented shared self-hosted runtime is not accepted as a universal connectivity solution. Next, consider it sufficient that an empty negative result reports every required positive property.
- C. Inspect the documented properties for Select integration patterns and runtimes; as a separate check, retain this evidence: source class, integration pattern, runtime type, network boundary, owner, and latency expectation.
- D. Run the positive inspection for Orchestrate idempotent ingestion and apply it to Select integration patterns and runtimes, and then take it as conclusive that any command from the same lane proves the current checkpoint.

## LAB13-Q07 — applied

Approval of 'Select integration patterns and runtimes' is questioned by operational system owners. A passing positive check does not by itself prove this negative assertion: An undocumented shared self-hosted runtime is not accepted as a universal connectivity solution. What recommendation gives the reviewers the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Select integration patterns and runtimes and report full compliance. Afterward, rely on the belief that every prohibited parallel state must therefore be absent.
- B. Verify the positive properties for Select integration patterns and runtimes; before approval, independently verify that an undocumented shared self-hosted runtime is not accepted as a universal connectivity solution.
- C. Prove only that an undocumented shared self-hosted runtime is not accepted as a universal connectivity solution and report the intended configuration as present; next, proceed on the belief that absence is equivalent to positive-state evidence.
- D. Use Serve governed analytical queries's negative assertion for Select integration patterns and runtimes. In addition, treat as decisive the assertion that negative assertions are interchangeable between checkpoints.

## LAB13-Q08 — applied

The implementation review has reached 'Select integration patterns and runtimes'. The board wants the Well-Architected consequence of mitigating this risk: A source protocol, firewall, or driver is unsupported by the selected runtime. Which action produces the consequence attributable to this checkpoint?

- A. Record this consequence: Reliability: explicit runtime ownership and network prerequisites make hybrid data movement recoverable; as an independent condition, tie it to LAB13-REQ-01.
- B. Use the Validate lineage operations and spend consequence as the result for Select integration patterns and runtimes; as an independent condition, treat it as established that a pillar statement remains valid when moved away from Validate lineage operations and spend.
- C. Remove the control responsible for the Select integration patterns and runtimes outcome; for the final assessment, use as justification the claim that a elevated cost classification outweighs the mandatory architecture state.
- D. Treat 'Reliability: explicit runtime ownership and network prerequisites make hybrid data movement recoverable' as proof that all five pillars pass. Then, accept without proof that the checkpoint 'Select integration patterns and runtimes' no longer needs its separate negative check.

## LAB13-Q09 — advanced

The approach to 'Select integration patterns and runtimes' is challenged by data engineering. A material change now applies: Executives mandate near-real-time operational dashboards for one telemetry stream while partner files remain hourly and analysts prefer a unified Microsoft Fabric experience; revise the platform boundary without forcing every flow into streaming. What best demonstrates the correct revision to the decision record?

- A. Re-score Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL and both alternatives for Select integration patterns and runtimes; then supersede the ADR using the changed evidence for LAB13-REQ-01.
- B. Retain Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL at Select integration patterns and runtimes without recalculating criteria or eligibility, and then base approval on the claim that the original weighted result is permanent.
- C. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse for Select integration patterns and runtimes without rechecking its mandatory constraints. Also, use the premise that being different from the current design is an architecture criterion.
- D. Keep Azure Databricks with Azure Data Factory orchestration and Delta Lake eligible at Select integration patterns and runtimes by downgrading LAB13-REQ-01; in a separate step, consider it sufficient that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB13-Q10 — advanced

A decision test for 'Select integration patterns and runtimes' includes analytics consumers. After a partial run, cleanup must follow this dependency: Stop and remove only a run-owned synthetic runtime after linked-service dependencies are removed. Which option is the dependency-safe cleanup plan?

- A. Verify exact run-state IDs and ownership tags for Select integration patterns and runtimes. Also, follow this dependency rule without purge: Stop and remove only a run-owned synthetic runtime after linked-service dependencies are removed.
- B. Apply the cleanup rule for Orchestrate idempotent ingestion before reconciling the current dependency. In addition, rely on the claim that removing a parent needed to identify Select integration patterns and runtimes is harmless.
- C. Delete candidates by display name before comparing the Select integration patterns and runtimes ownership tags; before approval, rely on the belief that the dependency rule 'Stop and remove only a run-owned synthetic runtime after linked-service dependencies are removed' is optional.
- D. Destroy recoverable copies before retaining the Select integration patterns and runtimes negative assertion 'An undocumented shared self-hosted runtime is not accepted as a universal connectivity solution'. Separately, proceed on the belief that remaining command logs are sufficient recovery evidence.

## LAB13-Q11 — foundational

The architecture board reconsiders 'Use managed identity for linked services' with analytics consumers. Approval requires a positive result plus this independent negative assertion: Inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence. Which response meets the need for the acceptance rule that makes LAB13-REQ-02 testable?

- A. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse before checking Use managed identity for linked services. As another control, accept without proof that a successful deployment will later prove the architecture constraint.
- B. Require the documented positive state for Use managed identity for linked services; next, verify that inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence.
- C. Use the passing result from Select integration patterns and runtimes to approve Use managed identity for linked services; as a separate check, rely on the claim that one control establishes an unrelated acceptance boundary.
- D. Choose Azure Databricks with Azure Data Factory orchestration and Delta Lake and skip the Use managed identity for linked services negative assertion. Afterward, rely on the belief that the candidate has the lowest implementation effort.

## LAB13-Q12 — foundational

A review of 'Use managed identity for linked services' begins with input from operational system owners. The selected architecture is Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL; object existence alone is not success. Which choice should be approved as the intended successful finding?

- A. Use only the negative assertion 'Inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence' as the success result; for this decision, consider it sufficient that absence proves every required positive property.
- B. Use the successful finding from Orchestrate idempotent ingestion as the result for Use managed identity for linked services. Before sign-off, take it as conclusive that a property from the current checkpoint does not need to be inspected.
- C. Record supported sources and sinks use the data-factory managed identity or a Key Vault reference with least privilege; for this decision, classify it as success for LAB13-REQ-02.
- D. Record the failure condition 'The connector lacks managed-identity support or the identity cannot traverse a private network boundary' as a successful state; as an independent condition, treat it as established that the command returned an object.

## LAB13-Q13 — foundational

'Use managed identity for linked services' awaits approval from data governance council. Evidence must address this risk without retaining credentials: The connector lacks managed-identity support or the identity cannot traverse a private network boundary. What should the team use as sufficient, properly scoped evidence?

- A. Substitute the evidence from Serve governed analytical queries for Use managed identity for linked services. Independently, proceed on the belief that a related checkpoint proves the current expected state.
- B. Retain linked-service name, connector type, authentication class, Key Vault reference label, and target scope. Then, exclude credentials and unrelated response fields.
- C. Store unredacted Use managed identity for linked services output with operator, tenant, token, and request context. Next, treat as decisive the assertion that reproduction requires every captured field.
- D. Record only the Use managed identity for linked services positive inspection's exit status, and then base approval on the claim that projected properties and assertion results can be reconstructed later.

## LAB13-Q14 — applied

'Use managed identity for linked services' is reopened at the request of data engineering. The target is Supported sources and sinks use the data-factory managed identity or a Key Vault reference with least privilege, but the latest evidence does not show it. Which option best establishes the most likely cause?

- A. Investigate the connector lacks managed-identity support or the identity cannot traverse a private network boundary. Next, isolate that cause before changing Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL.
- B. Treat 'Monitoring records show success while downstream reconciliation detects missing or duplicate rows' as grounds to reject Use managed identity for linked services. Afterward, use as justification the claim that validate lineage operations and spend's failure model applies unchanged here.
- C. Ignore the negative assertion 'Inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence'; next, accept without proof that a later material change will make it unnecessary.
- D. Investigate Select integration patterns and runtimes instead of diagnosing Use managed identity for linked services. In addition, rely on the claim that a passing result at Select integration patterns and runtimes identifies the current cause.

## LAB13-Q15 — applied

A design review of 'Use managed identity for linked services' includes analytics consumers. The run encountered this modeled failure: The connector lacks managed-identity support or the identity cannot traverse a private network boundary. Which answer identifies the safest recovery action?

- A. Perform cleanup immediately: Delete dependent datasets and pipelines before a run-owned linked service; never export secret properties; as an independent condition, use the premise that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'The connector lacks managed-identity support or the identity cannot traverse a private network boundary'; separately, consider it sufficient that the first state record and returned identifiers can be discarded.
- C. Change Select integration patterns and runtimes instead. Then, take it as conclusive that success at Select integration patterns and runtimes will repair the failed state at Use managed identity for linked services.
- D. Use a Key Vault-referenced short-lived credential only where required and document rotation and runtime access. As another control, preserve the current run identity and evidence.

## LAB13-Q16 — applied

The team asks operational system owners to assess 'Use managed identity for linked services'. Without making a new change, the team must inspect the risk 'The connector lacks managed-identity support or the identity cannot traverse a private network boundary' using the Azure PowerShell lane. What recommendation gives the reviewers the read-only, lane-correct inspection?

- A. Rerun the Use managed identity for linked services implementation command and infer the expected state, and then rely on the belief that absence of a shell error proves every property.
- B. Run only this negative inspection for Use managed identity for linked services: Inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence. Also, proceed on the belief that an empty negative result reports every required positive property.
- C. Run the positive inspection for Orchestrate idempotent ingestion and apply it to Use managed identity for linked services; in a separate step, treat as decisive the assertion that any command from the same lane proves the current checkpoint.
- D. Inspect the documented properties for Use managed identity for linked services. In addition, retain this evidence: linked-service name, connector type, authentication class, Key Vault reference label, and target scope.

## LAB13-Q17 — applied

A recommendation on 'Use managed identity for linked services' is requested by data governance council. A passing positive check does not by itself prove this negative assertion: Inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence. Which action produces the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Use managed identity for linked services and report full compliance. In addition, treat it as established that every prohibited parallel state must therefore be absent.
- B. Prove only that inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence and report the intended configuration as present; before approval, use as justification the claim that absence is equivalent to positive-state evidence.
- C. Use Serve governed analytical queries's negative assertion for Use managed identity for linked services. Separately, accept without proof that negative assertions are interchangeable between checkpoints.
- D. Verify the positive properties for Use managed identity for linked services. Before sign-off, independently verify that inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence.

## LAB13-Q18 — applied

'Use managed identity for linked services' is assigned to data engineering. The board wants the Well-Architected consequence of mitigating this risk: The connector lacks managed-identity support or the identity cannot traverse a private network boundary. What best demonstrates the consequence attributable to this checkpoint?

- A. Record this consequence: Security: managed identity and Key Vault references keep credentials out of pipeline definitions and evidence; afterward, tie it to LAB13-REQ-02.
- B. Use the Validate lineage operations and spend consequence as the result for Use managed identity for linked services. Then, base approval on the claim that a pillar statement remains valid when moved away from Validate lineage operations and spend.
- C. Remove the control responsible for the Use managed identity for linked services outcome; afterward, use the premise that a elevated cost classification outweighs the mandatory architecture state.
- D. Treat 'Security: managed identity and Key Vault references keep credentials out of pipeline definitions and evidence' as proof that all five pillars pass; then consider it sufficient that the checkpoint 'Use managed identity for linked services' no longer needs its separate negative check.

## LAB13-Q19 — advanced

An assurance review of 'Use managed identity for linked services' includes analytics consumers. A material change now applies: Executives mandate near-real-time operational dashboards for one telemetry stream while partner files remain hourly and analysts prefer a unified Microsoft Fabric experience; revise the platform boundary without forcing every flow into streaming. Which option is the correct revision to the decision record?

- A. Retain Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL at Use managed identity for linked services without recalculating criteria or eligibility; in a separate step, rely on the claim that the original weighted result is permanent.
- B. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse for Use managed identity for linked services without rechecking its mandatory constraints. As another control, rely on the belief that being different from the current design is an architecture criterion.
- C. Re-score Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL and both alternatives for Use managed identity for linked services, and then supersede the ADR using the changed evidence for LAB13-REQ-02.
- D. Keep Azure Databricks with Azure Data Factory orchestration and Delta Lake eligible at Use managed identity for linked services by downgrading LAB13-REQ-02; as a separate check, proceed on the belief that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB13-Q20 — advanced

Approval of 'Use managed identity for linked services' is questioned by operational system owners. After a partial run, cleanup must follow this dependency: Delete dependent datasets and pipelines before a run-owned linked service; never export secret properties. What should the architect select as the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Orchestrate idempotent ingestion before reconciling the current dependency. Separately, take it as conclusive that removing a parent needed to identify Use managed identity for linked services is harmless.
- B. Delete candidates by display name before comparing the Use managed identity for linked services ownership tags; for this decision, treat it as established that the dependency rule 'Delete dependent datasets and pipelines before a run-owned linked service; never export secret properties' is optional.
- C. Verify exact run-state IDs and ownership tags for Use managed identity for linked services; as a separate check, follow this dependency rule without purge: Delete dependent datasets and pipelines before a run-owned linked service; never export secret properties.
- D. Destroy recoverable copies before retaining the Use managed identity for linked services negative assertion 'Inline connection strings, account keys, and embedded credentials are absent from pipeline definitions and evidence'. Before sign-off, use as justification the claim that remaining command logs are sufficient recovery evidence.

## LAB13-Q21 — foundational

The implementation review has reached 'Orchestrate idempotent ingestion'. Approval requires a positive result plus this independent negative assertion: Empty pipelines and blind append operations without duplicate handling do not satisfy ingestion. Which choice should be approved as the acceptance rule that makes LAB13-REQ-03 testable?

- A. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse before checking Orchestrate idempotent ingestion. Afterward, consider it sufficient that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Select integration patterns and runtimes to approve Orchestrate idempotent ingestion; next, take it as conclusive that one control establishes an unrelated acceptance boundary.
- C. Choose Azure Databricks with Azure Data Factory orchestration and Delta Lake and skip the Orchestrate idempotent ingestion negative assertion. In addition, treat it as established that the candidate has the lowest implementation effort.
- D. Require the documented positive state for Orchestrate idempotent ingestion. Separately, verify that empty pipelines and blind append operations without duplicate handling do not satisfy ingestion.

## LAB13-Q22 — foundational

The approach to 'Orchestrate idempotent ingestion' is challenged by data governance council. The selected architecture is Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL; object existence alone is not success. What should the team use as the intended successful finding?

- A. Use only the negative assertion 'Empty pipelines and blind append operations without duplicate handling do not satisfy ingestion' as the success result; as an independent condition, proceed on the belief that absence proves every required positive property.
- B. Record the pipeline uses watermarks, deterministic paths, retry policy, and quarantine handling to make reruns safe; during the same review, classify it as success for LAB13-REQ-03.
- C. Use the successful finding from Use managed identity for linked services as the result for Orchestrate idempotent ingestion; independently, treat as decisive the assertion that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'A partial copy advances the watermark or rerun duplicates already committed data' as a successful state. Then, base approval on the claim that the command returned an object.

## LAB13-Q23 — foundational

A decision test for 'Orchestrate idempotent ingestion' includes data engineering. Evidence must address this risk without retaining credentials: A partial copy advances the watermark or rerun duplicates already committed data. Which option best establishes sufficient, properly scoped evidence?

- A. Retain definition hash, activity graph, retry policy, watermark owner, quarantine path, and synthetic run result. Independently, exclude credentials and unrelated response fields.
- B. Substitute the evidence from Serve governed analytical queries for Orchestrate idempotent ingestion, and then use as justification the claim that a related checkpoint proves the current expected state.
- C. Store unredacted Orchestrate idempotent ingestion output with operator, tenant, token, and request context. Also, accept without proof that reproduction requires every captured field.
- D. Record only the Orchestrate idempotent ingestion positive inspection's exit status; in a separate step, rely on the claim that projected properties and assertion results can be reconstructed later.

## LAB13-Q24 — applied

The architecture board reconsiders 'Orchestrate idempotent ingestion' with analytics consumers. The target is The pipeline uses watermarks, deterministic paths, retry policy, and quarantine handling to make reruns safe, but the latest evidence does not show it. Which answer identifies the most likely cause?

- A. Treat 'Monitoring records show success while downstream reconciliation detects missing or duplicate rows' as grounds to reject Orchestrate idempotent ingestion. In addition, use the premise that validate lineage operations and spend's failure model applies unchanged here.
- B. Ignore the negative assertion 'Empty pipelines and blind append operations without duplicate handling do not satisfy ingestion'; before approval, consider it sufficient that a later material change will make it unnecessary.
- C. Investigate a partial copy advances the watermark or rerun duplicates already committed data; in a separate step, isolate that cause before changing Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL.
- D. Investigate Select integration patterns and runtimes instead of diagnosing Orchestrate idempotent ingestion. Separately, take it as conclusive that a passing result at Select integration patterns and runtimes identifies the current cause.

## LAB13-Q25 — applied

A review of 'Orchestrate idempotent ingestion' begins with input from operational system owners. The run encountered this modeled failure: A partial copy advances the watermark or rerun duplicates already committed data. What recommendation gives the reviewers the safest recovery action?

- A. Perform cleanup immediately: Stop triggers and delete dependent datasets before deleting a run-owned pipeline. Then, rely on the belief that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'A partial copy advances the watermark or rerun duplicates already committed data'; afterward, proceed on the belief that the first state record and returned identifiers can be discarded.
- C. Reconcile the checkpoint table and target partition, then rerun only the idempotent failed slice; next, preserve the current run identity and evidence.
- D. Change Select integration patterns and runtimes instead; then treat as decisive the assertion that success at Select integration patterns and runtimes will repair the failed state at Orchestrate idempotent ingestion.

## LAB13-Q26 — applied

'Orchestrate idempotent ingestion' awaits approval from data governance council. Without making a new change, the team must inspect the risk 'A partial copy advances the watermark or rerun duplicates already committed data' using the Azure PowerShell lane. Which action produces the read-only, lane-correct inspection?

- A. Rerun the Orchestrate idempotent ingestion implementation command and infer the expected state; in a separate step, treat it as established that absence of a shell error proves every property.
- B. Inspect the documented properties for Orchestrate idempotent ingestion; for this decision, retain this evidence: definition hash, activity graph, retry policy, watermark owner, quarantine path, and synthetic run result.
- C. Run only this negative inspection for Orchestrate idempotent ingestion: Empty pipelines and blind append operations without duplicate handling do not satisfy ingestion. As another control, use as justification the claim that an empty negative result reports every required positive property.
- D. Run the positive inspection for Use managed identity for linked services and apply it to Orchestrate idempotent ingestion; as a separate check, accept without proof that any command from the same lane proves the current checkpoint.

## LAB13-Q27 — applied

'Orchestrate idempotent ingestion' is reopened at the request of data engineering. A passing positive check does not by itself prove this negative assertion: Empty pipelines and blind append operations without duplicate handling do not satisfy ingestion. What best demonstrates the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Orchestrate idempotent ingestion and report full compliance. Separately, base approval on the claim that every prohibited parallel state must therefore be absent.
- B. Prove only that empty pipelines and blind append operations without duplicate handling do not satisfy ingestion and report the intended configuration as present; for this decision, use the premise that absence is equivalent to positive-state evidence.
- C. Use Serve governed analytical queries's negative assertion for Orchestrate idempotent ingestion. Before sign-off, consider it sufficient that negative assertions are interchangeable between checkpoints.
- D. Verify the positive properties for Orchestrate idempotent ingestion. Then, independently verify that empty pipelines and blind append operations without duplicate handling do not satisfy ingestion.

## LAB13-Q28 — applied

A design review of 'Orchestrate idempotent ingestion' includes analytics consumers. The board wants the Well-Architected consequence of mitigating this risk: A partial copy advances the watermark or rerun duplicates already committed data. Which option is the consequence attributable to this checkpoint?

- A. Use the Validate lineage operations and spend consequence as the result for Orchestrate idempotent ingestion; then rely on the claim that a pillar statement remains valid when moved away from Validate lineage operations and spend.
- B. Record this consequence: Operational Excellence: watermarks, quarantine, and idempotent replay make partial pipeline failure supportable. Next, tie it to LAB13-REQ-03.
- C. Remove the control responsible for the Orchestrate idempotent ingestion outcome. Independently, rely on the belief that a elevated cost classification outweighs the mandatory architecture state.
- D. Treat 'Operational Excellence: watermarks, quarantine, and idempotent replay make partial pipeline failure supportable' as proof that all five pillars pass. Next, proceed on the belief that the checkpoint 'Orchestrate idempotent ingestion' no longer needs its separate negative check.

## LAB13-Q29 — advanced

The team asks operational system owners to assess 'Orchestrate idempotent ingestion'. A material change now applies: Executives mandate near-real-time operational dashboards for one telemetry stream while partner files remain hourly and analysts prefer a unified Microsoft Fabric experience; revise the platform boundary without forcing every flow into streaming. What should the architect select as the correct revision to the decision record?

- A. Re-score Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL and both alternatives for Orchestrate idempotent ingestion. As another control, supersede the ADR using the changed evidence for LAB13-REQ-03.
- B. Retain Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL at Orchestrate idempotent ingestion without recalculating criteria or eligibility; as a separate check, take it as conclusive that the original weighted result is permanent.
- C. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse for Orchestrate idempotent ingestion without rechecking its mandatory constraints. Afterward, treat it as established that being different from the current design is an architecture criterion.
- D. Keep Azure Databricks with Azure Data Factory orchestration and Delta Lake eligible at Orchestrate idempotent ingestion by downgrading LAB13-REQ-03; next, use as justification the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB13-Q30 — advanced

A recommendation on 'Orchestrate idempotent ingestion' is requested by data governance council. After a partial run, cleanup must follow this dependency: Stop triggers and delete dependent datasets before deleting a run-owned pipeline. Select the dependency-safe cleanup plan.

- A. Apply the cleanup rule for Use managed identity for linked services before reconciling the current dependency. Before sign-off, treat as decisive the assertion that removing a parent needed to identify Orchestrate idempotent ingestion is harmless.
- B. Delete candidates by display name before comparing the Orchestrate idempotent ingestion ownership tags; as an independent condition, base approval on the claim that the dependency rule 'Stop triggers and delete dependent datasets before deleting a run-owned pipeline' is optional.
- C. Verify exact run-state IDs and ownership tags for Orchestrate idempotent ingestion. In addition, follow this dependency rule without purge: Stop triggers and delete dependent datasets before deleting a run-owned pipeline.
- D. Destroy recoverable copies before retaining the Orchestrate idempotent ingestion negative assertion 'Empty pipelines and blind append operations without duplicate handling do not satisfy ingestion'; during the same review, use the premise that remaining command logs are sufficient recovery evidence.

## LAB13-Q31 — foundational

'Serve governed analytical queries' is assigned to data governance council. Approval requires a positive result plus this independent negative assertion: An always-on dedicated SQL pool without a cost owner is not introduced by default. What should the team use as the acceptance rule that makes LAB13-REQ-04 testable?

- A. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse before checking Serve governed analytical queries. In addition, proceed on the belief that a successful deployment will later prove the architecture constraint.
- B. Use the passing result from Select integration patterns and runtimes to approve Serve governed analytical queries; before approval, treat as decisive the assertion that one control establishes an unrelated acceptance boundary.
- C. Choose Azure Databricks with Azure Data Factory orchestration and Delta Lake and skip the Serve governed analytical queries negative assertion. Separately, base approval on the claim that the candidate has the lowest implementation effort.
- D. Require the documented positive state for Serve governed analytical queries; as an independent condition, verify that an always-on dedicated SQL pool without a cost owner is not introduced by default.

## LAB13-Q32 — foundational

An assurance review of 'Serve governed analytical queries' includes data engineering. The selected architecture is Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL; object existence alone is not success. Which option best establishes the intended successful finding?

- A. Record curated lake data is exposed through governed serverless SQL views, with a dedicated pool considered only for measured isolation or performance needs; then classify it as success for LAB13-REQ-04.
- B. Use only the negative assertion 'An always-on dedicated SQL pool without a cost owner is not introduced by default' as the success result. Then, use as justification the claim that absence proves every required positive property.
- C. Use the successful finding from Use managed identity for linked services as the result for Serve governed analytical queries; afterward, accept without proof that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'File layout, schema drift, or small-file volume makes serverless queries unstable or expensive' as a successful state; then rely on the claim that the command returned an object.

## LAB13-Q33 — foundational

Approval of 'Serve governed analytical queries' is questioned by analytics consumers. Evidence must address this risk without retaining credentials: File layout, schema drift, or small-file volume makes serverless queries unstable or expensive. Which answer identifies sufficient, properly scoped evidence?

- A. Substitute the evidence from Orchestrate idempotent ingestion for Serve governed analytical queries; in a separate step, use the premise that a related checkpoint proves the current expected state.
- B. Store unredacted Serve governed analytical queries output with operator, tenant, token, and request context. As another control, consider it sufficient that reproduction requires every captured field.
- C. Record only the Serve governed analytical queries positive inspection's exit status; as a separate check, take it as conclusive that projected properties and assertion results can be reconstructed later.
- D. Retain workspace label, serving mode, curated-zone path, semantic owner, authorization groups, and query-cost controls. Also, exclude credentials and unrelated response fields.

## LAB13-Q34 — applied

The implementation review has reached 'Serve governed analytical queries'. The target is Curated lake data is exposed through governed serverless SQL views, with a dedicated pool considered only for measured isolation or performance needs, but the latest evidence does not show it. What recommendation gives the reviewers the most likely cause?

- A. Treat 'Monitoring records show success while downstream reconciliation detects missing or duplicate rows' as grounds to reject Serve governed analytical queries. Separately, rely on the belief that validate lineage operations and spend's failure model applies unchanged here.
- B. Investigate file layout, schema drift, or small-file volume makes serverless queries unstable or expensive. Afterward, isolate that cause before changing Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL.
- C. Ignore the negative assertion 'An always-on dedicated SQL pool without a cost owner is not introduced by default'; for this decision, proceed on the belief that a later material change will make it unnecessary.
- D. Investigate Select integration patterns and runtimes instead of diagnosing Serve governed analytical queries. Before sign-off, treat as decisive the assertion that a passing result at Select integration patterns and runtimes identifies the current cause.

## LAB13-Q35 — applied

The approach to 'Serve governed analytical queries' is challenged by data governance council. The run encountered this modeled failure: File layout, schema drift, or small-file volume makes serverless queries unstable or expensive. Which action produces the safest recovery action?

- A. Perform cleanup immediately: Remove run-owned SQL artifacts before workspace deletion; retain no source data in evidence; then treat it as established that the failed operation and its returned identifiers do not need reconciliation.
- B. Compact and curate files, enforce schema contracts, and measure scanned bytes before provisioning dedicated capacity. Separately, preserve the current run identity and evidence.
- C. Create a different run identity before diagnosing 'File layout, schema drift, or small-file volume makes serverless queries unstable or expensive'. Independently, use as justification the claim that the first state record and returned identifiers can be discarded.
- D. Change Select integration patterns and runtimes instead. Next, accept without proof that success at Select integration patterns and runtimes will repair the failed state at Serve governed analytical queries.

## LAB13-Q36 — applied

A decision test for 'Serve governed analytical queries' includes data engineering. Without making a new change, the team must inspect the risk 'File layout, schema drift, or small-file volume makes serverless queries unstable or expensive' using the Azure PowerShell lane. What best demonstrates the read-only, lane-correct inspection?

- A. Rerun the Serve governed analytical queries implementation command and infer the expected state; as a separate check, base approval on the claim that absence of a shell error proves every property.
- B. Run only this negative inspection for Serve governed analytical queries: An always-on dedicated SQL pool without a cost owner is not introduced by default. Afterward, use the premise that an empty negative result reports every required positive property.
- C. Run the positive inspection for Use managed identity for linked services and apply it to Serve governed analytical queries; next, consider it sufficient that any command from the same lane proves the current checkpoint.
- D. Inspect the documented properties for Serve governed analytical queries; without relying on inference, retain this evidence: workspace label, serving mode, curated-zone path, semantic owner, authorization groups, and query-cost controls.

## LAB13-Q37 — applied

The architecture board reconsiders 'Serve governed analytical queries' with analytics consumers. A passing positive check does not by itself prove this negative assertion: An always-on dedicated SQL pool without a cost owner is not introduced by default. Which option is the assertion pair that proves both conditions independently?

- A. Verify only the positive result for Serve governed analytical queries and report full compliance. Before sign-off, rely on the claim that every prohibited parallel state must therefore be absent.
- B. Verify the positive properties for Serve governed analytical queries. Independently, independently verify that an always-on dedicated SQL pool without a cost owner is not introduced by default.
- C. Prove only that an always-on dedicated SQL pool without a cost owner is not introduced by default and report the intended configuration as present; as an independent condition, rely on the belief that absence is equivalent to positive-state evidence.
- D. Use Orchestrate idempotent ingestion's negative assertion for Serve governed analytical queries; for the recorded decision, proceed on the belief that negative assertions are interchangeable between checkpoints.

## LAB13-Q38 — applied

A review of 'Serve governed analytical queries' begins with input from operational system owners. The board wants the Well-Architected consequence of mitigating this risk: File layout, schema drift, or small-file volume makes serverless queries unstable or expensive. What should the architect select as the consequence attributable to this checkpoint?

- A. Use the Validate lineage operations and spend consequence as the result for Serve governed analytical queries. Next, take it as conclusive that a pillar statement remains valid when moved away from Validate lineage operations and spend.
- B. Record this consequence: Performance Efficiency: curated file layout and serverless views reduce scan amplification for analytical queries; in a separate step, tie it to LAB13-REQ-04.
- C. Remove the control responsible for the Serve governed analytical queries outcome, and then treat it as established that a elevated cost classification outweighs the mandatory architecture state.
- D. Treat 'Performance Efficiency: curated file layout and serverless views reduce scan amplification for analytical queries' as proof that all five pillars pass. Also, use as justification the claim that the checkpoint 'Serve governed analytical queries' no longer needs its separate negative check.

## LAB13-Q39 — advanced

'Serve governed analytical queries' awaits approval from data governance council. A material change now applies: Executives mandate near-real-time operational dashboards for one telemetry stream while partner files remain hourly and analysts prefer a unified Microsoft Fabric experience; revise the platform boundary without forcing every flow into streaming. Select the correct revision to the decision record.

- A. Retain Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL at Serve governed analytical queries without recalculating criteria or eligibility; next, treat as decisive the assertion that the original weighted result is permanent.
- B. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse for Serve governed analytical queries without rechecking its mandatory constraints. In addition, base approval on the claim that being different from the current design is an architecture criterion.
- C. Re-score Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL and both alternatives for Serve governed analytical queries; next, supersede the ADR using the changed evidence for LAB13-REQ-04.
- D. Keep Azure Databricks with Azure Data Factory orchestration and Delta Lake eligible at Serve governed analytical queries by downgrading LAB13-REQ-04; before approval, use the premise that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB13-Q40 — advanced

'Serve governed analytical queries' is reopened at the request of data engineering. After a partial run, cleanup must follow this dependency: Remove run-owned SQL artifacts before workspace deletion; retain no source data in evidence. Which response provides the dependency-safe cleanup plan?

- A. Apply the cleanup rule for Use managed identity for linked services before reconciling the current dependency; as another gate, accept without proof that removing a parent needed to identify Serve governed analytical queries is harmless.
- B. Delete candidates by display name before comparing the Serve governed analytical queries ownership tags. Then, rely on the claim that the dependency rule 'Remove run-owned SQL artifacts before workspace deletion; retain no source data in evidence' is optional.
- C. Destroy recoverable copies before retaining the Serve governed analytical queries negative assertion 'An always-on dedicated SQL pool without a cost owner is not introduced by default'; afterward, rely on the belief that remaining command logs are sufficient recovery evidence.
- D. Verify exact run-state IDs and ownership tags for Serve governed analytical queries; for this decision, follow this dependency rule without purge: Remove run-owned SQL artifacts before workspace deletion; retain no source data in evidence.

## LAB13-Q41 — foundational

A design review of 'Validate lineage operations and spend' includes data engineering. Approval requires a positive result plus this independent negative assertion: Stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence. Which option best establishes the acceptance rule that makes LAB13-REQ-05 testable?

- A. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse before checking Validate lineage operations and spend. Separately, use as justification the claim that a successful deployment will later prove the architecture constraint.
- B. Require the documented positive state for Validate lineage operations and spend; afterward, verify that stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence.
- C. Use the passing result from Select integration patterns and runtimes to approve Validate lineage operations and spend; for this decision, accept without proof that one control establishes an unrelated acceptance boundary.
- D. Choose Azure Databricks with Azure Data Factory orchestration and Delta Lake and skip the Validate lineage operations and spend negative assertion. Before sign-off, rely on the claim that the candidate has the lowest implementation effort.

## LAB13-Q42 — foundational

The team asks analytics consumers to assess 'Validate lineage operations and spend'. The selected architecture is Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL; object existence alone is not success. Which answer identifies the intended successful finding?

- A. Record a synthetic run links source slice, pipeline and activity IDs, curated output, status, duration, owner, and estimated service cost, and then classify it as success for LAB13-REQ-05.
- B. Use only the negative assertion 'Stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence' as the success result; then use the premise that absence proves every required positive property.
- C. Use the successful finding from Use managed identity for linked services as the result for Validate lineage operations and spend. Independently, consider it sufficient that a property from the current checkpoint does not need to be inspected.
- D. Record the failure condition 'Monitoring records show success while downstream reconciliation detects missing or duplicate rows' as a successful state. Next, take it as conclusive that the command returned an object.

## LAB13-Q43 — foundational

A recommendation on 'Validate lineage operations and spend' is requested by operational system owners. Evidence must address this risk without retaining credentials: Monitoring records show success while downstream reconciliation detects missing or duplicate rows. What recommendation gives the reviewers sufficient, properly scoped evidence?

- A. Substitute the evidence from Orchestrate idempotent ingestion for Validate lineage operations and spend; as a separate check, rely on the belief that a related checkpoint proves the current expected state.
- B. Store unredacted Validate lineage operations and spend output with operator, tenant, token, and request context. Afterward, proceed on the belief that reproduction requires every captured field.
- C. Retain sanitized run and activity IDs, statuses, durations, row counts, lineage references, and cost estimate; as a separate check, exclude credentials and unrelated response fields.
- D. Record only the Validate lineage operations and spend positive inspection's exit status; next, treat as decisive the assertion that projected properties and assertion results can be reconstructed later.

## LAB13-Q44 — applied

'Validate lineage operations and spend' is assigned to data governance council. The target is A synthetic run links source slice, pipeline and activity IDs, curated output, status, duration, owner, and estimated service cost, but the latest evidence does not show it. Which action produces the most likely cause?

- A. Treat 'File layout, schema drift, or small-file volume makes serverless queries unstable or expensive' as grounds to reject Validate lineage operations and spend. Before sign-off, treat it as established that serve governed analytical queries's failure model applies unchanged here.
- B. Investigate monitoring records show success while downstream reconciliation detects missing or duplicate rows; before approval, isolate that cause before changing Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL.
- C. Ignore the negative assertion 'Stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence'; as an independent condition, use as justification the claim that a later material change will make it unnecessary.
- D. Investigate Select integration patterns and runtimes instead of diagnosing Validate lineage operations and spend; as another gate, accept without proof that a passing result at Select integration patterns and runtimes identifies the current cause.

## LAB13-Q45 — applied

An assurance review of 'Validate lineage operations and spend' includes data engineering. The run encountered this modeled failure: Monitoring records show success while downstream reconciliation detects missing or duplicate rows. What best demonstrates the safest recovery action?

- A. Perform cleanup immediately: Cancel run-owned triggers and remove synthetic outputs in dependency order; preserve sanitized validation only. Next, base approval on the claim that the failed operation and its returned identifiers do not need reconciliation.
- B. Create a different run identity before diagnosing 'Monitoring records show success while downstream reconciliation detects missing or duplicate rows', and then use the premise that the first state record and returned identifiers can be discarded.
- C. Fail the validation, quarantine the slice, and reconcile business counts before republishing; as an independent condition, preserve the current run identity and evidence.
- D. Change Select integration patterns and runtimes instead. Also, consider it sufficient that success at Select integration patterns and runtimes will repair the failed state at Validate lineage operations and spend.

## LAB13-Q46 — applied

Approval of 'Validate lineage operations and spend' is questioned by analytics consumers. Without making a new change, the team must inspect the risk 'Monitoring records show success while downstream reconciliation detects missing or duplicate rows' using the Azure PowerShell lane. Which option is the read-only, lane-correct inspection?

- A. Rerun the Validate lineage operations and spend implementation command and infer the expected state; next, rely on the claim that absence of a shell error proves every property.
- B. Run only this negative inspection for Validate lineage operations and spend: Stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence. In addition, rely on the belief that an empty negative result reports every required positive property.
- C. Run the positive inspection for Use managed identity for linked services and apply it to Validate lineage operations and spend; before approval, proceed on the belief that any command from the same lane proves the current checkpoint.
- D. Inspect the documented properties for Validate lineage operations and spend; then retain this evidence: sanitized run and activity IDs, statuses, durations, row counts, lineage references, and cost estimate.

## LAB13-Q47 — applied

The implementation review has reached 'Validate lineage operations and spend'. A passing positive check does not by itself prove this negative assertion: Stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence. What should the architect select as the assertion pair that proves both conditions independently?

- A. Verify the positive properties for Validate lineage operations and spend. Also, independently verify that stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence.
- B. Verify only the positive result for Validate lineage operations and spend and report full compliance; without relying on inference, take it as conclusive that every prohibited parallel state must therefore be absent.
- C. Prove only that stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence and report the intended configuration as present. Then, treat it as established that absence is equivalent to positive-state evidence.
- D. Use Orchestrate idempotent ingestion's negative assertion for Validate lineage operations and spend; afterward, use as justification the claim that negative assertions are interchangeable between checkpoints.

## LAB13-Q48 — applied

The approach to 'Validate lineage operations and spend' is challenged by data governance council. The board wants the Well-Architected consequence of mitigating this risk: Monitoring records show success while downstream reconciliation detects missing or duplicate rows. Select the consequence attributable to this checkpoint.

- A. Use the Serve governed analytical queries consequence as the result for Validate lineage operations and spend. Also, treat as decisive the assertion that a pillar statement remains valid when moved away from Serve governed analytical queries.
- B. Remove the control responsible for the Validate lineage operations and spend outcome; in a separate step, base approval on the claim that a elevated cost classification outweighs the mandatory architecture state.
- C. Treat 'Cost Optimization: correlated run, activity, volume, and duration evidence attributes consumption to an owned pipeline' as proof that all five pillars pass. As another control, use the premise that the checkpoint 'Validate lineage operations and spend' no longer needs its separate negative check.
- D. Record this consequence: Cost Optimization: correlated run, activity, volume, and duration evidence attributes consumption to an owned pipeline. Afterward, tie it to LAB13-REQ-05.

## LAB13-Q49 — advanced

A decision test for 'Validate lineage operations and spend' includes data engineering. A material change now applies: Executives mandate near-real-time operational dashboards for one telemetry stream while partner files remain hourly and analysts prefer a unified Microsoft Fabric experience; revise the platform boundary without forcing every flow into streaming. Which response provides the correct revision to the decision record?

- A. Retain Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL at Validate lineage operations and spend without recalculating criteria or eligibility; before approval, accept without proof that the original weighted result is permanent.
- B. Select Microsoft Fabric Data Factory with OneLake and Fabric Warehouse for Validate lineage operations and spend without rechecking its mandatory constraints. Separately, rely on the claim that being different from the current design is an architecture criterion.
- C. Re-score Azure Data Factory with ADLS Gen2 and Azure Synapse serverless SQL and both alternatives for Validate lineage operations and spend. Separately, supersede the ADR using the changed evidence for LAB13-REQ-05.
- D. Keep Azure Databricks with Azure Data Factory orchestration and Delta Lake eligible at Validate lineage operations and spend by downgrading LAB13-REQ-05; for this decision, rely on the belief that stakeholder approval is unnecessary when that requirement blocks the candidate.

## LAB13-Q50 — advanced

The architecture board reconsiders 'Validate lineage operations and spend' with analytics consumers. After a partial run, cleanup must follow this dependency: Cancel run-owned triggers and remove synthetic outputs in dependency order; preserve sanitized validation only. Which choice gives the team the dependency-safe cleanup plan?

- A. Verify exact run-state IDs and ownership tags for Validate lineage operations and spend; separately, follow this dependency rule without purge: Cancel run-owned triggers and remove synthetic outputs in dependency order; preserve sanitized validation only.
- B. Apply the cleanup rule for Use managed identity for linked services before reconciling the current dependency; afterward, consider it sufficient that removing a parent needed to identify Validate lineage operations and spend is harmless.
- C. Delete candidates by display name before comparing the Validate lineage operations and spend ownership tags; then take it as conclusive that the dependency rule 'Cancel run-owned triggers and remove synthetic outputs in dependency order; preserve sanitized validation only' is optional.
- D. Destroy recoverable copies before retaining the Validate lineage operations and spend negative assertion 'Stalled runs, sensitive input or output payloads, and unowned spend are not treated as successful evidence'. Independently, treat it as established that remaining command logs are sufficient recovery evidence.
<!-- END GENERATED AZ305 V1 -->
