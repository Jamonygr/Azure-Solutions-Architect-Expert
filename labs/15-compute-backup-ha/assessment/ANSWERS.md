<!-- BEGIN GENERATED AZ305 V1 -->
# LAB-15 answer key

Use after completing the learner assessment. Every choice has a specific explanation.

## LAB15-Q01 — answer B

A decision test for 'Establish compute failure-domain evidence' includes clinical operations owner. Approval requires a positive result plus this independent negative assertion: Counting multiple VMs as highly available when they share an unexamined failure domain must fail. What should the architect select as the acceptance rule that makes LAB15-REQ-01 testable?

- ✗ **A. Select Availability sets with locally redundant vault protection before checking Establish compute failure-domain evidence; in a separate step, take it as conclusive that a successful deployment will later prove the architecture constraint.** — The failure model establishes that counting multiple VMs as highly available when they share an unexamined failure domain must fail. The requirement-to-evidence link establishes that a deployment result cannot prove LAB15-REQ-01, and Availability sets with locally redundant vault protection still has to meet the mandatory boundary.
- ✓ **B. Require the documented positive state for Establish compute failure-domain evidence. Then, verify that counting multiple VMs as highly available when they share an unexamined failure domain must fail.** — The traceable checkpoint outcome is that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. The requirement-to-evidence link establishes that the positive state and an independent negative assertion jointly make LAB15-REQ-01 testable.
- ✗ **C. Use the passing result from Verify backup coverage and policy intent to approve Establish compute failure-domain evidence. As another control, treat it as established that one control establishes an unrelated acceptance boundary.** — The recovery guidance assumes that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. The requirement-to-evidence link establishes that that outcome belongs to Verify backup coverage and policy intent and leaves Establish compute failure-domain evidence unverified.
- ✗ **D. Choose Azure Site Recovery warm replicas in a secondary region for every virtual machine and skip the Establish compute failure-domain evidence negative assertion; as a separate check, use as justification the claim that the candidate has the lowest implementation effort.** — The WAF consequence identifies that maintain appointment coordination through a datacenter failure and recover compromised virtual machines from trusted points without unsafe reconnection. The requirement-to-evidence link establishes that implementation effort cannot justify skipping the negative assertion or displace LAB15-REQ-01.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q02 — answer A

The architecture board reconsiders 'Establish compute failure-domain evidence' with infrastructure engineering lead. The selected architecture is Zone-spread virtual machine scale sets with Azure Backup and isolated restore; object existence alone is not success. Select the intended successful finding.

- ✓ **A. Record every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. Next, classify it as success for LAB15-REQ-01.** — The command-level assertion is anchored in the fact that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. The requirement-to-evidence link establishes that this is the authored target state for Establish compute failure-domain evidence and directly supports LAB15-REQ-01.
- ✗ **B. Use only the negative assertion 'Counting multiple VMs as highly available when they share an unexamined failure domain must fail' as the success result. Separately, treat as decisive the assertion that absence proves every required positive property.** — counting multiple VMs as highly available when they share an unexamined failure domain must fail. Consequently, this is the independent prohibited-state assertion, not a successful finding.
- ✗ **C. Use the successful finding from Evaluate zone-resilient capacity as the result for Establish compute failure-domain evidence; for this decision, base approval on the claim that a property from the current checkpoint does not need to be inspected.** — The controlling fact is that the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity. Consequently, evidence for Evaluate zone-resilient capacity cannot substitute for the properties required at Establish compute failure-domain evidence.
- ✗ **D. Record the failure condition 'Missing placement metadata can conceal a single-zone application dependency' as a successful state. Before sign-off, use the premise that the command returned an object.** — The authored acceptance boundary states that missing placement metadata can conceal a single-zone application dependency. Consequently, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q03 — answer A

A review of 'Establish compute failure-domain evidence' begins with input from security incident commander. Evidence must address this risk without retaining credentials: Missing placement metadata can conceal a single-zone application dependency. Which response provides sufficient, properly scoped evidence?

- ✓ **A. Retain export the VM placement matrix with tier, zone, disk, dependency, and owner fields. As another control, exclude credentials and unrelated response fields.** — The relevant observation is that export the VM placement matrix with tier, zone, disk, dependency, and owner fields. Consequently, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **B. Substitute the evidence from Design isolated restore and recovery validation for Establish compute failure-domain evidence; then accept without proof that a related checkpoint proves the current expected state.** — The checkpoint specifically records that record the selected recovery point, job timing, isolation controls, assertion results, and approval outcome. Consequently, that evidence supports Design isolated restore and recovery validation, so it cannot demonstrate Every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order.
- ✗ **C. Store unredacted Establish compute failure-domain evidence output with operator, tenant, token, and request context. Independently, rely on the claim that reproduction requires every captured field.** — The scenario makes clear that unredacted implementation output. Consequently, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Establish compute failure-domain evidence positive inspection's exit status. Next, rely on the belief that projected properties and assertion results can be reconstructed later.** — The architecture evidence must show that the positive inspection's exit status. Consequently, an exit code alone does not show whether every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q04 — answer B

'Establish compute failure-domain evidence' awaits approval from backup service owner. The target is Every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order, but the latest evidence does not show it. Which choice gives the team the most likely cause?

- ✗ **A. Treat 'Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration' as grounds to reject Establish compute failure-domain evidence; as a separate check, consider it sufficient that exercise failover ordering and rollback's failure model applies unchanged here.** — The review is governed by this fact: parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. Consequently, that condition belongs to Exercise failover ordering and rollback and does not by itself invalidate Zone-spread virtual machine scale sets with Azure Backup and isolated restore.
- ✓ **B. Investigate missing placement metadata can conceal a single-zone application dependency. In addition, isolate that cause before changing Zone-spread virtual machine scale sets with Azure Backup and isolated restore.** — The applicable design condition is that missing placement metadata can conceal a single-zone application dependency. Consequently, it is the checkpoint's causal failure model and should be isolated before retrying Establish compute failure-domain evidence.
- ✗ **C. Ignore the negative assertion 'Counting multiple VMs as highly available when they share an unexamined failure domain must fail'. Afterward, take it as conclusive that a later material change will make it unnecessary.** — The retained result must be reconciled with the fact that counting multiple VMs as highly available when they share an unexamined failure domain must fail. Consequently, the negative assertion must be evaluated now, independent of a later business change.
- ✗ **D. Investigate Verify backup coverage and policy intent instead of diagnosing Establish compute failure-domain evidence; next, treat it as established that a passing result at Verify backup coverage and policy intent identifies the current cause.** — The decision tension comes from the fact that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. Consequently, a passing result at Verify backup coverage and policy intent gives no causal evidence for the failure at Establish compute failure-domain evidence.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q05 — answer C

'Establish compute failure-domain evidence' is reopened at the request of finance partner. The run encountered this modeled failure: Missing placement metadata can conceal a single-zone application dependency. What is the safest recovery action?

- ✗ **A. Perform cleanup immediately: Remove the local inventory export after review; do not alter VM placement during discovery. Before sign-off, proceed on the belief that the failed operation and its returned identifiers do not need reconciliation.** — The traceable checkpoint outcome is that remove the local inventory export after review; do not alter VM placement during discovery. Consequently, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **B. Create a different run identity before diagnosing 'Missing placement metadata can conceal a single-zone application dependency'; as an independent condition, treat as decisive the assertion that the first state record and returned identifiers can be discarded.** — The failure model establishes that missing placement metadata can conceal a single-zone application dependency. Consequently, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✓ **C. Confirm supported zones and SKU capacity, update the design matrix, and rerun both placement assertions. Before sign-off, preserve the current run identity and evidence.** — The safe operating boundary says that confirm supported zones and SKU capacity, update the design matrix, and rerun both placement assertions. Consequently, it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **D. Change Verify backup coverage and policy intent instead; for the final assessment, base approval on the claim that success at Verify backup coverage and policy intent will repair the failed state at Establish compute failure-domain evidence.** — The recovery guidance assumes that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. Consequently, altering an already separate checkpoint does not repair the modeled failure at Establish compute failure-domain evidence.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q06 — answer A

A design review of 'Establish compute failure-domain evidence' includes clinical operations owner. Without making a new change, the team must inspect the risk 'Missing placement metadata can conceal a single-zone application dependency' using the Azure PowerShell lane. Which recommendation supplies the read-only, lane-correct inspection?

- ✓ **A. Inspect the documented properties for Establish compute failure-domain evidence; afterward, retain this evidence: export the VM placement matrix with tier, zone, disk, dependency, and owner fields.** — The WAF consequence identifies that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. Consequently, the read-only inspection directly tests the properties required at Establish compute failure-domain evidence.
- ✗ **B. Rerun the Establish compute failure-domain evidence implementation command and infer the expected state. Next, use as justification the claim that absence of a shell error proves every property.** — The command-level assertion is anchored in the fact that the implementation command. Consequently, it can mutate state and shell success does not independently assert the expected properties.
- ✗ **C. Run only this negative inspection for Establish compute failure-domain evidence: Counting multiple VMs as highly available when they share an unexamined failure domain must fail, and then accept without proof that an empty negative result reports every required positive property.** — the negative inspection. For this case, absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Evaluate zone-resilient capacity and apply it to Establish compute failure-domain evidence. Also, rely on the claim that any command from the same lane proves the current checkpoint.** — The controlling fact is that the positive inspection for Evaluate zone-resilient capacity. For this case, it is lane-correct but proves Evaluate zone-resilient capacity, not Establish compute failure-domain evidence.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q07 — answer B

The team asks infrastructure engineering lead to assess 'Establish compute failure-domain evidence'. A passing positive check does not by itself prove this negative assertion: Counting multiple VMs as highly available when they share an unexamined failure domain must fail. Choose the assertion pair that proves both conditions independently.

- ✗ **A. Verify only the positive result for Establish compute failure-domain evidence and report full compliance; next, use the premise that every prohibited parallel state must therefore be absent.** — The relevant observation is that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. For this case, the positive result alone does not test the explicit anti-condition 'Counting multiple VMs as highly available when they share an unexamined failure domain must fail'.
- ✓ **B. Verify the positive properties for Establish compute failure-domain evidence, and then independently verify that counting multiple VMs as highly available when they share an unexamined failure domain must fail.** — The authored acceptance boundary states that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order; Counting multiple VMs as highly available when they share an unexamined failure domain must fail. For this case, two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **C. Prove only that counting multiple VMs as highly available when they share an unexamined failure domain must fail and report the intended configuration as present. In addition, consider it sufficient that absence is equivalent to positive-state evidence.** — The checkpoint specifically records that counting multiple VMs as highly available when they share an unexamined failure domain must fail. For this case, absence evidence cannot demonstrate the required positive state 'Every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order'.
- ✗ **D. Use Design isolated restore and recovery validation's negative assertion for Establish compute failure-domain evidence; before approval, take it as conclusive that negative assertions are interchangeable between checkpoints.** — The scenario makes clear that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. For this case, the second assertion is valid for Design isolated restore and recovery validation but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q08 — answer D

A recommendation on 'Establish compute failure-domain evidence' is requested by security incident commander. The board wants the Well-Architected consequence of mitigating this risk: Missing placement metadata can conceal a single-zone application dependency. Which answer describes the consequence attributable to this checkpoint?

- ✗ **A. Use the Exercise failover ordering and rollback consequence as the result for Establish compute failure-domain evidence; separately, rely on the belief that a pillar statement remains valid when moved away from Exercise failover ordering and rollback.** — The applicable design condition is that cost Optimization: the safe analogue validates orchestration without maintaining a full secondary compute estate. For this case, that tradeoff belongs to Exercise failover ordering and rollback and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Establish compute failure-domain evidence outcome. Then, proceed on the belief that a low cost classification outweighs the mandatory architecture state.** — The review is governed by this fact: the required outcome at Establish compute failure-domain evidence. For this case, cost Optimization cannot remove the acceptance condition 'Every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order'.
- ✗ **C. Treat 'Reliability: explicit zone distribution reduces correlated compute failure' as proof that all five pillars pass; afterward, treat as decisive the assertion that the checkpoint 'Establish compute failure-domain evidence' no longer needs its separate negative check.** — The retained result must be reconciled with the fact that counting multiple VMs as highly available when they share an unexamined failure domain must fail. For this case, one positive command cannot establish every pillar, especially while the negative state remains unchecked.
- ✓ **D. Record this consequence: Reliability: explicit zone distribution reduces correlated compute failure; as a separate check, tie it to LAB15-REQ-01.** — The architecture evidence must show that reliability: explicit zone distribution reduces correlated compute failure. For this case, it states the authored pillar consequence of the control evaluated at Establish compute failure-domain evidence.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q09 — answer D

'Establish compute failure-domain evidence' is assigned to backup service owner. A material change now applies: Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect. What should be recorded as the correct revision to the decision record?

- ✗ **A. Retain Zone-spread virtual machine scale sets with Azure Backup and isolated restore at Establish compute failure-domain evidence without recalculating criteria or eligibility. Also, treat it as established that the original weighted result is permanent.** — The safe operating boundary says that zone-spread virtual machine scale sets with Azure Backup and isolated restore. For this case, the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Availability sets with locally redundant vault protection for Establish compute failure-domain evidence without rechecking its mandatory constraints; in a separate step, use as justification the claim that being different from the current design is an architecture criterion.** — The traceable checkpoint outcome is that availability sets with locally redundant vault protection. For this case, being different is not a criterion, and the candidate still must avoid the prohibited state at Establish compute failure-domain evidence.
- ✗ **C. Keep Azure Site Recovery warm replicas in a secondary region for every virtual machine eligible at Establish compute failure-domain evidence by downgrading LAB15-REQ-01. As another control, accept without proof that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The failure model establishes that lAB15-REQ-01. For this case, an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
- ✓ **D. Re-score Zone-spread virtual machine scale sets with Azure Backup and isolated restore and both alternatives for Establish compute failure-domain evidence; before approval, supersede the ADR using the changed evidence for LAB15-REQ-01.** — The decision tension comes from the fact that zone-spread virtual machine scale sets with Azure Backup and isolated restore at Establish compute failure-domain evidence. For this case, the material change 'Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect.' requires fresh eligibility, weighted analysis, and a superseding decision.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q10 — answer D

An assurance review of 'Establish compute failure-domain evidence' includes finance partner. After a partial run, cleanup must follow this dependency: Remove the local inventory export after review; do not alter VM placement during discovery. Which proposal supplies the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Evaluate zone-resilient capacity before reconciling the current dependency; before approval, base approval on the claim that removing a parent needed to identify Establish compute failure-domain evidence is harmless.** — The WAF consequence identifies that delete local calculation output; this checkpoint requests no quota and provisions no compute. For this case, a cleanup rule for Evaluate zone-resilient capacity cannot override the dependency declared for Establish compute failure-domain evidence.
- ✗ **B. Delete candidates by display name before comparing the Establish compute failure-domain evidence ownership tags. Separately, use the premise that the dependency rule 'Remove the local inventory export after review; do not alter VM placement during discovery' is optional.** — The command-level assertion is anchored in the fact that export the VM placement matrix with tier, zone, disk, dependency, and owner fields. For this case, names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **C. Destroy recoverable copies before retaining the Establish compute failure-domain evidence negative assertion 'Counting multiple VMs as highly available when they share an unexamined failure domain must fail'; for this decision, consider it sufficient that remaining command logs are sufficient recovery evidence.** — counting multiple VMs as highly available when they share an unexamined failure domain must fail. That evidence means irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.
- ✓ **D. Verify exact run-state IDs and ownership tags for Establish compute failure-domain evidence; as an independent condition, follow this dependency rule without purge: Remove the local inventory export after review; do not alter VM placement during discovery.** — The recovery guidance assumes that remove the local inventory export after review; do not alter VM placement during discovery. For this case, exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.

Remediation: [checkpoint-1](../README.md#checkpoint-1)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q11 — answer C

Approval of 'Verify backup coverage and policy intent' is questioned by infrastructure engineering lead. Approval requires a positive result plus this independent negative assertion: A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. Select the acceptance rule that makes LAB15-REQ-02 testable.

- ✗ **A. Select Availability sets with locally redundant vault protection before checking Verify backup coverage and policy intent; as a separate check, treat as decisive the assertion that a successful deployment will later prove the architecture constraint.** — The authored acceptance boundary states that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. That evidence means a deployment result cannot prove LAB15-REQ-02, and Availability sets with locally redundant vault protection still has to meet the mandatory boundary.
- ✗ **B. Use the passing result from Establish compute failure-domain evidence to approve Verify backup coverage and policy intent. Afterward, base approval on the claim that one control establishes an unrelated acceptance boundary.** — The relevant observation is that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. That evidence means that outcome belongs to Establish compute failure-domain evidence and leaves Verify backup coverage and policy intent unverified.
- ✓ **C. Require the documented positive state for Verify backup coverage and policy intent. Independently, verify that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup.** — The controlling fact is that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. That evidence means the positive state and an independent negative assertion jointly make LAB15-REQ-02 testable.
- ✗ **D. Choose Azure Site Recovery warm replicas in a secondary region for every virtual machine and skip the Verify backup coverage and policy intent negative assertion; next, use the premise that the candidate has the lowest implementation effort.** — The checkpoint specifically records that maintain appointment coordination through a datacenter failure and recover compromised virtual machines from trusted points without unsafe reconnection. That evidence means implementation effort cannot justify skipping the negative assertion or displace LAB15-REQ-02.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q12 — answer A

The implementation review has reached 'Verify backup coverage and policy intent'. The selected architecture is Zone-spread virtual machine scale sets with Azure Backup and isolated restore; object existence alone is not success. Which response provides the intended successful finding?

- ✓ **A. Record the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements; in a separate step, classify it as success for LAB15-REQ-02.** — The scenario makes clear that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. That evidence means this is the authored target state for Verify backup coverage and policy intent and directly supports LAB15-REQ-02.
- ✗ **B. Use only the negative assertion 'A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup' as the success result. Before sign-off, accept without proof that absence proves every required positive property.** — The architecture evidence must show that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. That evidence means this is the independent prohibited-state assertion, not a successful finding.
- ✗ **C. Use the successful finding from Evaluate zone-resilient capacity as the result for Verify backup coverage and policy intent; as an independent condition, rely on the claim that a property from the current checkpoint does not need to be inspected.** — The applicable design condition is that the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity. That evidence means evidence for Evaluate zone-resilient capacity cannot substitute for the properties required at Verify backup coverage and policy intent.
- ✗ **D. Record the failure condition 'Vault existence alone does not prove that every required disk is protected or retained correctly' as a successful state; separately, rely on the belief that the command returned an object.** — The review is governed by this fact: vault existence alone does not prove that every required disk is protected or retained correctly. That evidence means resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q13 — answer A

The approach to 'Verify backup coverage and policy intent' is challenged by backup service owner. Evidence must address this risk without retaining credentials: Vault existence alone does not prove that every required disk is protected or retained correctly. Which choice gives the team sufficient, properly scoped evidence?

- ✓ **A. Retain preserve vault configuration, policy exports, protected-item coverage, and approved exceptions; next, exclude credentials and unrelated response fields.** — The retained result must be reconciled with the fact that preserve vault configuration, policy exports, protected-item coverage, and approved exceptions. That evidence means it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **B. Substitute the evidence from Design isolated restore and recovery validation for Verify backup coverage and policy intent. Next, consider it sufficient that a related checkpoint proves the current expected state.** — The decision tension comes from the fact that record the selected recovery point, job timing, isolation controls, assertion results, and approval outcome. That evidence means that evidence supports Design isolated restore and recovery validation, so it cannot demonstrate The coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements.
- ✗ **C. Store unredacted Verify backup coverage and policy intent output with operator, tenant, token, and request context, and then take it as conclusive that reproduction requires every captured field.** — The safe operating boundary says that unredacted implementation output. That evidence means identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Verify backup coverage and policy intent positive inspection's exit status. Also, treat it as established that projected properties and assertion results can be reconstructed later.** — The traceable checkpoint outcome is that the positive inspection's exit status. That evidence means an exit code alone does not show whether the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q14 — answer D

A decision test for 'Verify backup coverage and policy intent' includes finance partner. The target is The coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements, but the latest evidence does not show it. What is the most likely cause?

- ✗ **A. Treat 'Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration' as grounds to reject Verify backup coverage and policy intent; next, proceed on the belief that exercise failover ordering and rollback's failure model applies unchanged here.** — The recovery guidance assumes that parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. That evidence means that condition belongs to Exercise failover ordering and rollback and does not by itself invalidate Zone-spread virtual machine scale sets with Azure Backup and isolated restore.
- ✗ **B. Ignore the negative assertion 'A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup'. In addition, treat as decisive the assertion that a later material change will make it unnecessary.** — The WAF consequence identifies that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. That evidence means the negative assertion must be evaluated now, independent of a later business change.
- ✗ **C. Investigate Establish compute failure-domain evidence instead of diagnosing Verify backup coverage and policy intent; before approval, base approval on the claim that a passing result at Establish compute failure-domain evidence identifies the current cause.** — The command-level assertion is anchored in the fact that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. That evidence means a passing result at Establish compute failure-domain evidence gives no causal evidence for the failure at Verify backup coverage and policy intent.
- ✓ **D. Investigate vault existence alone does not prove that every required disk is protected or retained correctly; for this decision, isolate that cause before changing Zone-spread virtual machine scale sets with Azure Backup and isolated restore.** — The failure model establishes that vault existence alone does not prove that every required disk is protected or retained correctly. That evidence means it is the checkpoint's causal failure model and should be isolated before retrying Verify backup coverage and policy intent.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q15 — answer B

The architecture board reconsiders 'Verify backup coverage and policy intent' with clinical operations owner. The run encountered this modeled failure: Vault existence alone does not prove that every required disk is protected or retained correctly. Which recommendation supplies the safest recovery action?

- ✗ **A. Perform cleanup immediately: Remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup; independently, use as justification the claim that the failed operation and its returned identifiers do not need reconciliation.** — The controlling fact is that remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup. The resulting architectural conclusion is that cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✓ **B. Correct the policy-to-workload mapping in the design and repeat the coverage reconciliation. Then, preserve the current run identity and evidence.** — correct the policy-to-workload mapping in the design and repeat the coverage reconciliation. The resulting architectural conclusion is that it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **C. Create a different run identity before diagnosing 'Vault existence alone does not prove that every required disk is protected or retained correctly'. Then, accept without proof that the first state record and returned identifiers can be discarded.** — The authored acceptance boundary states that vault existence alone does not prove that every required disk is protected or retained correctly. The resulting architectural conclusion is that discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **D. Change Establish compute failure-domain evidence instead; afterward, rely on the claim that success at Establish compute failure-domain evidence will repair the failed state at Verify backup coverage and policy intent.** — The relevant observation is that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. The resulting architectural conclusion is that altering an already separate checkpoint does not repair the modeled failure at Verify backup coverage and policy intent.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q16 — answer D

A review of 'Verify backup coverage and policy intent' begins with input from infrastructure engineering lead. Without making a new change, the team must inspect the risk 'Vault existence alone does not prove that every required disk is protected or retained correctly' using the Azure PowerShell lane. Choose the read-only, lane-correct inspection.

- ✗ **A. Rerun the Verify backup coverage and policy intent implementation command and infer the expected state. Also, use the premise that absence of a shell error proves every property.** — The scenario makes clear that the implementation command. The resulting architectural conclusion is that it can mutate state and shell success does not independently assert the expected properties.
- ✗ **B. Run only this negative inspection for Verify backup coverage and policy intent: A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup; in a separate step, consider it sufficient that an empty negative result reports every required positive property.** — The architecture evidence must show that the negative inspection. The resulting architectural conclusion is that absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **C. Run the positive inspection for Evaluate zone-resilient capacity and apply it to Verify backup coverage and policy intent. As another control, take it as conclusive that any command from the same lane proves the current checkpoint.** — The applicable design condition is that the positive inspection for Evaluate zone-resilient capacity. The resulting architectural conclusion is that it is lane-correct but proves Evaluate zone-resilient capacity, not Verify backup coverage and policy intent.
- ✓ **D. Inspect the documented properties for Verify backup coverage and policy intent. Next, retain this evidence: preserve vault configuration, policy exports, protected-item coverage, and approved exceptions.** — The checkpoint specifically records that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. The resulting architectural conclusion is that the read-only inspection directly tests the properties required at Verify backup coverage and policy intent.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q17 — answer B

'Verify backup coverage and policy intent' awaits approval from security incident commander. A passing positive check does not by itself prove this negative assertion: A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. Which answer describes the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Verify backup coverage and policy intent and report full compliance; before approval, rely on the belief that every prohibited parallel state must therefore be absent.** — The retained result must be reconciled with the fact that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. The resulting architectural conclusion is that the positive result alone does not test the explicit anti-condition 'A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup'.
- ✓ **B. Verify the positive properties for Verify backup coverage and policy intent. As another control, independently verify that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup.** — The review is governed by this fact: the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements; A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. The resulting architectural conclusion is that two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **C. Prove only that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup and report the intended configuration as present. Separately, proceed on the belief that absence is equivalent to positive-state evidence.** — The decision tension comes from the fact that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. The resulting architectural conclusion is that absence evidence cannot demonstrate the required positive state 'The coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements'.
- ✗ **D. Use Design isolated restore and recovery validation's negative assertion for Verify backup coverage and policy intent; for this decision, treat as decisive the assertion that negative assertions are interchangeable between checkpoints.** — The safe operating boundary says that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. The resulting architectural conclusion is that the second assertion is valid for Design isolated restore and recovery validation but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q18 — answer D

'Verify backup coverage and policy intent' is reopened at the request of backup service owner. The board wants the Well-Architected consequence of mitigating this risk: Vault existence alone does not prove that every required disk is protected or retained correctly. What should be recorded as the consequence attributable to this checkpoint?

- ✗ **A. Use the Exercise failover ordering and rollback consequence as the result for Verify backup coverage and policy intent; afterward, treat it as established that a pillar statement remains valid when moved away from Exercise failover ordering and rollback.** — The failure model establishes that cost Optimization: the safe analogue validates orchestration without maintaining a full secondary compute estate. The resulting architectural conclusion is that that tradeoff belongs to Exercise failover ordering and rollback and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Verify backup coverage and policy intent outcome; then use as justification the claim that a low cost classification outweighs the mandatory architecture state.** — The recovery guidance assumes that the required outcome at Verify backup coverage and policy intent. The resulting architectural conclusion is that cost Optimization cannot remove the acceptance condition 'The coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements'.
- ✗ **C. Treat 'Security: immutable, access-controlled recovery points improve resistance to destructive attacks' as proof that all five pillars pass. Independently, accept without proof that the checkpoint 'Verify backup coverage and policy intent' no longer needs its separate negative check.** — The WAF consequence identifies that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. The resulting architectural conclusion is that one positive command cannot establish every pillar, especially while the negative state remains unchecked.
- ✓ **D. Record this consequence: Security: immutable, access-controlled recovery points improve resistance to destructive attacks. In addition, tie it to LAB15-REQ-02.** — The traceable checkpoint outcome is that security: immutable, access-controlled recovery points improve resistance to destructive attacks. The resulting architectural conclusion is that it states the authored pillar consequence of the control evaluated at Verify backup coverage and policy intent.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q19 — answer D

A design review of 'Verify backup coverage and policy intent' includes finance partner. A material change now applies: Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect. Which proposal supplies the correct revision to the decision record?

- ✗ **A. Retain Zone-spread virtual machine scale sets with Azure Backup and isolated restore at Verify backup coverage and policy intent without recalculating criteria or eligibility. As another control, base approval on the claim that the original weighted result is permanent.** — zone-spread virtual machine scale sets with Azure Backup and isolated restore. Under the stated constraint, the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Availability sets with locally redundant vault protection for Verify backup coverage and policy intent without rechecking its mandatory constraints; as a separate check, use the premise that being different from the current design is an architecture criterion.** — The controlling fact is that availability sets with locally redundant vault protection. Under the stated constraint, being different is not a criterion, and the candidate still must avoid the prohibited state at Verify backup coverage and policy intent.
- ✗ **C. Keep Azure Site Recovery warm replicas in a secondary region for every virtual machine eligible at Verify backup coverage and policy intent by downgrading LAB15-REQ-02. Afterward, consider it sufficient that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The authored acceptance boundary states that lAB15-REQ-02. Under the stated constraint, an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.
- ✓ **D. Re-score Zone-spread virtual machine scale sets with Azure Backup and isolated restore and both alternatives for Verify backup coverage and policy intent. Before sign-off, supersede the ADR using the changed evidence for LAB15-REQ-02.** — The command-level assertion is anchored in the fact that zone-spread virtual machine scale sets with Azure Backup and isolated restore at Verify backup coverage and policy intent. The resulting architectural conclusion is that the material change 'Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect.' requires fresh eligibility, weighted analysis, and a superseding decision.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q20 — answer B

The team asks clinical operations owner to assess 'Verify backup coverage and policy intent'. After a partial run, cleanup must follow this dependency: Remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup. Which option best represents the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Evaluate zone-resilient capacity before reconciling the current dependency; for this decision, rely on the claim that removing a parent needed to identify Verify backup coverage and policy intent is harmless.** — The checkpoint specifically records that delete local calculation output; this checkpoint requests no quota and provisions no compute. Under the stated constraint, a cleanup rule for Evaluate zone-resilient capacity cannot override the dependency declared for Verify backup coverage and policy intent.
- ✓ **B. Verify exact run-state IDs and ownership tags for Verify backup coverage and policy intent; afterward, follow this dependency rule without purge: Remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup.** — The relevant observation is that remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup. Under the stated constraint, exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **C. Delete candidates by display name before comparing the Verify backup coverage and policy intent ownership tags. Before sign-off, rely on the belief that the dependency rule 'Remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup' is optional.** — The scenario makes clear that preserve vault configuration, policy exports, protected-item coverage, and approved exceptions. Under the stated constraint, names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **D. Destroy recoverable copies before retaining the Verify backup coverage and policy intent negative assertion 'A VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup'; as an independent condition, proceed on the belief that remaining command logs are sufficient recovery evidence.** — The architecture evidence must show that a VM shown in inventory but absent from the protected-item register must fail even if another VM has a successful backup. Under the stated constraint, irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-2](../README.md#checkpoint-2)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q21 — answer A

A recommendation on 'Evaluate zone-resilient capacity' is requested by security incident commander. Approval requires a positive result plus this independent negative assertion: A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. Which response provides the acceptance rule that makes LAB15-REQ-03 testable?

- ✓ **A. Require the documented positive state for Evaluate zone-resilient capacity. Also, verify that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail.** — The applicable design condition is that the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity. Under the stated constraint, the positive state and an independent negative assertion jointly make LAB15-REQ-03 testable.
- ✗ **B. Select Availability sets with locally redundant vault protection before checking Evaluate zone-resilient capacity; next, accept without proof that a successful deployment will later prove the architecture constraint.** — The review is governed by this fact: a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. Under the stated constraint, a deployment result cannot prove LAB15-REQ-03, and Availability sets with locally redundant vault protection still has to meet the mandatory boundary.
- ✗ **C. Use the passing result from Establish compute failure-domain evidence to approve Evaluate zone-resilient capacity. In addition, rely on the claim that one control establishes an unrelated acceptance boundary.** — The retained result must be reconciled with the fact that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. Under the stated constraint, that outcome belongs to Establish compute failure-domain evidence and leaves Evaluate zone-resilient capacity unverified.
- ✗ **D. Choose Azure Site Recovery warm replicas in a secondary region for every virtual machine and skip the Evaluate zone-resilient capacity negative assertion; before approval, rely on the belief that the candidate has the lowest implementation effort.** — The decision tension comes from the fact that maintain appointment coordination through a datacenter failure and recover compromised virtual machines from trusted points without unsafe reconnection. Under the stated constraint, implementation effort cannot justify skipping the negative assertion or displace LAB15-REQ-03.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q22 — answer A

'Evaluate zone-resilient capacity' is assigned to backup service owner. The selected architecture is Zone-spread virtual machine scale sets with Azure Backup and isolated restore; object existence alone is not success. Which choice gives the team the intended successful finding?

- ✓ **A. Record the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity. Afterward, classify it as success for LAB15-REQ-03.** — The safe operating boundary says that the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity. Under the stated constraint, this is the authored target state for Evaluate zone-resilient capacity and directly supports LAB15-REQ-03.
- ✗ **B. Use only the negative assertion 'A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail' as the success result; before approval, consider it sufficient that absence proves every required positive property.** — The traceable checkpoint outcome is that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. Under the stated constraint, this is the independent prohibited-state assertion, not a successful finding.
- ✗ **C. Use the successful finding from Verify backup coverage and policy intent as the result for Evaluate zone-resilient capacity. Then, take it as conclusive that a property from the current checkpoint does not need to be inspected.** — The failure model establishes that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. Under the stated constraint, evidence for Verify backup coverage and policy intent cannot substitute for the properties required at Evaluate zone-resilient capacity.
- ✗ **D. Record the failure condition 'Region-specific SKU restrictions can make a syntactically valid availability design undeployable' as a successful state; afterward, treat it as established that the command returned an object.** — The recovery guidance assumes that region-specific SKU restrictions can make a syntactically valid availability design undeployable. Under the stated constraint, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q23 — answer A

An assurance review of 'Evaluate zone-resilient capacity' includes finance partner. Evidence must address this risk without retaining credentials: Region-specific SKU restrictions can make a syntactically valid availability design undeployable. What is sufficient, properly scoped evidence?

- ✓ **A. Retain save the SKU capability result, capacity calculation, and quota-request lead-time assumption. Separately, exclude credentials and unrelated response fields.** — The WAF consequence identifies that save the SKU capability result, capacity calculation, and quota-request lead-time assumption. Under the stated constraint, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **B. Substitute the evidence from Design isolated restore and recovery validation for Evaluate zone-resilient capacity. Also, proceed on the belief that a related checkpoint proves the current expected state.** — The command-level assertion is anchored in the fact that record the selected recovery point, job timing, isolation controls, assertion results, and approval outcome. Under the stated constraint, that evidence supports Design isolated restore and recovery validation, so it cannot demonstrate The target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity.
- ✗ **C. Store unredacted Evaluate zone-resilient capacity output with operator, tenant, token, and request context; in a separate step, treat as decisive the assertion that reproduction requires every captured field.** — unredacted implementation output. This matters because identity, tenant, or token material exceeds the non-secret evidence contract.
- ✗ **D. Record only the Evaluate zone-resilient capacity positive inspection's exit status. As another control, base approval on the claim that projected properties and assertion results can be reconstructed later.** — The controlling fact is that the positive inspection's exit status. This matters because an exit code alone does not show whether the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q24 — answer D

Approval of 'Evaluate zone-resilient capacity' is questioned by clinical operations owner. The target is The target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity, but the latest evidence does not show it. Which recommendation supplies the most likely cause?

- ✗ **A. Treat 'Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration' as grounds to reject Evaluate zone-resilient capacity; before approval, use as justification the claim that exercise failover ordering and rollback's failure model applies unchanged here.** — The relevant observation is that parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. This matters because that condition belongs to Exercise failover ordering and rollback and does not by itself invalidate Zone-spread virtual machine scale sets with Azure Backup and isolated restore.
- ✗ **B. Ignore the negative assertion 'A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail'. Separately, accept without proof that a later material change will make it unnecessary.** — The checkpoint specifically records that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. This matters because the negative assertion must be evaluated now, independent of a later business change.
- ✗ **C. Investigate Establish compute failure-domain evidence instead of diagnosing Evaluate zone-resilient capacity; for this decision, rely on the claim that a passing result at Establish compute failure-domain evidence identifies the current cause.** — The scenario makes clear that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. This matters because a passing result at Establish compute failure-domain evidence gives no causal evidence for the failure at Evaluate zone-resilient capacity.
- ✓ **D. Investigate region-specific SKU restrictions can make a syntactically valid availability design undeployable; as another gate, isolate that cause before changing Zone-spread virtual machine scale sets with Azure Backup and isolated restore.** — The authored acceptance boundary states that region-specific SKU restrictions can make a syntactically valid availability design undeployable. This matters because it is the checkpoint's causal failure model and should be isolated before retrying Evaluate zone-resilient capacity.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q25 — answer C

The implementation review has reached 'Evaluate zone-resilient capacity'. The run encountered this modeled failure: Region-specific SKU restrictions can make a syntactically valid availability design undeployable. Choose the safest recovery action.

- ✗ **A. Perform cleanup immediately: Delete local calculation output; this checkpoint requests no quota and provisions no compute; afterward, use the premise that the failed operation and its returned identifiers do not need reconciliation.** — The applicable design condition is that delete local calculation output; this checkpoint requests no quota and provisions no compute. This matters because cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **B. Create a different run identity before diagnosing 'Region-specific SKU restrictions can make a syntactically valid availability design undeployable'; then consider it sufficient that the first state record and returned identifiers can be discarded.** — The review is governed by this fact: region-specific SKU restrictions can make a syntactically valid availability design undeployable. This matters because discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✓ **C. Evaluate the approved fallback SKU and recalculate cost and performance before reselection. Independently, preserve the current run identity and evidence.** — The architecture evidence must show that evaluate the approved fallback SKU and recalculate cost and performance before reselection. This matters because it corrects the narrow cause while retaining the same recovery trail and decision scope.
- ✗ **D. Change Establish compute failure-domain evidence instead. Independently, take it as conclusive that success at Establish compute failure-domain evidence will repair the failed state at Evaluate zone-resilient capacity.** — The retained result must be reconciled with the fact that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. This matters because altering an already separate checkpoint does not repair the modeled failure at Evaluate zone-resilient capacity.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q26 — answer B

The approach to 'Evaluate zone-resilient capacity' is challenged by security incident commander. Without making a new change, the team must inspect the risk 'Region-specific SKU restrictions can make a syntactically valid availability design undeployable' using the Azure PowerShell lane. Which answer describes the read-only, lane-correct inspection?

- ✗ **A. Rerun the Evaluate zone-resilient capacity implementation command and infer the expected state. As another control, rely on the belief that absence of a shell error proves every property.** — The safe operating boundary says that the implementation command. This matters because it can mutate state and shell success does not independently assert the expected properties.
- ✓ **B. Inspect the documented properties for Evaluate zone-resilient capacity; in a separate step, retain this evidence: save the SKU capability result, capacity calculation, and quota-request lead-time assumption.** — The decision tension comes from the fact that the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity. This matters because the read-only inspection directly tests the properties required at Evaluate zone-resilient capacity.
- ✗ **C. Run only this negative inspection for Evaluate zone-resilient capacity: A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail; as a separate check, proceed on the belief that an empty negative result reports every required positive property.** — The traceable checkpoint outcome is that the negative inspection. This matters because absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Verify backup coverage and policy intent and apply it to Evaluate zone-resilient capacity. Afterward, treat as decisive the assertion that any command from the same lane proves the current checkpoint.** — The failure model establishes that the positive inspection for Verify backup coverage and policy intent. This matters because it is lane-correct but proves Verify backup coverage and policy intent, not Evaluate zone-resilient capacity.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q27 — answer C

A decision test for 'Evaluate zone-resilient capacity' includes backup service owner. A passing positive check does not by itself prove this negative assertion: A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. What should be recorded as the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Evaluate zone-resilient capacity and report full compliance; for this decision, treat it as established that every prohibited parallel state must therefore be absent.** — The WAF consequence identifies that the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity. This matters because the positive result alone does not test the explicit anti-condition 'A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail'.
- ✗ **B. Prove only that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail and report the intended configuration as present. Before sign-off, use as justification the claim that absence is equivalent to positive-state evidence.** — The command-level assertion is anchored in the fact that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. This matters because absence evidence cannot demonstrate the required positive state 'The target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity'.
- ✓ **C. Verify the positive properties for Evaluate zone-resilient capacity; next, independently verify that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail.** — The recovery guidance assumes that the target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity; A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. This matters because two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **D. Use Design isolated restore and recovery validation's negative assertion for Evaluate zone-resilient capacity; as an independent condition, accept without proof that negative assertions are interchangeable between checkpoints.** — restoring directly onto the original VM or reconnecting before security and application acceptance must fail. The checkpoint therefore requires that the second assertion is valid for Design isolated restore and recovery validation but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q28 — answer B

The architecture board reconsiders 'Evaluate zone-resilient capacity' with finance partner. The board wants the Well-Architected consequence of mitigating this risk: Region-specific SKU restrictions can make a syntactically valid availability design undeployable. Which proposal supplies the consequence attributable to this checkpoint?

- ✗ **A. Use the Exercise failover ordering and rollback consequence as the result for Evaluate zone-resilient capacity. Independently, base approval on the claim that a pillar statement remains valid when moved away from Exercise failover ordering and rollback.** — The authored acceptance boundary states that cost Optimization: the safe analogue validates orchestration without maintaining a full secondary compute estate. The checkpoint therefore requires that that tradeoff belongs to Exercise failover ordering and rollback and does not explain this checkpoint's decision.
- ✓ **B. Record this consequence: Performance Efficiency: right-sized degraded-mode capacity protects clinical latency without permanent overprovisioning; for this decision, tie it to LAB15-REQ-03.** — The controlling fact is that performance Efficiency: right-sized degraded-mode capacity protects clinical latency without permanent overprovisioning. The checkpoint therefore requires that it states the authored pillar consequence of the control evaluated at Evaluate zone-resilient capacity.
- ✗ **C. Remove the control responsible for the Evaluate zone-resilient capacity outcome. Next, use the premise that a low cost classification outweighs the mandatory architecture state.** — The relevant observation is that the required outcome at Evaluate zone-resilient capacity. The checkpoint therefore requires that cost Optimization cannot remove the acceptance condition 'The target design documents supported zones, surge capacity, quota, health probes, disk constraints, and degraded-mode capacity'.
- ✗ **D. Treat 'Performance Efficiency: right-sized degraded-mode capacity protects clinical latency without permanent overprovisioning' as proof that all five pillars pass, and then consider it sufficient that the checkpoint 'Evaluate zone-resilient capacity' no longer needs its separate negative check.** — The checkpoint specifically records that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. The checkpoint therefore requires that one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q29 — answer C

A review of 'Evaluate zone-resilient capacity' begins with input from clinical operations owner. A material change now applies: Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect. Which option best represents the correct revision to the decision record?

- ✗ **A. Retain Zone-spread virtual machine scale sets with Azure Backup and isolated restore at Evaluate zone-resilient capacity without recalculating criteria or eligibility. Afterward, rely on the claim that the original weighted result is permanent.** — The architecture evidence must show that zone-spread virtual machine scale sets with Azure Backup and isolated restore. The checkpoint therefore requires that the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Availability sets with locally redundant vault protection for Evaluate zone-resilient capacity without rechecking its mandatory constraints; next, rely on the belief that being different from the current design is an architecture criterion.** — The applicable design condition is that availability sets with locally redundant vault protection. The checkpoint therefore requires that being different is not a criterion, and the candidate still must avoid the prohibited state at Evaluate zone-resilient capacity.
- ✓ **C. Re-score Zone-spread virtual machine scale sets with Azure Backup and isolated restore and both alternatives for Evaluate zone-resilient capacity. Then, supersede the ADR using the changed evidence for LAB15-REQ-03.** — The scenario makes clear that zone-spread virtual machine scale sets with Azure Backup and isolated restore at Evaluate zone-resilient capacity. The checkpoint therefore requires that the material change 'Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **D. Keep Azure Site Recovery warm replicas in a secondary region for every virtual machine eligible at Evaluate zone-resilient capacity by downgrading LAB15-REQ-03. In addition, proceed on the belief that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The review is governed by this fact: lAB15-REQ-03. The checkpoint therefore requires that an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q30 — answer C

'Evaluate zone-resilient capacity' awaits approval from infrastructure engineering lead. After a partial run, cleanup must follow this dependency: Delete local calculation output; this checkpoint requests no quota and provisions no compute. Which course of action provides the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Verify backup coverage and policy intent before reconciling the current dependency; as an independent condition, take it as conclusive that removing a parent needed to identify Evaluate zone-resilient capacity is harmless.** — The decision tension comes from the fact that remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup. The checkpoint therefore requires that a cleanup rule for Verify backup coverage and policy intent cannot override the dependency declared for Evaluate zone-resilient capacity.
- ✗ **B. Delete candidates by display name before comparing the Evaluate zone-resilient capacity ownership tags; for the recorded decision, treat it as established that the dependency rule 'Delete local calculation output; this checkpoint requests no quota and provisions no compute' is optional.** — The safe operating boundary says that save the SKU capability result, capacity calculation, and quota-request lead-time assumption. The checkpoint therefore requires that names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✓ **C. Verify exact run-state IDs and ownership tags for Evaluate zone-resilient capacity. Next, follow this dependency rule without purge: Delete local calculation output; this checkpoint requests no quota and provisions no compute.** — The retained result must be reconciled with the fact that delete local calculation output; this checkpoint requests no quota and provisions no compute. The checkpoint therefore requires that exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.
- ✗ **D. Destroy recoverable copies before retaining the Evaluate zone-resilient capacity negative assertion 'A design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail'. Then, use as justification the claim that remaining command logs are sufficient recovery evidence.** — The traceable checkpoint outcome is that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. The checkpoint therefore requires that irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.

Remediation: [checkpoint-3](../README.md#checkpoint-3)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q31 — answer B

'Design isolated restore and recovery validation' is reopened at the request of backup service owner. Approval requires a positive result plus this independent negative assertion: Restoring directly onto the original VM or reconnecting before security and application acceptance must fail. Which choice gives the team the acceptance rule that makes LAB15-REQ-04 testable?

- ✗ **A. Select Availability sets with locally redundant vault protection before checking Design isolated restore and recovery validation; before approval, consider it sufficient that a successful deployment will later prove the architecture constraint.** — The recovery guidance assumes that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. The checkpoint therefore requires that a deployment result cannot prove LAB15-REQ-04, and Availability sets with locally redundant vault protection still has to meet the mandatory boundary.
- ✓ **B. Require the documented positive state for Design isolated restore and recovery validation; as a separate check, verify that restoring directly onto the original VM or reconnecting before security and application acceptance must fail.** — The failure model establishes that the restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval. The checkpoint therefore requires that the positive state and an independent negative assertion jointly make LAB15-REQ-04 testable.
- ✗ **C. Use the passing result from Establish compute failure-domain evidence to approve Design isolated restore and recovery validation. Separately, take it as conclusive that one control establishes an unrelated acceptance boundary.** — The WAF consequence identifies that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. The checkpoint therefore requires that that outcome belongs to Establish compute failure-domain evidence and leaves Design isolated restore and recovery validation unverified.
- ✗ **D. Choose Azure Site Recovery warm replicas in a secondary region for every virtual machine and skip the Design isolated restore and recovery validation negative assertion; for this decision, treat it as established that the candidate has the lowest implementation effort.** — The command-level assertion is anchored in the fact that maintain appointment coordination through a datacenter failure and recover compromised virtual machines from trusted points without unsafe reconnection. The checkpoint therefore requires that implementation effort cannot justify skipping the negative assertion or displace LAB15-REQ-04.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q32 — answer C

A design review of 'Design isolated restore and recovery validation' includes finance partner. The selected architecture is Zone-spread virtual machine scale sets with Azure Backup and isolated restore; object existence alone is not success. What is the intended successful finding?

- ✗ **A. Use only the negative assertion 'Restoring directly onto the original VM or reconnecting before security and application acceptance must fail' as the success result; afterward, proceed on the belief that absence proves every required positive property.** — The controlling fact is that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. In the decision record, this is the independent prohibited-state assertion, not a successful finding.
- ✗ **B. Use the successful finding from Verify backup coverage and policy intent as the result for Design isolated restore and recovery validation; then treat as decisive the assertion that a property from the current checkpoint does not need to be inspected.** — The authored acceptance boundary states that the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. In the decision record, evidence for Verify backup coverage and policy intent cannot substitute for the properties required at Design isolated restore and recovery validation.
- ✓ **C. Record the restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval; before approval, classify it as success for LAB15-REQ-04.** — the restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval. In the decision record, this is the authored target state for Design isolated restore and recovery validation and directly supports LAB15-REQ-04.
- ✗ **D. Record the failure condition 'A technically completed restore can reintroduce compromise or inconsistent application state' as a successful state. Independently, base approval on the claim that the command returned an object.** — The relevant observation is that a technically completed restore can reintroduce compromise or inconsistent application state. In the decision record, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q33 — answer C

The team asks clinical operations owner to assess 'Design isolated restore and recovery validation'. Evidence must address this risk without retaining credentials: A technically completed restore can reintroduce compromise or inconsistent application state. Which recommendation supplies sufficient, properly scoped evidence?

- ✗ **A. Substitute the evidence from Evaluate zone-resilient capacity for Design isolated restore and recovery validation. As another control, use as justification the claim that a related checkpoint proves the current expected state.** — The scenario makes clear that save the SKU capability result, capacity calculation, and quota-request lead-time assumption. In the decision record, that evidence supports Evaluate zone-resilient capacity, so it cannot demonstrate The restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval.
- ✗ **B. Store unredacted Design isolated restore and recovery validation output with operator, tenant, token, and request context; as a separate check, accept without proof that reproduction requires every captured field.** — The architecture evidence must show that unredacted implementation output. In the decision record, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✓ **C. Retain record the selected recovery point, job timing, isolation controls, assertion results, and approval outcome; as an independent condition, exclude credentials and unrelated response fields.** — The checkpoint specifically records that record the selected recovery point, job timing, isolation controls, assertion results, and approval outcome. In the decision record, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **D. Record only the Design isolated restore and recovery validation positive inspection's exit status. Afterward, rely on the claim that projected properties and assertion results can be reconstructed later.** — The applicable design condition is that the positive inspection's exit status. In the decision record, an exit code alone does not show whether the restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q34 — answer A

A recommendation on 'Design isolated restore and recovery validation' is requested by infrastructure engineering lead. The target is The restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval, but the latest evidence does not show it. Choose the most likely cause.

- ✓ **A. Investigate a technically completed restore can reintroduce compromise or inconsistent application state; then isolate that cause before changing Zone-spread virtual machine scale sets with Azure Backup and isolated restore.** — The review is governed by this fact: a technically completed restore can reintroduce compromise or inconsistent application state. In the decision record, it is the checkpoint's causal failure model and should be isolated before retrying Design isolated restore and recovery validation.
- ✗ **B. Treat 'Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration' as grounds to reject Design isolated restore and recovery validation; for this decision, use the premise that exercise failover ordering and rollback's failure model applies unchanged here.** — The retained result must be reconciled with the fact that parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. In the decision record, that condition belongs to Exercise failover ordering and rollback and does not by itself invalidate Zone-spread virtual machine scale sets with Azure Backup and isolated restore.
- ✗ **C. Ignore the negative assertion 'Restoring directly onto the original VM or reconnecting before security and application acceptance must fail'. Before sign-off, consider it sufficient that a later material change will make it unnecessary.** — The decision tension comes from the fact that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. In the decision record, the negative assertion must be evaluated now, independent of a later business change.
- ✗ **D. Investigate Establish compute failure-domain evidence instead of diagnosing Design isolated restore and recovery validation; as an independent condition, take it as conclusive that a passing result at Establish compute failure-domain evidence identifies the current cause.** — The safe operating boundary says that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. In the decision record, a passing result at Establish compute failure-domain evidence gives no causal evidence for the failure at Design isolated restore and recovery validation.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q35 — answer D

'Design isolated restore and recovery validation' is assigned to security incident commander. The run encountered this modeled failure: A technically completed restore can reintroduce compromise or inconsistent application state. Which answer describes the safest recovery action?

- ✗ **A. Perform cleanup immediately: Remove only disposable restored resources after exact ownership checks; retain protected recovery points. Independently, rely on the belief that the failed operation and its returned identifiers do not need reconciliation.** — The failure model establishes that remove only disposable restored resources after exact ownership checks; retain protected recovery points. In the decision record, cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **B. Create a different run identity before diagnosing 'A technically completed restore can reintroduce compromise or inconsistent application state'. Next, proceed on the belief that the first state record and returned identifiers can be discarded.** — The recovery guidance assumes that a technically completed restore can reintroduce compromise or inconsistent application state. In the decision record, discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **C. Change Establish compute failure-domain evidence instead, and then treat as decisive the assertion that success at Establish compute failure-domain evidence will repair the failed state at Design isolated restore and recovery validation.** — The WAF consequence identifies that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. In the decision record, altering an already separate checkpoint does not repair the modeled failure at Design isolated restore and recovery validation.
- ✓ **D. Select the next trusted recovery point, preserve the failed evidence, and repeat validation in isolation. Also, preserve the current run identity and evidence.** — The traceable checkpoint outcome is that select the next trusted recovery point, preserve the failed evidence, and repeat validation in isolation. In the decision record, it corrects the narrow cause while retaining the same recovery trail and decision scope.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q36 — answer B

An assurance review of 'Design isolated restore and recovery validation' includes backup service owner. Without making a new change, the team must inspect the risk 'A technically completed restore can reintroduce compromise or inconsistent application state' using the Azure PowerShell lane. What should be recorded as the read-only, lane-correct inspection?

- ✗ **A. Rerun the Design isolated restore and recovery validation implementation command and infer the expected state. Afterward, treat it as established that absence of a shell error proves every property.** — the implementation command. The independent assertion shows why it can mutate state and shell success does not independently assert the expected properties.
- ✓ **B. Inspect the documented properties for Design isolated restore and recovery validation. Afterward, retain this evidence: record the selected recovery point, job timing, isolation controls, assertion results, and approval outcome.** — The command-level assertion is anchored in the fact that the restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval. In the decision record, the read-only inspection directly tests the properties required at Design isolated restore and recovery validation.
- ✗ **C. Run only this negative inspection for Design isolated restore and recovery validation: Restoring directly onto the original VM or reconnecting before security and application acceptance must fail; next, use as justification the claim that an empty negative result reports every required positive property.** — The controlling fact is that the negative inspection. The independent assertion shows why absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Verify backup coverage and policy intent and apply it to Design isolated restore and recovery validation. In addition, accept without proof that any command from the same lane proves the current checkpoint.** — The authored acceptance boundary states that the positive inspection for Verify backup coverage and policy intent. The independent assertion shows why it is lane-correct but proves Verify backup coverage and policy intent, not Design isolated restore and recovery validation.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q37 — answer B

Approval of 'Design isolated restore and recovery validation' is questioned by finance partner. A passing positive check does not by itself prove this negative assertion: Restoring directly onto the original VM or reconnecting before security and application acceptance must fail. Which proposal supplies the assertion pair that proves both conditions independently?

- ✗ **A. Verify only the positive result for Design isolated restore and recovery validation and report full compliance; as an independent condition, base approval on the claim that every prohibited parallel state must therefore be absent.** — The checkpoint specifically records that the restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval. The independent assertion shows why the positive result alone does not test the explicit anti-condition 'Restoring directly onto the original VM or reconnecting before security and application acceptance must fail'.
- ✓ **B. Verify the positive properties for Design isolated restore and recovery validation. Separately, independently verify that restoring directly onto the original VM or reconnecting before security and application acceptance must fail.** — The relevant observation is that the restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval; Restoring directly onto the original VM or reconnecting before security and application acceptance must fail. The independent assertion shows why two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **C. Prove only that restoring directly onto the original VM or reconnecting before security and application acceptance must fail and report the intended configuration as present; as another gate, use the premise that absence is equivalent to positive-state evidence.** — The scenario makes clear that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. The independent assertion shows why absence evidence cannot demonstrate the required positive state 'The restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval'.
- ✗ **D. Use Evaluate zone-resilient capacity's negative assertion for Design isolated restore and recovery validation. Then, consider it sufficient that negative assertions are interchangeable between checkpoints.** — The architecture evidence must show that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. The independent assertion shows why the second assertion is valid for Evaluate zone-resilient capacity but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q38 — answer A

The implementation review has reached 'Design isolated restore and recovery validation'. The board wants the Well-Architected consequence of mitigating this risk: A technically completed restore can reintroduce compromise or inconsistent application state. Which option best represents the consequence attributable to this checkpoint?

- ✓ **A. Record this consequence: Operational Excellence: a rehearsed isolated-restore workflow produces repeatable and auditable recovery; for the final assessment, tie it to LAB15-REQ-04.** — The applicable design condition is that operational Excellence: a rehearsed isolated-restore workflow produces repeatable and auditable recovery. The independent assertion shows why it states the authored pillar consequence of the control evaluated at Design isolated restore and recovery validation.
- ✗ **B. Use the Exercise failover ordering and rollback consequence as the result for Design isolated restore and recovery validation, and then rely on the claim that a pillar statement remains valid when moved away from Exercise failover ordering and rollback.** — The review is governed by this fact: cost Optimization: the safe analogue validates orchestration without maintaining a full secondary compute estate. The independent assertion shows why that tradeoff belongs to Exercise failover ordering and rollback and does not explain this checkpoint's decision.
- ✗ **C. Remove the control responsible for the Design isolated restore and recovery validation outcome. Also, rely on the belief that a low cost classification outweighs the mandatory architecture state.** — The retained result must be reconciled with the fact that the required outcome at Design isolated restore and recovery validation. The independent assertion shows why cost Optimization cannot remove the acceptance condition 'The restore plan uses a quarantined network, malware inspection, identity isolation, application checks, data-consistency checks, and explicit release approval'.
- ✗ **D. Treat 'Operational Excellence: a rehearsed isolated-restore workflow produces repeatable and auditable recovery' as proof that all five pillars pass; in a separate step, proceed on the belief that the checkpoint 'Design isolated restore and recovery validation' no longer needs its separate negative check.** — The decision tension comes from the fact that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. The independent assertion shows why one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q39 — answer B

The approach to 'Design isolated restore and recovery validation' is challenged by infrastructure engineering lead. A material change now applies: Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect. Which course of action provides the correct revision to the decision record?

- ✗ **A. Retain Zone-spread virtual machine scale sets with Azure Backup and isolated restore at Design isolated restore and recovery validation without recalculating criteria or eligibility. In addition, take it as conclusive that the original weighted result is permanent.** — The traceable checkpoint outcome is that zone-spread virtual machine scale sets with Azure Backup and isolated restore. The independent assertion shows why the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✓ **B. Re-score Zone-spread virtual machine scale sets with Azure Backup and isolated restore and both alternatives for Design isolated restore and recovery validation. Independently, supersede the ADR using the changed evidence for LAB15-REQ-04.** — The safe operating boundary says that zone-spread virtual machine scale sets with Azure Backup and isolated restore at Design isolated restore and recovery validation. The independent assertion shows why the material change 'Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **C. Select Availability sets with locally redundant vault protection for Design isolated restore and recovery validation without rechecking its mandatory constraints; before approval, treat it as established that being different from the current design is an architecture criterion.** — The failure model establishes that availability sets with locally redundant vault protection. The independent assertion shows why being different is not a criterion, and the candidate still must avoid the prohibited state at Design isolated restore and recovery validation.
- ✗ **D. Keep Azure Site Recovery warm replicas in a secondary region for every virtual machine eligible at Design isolated restore and recovery validation by downgrading LAB15-REQ-04. Separately, use as justification the claim that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The recovery guidance assumes that lAB15-REQ-04. The independent assertion shows why an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q40 — answer D

A decision test for 'Design isolated restore and recovery validation' includes security incident commander. After a partial run, cleanup must follow this dependency: Remove only disposable restored resources after exact ownership checks; retain protected recovery points. Which finding constitutes the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Verify backup coverage and policy intent before reconciling the current dependency. Then, treat as decisive the assertion that removing a parent needed to identify Design isolated restore and recovery validation is harmless.** — The command-level assertion is anchored in the fact that remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup. The independent assertion shows why a cleanup rule for Verify backup coverage and policy intent cannot override the dependency declared for Design isolated restore and recovery validation.
- ✗ **B. Delete candidates by display name before comparing the Design isolated restore and recovery validation ownership tags; afterward, base approval on the claim that the dependency rule 'Remove only disposable restored resources after exact ownership checks; retain protected recovery points' is optional.** — record the selected recovery point, job timing, isolation controls, assertion results, and approval outcome. Operationally, names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **C. Destroy recoverable copies before retaining the Design isolated restore and recovery validation negative assertion 'Restoring directly onto the original VM or reconnecting before security and application acceptance must fail'; then use the premise that remaining command logs are sufficient recovery evidence.** — The controlling fact is that restoring directly onto the original VM or reconnecting before security and application acceptance must fail. Operationally, irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.
- ✓ **D. Verify exact run-state IDs and ownership tags for Design isolated restore and recovery validation; in a separate step, follow this dependency rule without purge: Remove only disposable restored resources after exact ownership checks; retain protected recovery points.** — The WAF consequence identifies that remove only disposable restored resources after exact ownership checks; retain protected recovery points. The independent assertion shows why exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.

Remediation: [checkpoint-4](../README.md#checkpoint-4)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q41 — answer A

The architecture board reconsiders 'Exercise failover ordering and rollback' with finance partner. Approval requires a positive result plus this independent negative assertion: Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. What is the acceptance rule that makes LAB15-REQ-05 testable?

- ✓ **A. Require the documented positive state for Exercise failover ordering and rollback. In addition, verify that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result.** — The authored acceptance boundary states that the safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates. Operationally, the positive state and an independent negative assertion jointly make LAB15-REQ-05 testable.
- ✗ **B. Select Availability sets with locally redundant vault protection before checking Exercise failover ordering and rollback; for this decision, proceed on the belief that a successful deployment will later prove the architecture constraint.** — The relevant observation is that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. Operationally, a deployment result cannot prove LAB15-REQ-05, and Availability sets with locally redundant vault protection still has to meet the mandatory boundary.
- ✗ **C. Use the passing result from Establish compute failure-domain evidence to approve Exercise failover ordering and rollback. Before sign-off, treat as decisive the assertion that one control establishes an unrelated acceptance boundary.** — The checkpoint specifically records that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. Operationally, that outcome belongs to Establish compute failure-domain evidence and leaves Exercise failover ordering and rollback unverified.
- ✗ **D. Choose Azure Site Recovery warm replicas in a secondary region for every virtual machine and skip the Exercise failover ordering and rollback negative assertion; as an independent condition, base approval on the claim that the candidate has the lowest implementation effort.** — The scenario makes clear that maintain appointment coordination through a datacenter failure and recover compromised virtual machines from trusted points without unsafe reconnection. Operationally, implementation effort cannot justify skipping the negative assertion or displace LAB15-REQ-05.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q42 — answer B

A review of 'Exercise failover ordering and rollback' begins with input from clinical operations owner. The selected architecture is Zone-spread virtual machine scale sets with Azure Backup and isolated restore; object existence alone is not success. Which recommendation supplies the intended successful finding?

- ✗ **A. Use only the negative assertion 'Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result' as the success result. Independently, use as justification the claim that absence proves every required positive property.** — The applicable design condition is that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. Operationally, this is the independent prohibited-state assertion, not a successful finding.
- ✓ **B. Record the safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates. Before sign-off, classify it as success for LAB15-REQ-05.** — The architecture evidence must show that the safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates. Operationally, this is the authored target state for Exercise failover ordering and rollback and directly supports LAB15-REQ-05.
- ✗ **C. Use the successful finding from Verify backup coverage and policy intent as the result for Exercise failover ordering and rollback. Next, accept without proof that a property from the current checkpoint does not need to be inspected.** — The review is governed by this fact: the coverage matrix maps operating system and data disks to policy frequency, retention, vault redundancy, immutability, and soft-delete requirements. Operationally, evidence for Verify backup coverage and policy intent cannot substitute for the properties required at Exercise failover ordering and rollback.
- ✗ **D. Record the failure condition 'Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration' as a successful state, and then rely on the claim that the command returned an object.** — The retained result must be reconciled with the fact that parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. Operationally, resource existence or command output does not convert the documented failure condition into success.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q43 — answer C

'Exercise failover ordering and rollback' awaits approval from infrastructure engineering lead. Evidence must address this risk without retaining credentials: Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. Choose sufficient, properly scoped evidence.

- ✗ **A. Substitute the evidence from Evaluate zone-resilient capacity for Exercise failover ordering and rollback. Afterward, use the premise that a related checkpoint proves the current expected state.** — The safe operating boundary says that save the SKU capability result, capacity calculation, and quota-request lead-time assumption. Operationally, that evidence supports Evaluate zone-resilient capacity, so it cannot demonstrate The safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates.
- ✗ **B. Store unredacted Exercise failover ordering and rollback output with operator, tenant, token, and request context; next, consider it sufficient that reproduction requires every captured field.** — The traceable checkpoint outcome is that unredacted implementation output. Operationally, identity, tenant, or token material exceeds the non-secret evidence contract.
- ✓ **C. Retain save the simulated timeline, assertion-level outcomes, dependency delays, and remediation owners; afterward, exclude credentials and unrelated response fields.** — The decision tension comes from the fact that save the simulated timeline, assertion-level outcomes, dependency delays, and remediation owners. Operationally, it captures the checkpoint's observable properties while keeping the evidence boundary narrow.
- ✗ **D. Record only the Exercise failover ordering and rollback positive inspection's exit status. In addition, take it as conclusive that projected properties and assertion results can be reconstructed later.** — The failure model establishes that the positive inspection's exit status. Operationally, an exit code alone does not show whether the safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q44 — answer C

'Exercise failover ordering and rollback' is reopened at the request of security incident commander. The target is The safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates, but the latest evidence does not show it. Which answer describes the most likely cause?

- ✗ **A. Treat 'A technically completed restore can reintroduce compromise or inconsistent application state' as grounds to reject Exercise failover ordering and rollback; as an independent condition, rely on the belief that design isolated restore and recovery validation's failure model applies unchanged here.** — The WAF consequence identifies that a technically completed restore can reintroduce compromise or inconsistent application state. Operationally, that condition belongs to Design isolated restore and recovery validation and does not by itself invalidate Zone-spread virtual machine scale sets with Azure Backup and isolated restore.
- ✗ **B. Ignore the negative assertion 'Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result'; without relying on inference, proceed on the belief that a later material change will make it unnecessary.** — The command-level assertion is anchored in the fact that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. Operationally, the negative assertion must be evaluated now, independent of a later business change.
- ✓ **C. Investigate parallel recovery without dependency gates can create misleadingly fast but unusable service restoration, and then isolate that cause before changing Zone-spread virtual machine scale sets with Azure Backup and isolated restore.** — The recovery guidance assumes that parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. Operationally, it is the checkpoint's causal failure model and should be isolated before retrying Exercise failover ordering and rollback.
- ✗ **D. Investigate Establish compute failure-domain evidence instead of diagnosing Exercise failover ordering and rollback. Then, treat as decisive the assertion that a passing result at Establish compute failure-domain evidence identifies the current cause.** — every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. The requirement-to-evidence link establishes that a passing result at Establish compute failure-domain evidence gives no causal evidence for the failure at Exercise failover ordering and rollback.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q45 — answer D

A design review of 'Exercise failover ordering and rollback' includes backup service owner. The run encountered this modeled failure: Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. What should be recorded as the safest recovery action?

- ✗ **A. Perform cleanup immediately: Remove only run-owned simulation artifacts; do not start a test failover or delete recovery plans, and then treat it as established that the failed operation and its returned identifiers do not need reconciliation.** — The authored acceptance boundary states that remove only run-owned simulation artifacts; do not start a test failover or delete recovery plans. The requirement-to-evidence link establishes that cleanup before reconciliation can erase evidence or strand a partially created dependency.
- ✗ **B. Create a different run identity before diagnosing 'Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration'. Also, use as justification the claim that the first state record and returned identifiers can be discarded.** — The relevant observation is that parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. The requirement-to-evidence link establishes that discarding the original run identity breaks ownership reconciliation and can duplicate a partial operation.
- ✗ **C. Change Establish compute failure-domain evidence instead; in a separate step, accept without proof that success at Establish compute failure-domain evidence will repair the failed state at Exercise failover ordering and rollback.** — The checkpoint specifically records that every VM is mapped to an application tier, availability target, zone or fault-domain placement, and recovery order. The requirement-to-evidence link establishes that altering an already separate checkpoint does not repair the modeled failure at Exercise failover ordering and rollback.
- ✓ **D. Correct the recovery group order and repeat the failed wave, not the entire exercise, unless state is uncertain; as a separate check, preserve the current run identity and evidence.** — The controlling fact is that correct the recovery group order and repeat the failed wave, not the entire exercise, unless state is uncertain. The requirement-to-evidence link establishes that it corrects the narrow cause while retaining the same recovery trail and decision scope.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q46 — answer A

The team asks finance partner to assess 'Exercise failover ordering and rollback'. Without making a new change, the team must inspect the risk 'Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration' using the Azure PowerShell lane. Which proposal supplies the read-only, lane-correct inspection?

- ✓ **A. Inspect the documented properties for Exercise failover ordering and rollback; before approval, retain this evidence: save the simulated timeline, assertion-level outcomes, dependency delays, and remediation owners.** — The scenario makes clear that the safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates. The requirement-to-evidence link establishes that the read-only inspection directly tests the properties required at Exercise failover ordering and rollback.
- ✗ **B. Rerun the Exercise failover ordering and rollback implementation command and infer the expected state. In addition, base approval on the claim that absence of a shell error proves every property.** — The architecture evidence must show that the implementation command. The requirement-to-evidence link establishes that it can mutate state and shell success does not independently assert the expected properties.
- ✗ **C. Run only this negative inspection for Exercise failover ordering and rollback: Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result; before approval, use the premise that an empty negative result reports every required positive property.** — The applicable design condition is that the negative inspection. The requirement-to-evidence link establishes that absence of the prohibited condition is necessary but does not establish the positive architecture state.
- ✗ **D. Run the positive inspection for Verify backup coverage and policy intent and apply it to Exercise failover ordering and rollback. Separately, consider it sufficient that any command from the same lane proves the current checkpoint.** — The review is governed by this fact: the positive inspection for Verify backup coverage and policy intent. The requirement-to-evidence link establishes that it is lane-correct but proves Verify backup coverage and policy intent, not Exercise failover ordering and rollback.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q47 — answer A

A recommendation on 'Exercise failover ordering and rollback' is requested by clinical operations owner. A passing positive check does not by itself prove this negative assertion: Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. Which option best represents the assertion pair that proves both conditions independently?

- ✓ **A. Verify the positive properties for Exercise failover ordering and rollback; as an independent condition, independently verify that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result.** — The retained result must be reconciled with the fact that the safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates; Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. The requirement-to-evidence link establishes that two independent observations prevent a passing positive check from concealing an unsafe parallel state.
- ✗ **B. Verify only the positive result for Exercise failover ordering and rollback and report full compliance. Then, rely on the claim that every prohibited parallel state must therefore be absent.** — The decision tension comes from the fact that the safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates. The requirement-to-evidence link establishes that the positive result alone does not test the explicit anti-condition 'Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result'.
- ✗ **C. Prove only that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result and report the intended configuration as present; afterward, rely on the belief that absence is equivalent to positive-state evidence.** — The safe operating boundary says that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. The requirement-to-evidence link establishes that absence evidence cannot demonstrate the required positive state 'The safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates'.
- ✗ **D. Use Evaluate zone-resilient capacity's negative assertion for Exercise failover ordering and rollback; then proceed on the belief that negative assertions are interchangeable between checkpoints.** — The traceable checkpoint outcome is that a design that needs every zone at peak capacity, or assumes quota appears during an incident, must fail. The requirement-to-evidence link establishes that the second assertion is valid for Evaluate zone-resilient capacity but leaves this checkpoint's prohibited state untested.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q48 — answer C

'Exercise failover ordering and rollback' is assigned to infrastructure engineering lead. The board wants the Well-Architected consequence of mitigating this risk: Parallel recovery without dependency gates can create misleadingly fast but unusable service restoration. Which course of action provides the consequence attributable to this checkpoint?

- ✗ **A. Use the Design isolated restore and recovery validation consequence as the result for Exercise failover ordering and rollback; in a separate step, take it as conclusive that a pillar statement remains valid when moved away from Design isolated restore and recovery validation.** — The recovery guidance assumes that operational Excellence: a rehearsed isolated-restore workflow produces repeatable and auditable recovery. The requirement-to-evidence link establishes that that tradeoff belongs to Design isolated restore and recovery validation and does not explain this checkpoint's decision.
- ✗ **B. Remove the control responsible for the Exercise failover ordering and rollback outcome. As another control, treat it as established that a low cost classification outweighs the mandatory architecture state.** — The WAF consequence identifies that the required outcome at Exercise failover ordering and rollback. The requirement-to-evidence link establishes that cost Optimization cannot remove the acceptance condition 'The safe analogue sequences identity, data, application, and ingress tiers, measures the RTO, and defines rollback and failback gates'.
- ✓ **C. Record this consequence: Cost Optimization: the safe analogue validates orchestration without maintaining a full secondary compute estate; then tie it to LAB15-REQ-05.** — The failure model establishes that cost Optimization: the safe analogue validates orchestration without maintaining a full secondary compute estate. The requirement-to-evidence link establishes that it states the authored pillar consequence of the control evaluated at Exercise failover ordering and rollback.
- ✗ **D. Treat 'Cost Optimization: the safe analogue validates orchestration without maintaining a full secondary compute estate' as proof that all five pillars pass; as a separate check, use as justification the claim that the checkpoint 'Exercise failover ordering and rollback' no longer needs its separate negative check.** — The command-level assertion is anchored in the fact that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. The requirement-to-evidence link establishes that one positive command cannot establish every pillar, especially while the negative state remains unchecked.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q49 — answer C

An assurance review of 'Exercise failover ordering and rollback' includes security incident commander. A material change now applies: Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect. Which finding constitutes the correct revision to the decision record?

- ✗ **A. Retain Zone-spread virtual machine scale sets with Azure Backup and isolated restore at Exercise failover ordering and rollback without recalculating criteria or eligibility. Separately, treat as decisive the assertion that the original weighted result is permanent.** — The controlling fact is that zone-spread virtual machine scale sets with Azure Backup and isolated restore. Consequently, the original ADR remains historical evidence, but its score cannot answer a changed mandatory condition.
- ✗ **B. Select Availability sets with locally redundant vault protection for Exercise failover ordering and rollback without rechecking its mandatory constraints; for this decision, base approval on the claim that being different from the current design is an architecture criterion.** — The authored acceptance boundary states that availability sets with locally redundant vault protection. Consequently, being different is not a criterion, and the candidate still must avoid the prohibited state at Exercise failover ordering and rollback.
- ✓ **C. Re-score Zone-spread virtual machine scale sets with Azure Backup and isolated restore and both alternatives for Exercise failover ordering and rollback. Also, supersede the ADR using the changed evidence for LAB15-REQ-05.** — zone-spread virtual machine scale sets with Azure Backup and isolated restore at Exercise failover ordering and rollback. Consequently, the material change 'Security now requires a known-clean restore to be available within two hours even when the latest three recovery points are considered suspect.' requires fresh eligibility, weighted analysis, and a superseding decision.
- ✗ **D. Keep Azure Site Recovery warm replicas in a secondary region for every virtual machine eligible at Exercise failover ordering and rollback by downgrading LAB15-REQ-05. Before sign-off, use the premise that stakeholder approval is unnecessary when that requirement blocks the candidate.** — The relevant observation is that lAB15-REQ-05. Consequently, an architect cannot silently downgrade a stakeholder-owned mandatory requirement to protect a candidate.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)

## LAB15-Q50 — answer D

Approval of 'Exercise failover ordering and rollback' is questioned by backup service owner. After a partial run, cleanup must follow this dependency: Remove only run-owned simulation artifacts; do not start a test failover or delete recovery plans. Which recommendation delivers the dependency-safe cleanup plan?

- ✗ **A. Apply the cleanup rule for Verify backup coverage and policy intent before reconciling the current dependency; then accept without proof that removing a parent needed to identify Exercise failover ordering and rollback is harmless.** — The scenario makes clear that remove sanitized exports only; never disable soft delete, immutability, or protection as cleanup. Consequently, a cleanup rule for Verify backup coverage and policy intent cannot override the dependency declared for Exercise failover ordering and rollback.
- ✗ **B. Delete candidates by display name before comparing the Exercise failover ordering and rollback ownership tags. Independently, rely on the claim that the dependency rule 'Remove only run-owned simulation artifacts; do not start a test failover or delete recovery plans' is optional.** — The architecture evidence must show that save the simulated timeline, assertion-level outcomes, dependency delays, and remediation owners. Consequently, names are not ownership proof; deletion requires the exact recorded identifier and every required tag.
- ✗ **C. Destroy recoverable copies before retaining the Exercise failover ordering and rollback negative assertion 'Passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result'. Next, rely on the belief that remaining command logs are sufficient recovery evidence.** — The applicable design condition is that passing infrastructure health while a clinical transaction or rollback assertion fails must produce an overall failed result. Consequently, irreversible purge is outside the lab contract and destroys evidence needed for residual-state validation.
- ✓ **D. Verify exact run-state IDs and ownership tags for Exercise failover ordering and rollback. Afterward, follow this dependency rule without purge: Remove only run-owned simulation artifacts; do not start a test failover or delete recovery plans.** — The checkpoint specifically records that remove only run-owned simulation artifacts; do not start a test failover or delete recovery plans. Consequently, exact identity, complete ownership tags, and the authored dependency order constrain cleanup and preserve recovery.

Remediation: [checkpoint-5](../README.md#checkpoint-5)

Source: https://learn.microsoft.com/en-us/azure/reliability/reliability-virtual-machines (verified 2026-09-02)
<!-- END GENERATED AZ305 V1 -->
