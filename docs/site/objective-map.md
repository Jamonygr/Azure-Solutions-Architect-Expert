# Objective map and validation status

The official wording and ownership below are staged from `curriculum/blueprint.yml`. Each of the 49 measured skills has exactly one primary lab. Foundation objectives use `FD-*` identifiers and are explicitly non-exam prerequisites.

!!! note
    `offline-validated` means repository contracts, generated artifacts, tests, and synthetic fixtures passed. Live Azure and Microsoft Graph verification is outside this release, so `lastLiveVerified` remains `null`.

<!-- BEGIN GENERATED AZ305 DOCS OBJECTIVES -->
Objective rows are inserted by the deterministic documentation staging tool.
<!-- END GENERATED AZ305 DOCS OBJECTIVES -->

## How to use the map

Start with a weak objective, open its primary lab, and follow its requirement-to-checkpoint trace. After the checkpoint, answer the questions mapped to that exact objective and checkpoint. Use capstones for reinforcement only; they do not replace primary coverage.
